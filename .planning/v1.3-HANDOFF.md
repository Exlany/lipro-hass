# v1.3 Handoff

## Status

- Source milestone: `v1.2` closeout remains **archive-ready / handoff-ready** after the 2026-03-17 revalidation.
- This document is a pull-only continuation brief for the next maintainer. It does not override `.planning/PROJECT.md`、`.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md` 或 `.planning/STATE.md`.

## No-return zones

- 不得重建第二 protocol/runtime root，也不得让 handoff / audit / evidence assets 反向成为 authority source。
- diagnostics、system health、developer/support consumers 必须继续复用 shared `failure_summary`，不得退回 helper-local wording。
- lifecycle failure semantics 继续冻结为 named truth：`setup_auth_failed/setup_not_ready/setup_failed`、`unload_shutdown_degraded`、`reload_auth_failed/reload_not_ready/reload_failed`。

## Frozen continuation truth

- `Phase 30` 已完成 protocol/control typed contract tightening，并把 control lifecycle failure vocabulary 收口成 canonical truth。
- Phase 31 只承接 runtime/service/platform typed budget；protocol/control closeout 不得重新打开，也不得把 Phase 30 的收口成果降回 partial truth。
- `tests/meta/test_phase31_runtime_budget_guards.py` 已把 `sanctioned_any`、`backlog_any`、broad-catch allowlist 与 `type: ignore` no-growth contract 编码成 machine truth。

## First-phase seeds for the next route

1. 继续沿 runtime/service/platform touched zones inward split sanctioned hotspots，而不是扩写新的 legacy carrier。
2. 保持 governance / review / baseline / docs 的 single-source planning story，对任何 reopen 都必须显式记录 close / retain / defer disposition。
3. 若后续继续 typed-outcome / retry-budget / runtime narrowing，必须复用已经冻结的 `failure_summary` vocabulary 与 Phase 31 budget guards，而不是另起命名体系。

## Read order

1. `.planning/reviews/V1_2_EVIDENCE_INDEX.md`
2. `.planning/v1.2-MILESTONE-AUDIT.md`
3. `.planning/phases/30-protocol-control-typed-contract-tightening/30-VERIFICATION.md`
4. `.planning/phases/31-runtime-service-typed-budget-and-exception-closure/31-VERIFICATION.md`
5. `.planning/STATE.md`

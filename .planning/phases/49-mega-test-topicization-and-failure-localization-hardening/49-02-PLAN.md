---
phase: 49-mega-test-topicization-and-failure-localization-hardening
plan: "02"
type: execute
title: topicize coordinator and diagnostics test surfaces
wave: 1
depends_on: []
files_modified:
  - tests/core/test_coordinator.py
  - tests/core/coordinator/conftest.py
  - tests/core/coordinator/test_runtime_root.py
  - tests/core/coordinator/test_entity_lifecycle.py
  - tests/core/coordinator/test_runtime_polling.py
  - tests/core/coordinator/test_update_flow.py
  - tests/core/test_diagnostics.py
  - tests/core/test_diagnostics_redaction.py
  - tests/core/test_diagnostics_config_entry.py
  - tests/core/test_diagnostics_device.py
  - tests/core/test_coordinator_integration.py
autonomous: true
requirements:
  - TST-09
  - QLT-17
must_haves:
  truths:
    - coordinator root tests are separated by runtime facet instead of mixing root, lifecycle, polling, and update flow in one file
    - diagnostics coverage is split into redaction, config-entry, and device concerns without changing product behavior
    - topicization reuses the existing coordinator runtime tree rather than inventing a second testing story
  artifacts:
    - tests/core/test_coordinator.py
    - tests/core/coordinator/conftest.py
    - tests/core/coordinator/test_runtime_root.py
    - tests/core/coordinator/test_entity_lifecycle.py
    - tests/core/coordinator/test_runtime_polling.py
    - tests/core/coordinator/test_update_flow.py
    - tests/core/test_diagnostics.py
    - tests/core/test_diagnostics_redaction.py
    - tests/core/test_diagnostics_config_entry.py
    - tests/core/test_diagnostics_device.py
  key_links:
    - coordinator root fixture home -> focused coordinator topic files -> one runtime test topology
    - diagnostics helper shell -> redaction / config-entry / device topic files -> direct concern localization
---

# Plan 49-02: topicize coordinator and diagnostics test surfaces

<context>
@.planning/phases/49-mega-test-topicization-and-failure-localization-hardening/49-CONTEXT.md
@.planning/phases/49-mega-test-topicization-and-failure-localization-hardening/49-RESEARCH.md
@tests/core/test_coordinator.py
@tests/core/coordinator/runtime/test_status_runtime.py
@tests/core/coordinator/services/test_polling_service.py
@tests/core/test_coordinator_integration.py
@tests/core/test_diagnostics.py
@tests/core/test_control_plane.py
@tests/conftest.py
@tests/conftest_shared.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: split coordinator megatests by runtime facet while keeping one root fixture home</name>
  <files>tests/core/test_coordinator.py, tests/core/coordinator/conftest.py, tests/core/coordinator/test_runtime_root.py, tests/core/coordinator/test_entity_lifecycle.py, tests/core/coordinator/test_runtime_polling.py, tests/core/coordinator/test_update_flow.py, tests/core/test_coordinator_integration.py</files>
  <action>把 `tests/core/test_coordinator.py` 中的 runtime-root smoke、entity lifecycle、polling/status wrapper、update-flow arbitration 按 concern 下沉到 `tests/core/coordinator/` 自然 home；保留 `tests/core/test_coordinator.py` 作为更小的 root fixture / smoke shell，必要时新增 `tests/core/coordinator/conftest.py` 承载共享 arrange。不得把已有 `tests/core/coordinator/runtime/*` 与 `tests/core/coordinator/services/*` 的正式 topic tree 再复制一套，也不要在本计划内处理 stray top-level public/runtime tests——它们由 `49-03` 统一 re-home。</action>
  <acceptance_criteria>
    - coordinator root failures identify runtime-root, entity-lifecycle, polling, or update-flow facet directly
    - shared coordinator arrange lives in one obvious fixture home instead of being copy-pasted across files
    - integration coverage still validates the same Coordinator contract after topicization
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/test_coordinator.py tests/core/coordinator tests/core/test_coordinator_integration.py -q`</verify>
  <done>Coordinator megatest 被按 runtime facet 切开，且仍围绕同一个 runtime root 讲故事。</done>
</task>

<task type="auto">
  <name>Task 2: split diagnostics megatests into redaction, config-entry, and device topic homes</name>
  <files>tests/core/test_diagnostics.py, tests/core/test_diagnostics_redaction.py, tests/core/test_diagnostics_config_entry.py, tests/core/test_diagnostics_device.py</files>
  <action>把 `tests/core/test_diagnostics.py` 中的 redaction helpers、config-entry diagnostics、device diagnostics 拆成 focused files；让 `tests/core/test_diagnostics.py` 仅保留最小 shared helper / smoke anchor。为重复 arrange（entry/coordinator/share-manager setup）建立清晰复用点，并让断言天然按 concern 聚集，不再在单文件里跨 topic 跳转。</action>
  <acceptance_criteria>
    - diagnostics failures immediately reveal redaction, config-entry, or device concern
    - repeated diagnostics arrange code is consolidated instead of copy-pasted across unrelated tests
    - diagnostics topic files keep the same observable behavior and redaction honesty
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/test_diagnostics*.py tests/core/test_control_plane.py -q`</verify>
  <done>Diagnostics 巨石测试被切成 concern-oriented suites，失败定位不再需要在 700+ 行文件里手工剥洋葱。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest tests/core/test_coordinator.py tests/core/coordinator tests/core/test_coordinator_integration.py -q`
- [ ] `uv run pytest tests/core/test_diagnostics*.py tests/core/test_control_plane.py -q`
</verification>

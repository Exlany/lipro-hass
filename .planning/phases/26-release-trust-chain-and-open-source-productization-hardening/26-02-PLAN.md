---
phase: 26-release-trust-chain-and-open-source-productization-hardening
plan: "02"
type: execute
title: publish installer, sbom and provenance from the existing release tail
wave: 2
depends_on:
  - "01"
files_modified:
  - .github/workflows/release.yml
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - tests/meta/test_governance_guards.py
  - tests/meta/test_version_sync.py
  - tests/meta/test_toolchain_truth.py
autonomous: true
requirements:
  - GOV-21
must_haves:
  truths:
    - release workflow must continue to reuse ci.yml while publishing stronger identity evidence
    - release assets must include installer, checksum, sbom and provenance/attestation output or equivalent first-class identity proof
    - runbook must treat those assets as current posture rather than deferred aspirations
  artifacts:
    - .github/workflows/release.yml
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_governance_guards.py
    - tests/meta/test_version_sync.py
    - tests/meta/test_toolchain_truth.py
  key_links:
    - release workflow -> runbook -> governance/version/toolchain guards
---

# Plan 26-02: release-tail hardening without a second story line

<context>
@.github/workflows/release.yml
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@tests/meta/test_governance_guards.py
@tests/meta/test_version_sync.py
@tests/meta/test_toolchain_truth.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: extend release.yml with installer asset, sbom export and provenance attestation</name>
  <files>.github/workflows/release.yml, tests/meta/test_governance_guards.py, tests/meta/test_toolchain_truth.py</files>
  <read_first>
    - .github/workflows/release.yml
    - tests/meta/test_governance_guards.py
    - tests/meta/test_toolchain_truth.py
  </read_first>
  <action>在不改变 validate→build 主链的前提下，扩展 `release.yml`：发布 `install.sh`、导出 SBOM、为 release artifacts 生成 provenance attestation，并在权限与 step 命名上保持 pinned-action / explicit evidence 风格。同步更新元测试断言这些资产与权限存在。</action>
  <acceptance_criteria>
    - `.github/workflows/release.yml` contains `attest-build-provenance`
    - `.github/workflows/release.yml` contains `SBOM`
    - `.github/workflows/release.yml` contains `install.sh`
    - `uv run pytest -q tests/meta/test_governance_guards.py -k "release or workflow" tests/meta/test_toolchain_truth.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py -k "release or workflow" tests/meta/test_toolchain_truth.py`</verify>
  <done>release tail 继续复用 CI gate，但现在会发布 installer / SBOM / provenance attestation 等第一类 trust-chain assets。</done>
</task>

<task type="auto">
  <name>Task 2: promote stronger supply-chain posture into the maintainer runbook</name>
  <files>docs/MAINTAINER_RELEASE_RUNBOOK.md, tests/meta/test_version_sync.py, tests/meta/test_governance_guards.py</files>
  <read_first>
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_version_sync.py
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>把 runbook 中关于 release hardening 的描述从 defer-note 心态更新为当前 posture：明确 post-release 必须检查 zip / checksums / installer / SBOM / attestation，同时保留对单维护者现实与 no-silent-defer 的诚实记录。同步更新版本/治理测试以匹配新的措辞与资产列表。</action>
  <acceptance_criteria>
    - `docs/MAINTAINER_RELEASE_RUNBOOK.md` contains `attestation`
    - `docs/MAINTAINER_RELEASE_RUNBOOK.md` contains `SBOM`
    - `docs/MAINTAINER_RELEASE_RUNBOOK.md` contains `install.sh`
    - `uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_governance_guards.py -k "runbook or supply_chain"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_governance_guards.py -k "runbook or supply_chain"`</verify>
  <done>runbook 已把 stronger release trust chain 讲成当前 required posture，而不是“以后再说”的延后条目。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py`
</verification>

---
phase: 26-release-trust-chain-and-open-source-productization-hardening
plan: "03"
type: execute
title: unify public support security contributor and productization truth
wave: 3
depends_on:
  - "02"
files_modified:
  - SUPPORT.md
  - SECURITY.md
  - CONTRIBUTING.md
  - pyproject.toml
  - .github/CODEOWNERS
  - custom_components/lipro/manifest.json
  - tests/meta/test_governance_guards.py
  - tests/meta/test_version_sync.py
autonomous: true
requirements:
  - GOV-21
  - QLT-03
must_haves:
  truths:
    - support, security, contributor docs and metadata must expose the same install/support/version window story
    - single-maintainer reality must be stated honestly until redundancy actually exists
    - product metadata should point users to docs/support/security entry points instead of a generic repo root only
  artifacts:
    - SUPPORT.md
    - SECURITY.md
    - CONTRIBUTING.md
    - pyproject.toml
    - .github/CODEOWNERS
    - custom_components/lipro/manifest.json
    - tests/meta/test_governance_guards.py
    - tests/meta/test_version_sync.py
  key_links:
    - support/security/contributing docs -> metadata -> governance/version truth
---

# Plan 26-03: public productization contract sync

<context>
@SUPPORT.md
@SECURITY.md
@CONTRIBUTING.md
@pyproject.toml
@.github/CODEOWNERS
@custom_components/lipro/manifest.json
@tests/meta/test_governance_guards.py
@tests/meta/test_version_sync.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: unify support and security wording around supported paths and maintenance reality</name>
  <files>SUPPORT.md, SECURITY.md, CONTRIBUTING.md, tests/meta/test_governance_guards.py</files>
  <read_first>
    - SUPPORT.md
    - SECURITY.md
    - CONTRIBUTING.md
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>更新 support/security/contributing：明确 supported install paths、supported version window、`main` preview/unsupported reality、single-maintainer model 与 public/private routing；同步修正 governance guards 以验证这些 public entry points 讲同一条故事。</action>
  <acceptance_criteria>
    - `SUPPORT.md` contains `single-maintainer`
    - `SECURITY.md` contains `preview` or `best effort`
    - `CONTRIBUTING.md` contains `verified release assets`
    - `uv run pytest -q tests/meta/test_governance_guards.py -k "support or security or codeowners or readme"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py -k "support or security or codeowners or readme"`</verify>
  <done>public docs 对支持路径、版本窗口、单维护者现实与路由方式达成统一口径。</done>
</task>

<task type="auto">
  <name>Task 2: upgrade metadata entry points without faking maintainer redundancy</name>
  <files>pyproject.toml, .github/CODEOWNERS, custom_components/lipro/manifest.json, tests/meta/test_version_sync.py</files>
  <read_first>
    - pyproject.toml
    - .github/CODEOWNERS
    - custom_components/lipro/manifest.json
    - tests/meta/test_version_sync.py
  </read_first>
  <action>补齐 `pyproject.toml` 的 docs/support/security/discussions URLs，并在 metadata / CODEOWNERS 层诚实反映当前单维护者现实；若 `manifest.json` 无需变更则保持 minimal no-op，但要确保 metadata 与 public docs 没有冲突。同步版本真相测试。</action>
  <acceptance_criteria>
    - `pyproject.toml` contains `Documentation`
    - `pyproject.toml` contains `Support`
    - `pyproject.toml` contains `Security`
    - `uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py`</verify>
  <done>product metadata 现在能把用户导向正确的 docs/support/security entry points，同时不伪装 maintainer redundancy。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py`
</verification>

---
phase: 26-release-trust-chain-and-open-source-productization-hardening
plan: "01"
type: execute
title: harden installer and move supported shell installs to verified release assets
wave: 1
depends_on: []
files_modified:
  - install.sh
  - README.md
  - README_zh.md
  - docs/TROUBLESHOOTING.md
  - tests/meta/test_install_sh_guards.py
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - GOV-21
  - QLT-03
must_haves:
  truths:
    - the supported shell/manual install story must start from downloaded release assets plus verification
    - installer verification must not depend on the presence of sha256sum on the host
    - remote main/mirror/branch paths may exist only as explicit preview or unsupported flows
  artifacts:
    - install.sh
    - README.md
    - README_zh.md
    - docs/TROUBLESHOOTING.md
    - tests/meta/test_install_sh_guards.py
    - tests/meta/test_governance_guards.py
  key_links:
    - installer verification -> public install docs -> governance wording guards
---

# Plan 26-01: verified release-asset install path

<context>
@install.sh
@README.md
@README_zh.md
@docs/TROUBLESHOOTING.md
@tests/meta/test_install_sh_guards.py
@tests/meta/test_governance_guards.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: add first-class verified local release-asset install support</name>
  <files>install.sh, tests/meta/test_install_sh_guards.py</files>
  <read_first>
    - install.sh
    - tests/meta/test_install_sh_guards.py
  </read_first>
  <action>扩展 `install.sh`：支持本地 release asset 安装（例如 `--archive-file` / `--checksum-file` 或等价 env surface），并使用 Python 内建 hash 校验 release zip，而不是依赖 `sha256sum` 是否存在。对 tag release 缺失资产/校验文件要 fail-closed；仅对显式 `main` / branch preview 路径保留非 release archive 行为，并以 warning 标明 unsupported/preview。</action>
  <acceptance_criteria>
    - `install.sh` contains `--archive-file`
    - `install.sh` contains `--checksum-file`
    - `install.sh` contains `hashlib`
    - `uv run pytest -q tests/meta/test_install_sh_guards.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_install_sh_guards.py`</verify>
  <done>installer 具备本地 verified release-asset 安装路径，且 tag release 校验改为 fail-closed / tool-independent。</done>
</task>

<task type="auto">
  <name>Task 2: rewrite default shell/manual install docs around verified release assets</name>
  <files>README.md, README_zh.md, docs/TROUBLESHOOTING.md, tests/meta/test_governance_guards.py</files>
  <read_first>
    - README.md
    - README_zh.md
    - docs/TROUBLESHOOTING.md
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>把 README / README_zh / troubleshooting 的默认 shell/manual story 改成“下载 release 资产 → 校验 → 本地运行 install.sh”；保留 `ARCHIVE_TAG=main` / mirror 仅作为 advanced preview/unsupported。同步改写 governance guard，使其验证新的默认支持路径，而不是 `ARCHIVE_TAG=latest` 远程执行。</action>
  <acceptance_criteria>
    - `README.md` contains `--archive-file`
    - `README_zh.md` contains `--archive-file`
    - `README.md` does not contain `ARCHIVE_TAG=latest bash -`
    - `uv run pytest -q tests/meta/test_governance_guards.py -k "installer or latest or release"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py -k "installer or latest or release"`</verify>
  <done>公开安装文档已把 verified release assets 定义为默认支持路径；远程脚本执行不再被描述成默认推荐方案。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_install_sh_guards.py tests/meta/test_governance_guards.py -k "installer or latest or release"`
</verification>

---
phase: 07.4-protocol-replay-simulator-harness
plan: "01"
title: 建立 replay assets authority、fixture layout 与 deterministic driver
wave: 1
depends_on: []
files_modified:
  - tests/harness/protocol/__init__.py
  - tests/harness/protocol/replay_models.py
  - tests/harness/protocol/replay_loader.py
  - tests/harness/protocol/replay_driver.py
  - tests/fixtures/protocol_replay/README.md
  - tests/fixtures/protocol_replay/rest/
  - tests/fixtures/protocol_replay/mqtt/
  - .planning/baseline/AUTHORITY_MATRIX.md
  - .planning/reviews/FILE_MATRIX.md
autonomous: true
requirements:
  - SIM-01
  - SIM-02
---

<objective>
建立 `07.4` 的单一 replay harness formal home：用 authority-indexed manifests + deterministic driver 组织 replay corpus，并保证所有回放都走正式 façade / decoder 路径，不复制第二套实现。
</objective>

<tasks>
<task>新增 `tests/harness/protocol/` 作为 assurance-only replay home，以 `replay_models.py / replay_loader.py / replay_driver.py` 形成最小但正式的 harness family：只允许“装载/排序/执行/断言编排”，不允许获得 runtime 编排权。</task>
<task>新增 `tests/fixtures/protocol_replay/` 作为 replay manifest family：每个 scenario 必须记录 authority pointer、family、version、seed、clock baseline 与 expected assertion families；当 authority payload 已存在于 `tests/fixtures/api_contracts/` 或 `tests/fixtures/protocol_boundary/` 时，manifest 只能引用，不得复制第二份 payload。</task>
<task>在 `replay_driver.py` 明确 deterministic controls：固定输入顺序、固定 seed、固定时间基准、固定错误分类；driver 只允许调用正式 `LiproProtocolFacade` / `core/protocol/boundary/*` public path。</task>
<task>回写治理最小真源：
  1) 更新 `.planning/baseline/AUTHORITY_MATRIX.md`，登记 replay manifests 与 authority fixture family 的关系；
  2) 更新 `.planning/reviews/FILE_MATRIX.md`，登记 `tests/harness/protocol/*` 与 `tests/fixtures/protocol_replay/*` 的 plane / owner。</task>
</tasks>

## Verification Criteria

- `tests/harness/protocol/*` 成为唯一 replay harness formal home；
- replay manifests 只索引 authority payload，不复制第二套 fixture truth；
- deterministic driver 只走正式 façade / decoder path；
- `AUTHORITY_MATRIX / FILE_MATRIX` 已能回答 replay asset 的 owner 与真源关系。

## Must Haves

- authority-indexed replay manifests
- deterministic driver controls
- no duplicate fixture truth
- minimal governance registration

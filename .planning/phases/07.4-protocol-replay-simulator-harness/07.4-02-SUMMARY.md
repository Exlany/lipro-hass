---
phase: 07.4-protocol-replay-simulator-harness
plan: "02"
status: completed
completed: 2026-03-13
requirements:
  - SIM-01
  - SIM-02
---

# Summary 07.4-02

## Outcome

- 新增 `tests/harness/protocol/replay_assertions.py`，把 replay assertions 收口为三类正式 helper：canonical contract、drift guard、exporter-backed telemetry assertions。
- 新增 `tests/core/api/test_protocol_replay_rest.py` 与 `tests/core/mqtt/test_protocol_replay_mqtt.py`，分别把 representative REST/MQTT flows 接到同一 replay harness，而不是复制第二套 decoder / assertion 逻辑。
- 新增 `tests/integration/test_protocol_replay_harness.py`，建立 `manifest -> driver -> formal public path -> exporter-backed telemetry assertions` 的黑盒主链，证明 replay 读取的是 `07.3` exporter truth，而不是 runtime internals。
- `tests/core/api/test_protocol_contract_matrix.py` 已补 replay-coverage 断言，明确 REST replay manifests 继续复用 `Phase 1 / 7.1` 的 authority fixtures。

## Verification

- `uv run ruff check tests/harness/protocol tests/core/api/test_protocol_replay_rest.py tests/core/mqtt/test_protocol_replay_mqtt.py tests/core/api/test_protocol_contract_matrix.py tests/integration/test_protocol_replay_harness.py`
- `uv run pytest -q -x tests/core/api/test_protocol_replay_rest.py tests/core/mqtt/test_protocol_replay_mqtt.py tests/core/api/test_protocol_contract_matrix.py tests/integration/test_protocol_replay_harness.py`

## Governance Notes

- replay assertions 通过 `RuntimeTelemetryExporter` 生成 telemetry 证据，因此 `07.4` 只消费 `07.3` exporter truth，不改写 telemetry schema。
- `07.4-03` 将继续把 replay run summary、meta guards 与 residual handoff 收拢成可仲裁 assurance 输出。

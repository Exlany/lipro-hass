---
phase: 07.4-protocol-replay-simulator-harness
plan: "03"
title: 补 operability diagnostics 与 replay-based regression suite
wave: 3
depends_on:
  - 07.4-02
files_modified:
  - tests/harness/protocol/replay_report.py
  - tests/integration/test_protocol_replay_harness.py
  - tests/meta/test_protocol_replay_assets.py
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
autonomous: true
requirements:
  - SIM-01
  - SIM-02
---

<objective>
补齐 replay harness 的 operability 证据：用 replay run summary、authority/drift guards 与 replay-based regression suite，把 `07.4` 从“可运行测试”升级为“可仲裁 assurance 资产”。
</objective>

<tasks>
<task>新增 `tests/harness/protocol/replay_report.py`：生成结构化 replay run summary，至少包含 scenario、family、version、result、drift flags、telemetry alignment 摘要、真实时间戳与伪匿名引用字段；该 summary 必须成为 `07.5` closeout 与 `08` evidence pack 的可 pull 上游输入。</task>
<task>补强 `tests/integration/test_protocol_replay_harness.py`：建立 replay-based regression suite，覆盖代表性 REST/MQTT flow 的通过、drift、异常分类与 telemetry 对齐证据。</task>
<task>新增 `tests/meta/test_protocol_replay_assets.py`：审计 replay manifests 是否具备 authority pointer、版本戳、deterministic controls，且未复制第二份 authority payload truth。</task>
<task>更新 `.planning/baseline/VERIFICATION_MATRIX.md` 与 `.planning/reviews/RESIDUAL_LEDGER.md`：登记 replay summary / meta guards 的 requirement evidence，并把暂未覆盖的 replay families 显式写成 residual / next gate，供 `07.5` 收尾。</task>
</tasks>

## Verification Criteria

- replay run summary 成为稳定、可 pull、可追溯的 assurance 输出；
- replay-based regression suite 可证明 canonical / drift / telemetry 三类证据同时成立；
- replay assets 的 authority/version/deterministic rules 有 meta-level 守卫；
- 未覆盖的 replay family 不被隐性遗忘，而是进入 residual ledger / delete gate。

## Must Haves

- replay run summary
- replay asset meta guards
- regression evidence suite
- residual handoff to 07.5

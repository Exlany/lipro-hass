---
phase: 07.4-protocol-replay-simulator-harness
plan: "02"
title: 为 REST/MQTT contract flows 接入 replay assertions
wave: 2
depends_on:
  - 07.4-01
files_modified:
  - tests/harness/protocol/replay_assertions.py
  - tests/core/api/test_protocol_replay_rest.py
  - tests/core/mqtt/test_protocol_replay_mqtt.py
  - tests/core/api/test_protocol_contract_matrix.py
  - tests/integration/test_protocol_replay_harness.py
  - .planning/baseline/VERIFICATION_MATRIX.md
autonomous: true
requirements:
  - SIM-01
  - SIM-02
---

<objective>
把 representative REST/MQTT contract flows 接到 replay harness：以同一套 assertion family 验证 canonical contracts、drift flags 与 telemetry alignment，并确保 replay 只消费 `07.3` exporter truth。
</objective>

<tasks>
<task>新增 `tests/harness/protocol/replay_assertions.py`，统一三类断言：
  1) canonical contract assertions；
  2) drift assertions；
  3) telemetry assertions（读取正式 exporter / telemetry surface 输出，而不是直接摸 runtime internals）。</task>
<task>为 REST representative flows 接入 replay tests：优先复用 `tests/fixtures/api_contracts/*` 与现有 `test_protocol_contract_matrix.py` 的 truth，不再复制一份 REST payload 或 decoder logic。</task>
<task>为 MQTT representative flows 接入 replay tests：优先复用 `tests/fixtures/protocol_boundary/*` 与现有 MQTT decoder / payload tests 的 truth，不得直接消费 raw payload 之外的未登记内部状态。</task>
<task>在 `tests/integration/test_protocol_replay_harness.py` 中建立黑盒主链：`manifest -> driver -> formal public path -> assertions`，并对齐 `07.3` exporter 的四类核心 telemetry 信号；允许真实时间戳用于回放对齐，但敏感字段与长期可关联真实标识必须被阻断。</task>
<task>更新 `.planning/baseline/VERIFICATION_MATRIX.md`：把 `SIM-01 / SIM-02` 的 evidence 映射到 replay REST/MQTT/integration assertions，并明确 telemetry assertions 来自 exporter truth。</task>
</tasks>

## Verification Criteria

- REST/MQTT replay assertions 共用同一 harness/assertion family；
- replay harness 只复用正式 public path，不复制第二套实现；
- telemetry assertions 明确依赖 `07.3` exporter 输出；
- `VERIFICATION_MATRIX` 已登记 replay evidence 对应的 requirement 映射。

## Must Haves

- REST replay assertions
- MQTT replay assertions
- exporter-backed telemetry assertions
- replay evidence mapped to verification matrix

---
status: testing
phase: 07.4-protocol-replay-simulator-harness
source: 07.4-01-SUMMARY.md, 07.4-02-SUMMARY.md, 07.4-03-SUMMARY.md
started: 2026-03-13T15:28:49Z
updated: 2026-03-13T15:41:15Z
---

## Current Test

number: 6
name: Replay asset meta guards 生效
expected: |
  运行 `uv run pytest -q -x tests/meta/test_protocol_replay_assets.py tests/meta/test_external_boundary_authority.py tests/meta/test_external_boundary_fixtures.py` 应通过。对你的目的来说，这意味着 replay 资产本身也被规则约束着：不会悄悄漂移、不会乱复制真相、不会让后续 AI 分析吃到混乱证据。
awaiting: user response

## Tests

### 1. Replay formal home 已建立
expected: 仓库中存在 `tests/harness/protocol/` 与 `tests/fixtures/protocol_replay/`，并且 manifests 只索引 authority fixtures，不复制第二份 payload 真相。
result: pass

### 2. REST replay 走统一 protocol root
expected: 运行 `uv run pytest -q -x tests/core/api/test_protocol_replay_rest.py` 应通过，并证明 REST replay 走 `LiproProtocolFacade.contracts.normalize_mqtt_config` 这条正式 public path。
result: pass

### 3. MQTT replay 走 boundary decoder 公共路径
expected: 运行 `uv run pytest -q -x tests/core/mqtt/test_protocol_replay_mqtt.py` 应通过，并证明 MQTT replay 走 `core.protocol.boundary.decode_mqtt_properties_payload` 这条正式 public path。
result: pass

### 4. Integration replay 主链成立
expected: 运行 `uv run pytest -q -x tests/integration/test_protocol_replay_harness.py` 应通过，并证明 `manifest -> driver -> formal public path -> assertions` 黑盒主链成立。
result: pass

### 5. Telemetry assertions pull 07.3 exporter truth
expected: 运行 `uv run pytest -q -x tests/integration/test_protocol_replay_harness.py tests/core/telemetry/test_exporter.py` 应通过，并证明 replay telemetry 断言来自 `07.3` exporter truth，而不是 runtime internals。
result: pass

### 6. Replay asset meta guards 生效
expected: 运行 `uv run pytest -q -x tests/meta/test_protocol_replay_assets.py tests/meta/test_external_boundary_authority.py tests/meta/test_external_boundary_fixtures.py` 应通过，并证明 authority pointer、版本戳、deterministic controls 与 no-duplicate-payload 规则成立。
result: [pending]

## Summary

total: 6
passed: 5
issues: 0
pending: 1
skipped: 0

## Gaps

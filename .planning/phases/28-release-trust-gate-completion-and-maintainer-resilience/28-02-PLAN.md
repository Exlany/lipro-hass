---
phase: 28-release-trust-gate-completion-and-maintainer-resilience
plan: "02"
type: execute
title: close code-scanning and security gate posture on the release path
wave: 2
depends_on:
  - "01"
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - CONTRIBUTING.md
  - tests/meta/test_governance_guards.py
  - tests/meta/test_version_sync.py
autonomous: true
requirements:
  - QLT-04
must_haves:
  truths:
    - release-specific security controls must be repo-visible and blocking rather than implicit best-effort expectations
    - the phase must not claim code-scanning or equivalent machine gates exist unless the workflow and docs actually expose them
    - release validation must remain anchored to the canonical ci.yml -> release.yml story
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - CONTRIBUTING.md
    - tests/meta/test_governance_guards.py
    - tests/meta/test_version_sync.py
  key_links:
    - ci security posture -> release gate -> contributor/runbook truth
---

# Plan 28-02: machine-enforced release security gate closure

<context>
@.github/workflows/ci.yml
@.github/workflows/release.yml
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@CONTRIBUTING.md
@tests/meta/test_governance_guards.py
@tests/meta/test_version_sync.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: make release security gates repo-visible and blocking</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, tests/meta/test_governance_guards.py</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>把 code-scanning 或等价 machine-enforced release security control 收口成 repo-visible truth：要么在现有 workflow 中加入明确的 blocking security gate，要么引入 repo-visible workflow 并把 release 依赖显式接到它；同时更新治理测试，确保仓库不会再宣称不存在的 hard gate，也不会让发布链绕开这些门禁。</action>
  <acceptance_criteria>
    - repo contains a repo-visible blocking release security gate beyond generic docs wording
    - `.github/workflows/release.yml` or its upstream validation story explicitly references the required gate
    - `uv run pytest -q tests/meta/test_governance_guards.py -k "security or code or scanning or release"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py -k "security or code or scanning or release"`</verify>
  <done>release 安全门禁不再只是口头期望，而是 repo-visible、可阻断的正式链路。</done>
</task>

<task type="auto">
  <name>Task 2: sync contributor and maintainer guidance to the new gate truth</name>
  <files>docs/MAINTAINER_RELEASE_RUNBOOK.md, CONTRIBUTING.md, tests/meta/test_version_sync.py, tests/meta/test_governance_guards.py</files>
  <read_first>
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - CONTRIBUTING.md
    - tests/meta/test_version_sync.py
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>把 runbook 与 CONTRIBUTING 中关于 release/security gate 的表述统一到新事实：哪些 gate 是 blocking、哪些仍是 advisory、哪些命令在触及 release/security boundary 时应优先复核。同步更新版本/治理测试，使其能阻止 wording 漂移。</action>
  <acceptance_criteria>
    - `docs/MAINTAINER_RELEASE_RUNBOOK.md` and `CONTRIBUTING.md` tell the same release-security story
    - advisory vs blocking gates are explicitly distinguished
    - `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py -k "security or contributor or runbook"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py -k "security or contributor or runbook"`</verify>
  <done>release/security gate truth 已在 maintainer 与 contributor 文档中同源一致。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py -k "security or code or scanning or release or contributor or runbook"`
</verification>

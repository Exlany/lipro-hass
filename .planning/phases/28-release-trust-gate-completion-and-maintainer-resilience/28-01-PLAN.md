---
phase: 28-release-trust-gate-completion-and-maintainer-resilience
plan: "01"
type: execute
title: harden release identity posture and verifiable release evidence
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - tests/meta/test_governance_guards.py
  - tests/meta/test_toolchain_truth.py
autonomous: true
requirements:
  - QLT-04
  - GOV-22
must_haves:
  truths:
    - release identity posture must move beyond passive attestation wording into explicit machine-verified release evidence
    - release hardening must continue to reuse the existing ci.yml -> release.yml chain rather than inventing a second release story
    - public and maintainer-facing wording must distinguish attestation provenance from signing truth without concept drift
  artifacts:
    - .github/workflows/release.yml
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_governance_guards.py
    - tests/meta/test_toolchain_truth.py
  key_links:
    - release workflow -> runbook -> governance/toolchain guards
---

# Plan 28-01: release identity hardening without a second release path

<context>
@.github/workflows/release.yml
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@tests/meta/test_governance_guards.py
@tests/meta/test_toolchain_truth.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: strengthen release identity evidence into an explicit machine-verified posture</name>
  <files>.github/workflows/release.yml, tests/meta/test_governance_guards.py, tests/meta/test_toolchain_truth.py</files>
  <read_first>
    - .github/workflows/release.yml
    - tests/meta/test_governance_guards.py
    - tests/meta/test_toolchain_truth.py
  </read_first>
  <action>在不改变 `validate -> build -> publish` 正式主链的前提下，强化 `release.yml` 的 identity posture：显式约束 release identity evidence（例如更明确的 artifact identity/signature/provenance outputs、step naming、permissions、required assets 或等价 machine-verifiable control），并同步元测试断言这些资产/门禁确实存在。关键是把“已有 attestation”提升为仓库可验证、可审计的第一类 release identity story，而不是继续停留在松散说明层。</action>
  <acceptance_criteria>
    - `.github/workflows/release.yml` contains stronger release identity control beyond bare asset upload
    - `.github/workflows/release.yml` continues to reuse `validate`
    - `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_toolchain_truth.py -k "release or attestation or provenance or signing"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_toolchain_truth.py -k "release or attestation or provenance or signing"`</verify>
  <done>release identity posture 变成 repo-visible、machine-verifiable 的正式能力，而不是只有 attestation 存在但 story 仍偏软。</done>
</task>

<task type="auto">
  <name>Task 2: align the maintainer runbook with the hardened release identity contract</name>
  <files>docs/MAINTAINER_RELEASE_RUNBOOK.md, tests/meta/test_governance_guards.py</files>
  <read_first>
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>把 runbook 中关于 release identity 的表述从“可选/延后”升级为当前 required posture：明确哪些 release evidence、哪些验证动作、哪些失败条件会阻止发布，且保持对 attestation 与 signing 真相的准确区分；同步治理测试，防止日后重新回落成模糊 wording。</action>
  <acceptance_criteria>
    - `docs/MAINTAINER_RELEASE_RUNBOOK.md` contains explicit required release identity checks
    - `docs/MAINTAINER_RELEASE_RUNBOOK.md` does not blur attestation and signing into one concept
    - `uv run pytest -q tests/meta/test_governance_guards.py -k "runbook or release"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py -k "runbook or release"`</verify>
  <done>runbook 已把 hardened release identity 讲成当前正式 posture，并保持术语诚实。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_toolchain_truth.py -k "release or attestation or provenance or signing"`
</verification>

---
phase: 105-governance-rule-datafication-and-milestone-freeze
plan: "02"
type: execute
title: datafy file-matrix registry classifier families and recent governance ownership
wave: 1
depends_on: []
files_modified:
  - scripts/check_file_matrix_registry_shared.py
  - scripts/check_file_matrix_registry_classifiers.py
  - scripts/check_file_matrix_registry_overrides.py
  - scripts/check_file_matrix_registry.py
  - .planning/reviews/FILE_MATRIX.md
autonomous: true
requirements:
  - GOV-69
must_haves:
  truths:
    - registry classifier rules are expressed through shared family builders instead of scattered bare tuples
    - recent governance helper/guard files have explicit Assurance ownership under Phase 105 freeze
    - file-matrix regeneration remains deterministic and duplicate-safe
  artifacts:
    - .planning/reviews/FILE_MATRIX.md
---

# Plan 105-02: datafy file-matrix registry classifier families and recent governance ownership

<objective>
继续把 `scripts/check_file_matrix_registry_*.py` 的 classifier truth machine-shaped 化，让 recent governance / focused-guard / helper homes 的 ownership 不再依赖手写 tuple duplication。
</objective>

<tasks>
<task>
<name>introduce classifier family builders and refit registry tables</name>
<files>scripts/check_file_matrix_registry_shared.py, scripts/check_file_matrix_registry_classifiers.py, scripts/check_file_matrix_registry_overrides.py, scripts/check_file_matrix_registry.py</files>
<done>classifier exact/prefix truth 与 override families 一样具备明确 family builder，recent governance files 不再散落在裸 tuple metadata 中。</done>
</task>
<task>
<name>regenerate file matrix with phase105 governance ownership</name>
<files>.planning/reviews/FILE_MATRIX.md</files>
<done>新 helper/guard 与 predecessor guard wording 已被 file matrix truth 正式接管，`--write` 与 `--check` 都能稳定通过。</done>
</task>
</tasks>

<verification>
- `uv run python scripts/check_file_matrix.py --write`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run ruff check scripts/check_file_matrix_registry_shared.py scripts/check_file_matrix_registry_classifiers.py scripts/check_file_matrix_registry_overrides.py scripts/check_file_matrix_registry.py`
</verification>

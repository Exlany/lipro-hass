---
phase: 50-rest-typed-surface-reduction-and-command-result-ownership-convergence
plan: "01"
type: execute
title: reduce REST request and endpoint helper Any surfaces
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/api/endpoint_surface.py
  - custom_components/lipro/core/api/rest_facade_endpoint_methods.py
  - tests/core/api/test_api_status_service.py
autonomous: true
requirements:
  - TYP-12
must_haves:
  truths:
    - endpoint-surface collaborator typing is narrower and more honest without promoting localized collaborators into new public roots
    - endpoint wrapper return types prefer existing stable API response aliases over ad hoc dict/list Any shells wherever current types already exist
    - remaining endpoint-facing width is explicit, localized, and ready for machine classification rather than hidden behind cast-heavy wrappers
  artifacts:
    - custom_components/lipro/core/api/endpoint_surface.py
    - custom_components/lipro/core/api/rest_facade_endpoint_methods.py
    - tests/core/api/test_api_status_service.py
  key_links:
    - endpoint surface -> rest facade endpoint methods -> stable REST child façade surface
    - typed callback/response honesty -> focused API tests -> no behavior drift
---

# Plan 50-01: reduce REST request and endpoint helper Any surfaces

<context>
@.planning/phases/50-rest-typed-surface-reduction-and-command-result-ownership-convergence/50-CONTEXT.md
@.planning/phases/50-rest-typed-surface-reduction-and-command-result-ownership-convergence/50-RESEARCH.md
@.planning/phases/50-rest-typed-surface-reduction-and-command-result-ownership-convergence/50-VALIDATION.md
@custom_components/lipro/core/api/types.py
@custom_components/lipro/core/api/endpoint_surface.py
@custom_components/lipro/core/api/rest_facade_endpoint_methods.py
@tests/core/api/test_api_status_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: narrow endpoint-surface collaborator contracts and wrapper returns</name>
  <files>custom_components/lipro/core/api/endpoint_surface.py, custom_components/lipro/core/api/rest_facade_endpoint_methods.py</files>
  <action>收紧 `_RestEndpointSurfacePort` 与 endpoint wrapper 的宽口签名：本 plan 只复用 `core/api/types.py` 中已存在的 typed truth，不新增或改写 `types.py`；优先把 `get_mqtt_config`、`query_command_result`、`get_city`、`query_user_cloud` 等可直接复用既有 alias 的返回类型接回 formal truth，并对 `on_batch_metric` 这类测试已揭示真实形状的 callback seam 给出更诚实的 local typed alias / protocol；减少纯粹为了包裹 endpoint 调用而存在的 `cast(...)`。</action>
  <acceptance_criteria>
    - endpoint-facing helper surface 的 `Any` 明显减少，且缩减发生在真实 wrapper/port seam，而非仅通过更深层 cast 隐藏
    - `LiproRestFacade` 仍是唯一 canonical REST child façade；`endpoint_surface.py` / endpoint methods 继续只是 localized collaborator / thin wrapper
    - 若某些 endpoint payload 在本 plan 内无法进一步收紧，必须保留为最小诚实边界，交由后续 typed-budget guard 显式分类，而不是继续模糊成“大而化之的 dict[str, Any]”
    - 本 plan 不得临时改写 `custom_components/lipro/core/api/types.py`；若发现必须新增 shared alias，应留给 `50-02` 统一处理，避免 Wave 1 写集碰撞
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/api/test_api_status_service.py -q`</verify>
  <done>endpoint helper typed surface 已从 cast-heavy wrapper 收口为更诚实、更窄的 localized contract。</done>
</task>

<task type="auto">
  <name>Task 2: freeze endpoint behavior truth with focused API regressions</name>
  <files>tests/core/api/test_api_status_service.py</files>
  <action>根据 endpoint typed-surface 收口后的行为边界补齐或调整 focused status/misc API 回归，确保 callback shape、stable response contract 与 façade endpoint wrapper 行为仍被明确覆盖。</action>
  <acceptance_criteria>
    - focused status/misc API tests 能直接证明 endpoint helper 收口没有引入 observable behavior drift
    - 不新增 package-level exports、compat shortcuts 或第二条 façade/public-surface 叙事
    - `.planning/baseline/PUBLIC_SURFACES.md`、`.planning/baseline/DEPENDENCY_MATRIX.md`、`.planning/reviews/FILE_MATRIX.md` 若未受 formal-home wording 影响则保持 unchanged
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/api/test_api_status_service.py -q`</verify>
  <done>endpoint typed-surface 变化已由 focused API regressions 锁定为无行为漂移的 current truth。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest tests/core/api/test_api_status_service.py -q`
</verification>

---
phase: 50-rest-typed-surface-reduction-and-command-result-ownership-convergence
plan: "03"
type: execute
title: converge command-result policy ownership into one formal home
wave: 2
depends_on:
  - 50-01
  - 50-02
files_modified:
  - custom_components/lipro/core/command/result.py
  - custom_components/lipro/core/command/result_policy.py
  - custom_components/lipro/core/command/dispatch.py
  - tests/core/test_command_result.py
autonomous: true
requirements:
  - ARC-07
must_haves:
  truths:
    - polling/retry/delayed-refresh implementation lives in one formal command-result policy home instead of being duplicated across result and policy modules
    - core.command.result remains the stable import/export home for downstream consumers unless a stronger current-truth reason demands otherwise
    - ownership convergence does not change command-result failure semantics, trace payload meaning, or downstream import compatibility
  artifacts:
    - custom_components/lipro/core/command/result.py
    - custom_components/lipro/core/command/result_policy.py
    - custom_components/lipro/core/command/dispatch.py
    - tests/core/test_command_result.py
  key_links:
    - result_policy implementation home -> result stable exports -> runtime/diagnostics/test consumers
    - ownership convergence -> one command-result story -> no helper/service/API drift
---

# Plan 50-03: converge command-result policy ownership into one formal home

<context>
@.planning/phases/50-rest-typed-surface-reduction-and-command-result-ownership-convergence/50-CONTEXT.md
@.planning/phases/50-rest-typed-surface-reduction-and-command-result-ownership-convergence/50-RESEARCH.md
@.planning/phases/50-rest-typed-surface-reduction-and-command-result-ownership-convergence/50-VALIDATION.md
@custom_components/lipro/core/command/result.py
@custom_components/lipro/core/command/result_policy.py
@custom_components/lipro/core/command/dispatch.py
@tests/core/test_command_result.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: centralize command-result polling and retry policy implementation</name>
  <files>custom_components/lipro/core/command/result.py, custom_components/lipro/core/command/result_policy.py, custom_components/lipro/core/command/dispatch.py</files>
  <action>把 `result.py` 与 `result_policy.py` 之间重复持有的 polling/retry/delayed-refresh 逻辑收敛到单一 formal implementation home；保留 `core.command.result` 对外稳定导出职责，并在必要时同步 `dispatch.py` 或相邻 importers，以避免 implementation home 与 stable export home 再次漂移。</action>
  <acceptance_criteria>
    - command-result policy 只有一个正式实现 home，不再在 `result.py` / `result_policy.py` 双持同类算法
    - `core.command.result` 对现有 runtime/diagnostics/test 消费者保持稳定，除非有明确 current-truth 理由要求同步 import path；若同步则必须是薄层兼容而不是第二故事线
    - failure payload、trace payload、attempt/budget 语义保持稳定，不因 ownership convergence 发生行为漂移
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/test_command_result.py -q`</verify>
  <done>command-result polling/retry 语义已回收到单一 formal home，导出面与实现面职责清晰分离。</done>
</task>

<task type="auto">
  <name>Task 2: lock the converged ownership through focused command-result regressions</name>
  <files>tests/core/test_command_result.py</files>
  <action>以 focused tests 覆盖 progressive retry、delayed refresh、poll-result classification、failure payload composition 与 stable exports，确保 ownership convergence 不是“语义暗改”。</action>
  <acceptance_criteria>
    - tests 直接证明 `result.py` 与 `result_policy.py` 的新职责边界仍满足既有 must-have semantics
    - `.planning/reviews/FILE_MATRIX.md`、`.planning/reviews/RESIDUAL_LEDGER.md`、`.planning/reviews/KILL_LIST.md` 若未受 formal-home wording 影响则保持 unchanged
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/test_command_result.py -q`</verify>
  <done>command-result ownership convergence 已被 focused regressions 固化为稳定 current truth。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest tests/core/test_command_result.py -q`
</verification>

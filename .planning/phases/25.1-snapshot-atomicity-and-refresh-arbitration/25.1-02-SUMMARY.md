# 25.1-02 Summary

## Outcome

- `Coordinator` 现已把 snapshot rejection 走进正式 `UpdateFailed` / runtime telemetry failure path；被拒绝刷新不会清空或缩小 `_state.devices`，也不会继续发布 listener 更新。
- `LiproApiError` during fetch 现重新走 `UpdateFailed`，`LiproAuthError` during fetch 现重新走 `ConfigEntryAuthFailed`；设备列表抓取失败不再被包装成“空设备成功”。
- `CoordinatorDeviceRefreshService` 继续保持 thin adapter，只透传 canonical refresh callback，不新长第二套仲裁逻辑。

## Key Files

- `custom_components/lipro/core/coordinator/coordinator.py`
- `tests/core/test_coordinator.py`
- `tests/core/test_coordinator_integration.py`
- `tests/core/coordinator/services/test_device_refresh_service.py`

## Validation

- `uv run pytest -q tests/core/test_coordinator.py tests/core/test_coordinator_integration.py tests/core/coordinator/services/test_device_refresh_service.py`
- `uv run pytest -q tests/core/test_init.py -k first_refresh_failure_cleans_up`

## Notes

- coordinator 主链现在是 fail-closed，而不是“先覆盖 state，再靠 debug log 解释失败”。

# Phase 25.1 Verification

status: passed

## Goal

- 核验 `Phase 25.1: snapshot atomicity and refresh arbitration` 是否完成 `RUN-06` / `ERR-03`：任何 fetch / parse / enrich failure 都不会再把 partial snapshot 发布成新的全量 truth，且 refresh rejection 会沿正式 runtime/coordinator 主链暴露并保留 last-known-good。
- 终审结论：**`RUN-06` / `ERR-03` 已在 2026-03-17 完成；`Phase 25.1` 现已关闭，下一 tranche 为 `Phase 25.2`。**

## Reviewed Assets

- Phase 资产：`25.1-CONTEXT.md`、`25.1-RESEARCH.md`、`25.1-VALIDATION.md`
- 已生成 summaries：`25.1-01-SUMMARY.md`、`25.1-02-SUMMARY.md`、`25.1-03-SUMMARY.md`
- touched code/tests：`snapshot.py`、`device_runtime.py`、`coordinator.py`、相关 runtime/coordinator/init test slices
- closeout truth：`.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md`、`.planning/STATE.md`

## Must-Haves

- **1. Atomic snapshot build — PASS**
  - 分页抓取、payload 形状、坏行、设备 parse 与 mesh-group enrich 失败都不再返回 partial snapshot。
  - `SnapshotBuilder` 只在 full success 时返回 `FetchedDeviceSnapshot`。

- **2. Explicit last-known-good arbitration — PASS**
  - `DeviceRuntime` 仅在 full success 时更新 `_last_snapshot` 与 refresh strategy。
  - 被拒绝刷新时旧快照保留、刷新压力保留，并留下结构化 failure view。

- **3. Coordinator fail-closed behavior — PASS**
  - snapshot rejection 不会覆盖 `_state.devices`，并通过 `UpdateFailed` / telemetry failure summary 主链显式暴露。
  - fetch 阶段的 protocol/auth failures 重新回到各自正式 failure path。

## Evidence

- `uv run pytest -q tests/core/test_device_refresh.py tests/core/coordinator/runtime/test_device_runtime.py` → `42 passed`
- `uv run pytest -q tests/core/test_coordinator.py tests/core/test_coordinator_integration.py tests/core/coordinator/services/test_device_refresh_service.py` → `49 passed`
- `uv run pytest -q tests/core/test_init.py -k first_refresh_failure_cleans_up` → `1 passed`
- `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py` → `54 passed`
- `uv run ruff check custom_components/lipro/core/coordinator/runtime/device/snapshot.py custom_components/lipro/core/coordinator/runtime/device_runtime.py custom_components/lipro/core/coordinator/coordinator.py tests/core/test_device_refresh.py tests/core/coordinator/runtime/test_device_runtime.py tests/core/test_coordinator.py tests/core/test_coordinator_integration.py tests/core/coordinator/services/test_device_refresh_service.py` → passed

## Risks / Notes

- 本 phase 采用 correctness-first 裁决，因此退休了旧的 partial-success 语义；若未来要重新放宽，必须作为显式新裁决进入 roadmap，而不能通过测试夹具偷偷回流。
- `.planning/reviews/FILE_MATRIX.md`、`.planning/reviews/RESIDUAL_LEDGER.md`、`.planning/reviews/KILL_LIST.md`、`.planning/baseline/VERIFICATION_MATRIX.md`、`.planning/baseline/AUTHORITY_MATRIX.md` 已复核为 checked-no-delta，本次无需额外改写。

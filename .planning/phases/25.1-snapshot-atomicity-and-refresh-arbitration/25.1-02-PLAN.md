---
phase: 25.1-snapshot-atomicity-and-refresh-arbitration
plan: "02"
type: execute
title: wire coordinator arbitration onto the canonical refresh path
wave: 2
depends_on:
  - "01"
files_modified:
  - custom_components/lipro/core/coordinator/coordinator.py
  - tests/core/test_coordinator.py
  - tests/core/test_coordinator_integration.py
  - tests/core/coordinator/services/test_device_refresh_service.py
autonomous: true
requirements:
  - RUN-06
  - ERR-03
must_haves:
  truths:
    - coordinator must only publish full-success snapshots
    - refresh rejection must reach UpdateFailed or auth failure paths explicitly
    - manual refresh and scheduled refresh must share the same arbitration story
  artifacts:
    - custom_components/lipro/core/coordinator/coordinator.py
    - tests/core/test_coordinator.py
    - tests/core/test_coordinator_integration.py
    - tests/core/coordinator/services/test_device_refresh_service.py
  key_links:
    - device runtime rejection -> coordinator update failure -> state preservation
---

# Plan 25.1-02: 把仲裁语义接到 coordinator 正式刷新主链

<context>
@custom_components/lipro/core/coordinator/coordinator.py
@tests/core/test_coordinator.py
@tests/core/test_coordinator_integration.py
@tests/core/coordinator/services/test_device_refresh_service.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: make coordinator fail closed on snapshot rejection</name>
  <files>custom_components/lipro/core/coordinator/coordinator.py, tests/core/test_coordinator.py, tests/core/test_coordinator_integration.py</files>
  <read_first>
    - custom_components/lipro/core/coordinator/coordinator.py
    - custom_components/lipro/core/coordinator/runtime/device_runtime.py
    - custom_components/lipro/core/coordinator/runtime/device/snapshot.py
    - tests/core/test_coordinator.py
    - tests/core/test_coordinator_integration.py
  </read_first>
  <action>修改 `Coordinator._async_refresh_device_snapshot()` 与 `_async_update_data()`：snapshot rejection 时不得清空/覆盖 `_state.devices`；对 `RuntimeSnapshotRefreshRejectedError` 走稳定 `UpdateFailed(str(err))` 路径，对 `LiproAuthError` / `LiproRefreshTokenExpiredError` during fetch 继续走 auth failure；确保 `async_refresh_devices()` 失败时也不 `async_set_updated_data()`。同步改写 coordinator tests：旧的“device fetch API/auth error returns empty list”断言改成显式失败；补一条“已有 devices 时 refresh 被拒绝后 state 保持旧值”的回归。</action>
  <acceptance_criteria>
    - `custom_components/lipro/core/coordinator/coordinator.py` contains `RuntimeSnapshotRefreshRejectedError`
    - `tests/core/test_coordinator_integration.py` does not contain `returns empty device list`
    - `tests/core/test_coordinator.py` contains `last_runtime_failure_stage`
    - `uv run pytest -q tests/core/test_coordinator.py tests/core/test_coordinator_integration.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/test_coordinator.py tests/core/test_coordinator_integration.py`</verify>
  <done>`Coordinator` 只会发布 full-success snapshot；snapshot rejection 通过 `UpdateFailed` / auth failure 主链显式暴露，并保留既有 `_state.devices` 与 telemetry failure summary。</done>
</task>

<task type="auto">
  <name>Task 2: keep device-refresh service as thin canonical adapter</name>
  <files>tests/core/coordinator/services/test_device_refresh_service.py</files>
  <read_first>
    - custom_components/lipro/core/coordinator/services/device_refresh_service.py
    - tests/core/coordinator/services/test_device_refresh_service.py
  </read_first>
  <action>仅在测试层确认 `CoordinatorDeviceRefreshService` 仍只是调用 canonical refresh callback，不自己吞掉 snapshot rejection；若需要，补一条异常透传断言，但不要给 service 添加第二套仲裁逻辑。</action>
  <acceptance_criteria>
    - `tests/core/coordinator/services/test_device_refresh_service.py` contains `pytest.raises`
    - `custom_components/lipro/core/coordinator/services/device_refresh_service.py` does not contain `try:`
    - `uv run pytest -q tests/core/coordinator/services/test_device_refresh_service.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/coordinator/services/test_device_refresh_service.py`</verify>
  <done>`CoordinatorDeviceRefreshService` 继续保持 thin adapter 身份：只透传 canonical refresh callback，不吞错、不另立仲裁逻辑。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_coordinator.py tests/core/test_coordinator_integration.py tests/core/coordinator/services/test_device_refresh_service.py`
</verification>

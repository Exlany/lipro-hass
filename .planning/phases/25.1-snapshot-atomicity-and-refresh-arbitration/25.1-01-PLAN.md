---
phase: 25.1-snapshot-atomicity-and-refresh-arbitration
plan: "01"
type: execute
title: enforce atomic snapshot rejection in builder and runtime
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/coordinator/runtime/device/snapshot.py
  - custom_components/lipro/core/coordinator/runtime/device_runtime.py
  - tests/core/test_device_refresh.py
  - tests/core/coordinator/runtime/test_device_runtime.py
autonomous: true
requirements:
  - RUN-06
  - ERR-03
must_haves:
  truths:
    - fetch/parse/enrich failures must reject the new snapshot instead of returning partial truth
    - last-known-good snapshot may be retained but refresh itself must fail explicitly
    - runtime failure semantics must be testable without reading debug logs
  artifacts:
    - custom_components/lipro/core/coordinator/runtime/device/snapshot.py
    - custom_components/lipro/core/coordinator/runtime/device_runtime.py
    - tests/core/test_device_refresh.py
    - tests/core/coordinator/runtime/test_device_runtime.py
  key_links:
    - snapshot builder -> device runtime -> last-known-good arbitration
---

# Plan 25.1-01: 在 builder/runtime 中建立原子拒绝语义

<context>
@custom_components/lipro/core/coordinator/runtime/device/snapshot.py
@custom_components/lipro/core/coordinator/runtime/device_runtime.py
@tests/core/test_device_refresh.py
@tests/core/coordinator/runtime/test_device_runtime.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: reject partial snapshots at fetch/parse/enrich boundaries</name>
  <files>custom_components/lipro/core/coordinator/runtime/device/snapshot.py, tests/core/test_device_refresh.py</files>
  <read_first>
    - custom_components/lipro/core/coordinator/runtime/device/snapshot.py
    - tests/core/test_device_refresh.py
    - custom_components/lipro/core/telemetry/models.py
  </read_first>
  <action>在 `snapshot.py` 引入稳定异常类型（名称中必须包含 `Runtime`，并携带 `stage`、`page`、`cause_type`、`kept_last_known_good` 之类的可测试字段）；把分页抓取失败、`devices` payload 不是 `list`、存在 non-dict row、设备 parse 失败、mesh-group topology enrich 失败、超过 `max_pages` 等情况统一改成“拒绝生成新 snapshot 并抛异常”，而不是 `break`、`continue` 或 debug 后继续返回 partial snapshot。同步改写 `tests/core/test_device_refresh.py`：新增/改断言覆盖 page-2 failure、malformed payload、non-dict row、parse failure、mesh enrich failure 全部抛出稳定异常。</action>
  <acceptance_criteria>
    - `custom_components/lipro/core/coordinator/runtime/device/snapshot.py` contains `class RuntimeSnapshotRefreshRejectedError`
    - `custom_components/lipro/core/coordinator/runtime/device/snapshot.py` does not contain `stopping pagination`
    - `tests/core/test_device_refresh.py` contains `RuntimeSnapshotRefreshRejectedError`
    - `uv run pytest -q tests/core/test_device_refresh.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/test_device_refresh.py`</verify>
  <done>分页抓取、payload 形状、坏行、设备 parse 与 mesh-group enrich 都不会再返回 partial snapshot；builder 只在 full success 时返回 `FetchedDeviceSnapshot`。</done>
</task>

<task type="auto">
  <name>Task 2: preserve last-known-good without faking refresh success</name>
  <files>custom_components/lipro/core/coordinator/runtime/device_runtime.py, tests/core/coordinator/runtime/test_device_runtime.py</files>
  <read_first>
    - custom_components/lipro/core/coordinator/runtime/device_runtime.py
    - custom_components/lipro/core/coordinator/runtime/device/snapshot.py
    - tests/core/coordinator/runtime/test_device_runtime.py
  </read_first>
  <action>修改 `DeviceRuntime.refresh_devices()`：只有 full success 才更新 `_last_snapshot` 并 `mark_refreshed()`；若 refresh 被 `RuntimeSnapshotRefreshRejectedError` 或 protocol failure 拒绝，旧 `_last_snapshot` 必须保持不变，refresh 调用本身继续抛错。为 runtime 增加一个最小的结构化 failure view（例如 `get_last_refresh_failure()`）或等价正式方法，能让测试验证 `stage/page/kept_last_known_good`。同步改写 `tests/core/coordinator/runtime/test_device_runtime.py`：旧的“API error returns empty snapshot”“partial page failure keeps first page”断言必须退休，改成显式抛错 + 保留旧快照断言。</action>
  <acceptance_criteria>
    - `custom_components/lipro/core/coordinator/runtime/device_runtime.py` contains `get_last_refresh_failure`
    - `tests/core/coordinator/runtime/test_device_runtime.py` does not contain `return empty snapshot`
    - `tests/core/coordinator/runtime/test_device_runtime.py` contains `kept_last_known_good`
    - `uv run pytest -q tests/core/coordinator/runtime/test_device_runtime.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/coordinator/runtime/test_device_runtime.py`</verify>
  <done>`DeviceRuntime` 只有在接受新快照时才 `mark_refreshed()` / 更新 `_last_snapshot`；被拒绝刷新时保留旧快照、保留刷新压力，并留下可测试的结构化 failure view。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_device_refresh.py tests/core/coordinator/runtime/test_device_runtime.py`
</verification>

---
phase: 18-host-neutral-boundary-nucleus-extraction
plan: "03"
title: host-neutral locality guards and focused regression hardening
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/DEPENDENCY_MATRIX.md
  - .planning/baseline/ARCHITECTURE_POLICY.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - tests/meta/test_dependency_guards.py
  - tests/meta/test_public_surface_guards.py
  - tests/flows/test_config_flow.py
  - tests/core/test_init.py
  - tests/core/device/test_capabilities.py
  - tests/core/capability/test_registry.py
requirements:
  - CORE-01
  - CORE-03
---

<objective>
把 `Phase 18` 新形成的 host-neutral nucleus locality、adapter projection demotion 与 public-surface truth 固定为可执行治理规则：既阻断 `homeassistant` imports / HA-only projection 再回流到 nucleus，也明确告诉未来维护者这些 nucleus 是 formal helper/contract family，而不是新的 public root 或第二条故事线。
</objective>

<tasks>
<task>更新 `.planning/baseline/PUBLIC_SURFACES.md` 与 `.planning/baseline/DEPENDENCY_MATRIX.md`：正式记录 host-neutral auth/device nucleus family 的身份、allowed consumers、adapter seam 位置与非 root 属性；明写它们不是第二 public root。</task>
<task>扩展 `.planning/baseline/ARCHITECTURE_POLICY.md`：新增或收紧针对 nucleus locality 的规则，至少覆盖 “nucleus homes 不得 import `homeassistant`” 与 “HA-only platform projection 不得再次登记为 core capability truth” 两类约束。</task>
<task>在 `tests/meta/test_dependency_guards.py` 与必要的 public-surface/meta tests 中补充 focused guard，使新规则既能本地快速失败，也能在 CI 中阻断回流。</task>
<task>回写 `.planning/reviews/FILE_MATRIX.md`、`.planning/reviews/RESIDUAL_LEDGER.md`、`.planning/reviews/KILL_LIST.md`：记录哪些 host-specific helpers 已 demote、哪些 transitional projection 仍短期存在、delete gate 是什么、谁负责清退。</task>
<task>同步刷新 `tests/flows/test_config_flow.py`、`tests/core/test_init.py`、`tests/core/device/test_capabilities.py`、`tests/core/capability/test_registry.py` 等 focused regression，确保 governance wording、guard rules 与代码事实对齐，不留下“文档说 host-neutral，代码仍混 HA 语义”的裂缝。</task>
</tasks>

## Verification Commands

- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`
- `uv run pytest -q tests/flows/test_config_flow.py tests/core/test_init.py tests/core/device/test_capabilities.py tests/core/capability/test_registry.py`
- `uv run ruff check .planning/baseline .planning/reviews tests/meta`

## Verification Criteria

- baseline/docs/guards 对 host-neutral nucleus 讲的是同一条故事：formal helper/contract family，而非新 root；
- architecture policy 能阻断 `homeassistant` imports 或 HA-only platform projection 回流到 nucleus homes；
- residual/review docs 已记录任何 transitional seam 与 delete gate，不存在 silent carry-forward；
- focused meta + regression tests 能在 Phase 18 之后第一时间发现 locality/backflow 回归。

## Must-Haves

- 不得让 governance 文案暗示 `core` 已经变成独立跨平台 SDK
- 不得把 replay/evidence/exception hardening 冒充为本 plan 的主目标
- 不得只写文档不写 guard，也不得只写 guard 不同步 public-surface / review truth
- 所有规则都必须来自 north-star/baseline，同步到单一 enforcement chain，而不是新造第二套规则真相

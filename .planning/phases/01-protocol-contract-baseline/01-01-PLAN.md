---
phase: 01-protocol-contract-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/api_contracts/README.md
  - tests/fixtures/api_contracts/get_mqtt_config.direct.json
  - tests/fixtures/api_contracts/get_mqtt_config.wrapped.json
  - tests/fixtures/api_contracts/get_city.success.json
  - tests/fixtures/api_contracts/query_user_cloud.success.json
  - tests/core/api/test_protocol_contract_matrix.py
autonomous: true
requirements:
  - PROT-01
  - PROT-02
  - ASSR-01
must_haves:
  - Golden fixtures exist for the first three high-drift protocol boundaries
  - `get_mqtt_config` direct/wrapped shapes normalize to the same canonical contract
  - `get_city` and `query_user_cloud` helper contracts are asserted independently of `LiproClient` inheritance shape
  - Targeted contract suite and adjacent regression tests pass
---

# Plan 01-01: 建立协议契约矩阵与首批 Golden Fixtures

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-contract-baseline/01-CONTEXT.md
@.planning/phases/01-protocol-contract-baseline/01-RESEARCH.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@docs/archive/NORTH_STAR_EXECUTION_PLAN_2026-03-12.md
@tests/core/api/test_helper_modules.py
@tests/core/api/test_api_diagnostics_service.py
@tests/snapshots/test_api_snapshots.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 contract matrix 与 fixture 目录</name>
  <files>tests/fixtures/api_contracts/README.md, tests/fixtures/api_contracts/*.json</files>
  <action>建立首批协议契约矩阵，只覆盖 `get_mqtt_config`、`get_city`、`query_user_cloud`。fixture 要描述 canonical output，而不是复制当前实现噪音或继承结构。</action>
  <verify>检查 fixture 文件存在且 JSON 可解析。</verify>
  <done>目录、矩阵、4 个 fixture 文件已创建并可读。</done>
</task>

<task type="auto">
  <name>Task 2: 新增 targeted protocol contract tests</name>
  <files>tests/core/api/test_protocol_contract_matrix.py</files>
  <action>新增最小但高价值的 targeted tests，验证 direct/wrapped MQTT config contract 归一、city mapping contract、user cloud helper contract。</action>
  <verify>`uv run pytest tests/core/api/test_protocol_contract_matrix.py -q`</verify>
  <done>测试独立通过，且不依赖 `LiproClient` 多重继承形态。</done>
</task>

<task type="auto">
  <name>Task 3: 用现有 helper/snapshot tests 做回归护栏</name>
  <files>tests/core/api/test_helper_modules.py, tests/core/api/test_api_diagnostics_service.py, tests/snapshots/test_api_snapshots.py</files>
  <action>运行与首批 contract 直接相关的现有测试，证明新 contract baseline 没有破坏当前 helper 行为，并为 `01-02` snapshot 收尾提供稳定输入。</action>
  <verify>`uv run pytest tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_helper_modules.py tests/core/api/test_api_diagnostics_service.py tests/snapshots/test_api_snapshots.py -q`</verify>
  <done>首批协议契约套件与相关回归测试全部通过。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_helper_modules.py tests/core/api/test_api_diagnostics_service.py tests/snapshots/test_api_snapshots.py -q`
- [ ] golden fixtures 与 canonical outputs 一一对应
- [ ] `get_mqtt_config` 的双 shape 已被视为输入差异而非双 contract 真相
- [ ] 该计划不把当前 `LiproClient` 的 mixin 结构写进 contract 真源
</verification>

<success_criteria>

- 首批高漂移协议边界已有可读、可执行、可回归的 golden fixtures
- Protocol contract baseline 已从“理念”变成“测试资产”
- `01-02` 可以建立在稳定 canonical contracts 之上完成治理收尾
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-contract-baseline/01-01-SUMMARY.md`
</output>

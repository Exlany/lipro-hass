---
phase: 02-api-client-de-mixin
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/phases/02-api-client-de-mixin/02-ARCHITECTURE.md
  - .planning/phases/02-api-client-de-mixin/02-01-SUMMARY.md
autonomous: true
requirements:
  - ARCH-01
  - PROT-03
  - PROT-04
  - PROT-07
must_haves:
  - Phase 2 slice has an explicit `LiproRestFacade` target architecture, not just criticism of the old client
  - `core/api` files and direct test consumers are classified for keep/refactor/adapter/delete handling
  - Compat behavior and legacy public names are tracked in residual / kill truth before code rewrites begin
  - Phase 2 exit contract is reflected in `VERIFICATION_MATRIX.md`
---

# Plan 02-01: 建立 REST protocol target design 与文件治理矩阵

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@.planning/phases/02-api-client-de-mixin/02-CONTEXT.md
@.planning/phases/02-api-client-de-mixin/02-ARCHITECTURE.md
@.planning/phases/02-api-client-de-mixin/02-RESEARCH.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 完成 Phase 2 slice 文件治理矩阵</name>
  <files>.planning/reviews/FILE_MATRIX.md, .planning/reviews/RESIDUAL_LEDGER.md, .planning/reviews/KILL_LIST.md</files>
  <action>补齐 `custom_components/lipro/core/api/**/*.py`、`tests/core/api/**/*.py` 及直接 consumer tests 的分类与残留登记，明确哪些模块保留、重构、迁移适配或删除，并把已裁定的 compat/mixin/public-name 清理对象写入 kill list。</action>
  <verify>人工检查矩阵是否覆盖 `core/api`、direct tests 与主要 direct consumers，并与 residual/kill 口径一致。</verify>
  <done>Phase 2 不再以模糊文件范围启动。</done>
</task>

<task type="auto">
  <name>Task 2: 锁定 REST protocol 正式目标结构</name>
  <files>.planning/phases/02-api-client-de-mixin/02-ARCHITECTURE.md</files>
  <action>把 `LiproRestFacade`、session/auth state、transport、request policy、auth recovery、normalizers、compat shell 的职责边界写成正式设计文档，作为后续重构真源。</action>
  <verify>人工检查设计文档是否回答：正式 public surface 是什么、compat 在哪里、什么必须删除、如何挂接到 `LiproProtocolFacade`。</verify>
  <done>Phase 2 具备可执行、可仲裁的目标设计。</done>
</task>

<task type="auto">
  <name>Task 3: 固化 Phase 2 exit contract</name>
  <files>.planning/baseline/VERIFICATION_MATRIX.md, tests/core/api/test_protocol_contract_matrix.py, tests/snapshots/test_api_snapshots.py</files>
  <action>把 Phase 2 的 exit contract 写入 verification matrix，并运行最小协议护栏测试，确认 Phase 2 仍建立在 Phase 1 冻结的协议行为之上。</action>
  <verify>`uv run pytest tests/core/api/test_protocol_contract_matrix.py tests/snapshots/test_api_snapshots.py -q`</verify>
  <done>Phase 2 的设计与治理建立在稳定边界之上。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/core/api/test_protocol_contract_matrix.py tests/snapshots/test_api_snapshots.py -q`
- [ ] `FILE_MATRIX.md` 已覆盖 Phase 2 slice 与 direct consumers
- [ ] `RESIDUAL_LEDGER.md` 已登记 mixin inheritance、compat wrappers、legacy public names
- [ ] `KILL_LIST.md` 已列出本阶段预计清退对象或明确“本计划只登记、不删除”
- [ ] `VERIFICATION_MATRIX.md` 已记录 Phase 2 exit contract
</verification>

<success_criteria>

- Phase 2 slice 有正式设计文档与治理矩阵
- compat 行为与旧 public names 不再以隐性 folklore 形式存在
- 后续代码重构有清晰边界、文件清单、删除方向与 exit contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-client-de-mixin/02-01-SUMMARY.md`
</output>

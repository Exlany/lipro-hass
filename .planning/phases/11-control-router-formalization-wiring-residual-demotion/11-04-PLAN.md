---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "04"
title: protocol and runtime formal surface convergence
wave: 4
depends_on:
  - "03"
files_modified:
  - custom_components/lipro/core/api/client.py
  - custom_components/lipro/core/protocol/facade.py
  - custom_components/lipro/core/coordinator/runtime/device/snapshot.py
  - custom_components/lipro/core/coordinator/orchestrator.py
  - tests/core/api/test_protocol_contract_matrix.py
  - tests/core/coordinator/runtime/test_device_runtime.py
  - tests/core/test_coordinator_integration.py
  - tests/core/test_coordinator.py
  - tests/benchmarks/test_coordinator_performance.py
  - tests/conftest.py
  - tests/meta/test_public_surface_guards.py
autonomous: true
requirements:
  - SURF-01
---

<objective>
收口 Phase 11 之后仍残留的 protocol/runtime formal surface 偏差：去掉 REST child dynamic surface、移除 runtime 对 `get_device_list()` compat fallback 的生产依赖，并阻断 orchestrator 继续探测 ghost child surface。
</objective>

<tasks>
<task>把 `custom_components/lipro/core/api/client.py` 中 `LiproRestFacade` 的 `_endpoint_exports` + `__getattr__` / `__dir__` 隐式扩面替换成显式可枚举 surface；必要 collaborator 以显式属性或 formal methods 暴露。</task>
<task>迁移 `custom_components/lipro/core/coordinator/runtime/device/snapshot.py` 与其测试，移除对 `get_device_list()` compat seam 的生产级 fallback，只保留 formal canonical path。</task>
<task>重构 `custom_components/lipro/core/coordinator/orchestrator.py` 及相关测试/benchmark，禁止继续探测 `self.client.status.query_device_status` 等 ghost child surface；统一走 formal protocol contract。</task>
<task>补 `tests/meta/test_public_surface_guards.py` 与 targeted regression，确保 REST child façade、runtime refresh path 与 orchestrator wiring 的 surface truth 可被自动化锁定。</task>
</tasks>

## Verification Criteria

- `LiproRestFacade` formal surface 可静态枚举；
- runtime refresh path 不再依赖 `get_device_list()` compat fallback；
- orchestrator 不再探测 ghost child surface；
- public-surface guards 与 targeted tests 证明新事实成立。

## Must Haves

- no dynamic REST child surface
- no runtime compat fallback in production path
- no ghost child surface probing
- guards and tests reflect explicit contracts

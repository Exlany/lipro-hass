---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "03"
title: governance synchronization and verification closeout
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - docs/developer_architecture.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/phases/11-control-router-formalization-wiring-residual-demotion/11-VERIFICATION.md
autonomous: true
requirements:
  - CTRL-03
---

<objective>
让 governance truth、developer architecture、residual ledger 与代码/测试事实重新一致，并以 targeted + full verification 证明 control-plane formalization 已完成。
</objective>

<tasks>
<task>回写 `docs/developer_architecture.md` 与 `.planning/reviews/FILE_MATRIX.md` / `RESIDUAL_LEDGER.md` / `KILL_LIST.md` / `PUBLIC_SURFACES.md`，明确 `control/service_router.py` 已成为真实 formal router，`services/wiring.py` 只剩 compat shell 或进一步收缩后的过渡面。</task>
<task>运行与 control-plane formalization 相匹配的 targeted regression、architecture guards 与全量静态检查 / 测试；如发现回归，优先修根因而不是扩大 compat。</task>
<task>记录本 phase 的验证结论与后续 delete gate，确保 `services/wiring.py` 不会在后续相位中重新长回正式根。</task>
</tasks>

## Verification Criteria

- 文档、治理资产、代码事实一致；
- architecture guards 与 control-plane slice tests 通过；
- `ruff` / `mypy` / `pytest tests -q` 通过；
- residual delete gate 明确收紧。

## Must Haves

- governance truth matches implementation truth
- verification evidence is explicit
- residual gate is narrower than before
- no reopening of legacy formal root

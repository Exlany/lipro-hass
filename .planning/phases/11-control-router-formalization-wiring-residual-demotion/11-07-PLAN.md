---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "07"
title: firmware update hotspot slimming
wave: 6
depends_on:
  - "05"
files_modified:
  - custom_components/lipro/entities/firmware_update.py
  - custom_components/lipro/core/ota/candidate.py
  - custom_components/lipro/core/ota/rows_cache.py
  - custom_components/lipro/core/ota/row_selector.py
  - tests/core/ota/test_firmware_manifest.py
  - tests/platforms/test_update.py
  - tests/core/test_diagnostics.py
autonomous: true
requirements:
  - ENT-02
---

<objective>
把 `entities/firmware_update.py` 从跨层职责热点收口为平台投影层：实体只负责 HA update surface，OTA candidate / cache / confirmation / arbitration 逻辑进一步下沉到 formal OTA helpers。
</objective>

<tasks>
<task>盘点 `custom_components/lipro/entities/firmware_update.py` 当前混合承担的 OTA candidate、shared cache、manifest arbitration、confirmation window、error observation 等职责，明确哪些应下沉到 `core/ota/*` formal helper cluster。</task>
<task>拆分并迁移实体内的重逻辑，使 update entity 聚焦 platform projection / user interaction / minimal orchestration，不再成为 OTA policy mega-file。</task>
<task>补 firmware/update/diagnostics targeted tests，证明拆分后行为不变、错误可观测性不退化、共享缓存与 certification arbitration 仍保持同一真源。</task>
</tasks>

## Verification Criteria

- firmware update entity 明显变薄；
- OTA policy/candidate/cache/arbitration helpers 有更清晰 formal home；
- targeted tests 证明行为和 diagnostics 语义不退化。

## Must Haves

- slimmer update entity
- OTA policy extracted to formal helpers
- no behavior drift
- diagnostics remain observable

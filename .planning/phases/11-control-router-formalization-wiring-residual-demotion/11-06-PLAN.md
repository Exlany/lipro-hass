---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "06"
title: entity and platform truth convergence
wave: 6
depends_on:
  - "05"
files_modified:
  - custom_components/lipro/switch.py
  - custom_components/lipro/select.py
  - custom_components/lipro/sensor.py
  - custom_components/lipro/update.py
  - custom_components/lipro/helpers/platform.py
  - custom_components/lipro/const/categories.py
  - custom_components/lipro/entities/commands.py
  - custom_components/lipro/entities/descriptors.py
  - tests/platforms/test_switch.py
  - tests/platforms/test_select.py
  - tests/platforms/test_platform_entities_behavior.py
  - tests/entities/test_descriptors.py
autonomous: true
requirements:
  - ENT-01
---

<objective>
把 supplemental entity 暴露规则、unknown-enum 策略与平台写侧特例收口到单一领域真源，修复当前 `hasattr()` 弱判定与平台层重复事实导致的误建模。
</objective>

<tasks>
<task>盘点并收口 `switch.py` / `select.py` / `sensor.py` / `update.py` 的 supplemental entity 暴露规则，为其建立单一领域真源（可位于 formal helper / const / domain projection home），移除各平台各写一套事实的状态。</task>
<task>修复 `custom_components/lipro/switch.py` 中基于 `hasattr()` 的弱判定，确保 plain light / panel 等设备不会错误暴露 supplemental switches。</task>
<task>为 unknown supplier enums 建立显式且可观测的处理策略，避免继续静默回退默认 UI 值；必要时补 diagnostics / logging / fallback sentinel。</task>
<task>收口平台写侧特例（如 `select.py` 对 coordinator listener 的直戳），优先通过 formal command / refresh / sync seam 表达；补负向与语义测试。</task>
</tasks>

## Verification Criteria

- supplemental entity rules 有单一正式真源；
- `hasattr()` 弱判定不再导致误建模；
- unknown enums 不再静默坍缩；
- 平台写侧特例减少，测试覆盖负向矩阵。

## Must Haves

- one truth for supplemental entity exposure
- no weak hasattr-based mis-modeling
- observable unknown-enum handling
- platform writes use formal seams

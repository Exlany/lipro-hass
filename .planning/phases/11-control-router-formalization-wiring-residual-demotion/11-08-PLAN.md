---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "08"
title: governance release and open-source coherence
wave: 7
depends_on:
  - "04"
  - "05"
  - "06"
  - "07"
files_modified:
  - docs/README.md
  - AGENTS.md
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/ISSUE_TEMPLATE/bug.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/pull_request_template.md
  - CONTRIBUTING.md
  - tests/meta/test_governance_guards.py
  - tests/meta/test_version_sync.py
  - SECURITY.md
autonomous: true
requirements:
  - GOV-08
---

<objective>
统一 phase 资产身份、发布门禁、贡献者口径与安全披露，让治理/文档/CI/release 对外说同一种话，并把 meta guards 从高脆弱文本匹配进一步收敛到结构级断言。
</objective>

<tasks>
<task>裁决 `.planning/phases/**` 在 `docs/README.md`、`AGENTS.md` 与 governance guards 中的正式身份，消除“本地痕迹 vs CI 强制证据”冲突。</task>
<task>让 `release.yml` 复用或串接 CI / governance / version-sync 的最小充分门禁，阻断旁路发版；同步修正 issue template 的 HA 版本口径。</task>
<task>统一 `CONTRIBUTING.md`、PR 模板、CI 对 benchmark / full test / smoke test 的契约；必要时引入 marker 或显式排除策略，而不是文档各说各话。</task>
<task>继续把 `tests/meta/test_governance_guards.py` 从文案级断言降到结构级 / invariant 级断言；补 `SECURITY.md` 与 private disclosure path。</task>
</tasks>

## Verification Criteria

- phase 资产身份有单一口径；
- release 不再绕过 CI/governance/version checks；
- issue / contributing / PR template / CI 说同一种测试与版本语言；
- governance guards 较少依赖整句文本；
- 安全披露入口明确存在。

## Must Haves

- one governance story across docs and workflows
- release reuses validation gates
- contributor-facing guidance matches CI
- security disclosure path exists

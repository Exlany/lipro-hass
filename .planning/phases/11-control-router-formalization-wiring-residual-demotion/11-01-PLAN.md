---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "01"
title: formal router implementation inversion
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/control/service_router.py
  - custom_components/lipro/services/registrations.py
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
---

<objective>
让 `custom_components/lipro/control/service_router.py` 真正承载 HA service callback 的实现逻辑，使 control-plane formal home 与 implementation home 合一，同时不改变服务行为、签名与响应结构。
</objective>

<tasks>
<task>把 `custom_components/lipro/services/wiring.py` 中的主要 helper、developer gating、service callback wrapper 与导出面迁入 `custom_components/lipro/control/service_router.py`，保留现有 service 行为与协作者装配方式。</task>
<task>确保迁入后的 formal router 只依赖 `services` 下叶子协作者模块，而不是继续回指 `services/wiring.py`，避免 control-plane formal root 仍受 legacy carrier 反向定义。</task>
<task>确认 `custom_components/lipro/services/registrations.py` 继续绑定 control-plane router handlers；若存在 naming / alias 需要整理，必须保持 schema、supports_response 与服务名不漂移。</task>
</tasks>

## Verification Criteria

- `control/service_router.py` 不再从 `services/wiring.py` 导入主要实现；
- `services/registrations.py` 继续通过 control-plane router 绑定 handlers；
- public / developer services 的行为与返回结构不变。

## Must Haves

- formal router owns real implementation
- no reverse dependency on legacy carrier
- registrations stay bound to control plane
- no service contract drift

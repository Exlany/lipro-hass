---
phase: 11-control-router-formalization-wiring-residual-demotion
plan: "02"
title: legacy wiring demotion and test truth alignment
wave: 2
depends_on:
  - "01"
files_modified:
  - custom_components/lipro/services/wiring.py
  - tests/core/test_init.py
  - tests/services/test_service_resilience.py
  - tests/services/test_services_registry.py
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
---

<objective>
把 `custom_components/lipro/services/wiring.py` 降为极薄 compat shell，并让测试改为验证 formal router truth，而不是继续把 legacy carrier 当作 patch-first 真源。
</objective>

<tasks>
<task>将 `custom_components/lipro/services/wiring.py` 缩减为显式 compat re-export shell；不得继续保留主要 orchestration、helper 闭包或正式 ownership。</task>
<task>迁移 `tests/core/test_init.py`、`tests/services/test_service_resilience.py` 以及其他相关测试的 import / patch 路径到 `custom_components.lipro.control.service_router`，让测试与 formal control-plane 叙事一致。</task>
<task>如需要，补充或调整 targeted assertions，证明 legacy shell 仅剩 compat 角色，而 service registry / runtime behavior 仍经 formal router 运转。</task>
</tasks>

## Verification Criteria

- `services/wiring.py` 仅剩极薄 compat seam；
- 仓库内测试不再依赖 `services/wiring.py` 作为主 patch 面；
- targeted service/control tests 继续通过。

## Must Haves

- wiring reduced to compat-only shell
- tests target formal router seams
- no behavior regression
- no test-driven architecture backslide

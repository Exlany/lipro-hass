---
phase: 111-entity-runtime-boundary-sealing-and-dependency-guard-hardening
plan: "01"
type: execute
title: replace concrete entity coordinator binding with sanctioned runtime surface
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/entities/base.py
  - custom_components/lipro/runtime_types.py
  - tests/platforms/test_entity_base.py
  - tests/platforms/test_firmware_update_entity_edges.py
  - .planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-01-SUMMARY.md
autonomous: true
requirements:
  - ARC-28
must_haves:
  truths:
    - entity adapters stop importing or casting concrete `Coordinator`; HA base typing becomes one local bridge rather than a concrete runtime dependency
    - `LiproRuntimeCoordinator` remains the minimal runtime verb surface for entity interactions, without inflating outward public surface or reintroducing leaked service handles
    - runtime behavior for command dispatch, optimistic state, entity registration, refresh, and OTA info remains unchanged to outward callers
  artifacts:
    - custom_components/lipro/entities/base.py
    - custom_components/lipro/runtime_types.py
    - tests/platforms/test_entity_base.py
    - tests/platforms/test_firmware_update_entity_edges.py
    - .planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-01-SUMMARY.md
  key_links:
    - `entities/base.py` -> `runtime_types.py` minimal verb contract stays authoritative
    - `firmware_update.py` OTA path continues through `async_query_device_ota_info()`
    - no new wrapper, compat shell, or second runtime root is introduced
---

# Plan 111-01: replace concrete entity coordinator binding with sanctioned runtime surface

<objective>
把 `custom_components/lipro/entities/base.py` 对 concrete `Coordinator` 的 import / generic binding / cast 退回更局部的 typed bridge，同时保持 entity 与 runtime 的正式协作仍只通过 `LiproRuntimeCoordinator` 暴露的最小 verbs 完成。

Purpose: 落实 `ARC-28`，消除当前最直接的 north-star 违规点，而不改变 entity outward behavior。
Output: 更诚实的 entity typed bridge、保持最小 runtime verbs 的 `runtime_types.py`，以及覆盖这个桥接变更的 focused entity tests。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-CONTEXT.md
@.planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-RESEARCH.md
@custom_components/lipro/entities/base.py
@custom_components/lipro/runtime_types.py
@custom_components/lipro/entities/firmware_update.py
@tests/platforms/test_entity_base.py
@tests/platforms/test_firmware_update_entity_edges.py

Decision trace for this phase:
- `D-01`: entity adapters only depend on runtime public surface / control contracts.
- `D-02`: HA `CoordinatorEntity` typing must be satisfied via a local typed bridge, not via concrete runtime root imports.
- `D-03`: `LiproRuntimeCoordinator` remains the authoritative minimal runtime verb surface.
- `D-04`: no compat shell, no second root, no leaked service-handle comeback.
</context>

<tasks>
<task type="auto">
  <name>Task 111-01-01: remove concrete `Coordinator` import/generic binding from `entities/base.py` per D-01/D-02</name>
  <files>custom_components/lipro/entities/base.py, custom_components/lipro/runtime_types.py</files>
  <read_first>
    - custom_components/lipro/entities/base.py
    - custom_components/lipro/runtime_types.py
    - custom_components/lipro/entities/firmware_update.py
    - .planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-CONTEXT.md
    - .planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-RESEARCH.md
  </read_first>
  <action>把 `custom_components/lipro/entities/base.py` 中 `core.coordinator.coordinator.Coordinator` 的 direct import、`CoordinatorEntity[Coordinator]` 绑定与 `cast(Coordinator, coordinator)` 移除，改为本地 typed bridge（例如基于 `DataUpdateCoordinator[...]` 或等价最小 HA typed base）来满足 HA 基类要求。只保留 `LiproRuntimeCoordinator` 作为 entity 的正式 runtime 协作面；必要时在 `runtime_types.py` 微调 protocol / alias，使 entity 所需 verbs 更清晰，但不要暴露 `command_service`、`protocol_service`、`get_device_lock` 等 leaked handles。</action>
  <acceptance_criteria>
    - `entities/base.py` 不再 import concrete `Coordinator`
    - entity generic / cast 不再把 concrete runtime root 暴露给 adapter 层
    - `runtime_types.py` 仍以最小 verbs 表达 entity 所需 surface，而不是扩大 outward contract
    - `firmware_update.py` 与其他 entity consumers 无需回退到 leaked service handles 才能工作
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/platforms/test_entity_base.py tests/platforms/test_firmware_update_entity_edges.py</automated>
  </verify>
  <done>entity typed bridge 已脱离 concrete runtime root，最小 runtime verbs 仍然稳定可用。</done>
</task>

<task type="auto">
  <name>Task 111-01-02: prove entity behavior stays stable after the typed bridge change per D-03/D-04</name>
  <files>tests/platforms/test_entity_base.py, tests/platforms/test_firmware_update_entity_edges.py</files>
  <read_first>
    - tests/platforms/test_entity_base.py
    - tests/platforms/test_firmware_update_entity_edges.py
    - custom_components/lipro/entities/base.py
    - custom_components/lipro/entities/firmware_update.py
    - custom_components/lipro/runtime_types.py
  </read_first>
  <action>补足或调整 focused entity tests，证明 command dispatch、optimistic state、refresh fallback、entity register/unregister、OTA-related interactions 在 typed bridge 收口后保持 outward behavior 不变。若测试中仍隐式依赖 concrete coordinator typing，改为最小 runtime verb contract，而不是重新引入 concrete import。</action>
  <acceptance_criteria>
    - entity tests 不再把 concrete `Coordinator` 当作行为前提
    - command / optimistic-state / refresh / OTA paths 的 outward behavior 与既有 contract 一致
    - 没有任何测试通过“重新暴露 concrete runtime root”来达成绿灯
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/platforms/test_entity_base.py tests/platforms/test_firmware_update_entity_edges.py tests/platforms/test_update_install_flow.py</automated>
  </verify>
  <done>entity outward behavior 被 focused tests 冻结，bridge change 不会把违规 concrete binding 带回来。</done>
</task>
</tasks>

<verification>
- `uv run pytest -q tests/platforms/test_entity_base.py tests/platforms/test_firmware_update_entity_edges.py tests/platforms/test_update_install_flow.py`
- `uv run ruff check custom_components/lipro/entities/base.py custom_components/lipro/runtime_types.py tests/platforms/test_entity_base.py tests/platforms/test_firmware_update_entity_edges.py tests/platforms/test_update_install_flow.py`
- `uv run mypy custom_components/lipro/entities/base.py custom_components/lipro/runtime_types.py custom_components/lipro/entities/firmware_update.py`
</verification>

<success_criteria>
- entity adapters no longer import or cast the concrete runtime coordinator.
- HA base typing is satisfied by a local typed bridge, not by re-exposing runtime internals.
- entity runtime verbs and OTA-specific capability remain stable to outward consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/111-entity-runtime-boundary-sealing-and-dependency-guard-hardening/111-01-SUMMARY.md`.
</output>

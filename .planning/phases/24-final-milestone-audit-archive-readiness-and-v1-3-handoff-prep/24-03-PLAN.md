---
phase: 24-final-milestone-audit-archive-readiness-and-v1-3-handoff-prep
plan: "03"
type: execute
title: write v1.3 handoff and transition lifecycle truth
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - .planning/v1.3-HANDOFF.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - GOV-18
must_haves:
  truths:
    - v1.3 handoff 必须是显式资产，而不是只有一行 next command
    - deferred debt / no-return zones / next-phase seed 必须清楚列出
    - lifecycle docs 必须准确反映 v1.2 closeout 与 v1.3 handoff-ready 状态
  artifacts:
    - .planning/v1.3-HANDOFF.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - milestone audit -> handoff doc -> next milestone seed -> lifecycle truth
---

# Plan 24-03: 写清 v1.3 handoff 与 lifecycle transition truth

<preconditions>
- `24-01` 与 `24-02` 已交付 final audit 与 archive-ready bundle。
- 本 plan 不再做 final implementation fixes，只写 handoff/transition truth。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/MILESTONES.md
@.planning/reviews/V1_2_EVIDENCE_INDEX.md
@.planning/phases/24-final-milestone-audit-archive-readiness-and-v1-3-handoff-prep/24-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: write explicit v1.3 handoff doc and next-phase seed</name>
  <files>.planning/v1.3-HANDOFF.md</files>
  <action>编写 `v1.3-HANDOFF.md`：明确记录 defer 到下一轮的 debt、禁止回流点、推荐的第一相位 seed、进入下一里程碑前必须维持的北极星约束，以及任何已接受风险的边界。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`</verify>
  <done>下一轮维护者可以直接据此启动工作，而不是依赖对话历史或 phase 目录残影。</done>
</task>

<task type="auto">
  <name>Task 2: sync lifecycle docs to the new post-v1.2 state</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>把 lifecycle docs 切换到 `v1.2 complete / archive-ready / handoff-ready` 的正式状态，并为下一里程碑留下清晰 next-step；如果尚未立即 archive，也必须把“为何仍未 archive”写清，而不是维持模糊 active 状态。</action>
  <verify>`uv run python scripts/check_architecture_policy.py --check && uv run python scripts/check_file_matrix.py --check && uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`</verify>
  <done>生命周期真相与 handoff doc 完整对齐，不再残留旧 milestone 执行态假象。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`
- [ ] `uv run python scripts/check_architecture_policy.py --check && uv run python scripts/check_file_matrix.py --check`
- [ ] `uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`
- [ ] `uv run ruff check .`
- [ ] `uv run mypy`
</verification>

<success_criteria>
- `GOV-18` 的 handoff / transition 部分完成。
- `v1.3-HANDOFF.md` 与 lifecycle docs 对同一 transition state 讲同一条故事。
- 下一轮不会再携带 silent defer 或模糊 next-step。
</success_criteria>

<output>
After completion, create `.planning/phases/24-final-milestone-audit-archive-readiness-and-v1-3-handoff-prep/24-03-SUMMARY.md`
</output>

---
phase: 42-delivery-trust-gates-and-validation-hardening
plan: "02"
type: execute
title: add release artifact install smoke and tagged-python parity
wave: 2
depends_on:
  - 42-01
files_modified:
  - .github/workflows/release.yml
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - tests/meta/test_governance_release_contract.py
  - tests/meta/test_version_sync.py
autonomous: true
requirements:
  - QLT-12
must_haves:
  truths:
    - tagged release validation must verify the distributed artifact, not just the source tree
    - release security gate must use explicit Python setup consistent with the rest of the repo's workflow story
    - runbook and tests must describe the same release smoke contract
  artifacts:
    - .github/workflows/release.yml
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_governance_release_contract.py
    - tests/meta/test_version_sync.py
  key_links:
    - release workflow -> install smoke -> runbook -> release/version guards
---

# Plan 42-02: add release artifact install smoke and tagged-Python parity

<context>
@.planning/phases/42-delivery-trust-gates-and-validation-hardening/42-CONTEXT.md
@.github/workflows/release.yml
@install.sh
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@tests/meta/test_governance_release_contract.py
@tests/meta/test_version_sync.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: add a tagged release smoke path that installs the release artifact</name>
  <files>.github/workflows/release.yml, docs/MAINTAINER_RELEASE_RUNBOOK.md, tests/meta/test_governance_release_contract.py, tests/meta/test_version_sync.py</files>
  <action>在 release workflow 中加入对 release zip + `install.sh` 的真实 artifact install / uninstall smoke，并让 `security_gate` 显式 `setup-python`；同时把 runbook 和守卫更新为“发布产物经过真实安装验证”的正式合同。</action>
  <acceptance_criteria>
    - release workflow sets up Python explicitly before tagged security validation
    - release workflow proves the distributed install path works against a temporary Home Assistant-style target tree
    - runbook and governance tests document and enforce the artifact install smoke contract
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_release_contract.py tests/meta/test_version_sync.py`</verify>
  <done>The tagged release workflow now validates the actual release artifact install path and keeps Python setup truth aligned.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_release_contract.py tests/meta/test_version_sync.py`
</verification>

# Plan 106-02

## Goal

收窄两个继续增稠的生产热点：状态批处理 orchestration 与 options flow schema composition。

## Tasks

1. 将 `query_device_status()` 拆成 task builder / batch executor / result merger 三段
2. 保持 batch concurrency、adaptive batch size、metric callback 行为不变
3. 将 options flow 的 schema field 组装提升为 reusable helpers + spec tuples
4. 通过 focused tests 冻结 advanced schema defaults、batch split 与 merge 行为

## Done Condition

- `query_device_status()` 与 `_build_advanced_schema()` 的职责更单一
- 行为无回归
- 不新增 helper-owned second story

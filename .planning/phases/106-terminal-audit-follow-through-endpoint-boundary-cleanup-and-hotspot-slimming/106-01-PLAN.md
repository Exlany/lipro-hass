# Plan 106-01

## Goal

去掉 REST endpoint adapter 对私有 auth collaborator 的穿透依赖，并同步修正文档术语漂移。

## Tasks

1. 为 `custom_components/lipro/core/api/rest_facade.py` 增加显式 `auth_api` property
2. 让 `custom_components/lipro/core/api/endpoints/payloads.py` 通过 `auth_api` surface 访问 auth collaborator
3. 删除这一处私有字段穿透与相关 `SLF001` 豁免
4. 更新 `docs/adr/0001-coordinator-as-single-orchestration-root.md` 的 `Client` 术语
5. 补一个 focused test，冻结 `LiproRestFacade` 的 sanctioned auth surface

## Done Condition

- endpoint adapter 不再读取 `client._auth_api`
- outward contract 不变
- focused API + doc tests 通过

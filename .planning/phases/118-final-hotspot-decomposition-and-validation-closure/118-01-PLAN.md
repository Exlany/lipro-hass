---
phase: 118-final-hotspot-decomposition-and-validation-closure
plan: "01"
type: execute
title: resync route truth to the live phase118 execution path
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - tests/meta/governance_current_truth.py
  - tests/meta/test_governance_route_handoff_smoke.py
  - tests/meta/governance_followup_route_current_milestones.py
autonomous: true
requirements:
  - GOV-73
must_haves:
  truths:
    - planning selector family and focused governance tests all agree that v1.32 is still active and phase118 is now the live execution path
    - roadmap stops treating phase118 as a tail orphan and instead places it inside the v1.32 route with real goal, plan count, and next-step wording
    - the live default next command points to phase118 execution rather than prematurely jumping to milestone closeout
  artifacts:
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/MILESTONES.md
    - tests/meta/governance_current_truth.py
    - tests/meta/test_governance_route_handoff_smoke.py
    - tests/meta/governance_followup_route_current_milestones.py
---

# Plan 118-01: resync route truth to the live phase118 execution path

<objective>
把 `Phase 118` 从“`Phase 117 complete; closeout-ready` + `ROADMAP` 尾部孤儿占位”的 stale live story 拉回 `v1.32` 正式 active route，并把默认下一步切换到 `Phase 118` 执行而不是直接 milestone closeout。

Purpose: 继续兑现 `GOV-73` 的 single-source route truth，但这次只做 `Phase 118` 激活与 selector 对齐，不改写 `v1.31` archived facts。
Output: 对齐后的 route-contract docs、phase118 正式落位的 `ROADMAP` 条目，以及与之匹配的 focused governance guards。
</objective>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/phases/118-final-hotspot-decomposition-and-validation-closure/118-CONTEXT.md
@.planning/phases/118-final-hotspot-decomposition-and-validation-closure/118-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONES.md
@tests/meta/governance_current_truth.py
@tests/meta/test_governance_route_handoff_smoke.py
@tests/meta/governance_followup_route_current_milestones.py
</context>

<tasks>
<task type="auto">
  <name>Task 118-01-01: repair the v1.32 route contract and phase118 roadmap placement</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/MILESTONES.md</files>
  <read_first>.planning/phases/118-final-hotspot-decomposition-and-validation-closure/118-CONTEXT.md, .planning/phases/118-final-hotspot-decomposition-and-validation-closure/118-RESEARCH.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/MILESTONES.md</read_first>
  <action>按 `D-01` 修复 live selector truth：把 `ROADMAP.md` 中错误追加在文件尾部的 `Phase 118` 拉回 `v1.32` 段内，统一 `PROJECT/ROADMAP/REQUIREMENTS/STATE/MILESTONES` 的 machine-readable contract、human-readable current route、phase counters、current focus、default next command 与 progress wording 到 `active / phase 118 execution-ready (2026-04-01)` + `$gsd-execute-phase 118`；保留 `115 -> 117` 已完成历史与 `v1.31` latest archived baseline 身份，不把 archived closeout story 改写成 active truth，也不凭空发明新的 milestone basket。</action>
  <verify>`rg -n 'active / phase 118 execution-ready \(2026-04-01\)|\$gsd-execute-phase 118|### Phase 118: Final hotspot decomposition and validation closure' .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/MILESTONES.md` returns the live phase118 markers in every owned planning doc</verify>
  <acceptance_criteria>
    - no owned planning doc still presents `Phase 117 complete; closeout-ready` as the live route
    - `ROADMAP.md` shows phase118 inside the `v1.32` section with real goal, requirements, and three-plan breakdown placeholder ready to execute
    - current focus, phase counters, and default next command all point at executing phase118 instead of closing out the milestone early
  </acceptance_criteria>
  <done>The planning docs become a single, phase118-centered live route story again.</done>
</task>

<task type="auto">
  <name>Task 118-01-02: re-freeze the machine-readable governance guards around phase118</name>
  <files>tests/meta/governance_current_truth.py, tests/meta/test_governance_route_handoff_smoke.py, tests/meta/governance_followup_route_current_milestones.py</files>
  <read_first>tests/meta/governance_current_truth.py, tests/meta/test_governance_route_handoff_smoke.py, tests/meta/governance_followup_route_current_milestones.py, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/MILESTONES.md</read_first>
  <action>按 `D-01` 更新 focused governance tests 与 shared route constants：`PLANNING_ROUTE_CONTRACT`、smoke handoff assertions、follow-up current-milestone expectations 都必须只承认 `Phase 118` 的 live execution story，并显式拒绝 stale 的 `$gsd-complete-milestone v1.32` / `phase 117 closeout-ready` 作为当前下一步；同时保留 `v1.31` latest archived pointer 与 historical archive markers 为 pull-only archived truth。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_route_handoff_smoke.py tests/meta/governance_followup_route_current_milestones.py` passes</verify>
  <acceptance_criteria>
    - governance constants and focused tests assert the same phase118 execution-ready contract
    - stale phase117 live-route wording causes the updated tests to fail instead of passing silently
    - the archived pointer remains `v1.31`, with no second active route or rewritten history introduced
  </acceptance_criteria>
  <done>Focused governance guards now enforce the phase118 live route instead of the stale phase117 closeout story.</done>
</task>
</tasks>

---
phase: 121-residual-contract-closure-flow-invariant-tightening-surface-hygiene-cleanup
plan: "02"
type: execute
title: fail closed on malformed auth-session projections and converge existing-entry flow validation
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/flow/login.py
  - custom_components/lipro/flow/submission.py
  - custom_components/lipro/config_flow.py
  - tests/flows/test_flow_submission.py
  - tests/flows/test_config_flow_user.py
  - tests/flows/test_config_flow_reauth.py
  - tests/flows/test_config_flow_reconfigure.py
autonomous: true
requirements:
  - QLT-48
  - HOT-53
  - TST-43
must_haves:
  truths:
    - malformed auth-session payloads no longer silently become empty config-entry fields
    - user, reauth, and reconfigure flows still surface the same outward error taxonomy while rejecting malformed projections honestly
    - existing-entry validators share one explicit invariant path instead of parallel reauth/reconfigure branches
  artifacts:
    - custom_components/lipro/flow/login.py
    - custom_components/lipro/flow/submission.py
    - custom_components/lipro/config_flow.py
    - tests/flows/test_flow_submission.py
  key_links:
    - config-flow entry-data projection catches malformed auth sessions exactly once and maps them to `invalid_response`
    - reauth/reconfigure validators share one helper-owned invariant path while preserving existing field/base error keys
    - focused flow tests freeze both malformed-session honesty and validator convergence
---

# Plan 121-02: fail closed on malformed auth-session projections and converge existing-entry flow validation

<objective>
让 config-flow 在 auth/session 投影边界上停止 silent fallback，并把 `reauth` / `reconfigure` existing-entry validator 收口为单一 invariant path，同时保持 outward flow taxonomy 稳定。

Purpose: 落实 `QLT-48` / `HOT-53` / `TST-43`，把 flow family 从“语义已基本正确”推进到“projection / invariant 也一致且 fail-closed”。
Output: 更新后的 `flow/login.py`、`flow/submission.py`、`config_flow.py` 与 focused flow regressions。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/121-residual-contract-closure-flow-invariant-tightening-surface-hygiene-cleanup/121-CONTEXT.md
@.planning/phases/121-residual-contract-closure-flow-invariant-tightening-surface-hygiene-cleanup/121-RESEARCH.md
@custom_components/lipro/flow/login.py
@custom_components/lipro/flow/submission.py
@custom_components/lipro/config_flow.py
@tests/flows/test_flow_submission.py
@tests/flows/test_config_flow_user.py
@tests/flows/test_config_flow_reauth.py
@tests/flows/test_config_flow_reconfigure.py
</context>

<mapping>
<decisions>
- D-03: malformed auth/session payloads must fail honestly; no empty-string / `0` default projection may persist.
- D-04: `reauth` / `reconfigure` existing-entry validation must share one explicit invariant path while preserving outward error semantics.
</decisions>
<north_star>
- Rule 3 / 边界归一化优于原始形态穿透：auth/session projection must validate before entering config-entry data.
- Rule 6 / 可验证、可仲裁：flow tests must prove malformed sessions become `invalid_response`, not silent defaults.
</north_star>
<requirements>
- `QLT-48`: malformed auth/session payloads are rejected at the projection edge and mapped consistently.
- `HOT-53`: existing-entry validator duplication in `flow/submission.py` is converged to one explicit helper-owned invariant path.
- `TST-43`: focused flow regressions freeze malformed-session honesty and validator convergence.
</requirements>
</mapping>

<tasks>

<task type="auto" tdd="true">
  <name>Task 121-02-01: harden config-entry projection against malformed auth sessions</name>
  <files>custom_components/lipro/flow/login.py, custom_components/lipro/config_flow.py, tests/flows/test_config_flow_user.py, tests/flows/test_config_flow_reauth.py, tests/flows/test_config_flow_reconfigure.py</files>
  <behavior>
    - missing/empty access tokens, refresh tokens, or invalid user IDs no longer project to empty strings / `0`
    - malformed auth-session projection failures map to `invalid_response` across user / reauth / reconfigure
    - successful flows keep their existing outward behavior and entry-data shape
  </behavior>
  <action>按 D-03 执行：把 required auth/session field validation 收到 `ConfigEntryLoginProjection` 或等价 shared helper 中，缺失值时显式失败；在 `config_flow.py` 统一捕获这类 projection failure 并映射为 `invalid_response`，而不是让 malformed session silently project into entry data。同步补齐 user / reauth / reconfigure focused regressions，证明 malformed projection 与 real success path 都保持一致。</action>
  <verify>
    <automated>uv run pytest tests/flows/test_config_flow_user.py tests/flows/test_config_flow_reauth.py tests/flows/test_config_flow_reconfigure.py -q</automated>
  </verify>
  <done>config-flow entry-data projection fails closed on malformed auth sessions and still surfaces stable outward flow errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 121-02-02: converge existing-entry submission invariants into one helper path</name>
  <files>custom_components/lipro/flow/submission.py, tests/flows/test_flow_submission.py, tests/flows/test_config_flow_reauth.py, tests/flows/test_config_flow_reconfigure.py</files>
  <behavior>
    - reauth/reconfigure reuse one helper-owned path for phone_id resolution, remember-password defaults, password validation, and `ExistingEntrySubmission` assembly
    - reauth still uses stored phone while reconfigure still validates phone from user input
    - outward field/base error keys remain unchanged
  </behavior>
  <action>按 D-04 执行：重构 `flow/submission.py`，把 `validate_reauth_submission()` 与 `validate_reconfigure_submission()` 共用的 existing-entry invariants 收敛到一个明确 helper；只保留 phone source 的差异化输入。同步更新 `tests/flows/test_flow_submission.py` 与相关 flow tests，冻结 convergence 后的 outward behavior，不得因为去重而重写 translation/error contract。</action>
  <verify>
    <automated>uv run pytest tests/flows/test_flow_submission.py tests/flows/test_config_flow_reauth.py tests/flows/test_config_flow_reconfigure.py -q</automated>
  </verify>
  <done>existing-entry flow validation has one invariant path, while reauth/reconfigure semantics stay stable.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/flows/test_flow_submission.py tests/flows/test_config_flow_user.py tests/flows/test_config_flow_reauth.py tests/flows/test_config_flow_reconfigure.py -q`
- malformed auth sessions become `invalid_response`, not silent empty-string / `0` projections
- reauth/reconfigure validation share one helper-owned invariant path without breaking outward error semantics
</verification>

<success_criteria>
- `QLT-48` is satisfied through fail-closed auth-session projection
- `HOT-53` is satisfied through converged existing-entry validation
- `TST-43` is satisfied for the flow slice via focused regression coverage
</success_criteria>

<output>
After completion, create `.planning/phases/121-residual-contract-closure-flow-invariant-tightening-surface-hygiene-cleanup/121-02-SUMMARY.md`
</output>

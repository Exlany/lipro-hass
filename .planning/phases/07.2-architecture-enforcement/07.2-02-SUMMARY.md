---
phase: 07.2-architecture-enforcement
plan: "02"
status: completed
completed: 2026-03-13
requirements:
  - ENF-01
  - ENF-02
---

# Summary 07.2-02

## Outcome

- 新增 `tests/helpers/architecture_policy.py` 与 `tests/helpers/ast_guard_utils.py`，把 policy 解析与 AST/import/export/property 工具收口为 shared helper，消除了 meta guard 之间的重复实现。
- 新增 `scripts/check_architecture_policy.py` 作为 local-fast / CI-fail-fast 的第一层架构门禁，使 plane/root/surface/authority 偏航能在功能测试前被明确阻断。
- `tests/meta/test_dependency_guards.py`、`tests/meta/test_public_surface_guards.py`、`tests/meta/test_governance_guards.py` 已改为消费 shared policy rule ids，而不是继续各自维护散落常量。
- `.github/workflows/ci.yml` 的 `governance` job 已升级为真正的 fail-fast 顺序：先 `architecture policy`，再 `file matrix`，最后 focused meta guards。
- `FILE_MATRIX.md` 已重生成并收录本 phase 新增的 3 个 Python 文件，总 Python 文件数现为 `412`。

## Verification

- `uv run ruff check scripts/check_architecture_policy.py tests/helpers/architecture_policy.py tests/helpers/ast_guard_utils.py tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py`
- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest -q -x tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py`

## Governance Notes

- `scripts/check_file_matrix.py` 继续只负责 inventory / active-doc / governance drift；architecture checks 已独立到 `scripts/check_architecture_policy.py`，避免 mega-checker 回归。
- `tests/helpers/architecture_policy.py`、`tests/helpers/ast_guard_utils.py`、`scripts/check_architecture_policy.py` 已登记到 `FILE_MATRIX.md`，owner phase 为 `Phase 7.2`。
- `07.3 / 07.4` 若需要新增 observer-only / assurance-only 例外，必须先改 `ARCHITECTURE_POLICY.md` 与 baseline docs，再改 helper / tests / script。

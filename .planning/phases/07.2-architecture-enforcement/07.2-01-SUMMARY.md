---
phase: 07.2-architecture-enforcement
plan: "01"
status: completed
completed: 2026-03-13
requirements:
  - ENF-01
  - ENF-02
---

# Summary 07.2-01

## Outcome

- 新增 `.planning/baseline/ARCHITECTURE_POLICY.md`，正式把 `plane / root / surface / boundary-local / raw-payload / compat / authority / backdoor` 八类架构规则收口为单一 baseline truth。
- `DEPENDENCY_MATRIX.md`、`PUBLIC_SURFACES.md`、`AUTHORITY_MATRIX.md`、`VERIFICATION_MATRIX.md` 已同步回写，不再各自维持一套 seed 叙事；`07.2` 起 helper / script / tests / CI 都从同一 policy baseline 派生。
- 明确区分了 `Structural Rules` 与 `Targeted Regression Bans`：前者承载长期架构边界，后者只阻断已知回归洞位，不再混在一起偷长成第二套架构。
- `07.3 telemetry exporter` 与 `07.4 replay harness` 的 extension hooks 已正式落表：分别以 `observer-only surface` 与 `assurance-only boundary consumer` 的形式挂入既有 taxonomy。

## Verification

- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest -q -x tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py`

## Governance Notes

- 本计划**无新增 residual family**；只是把 architecture enforcement 从散落规则升级为 baseline truth。
- `RESIDUAL_LEDGER.md` 已新增 `Phase 07.2` delta，明确未来 exporter / replay 的例外必须先回写 policy，再进入 allowlist。
- `KILL_LIST.md` 本轮无变化：未新增删除裁决，也未改变既有 delete gate。

---
phase: 07.2-architecture-enforcement
plan: "02"
title: 升级 meta guards、CI gates 与开发期快速校验链
wave: 2
depends_on:
  - 07.2-01
files_modified:
  - scripts/check_architecture_policy.py
  - scripts/check_file_matrix.py
  - tests/helpers/architecture_policy.py
  - tests/helpers/ast_guard_utils.py
  - tests/meta/test_dependency_guards.py
  - tests/meta/test_public_surface_guards.py
  - tests/meta/test_governance_guards.py
  - .github/workflows/ci.yml
  - .planning/baseline/VERIFICATION_MATRIX.md
autonomous: true
requirements:
  - ENF-01
  - ENF-02
---

<objective>
把 `07.2-01` 定义的 policy baseline 接到 developer-local 与 CI-governance 的执行链中，使 plane/root/surface/authority 偏航能被快速、明确、可复用地阻断。
</objective>

<tasks>
<task>新增 `tests/helpers/architecture_policy.py` 与 `tests/helpers/ast_guard_utils.py`，把 policy 读取与 AST/import/export/property 小工具统一收口，消除 guard 之间的重复逻辑。</task>
<task>新增 `scripts/check_architecture_policy.py` 作为 local-fast / CI-fail-fast 第一层门禁，专管结构规则，不与 file-matrix inventory 职责混杂。</task>
<task>重构 `test_dependency_guards.py` 与 `test_public_surface_guards.py`，使其从 shared policy helper 派生核心规则，只保留少量 targeted regression bans。</task>
<task>保持 `test_governance_guards.py` 作为薄封装回归壳，同时确保 `scripts/check_file_matrix.py` 继续专注 inventory / active-doc / governance drift，而不是演化成 mega-checker。</task>
<task>升级 `.github/workflows/ci.yml` 的 `governance` job 顺序：先跑 `check_architecture_policy.py`，再跑 `check_file_matrix.py --check`，最后跑 focused meta guards，形成真正的 fail-fast 链。</task>
<task>锁定本地最小充分校验命令，并把 `07.3 / 07.4` 所需的 observer-only / assurance-only extension path 保留在 shared policy 入口中。</task>
</tasks>

## Verification Criteria

- `uv run python scripts/check_architecture_policy.py --check` 可独立完成结构快检；
- meta guards 已消费 shared policy，而不是继续各自维护私有规则表；
- governance CI 先于功能测试 fail-fast；
- 新增 exporter / replay 组件只需登记到既有 taxonomy，而无需改写 enforcement 机制。

## Must Haves

- one policy chain for scripts and tests
- local-fast before functional suites
- CI fail-fast ordering
- no mega-checker / no second truth

---
phase: 66-release-target-fidelity-adapter-root-cleanup-and-focused-protocol-coverage-hardening
plan: "01"
type: execute
title: align release-target validation with tagged refs and remove stale current-story install/governance drift
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - README.md
  - README_zh.md
  - docs/README.md
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/AUTHORITY_MATRIX.md
  - .planning/reviews/README.md
  - tests/meta/test_governance_release_contract.py
autonomous: true
requirements:
  - GOV-50
  - OSS-07
  - HOT-22
must_haves:
  truths:
    - tagged release validation and tagged release build/publish use the same release ref story
    - public install examples no longer encode one stale literal tag as canonical truth
    - baseline/review/docs active-route and freshness notes agree on the same current story
  artifacts:
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - README.md
    - README_zh.md
    - docs/README.md
    - .planning/baseline/PUBLIC_SURFACES.md
    - .planning/baseline/AUTHORITY_MATRIX.md
    - .planning/reviews/README.md
    - tests/meta/test_governance_release_contract.py
  key_links:
    - reusable CI ref input -> release validate/build same target
    - freshness-safe release examples -> no stale public install story
    - baseline/review/docs sync -> one governance route truth
---

# Plan 66-01: align release-target validation with tagged refs and remove stale current-story install/governance drift

<objective>
修复 release workflow 在 `workflow_dispatch` 下“校验分支 HEAD、构建 tag”的潜在错位，同时清除 README / baseline / reviews 中仍残留的 stale route 与 stale release-example drift，让 public docs、baseline、review 与 meta guards 重新讲回同一条 current-story。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/AUTHORITY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/README.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@README.md
@README_zh.md
@docs/README.md
@tests/meta/test_governance_release_contract.py
@.planning/phases/66-release-target-fidelity-adapter-root-cleanup-and-focused-protocol-coverage-hardening/66-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: make reusable CI and release workflow validate the same release ref</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, tests/meta/test_governance_release_contract.py</files>
  <read_first>
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - tests/meta/test_governance_release_contract.py
  </read_first>
  <action>给 `.github/workflows/ci.yml` 增加可复用 `workflow_call` ref 输入，并在各 job 的 checkout 步骤显式使用该 ref；随后让 `release.yml` 的 `validate` job 传入 `refs/tags/${{ env.RELEASE_TAG }}`，确保 tagged push 与 manual rehearsal 都对同一 release ref 执行 CI gate。同步增强 `tests/meta/test_governance_release_contract.py`，让它断言 release validate 会把 tagged ref 传给 reusable CI，而不是只断言“复用 CI”。</action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains one `workflow_call` input for release/ref checkout targeting
    - `.github/workflows/release.yml` validate job passes `refs/tags/${{ env.RELEASE_TAG }}` into the reusable CI workflow
    - `tests/meta/test_governance_release_contract.py` asserts same-ref validation instead of only `uses: ./.github/workflows/ci.yml`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/meta/test_governance_release_contract.py</automated>
  </verify>
  <done>Release validate/build/publish now share one tagged-ref truth.</done>
</task>

<task type="auto">
  <name>Task 2: replace stale release examples and stale route prose with freshness-safe governance wording</name>
  <files>README.md, README_zh.md, docs/README.md, .planning/baseline/PUBLIC_SURFACES.md, .planning/baseline/AUTHORITY_MATRIX.md, .planning/reviews/README.md, tests/meta/test_governance_release_contract.py</files>
  <read_first>
    - README.md
    - README_zh.md
    - docs/README.md
    - .planning/baseline/PUBLIC_SURFACES.md
    - .planning/baseline/AUTHORITY_MATRIX.md
    - .planning/reviews/README.md
    - tests/meta/test_governance_release_contract.py
  </read_first>
  <action>把 README / README_zh 中固定写死的 `v1.0.0` release asset 示例改成 freshness-safe placeholder story（例如 `<release-tag>` 或 `${RELEASE_TAG}`）并保留 verified release assets contract；让 `tests/meta/test_governance_release_contract.py` 只校验命令结构与 placeholder truth。同步把 `.planning/baseline/PUBLIC_SURFACES.md` 与 `.planning/baseline/AUTHORITY_MATRIX.md` 中 stale `v1.14 / Phase 63` active-route 说明更新到 `Phase 66` current route，并刷新 `.planning/reviews/README.md` 对 promoted phase assets 的说明，改成 non-exhaustive / allowlist-first 叙事。</action>
  <acceptance_criteria>
    - `README.md` and `README_zh.md` no longer require literal `v1.0.0` in the supported shell install example
    - `tests/meta/test_governance_release_contract.py` validates release example structure without pinning one stale tag literal
    - `.planning/baseline/PUBLIC_SURFACES.md`, `.planning/baseline/AUTHORITY_MATRIX.md`, and `.planning/reviews/README.md` describe the current active-route / promoted-assets story without stale Phase 63 or stale exhaustive lists
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/meta/test_governance_release_contract.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py</automated>
  </verify>
  <done>Public docs and governance baselines now expose one freshness-safe release/current-story contract.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_release_contract.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`
</verification>

<success_criteria>
- release validation and release build use the same tagged-ref truth.
- public release examples and baseline/review route notes no longer drift from the active milestone story.
</success_criteria>

<output>
After completion, create `.planning/phases/66-release-target-fidelity-adapter-root-cleanup-and-focused-protocol-coverage-hardening/66-01-SUMMARY.md`.
</output>

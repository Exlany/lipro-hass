---
phase: 33-contract-truth-unification-hotspot-slimming-and-productization-hardening
plan: "05"
type: execute
title: harden CI/pre-push/benchmark/release-evidence gates and reproducibility posture
wave: 4
depends_on:
  - 33-04
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .pre-commit-config.yaml
  - pyproject.toml
  - custom_components/lipro/manifest.json
  - scripts/check_translations.py
  - tests/meta/test_toolchain_truth.py
  - tests/meta/test_governance_guards.py
  - tests/meta/test_version_sync.py
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - SUPPORT.md
  - SECURITY.md
autonomous: true
requirements:
  - QLT-06
  - QLT-07
  - GOV-28
must_haves:
  truths:
    - local, CI, release, and benchmark gates must describe the same blocking vs advisory reality
    - duplicate snapshot execution and other redundant cost centers should be removed from CI where they add noise without extra signal
    - dependency floor, version truth, and support posture must remain explicit and machine-checkable
  artifacts:
    - .github/workflows/ci.yml
    - .pre-commit-config.yaml
    - pyproject.toml
    - custom_components/lipro/manifest.json
    - tests/meta/test_toolchain_truth.py
  key_links:
    - local hooks -> CI -> release workflow -> version/support/security truth
---

# Plan 33-05: assurance hardening and reproducibility follow-through

<context>
@.planning/phases/33-contract-truth-unification-hotspot-slimming-and-productization-hardening/33-CONTEXT.md
@.github/workflows/ci.yml
@.github/workflows/release.yml
@.pre-commit-config.yaml
@pyproject.toml
@custom_components/lipro/manifest.json
@tests/meta/test_toolchain_truth.py
@tests/meta/test_governance_guards.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: converge local and CI gates, remove duplicate snapshot cost, and harden the benchmark lane truth</name>
  <files>.github/workflows/ci.yml, .pre-commit-config.yaml, scripts/check_translations.py, tests/meta/test_toolchain_truth.py, tests/meta/test_governance_guards.py</files>
  <read_first>
    - .github/workflows/ci.yml
    - .pre-commit-config.yaml
    - scripts/check_translations.py
    - tests/meta/test_toolchain_truth.py
    - tests/meta/test_governance_guards.py
  </read_first>
  <action>去掉 CI 里的重复 snapshot 执行，增强 pre-push 的最小治理组合，并把 benchmark 路线明确固定为“PR 默认非阻塞，但 schedule/manual 产出 artifact + baseline diff + budget tracking”的 advisory-with-budget lane；同时把 translation truth 校验纳入本地/CI 最小组合，避免 bilingual drift。</action>
  <acceptance_criteria>
    - snapshot tests are not redundantly executed twice in CI
    - local pre-push truth is meaningfully closer to CI on architecture/file-matrix/toolchain/translation checks
    - benchmark lane is explicitly defined as non-blocking on PRs but artifact-producing and budget-tracked on schedule/manual runs, and docs/guards encode that exact policy
    - `uv run python scripts/check_translations.py && uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_governance_guards.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run python scripts/check_translations.py && uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_governance_guards.py`</verify>
  <done>toolchain feedback arrives earlier and CI noise is reduced without weakening assurance.</done>
</task>

<task type="auto">
  <name>Task 2: align dependency, compatibility, and release evidence posture into one machine-checkable story</name>
  <files>pyproject.toml, custom_components/lipro/manifest.json, .github/workflows/release.yml, docs/MAINTAINER_RELEASE_RUNBOOK.md, SUPPORT.md, SECURITY.md, tests/meta/test_version_sync.py</files>
  <read_first>
    - pyproject.toml
    - custom_components/lipro/manifest.json
    - .github/workflows/release.yml
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - SUPPORT.md
    - SECURITY.md
    - tests/meta/test_version_sync.py
  </read_first>
  <action>收紧依赖/兼容/支持窗口/发版证据口径：`requires-python`、runtime dependency bounds、support lifecycle、release evidence 与 defer truth 必须在 manifest、pyproject、runbook、support/security docs 与 meta guards 中讲同一条故事，并把 `attestation / provenance / signing / code scanning` 的当前状态拆分得足够明确。</action>
  <acceptance_criteria>
    - version / compatibility / support posture is synchronized across code, workflow, and public docs
    - release evidence wording clearly separates attestation, provenance, signing, and code scanning states
    - `uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py tests/meta/test_governance_guards.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py tests/meta/test_governance_guards.py`</verify>
  <done>reproducibility and release truth become explicit, synced, and guardable instead of semi-manual lore.</done>
</task>
</tasks>

<verification>
- [ ] `uv run python scripts/check_translations.py && uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_governance_guards.py`
- [ ] `uv run pytest -q tests/meta/test_version_sync.py tests/meta/test_toolchain_truth.py tests/meta/test_governance_guards.py`
</verification>

---
phase: 109-anonymous-share-manager-inward-decomposition
plan: "01"
type: execute
title: wire anonymous-share manager scoped orchestration into inward collaborators
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/anonymous_share/manager.py
  - custom_components/lipro/core/anonymous_share/manager_scope.py
  - custom_components/lipro/core/anonymous_share/manager_support.py
  - .planning/phases/109-anonymous-share-manager-inward-decomposition/109-01-SUMMARY.md
autonomous: true
requirements:
  - HOT-47
must_haves:
  truths:
    - `AnonymousShareManager` remains the single outward anonymous-share home
    - scoped/aggregate view orchestration uses `manager_scope.py` instead of ad-hoc manager internals
    - pending payload/finalize submit mechanics are delegated to `manager_support.py`
  artifacts:
    - .planning/phases/109-anonymous-share-manager-inward-decomposition/109-01-SUMMARY.md
---

# Plan 109-01: wire anonymous-share manager scoped orchestration into inward collaborators

<objective>
把 `manager.py` 中可机械下沉的 scoped/aggregate orchestration 与 pending/finalize 逻辑接线到 `manager_scope.py` / `manager_support.py`，保持 outward API 行为不变。
</objective>

<tasks>
<task>
<name>connect manager scope orchestration to dedicated scope view collaborator</name>
<files>custom_components/lipro/core/anonymous_share/manager.py, custom_components/lipro/core/anonymous_share/manager_scope.py</files>
<action>把 `for_scope`、`iter_scope_states`、`iter_scoped_managers`、`primary_manager`、aggregate enabled/pending count 路径收口到 `AnonymousShareScopeViews`，保留每个 manager 实例的 scoped cache 语义。</action>
<verify>`uv run pytest -q tests/core/anonymous_share/test_manager_recording.py tests/core/anonymous_share/test_manager_submission.py`</verify>
<done>`manager.py` 不再维护重复 scoped orchestration 机械分支，scope-view 协作路径保持单一。</done>
</task>
<task>
<name>delegate pending-report and finalize-submit mechanics to support helper</name>
<files>custom_components/lipro/core/anonymous_share/manager.py, custom_components/lipro/core/anonymous_share/manager_support.py</files>
<action>让 `get_pending_report()` 使用 `build_pending_report_payload(...)`，让 `async_finalize_successful_submit()` 使用 `finalize_successful_submit_state(...)`，并保留 thread offload 避免阻塞事件循环。</action>
<verify>`uv run pytest -q tests/core/anonymous_share/test_manager_submission.py tests/services/test_services_share.py`</verify>
<done>pending-report/finalize-submit 机械逻辑统一收口到 support helper，manager 仅保留协调角色。</done>
</task>
</tasks>

<verification>
- `uv run ruff check custom_components/lipro/core/anonymous_share/manager.py custom_components/lipro/core/anonymous_share/manager_scope.py custom_components/lipro/core/anonymous_share/manager_support.py`
- `uv run pytest -q tests/core/anonymous_share/test_manager_recording.py tests/core/anonymous_share/test_manager_submission.py tests/services/test_services_share.py`
</verification>


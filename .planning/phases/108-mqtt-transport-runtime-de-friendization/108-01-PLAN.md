---
phase: 108-mqtt-transport-runtime-de-friendization
plan: "01"
type: execute
title: replace mqtt runtime friend-style reach-through with explicit owner/state contract
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/mqtt/transport.py
  - custom_components/lipro/core/mqtt/transport_runtime.py
  - .planning/phases/108-mqtt-transport-runtime-de-friendization/108-01-SUMMARY.md
autonomous: true
requirements:
  - RUN-10
  - ARC-27
must_haves:
  truths:
    - mqtt runtime no longer depends on whole-owner private-state reach-through
    - transport-facing message/connect entrypoints remain patchable without restoring friend-class coupling
    - `MqttTransport` remains the sole concrete MQTT root
  artifacts:
    - .planning/phases/108-mqtt-transport-runtime-de-friendization/108-01-SUMMARY.md
---

# Plan 108-01: replace mqtt runtime friend-style reach-through with explicit owner/state contract

<objective>
把 `transport_runtime.py` 的 whole-owner friend-style 协作面压缩成显式 owner/state contract，同时保留 `MqttTransport` 作为唯一 concrete transport root。
</objective>

<tasks>
<task>
<name>introduce explicit runtime owner/state contract</name>
<files>custom_components/lipro/core/mqtt/transport.py, custom_components/lipro/core/mqtt/transport_runtime.py</files>
<action>在 `transport_runtime.py` 中定义 `MqttTransportRuntimeOwner` / `MqttTransportOwnerState` / callback contract，并把 runtime 对连接状态、任务引用、broker client、订阅状态与 collaborator 的访问全部改为经显式 owner/state providers 完成。</action>
<verify>`uv run pytest -q tests/core/mqtt/test_transport_refactored.py tests/core/mqtt/test_transport_runtime_connection_loop.py`</verify>
<done>`MqttTransportRuntime` 只消费 `MqttTransportRuntimeOwner` / `MqttTransportOwnerState` 与显式 collaborator providers，不再整段穿透 transport 私有状态。</done>
</task>
<task>
<name>preserve transport-facing entrypoint patchability without reintroducing a second root</name>
<files>custom_components/lipro/core/mqtt/transport.py, custom_components/lipro/core/mqtt/transport_runtime.py</files>
<action>让 runtime 通过显式 entrypoint provider 回接 `transport` 侧 patch seam，并在 `transport.py` 内保留最小兼容投影，确保测试可以继续 patch transport-facing 入口而不把 friend-class whole-owner 注入重新写回主线。</action>
<verify>`uv run ruff check custom_components/lipro/core/mqtt/transport.py custom_components/lipro/core/mqtt/transport_runtime.py tests/core/mqtt/test_transport_refactored.py tests/core/mqtt/test_transport_runtime_connection_loop.py`</verify>
<done>`process_message` 与 `connect_and_listen` 通过显式 entrypoint provider 回接 transport-facing seam，测试与 outward behavior 仍可冻结真实入口。</done>
</task>
</tasks>

<verification>
- `uv run pytest -q tests/core/mqtt/test_transport_refactored.py tests/core/mqtt/test_transport_runtime_connection_loop.py`
- `uv run ruff check custom_components/lipro/core/mqtt/transport.py custom_components/lipro/core/mqtt/transport_runtime.py tests/core/mqtt/test_transport_refactored.py tests/core/mqtt/test_transport_runtime_connection_loop.py`
- `uv run mypy custom_components/lipro/core/mqtt/transport.py custom_components/lipro/core/mqtt/transport_runtime.py`
</verification>

---
phase: 127-runtime-access-de-reflection-typed-runtime-entry-contracts-and-hotspot-continuation
plan: "01"
type: execute
title: converge runtime-access on the typed system-health telemetry contract
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/control/runtime_access.py
  - custom_components/lipro/control/telemetry_surface.py
autonomous: true
requirements:
  - ARC-39
must_haves:
  truths:
    - runtime_access consumes the formal typed system-health telemetry view instead of a stringly dict surrogate
    - runtime snapshot and diagnostics projection keep current degraded fallbacks without reintroducing runtime backdoors
    - telemetry_surface stays a thin observer bridge and does not grow a second runtime-reading story
  artifacts:
    - custom_components/lipro/control/runtime_access.py
    - custom_components/lipro/control/telemetry_surface.py
  key_links:
    - runtime telemetry exporter -> telemetry_surface system-health view -> runtime_access snapshot projection
---

# Plan 127-01: converge runtime-access on the typed system-health telemetry contract

<preconditions>
- `Phase 126` 已完成 diagnostics helper shell thinning；本 plan 只收口 `runtime_access` 的 telemetry seam，不重开 service-router / diagnostics topology。
- 不改变 `system_health` 对外 payload 语义，不允许把 `dict[str, object]` fallback 重新合法化为第二套 typed truth。
</preconditions>

<context>
@.planning/phases/127-runtime-access-de-reflection-typed-runtime-entry-contracts-and-hotspot-continuation/127-CONTEXT.md
@custom_components/lipro/control/runtime_access.py
@custom_components/lipro/control/telemetry_surface.py
@custom_components/lipro/core/telemetry/models.py
@custom_components/lipro/control/models.py
@tests/core/test_runtime_access.py
@tests/core/test_control_plane.py
@tests/core/test_system_health.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: narrow the control telemetry bridge to the formal system-health sink</name>
  <files>custom_components/lipro/control/runtime_access.py, custom_components/lipro/control/telemetry_surface.py</files>
  <action>让 `telemetry_surface.build_entry_system_health_view()` 明确返回 `SystemHealthTelemetryView | None`，并让 `runtime_access.build_entry_system_health_view()` 直接复用这条 typed bridge；删除/替换 `RuntimeTelemetryView = dict[str, object]` 这类 stringly alias，不再先 materialize 宽泛 dict 再回头解释字段。保持 exporter 缺失时的空路径语义稳定，但不要让 `telemetry_surface.py` 变成第二条 runtime-access 读取链。</action>
  <verify>`uv run pytest -q tests/core/test_control_plane.py tests/core/test_system_health.py tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py`</verify>
  <done>`SystemHealthTelemetryView` 成为跨越 `runtime_access` telemetry seam 的唯一正式 contract；control telemetry bridge 仍是 observer-only formal home。</done>
</task>

<task type="auto">
  <name>Task 2: rebuild runtime snapshot projection around typed telemetry fields</name>
  <files>custom_components/lipro/control/runtime_access.py</files>
  <action>用 typed `SystemHealthTelemetryView` 重写 `build_runtime_snapshot()` / `build_runtime_diagnostics_projection()` 所依赖的 field-coercion：`device_count`、`mqtt_connected`、`last_update_success`、`failure_summary` 只消费 typed fields，再在本地处理 `None` / degraded fallback / coordinator-derived fallback。保留 `entry_ref` 传播、device-cache 缺失时的 degraded 标记与“不得回头直接摸 runtime internals”的 discipline。</action>
  <verify>`uv run pytest -q tests/core/test_runtime_access.py tests/core/test_control_plane.py tests/core/test_system_health.py`</verify>
  <done>`build_runtime_snapshot()` 不再依赖 string-key 回捞宽泛 dict；runtime diagnostics projection 继续提供稳定 snapshot/degraded 语义。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_control_plane.py tests/core/test_system_health.py tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py`
- [ ] `uv run pytest -q tests/core/test_runtime_access.py tests/core/test_control_plane.py tests/core/test_system_health.py`
</verification>

<success_criteria>
- `runtime_access.build_entry_system_health_view()` 直接消费 formal typed system-health sink。
- `build_runtime_snapshot()` 与 `build_runtime_diagnostics_projection()` 保持现有 fallback 语义，但不再依赖 stringly dict probing。
- `telemetry_surface.py` 仍是 control-plane observer bridge，而不是新的 runtime root。
</success_criteria>

<output>
After completion, create `.planning/phases/127-runtime-access-de-reflection-typed-runtime-entry-contracts-and-hotspot-continuation/127-01-SUMMARY.md`
</output>

---
phase: 87-assurance-hotspot-decomposition-and-no-regrowth-guards
plan: "03"
type: execute
title: topicize MQTT runtime assurance suite with inward support helper
wave: 1
depends_on: []
files_modified:
  - tests/core/coordinator/runtime/test_mqtt_runtime.py
  - tests/core/coordinator/runtime/test_mqtt_runtime_init.py
  - tests/core/coordinator/runtime/test_mqtt_runtime_connection.py
  - tests/core/coordinator/runtime/test_mqtt_runtime_messages.py
  - tests/core/coordinator/runtime/test_mqtt_runtime_notifications.py
  - tests/core/coordinator/runtime/test_mqtt_runtime_support.py
autonomous: true
requirements:
  - HOT-38
  - TST-27
must_haves:
  truths:
    - `test_mqtt_runtime.py` 不再同时承载 init/DI、connection/reconnect、message handling、notification/reset 等全部 runtime facets。
    - shared fixtures / builders 如需抽取，必须 inward 收敛到同目录 `*_support.py`，不新造跨树 helper root。
    - runtime assurance 拆分后仍保留完整 reconnect / dedup / notification / reset coverage。
  artifacts:
    - tests/core/coordinator/runtime/test_mqtt_runtime.py
    - tests/core/coordinator/runtime/test_mqtt_runtime_init.py
    - tests/core/coordinator/runtime/test_mqtt_runtime_connection.py
    - tests/core/coordinator/runtime/test_mqtt_runtime_messages.py
    - tests/core/coordinator/runtime/test_mqtt_runtime_notifications.py
    - tests/core/coordinator/runtime/test_mqtt_runtime_support.py
  key_links:
    - runtime facet split -> local support helper -> smaller honest runtime suites
---

# Plan 87-03: topicize MQTT runtime assurance suite with inward support helper

<objective>
把 `tests/core/coordinator/runtime/test_mqtt_runtime.py` 按 runtime facet 切薄，并复用同目录 support-helper 模式，让 init/DI、connection/reconnect、message handling、notification/reset 等失败面能直接暴露具体 concern，而不是继续共享 600+ 行单文件。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/phases/87-assurance-hotspot-decomposition-and-no-regrowth-guards/87-CONTEXT.md
@.planning/phases/87-assurance-hotspot-decomposition-and-no-regrowth-guards/87-RESEARCH.md
@.planning/reviews/V1_23_TERMINAL_AUDIT.md
@.planning/reviews/RESIDUAL_LEDGER.md
@tests/core/coordinator/runtime/test_mqtt_runtime.py
@tests/core/coordinator/runtime/test_command_runtime.py
@tests/core/coordinator/runtime/test_command_runtime_support.py

<interfaces>
执行后 MQTT runtime assurance 必须满足：
- runtime facet 分层清晰，可直接从 suite 名定位 init/connection/messages/notifications
- support helper 若存在，仅作为 local inward helper
- 原始 root 路径如保留，只做 shell/anchor，不做 giant suite 回流
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: split MQTT runtime tests by runtime facet and keep coverage honest</name>
  <files>tests/core/coordinator/runtime/test_mqtt_runtime.py, tests/core/coordinator/runtime/test_mqtt_runtime_init.py, tests/core/coordinator/runtime/test_mqtt_runtime_connection.py, tests/core/coordinator/runtime/test_mqtt_runtime_messages.py, tests/core/coordinator/runtime/test_mqtt_runtime_notifications.py</files>
  <action>按真实 runtime facet 拆分 giant suite：初始化/依赖注入、连接与重连、消息与 dedup、disconnect notification/reset 等至少回到多个 topical files。原 root 如保留，只能是 thin shell/anchor。不得因拆分而丢 reconnect/backoff/notification 的 guard strength。</action>
  <acceptance_criteria>
    - topical suite 文件名能直接暴露 runtime facet
    - bulk assertions 不再全部留在 `test_mqtt_runtime.py`
    - reconnect / dedup / notification / reset coverage 继续完整
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/coordinator/runtime/test_mqtt_runtime.py tests/core/coordinator/runtime/test_mqtt_runtime_*.py</automated>
  </verify>
  <done>MQTT runtime assurance 已从 giant root 收敛到更诚实的 facet-local suites。</done>
</task>

<task type="auto">
  <name>Task 2: extract the minimum local support helper for repeated runtime setup</name>
  <files>tests/core/coordinator/runtime/test_mqtt_runtime_support.py</files>
  <action>若 topical suites 需要共享 mock factory、fixture builder 或 typed helper，抽取最小 support helper，并参考 `test_command_runtime_support.py` 的模式。helper 只允许在当前 runtime topical family 内复用，不得变成跨树 عمومی util。</action>
  <acceptance_criteria>
    - support helper 命名与职责保持局部、明确
    - shared runtime setup 不再在多个 topical suites 中重复复制
    - 没有新增第二条 runtime 测试 story
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/coordinator/runtime/test_mqtt_runtime.py tests/core/coordinator/runtime/test_mqtt_runtime_*.py</automated>
  </verify>
  <done>runtime support helper 若存在，也只是 inward/local collaborator。</done>
</task>
</tasks>

<risks>
- 不要把 `test_mqtt_runtime.py` 变成新的 import-heavy fake shell。
- 不要把 runtime helpers 提升到 `tests/helpers/**` 形成第二真源。
- 不要改动生产侧 `MqttRuntime` 行为，只收敛 assurance topology。
</risks>

<non_goals>
- 不处理 API diagnostics 或 protocol contract matrix 热点。
- 不在本 plan 内修改 file-matrix / verification docs。
</non_goals>

<verification>
- `uv run pytest -q tests/core/coordinator/runtime/test_mqtt_runtime.py tests/core/coordinator/runtime/test_mqtt_runtime_*.py`
- `uv run ruff check tests/core/coordinator/runtime/test_mqtt_runtime.py tests/core/coordinator/runtime/test_mqtt_runtime_*.py`
</verification>

<success_criteria>
- MQTT runtime hotspot 已按 runtime facet 切薄
- 共享 setup 只以内向 support helper 形式存在
- original root 不再承担 giant truth-carrier 职责
</success_criteria>

---
phase: 87-assurance-hotspot-decomposition-and-no-regrowth-guards
plan: "02"
type: execute
title: topicize protocol contract matrix into boundary, facade, and authority suites
wave: 1
depends_on: []
files_modified:
  - tests/core/api/test_protocol_contract_matrix.py
  - tests/core/api/test_protocol_contract_boundary_decoders.py
  - tests/core/api/test_protocol_contract_facade_runtime.py
  - tests/core/api/test_protocol_contract_fixture_authority.py
autonomous: true
requirements:
  - HOT-38
  - TST-27
must_haves:
  truths:
    - protocol contract truth 继续由 north-star root / boundary decoder / fixture authority 共同仲裁，不因 topicization 扩散成多套影子合同。
    - `test_protocol_contract_matrix.py` 若保留，必须退为 thin shell / anchor，而不是继续承载全部断言。
    - replay manifest / fixture authority / unified-root smoke 在 topical suites 中仍可 machine-checkable。
  artifacts:
    - tests/core/api/test_protocol_contract_matrix.py
    - tests/core/api/test_protocol_contract_boundary_decoders.py
    - tests/core/api/test_protocol_contract_facade_runtime.py
    - tests/core/api/test_protocol_contract_fixture_authority.py
  key_links:
    - unified protocol root -> boundary decoder families -> replay/fixture authority
---

# Plan 87-02: topicize protocol contract matrix into boundary, facade, and authority suites

<objective>
把 `tests/core/api/test_protocol_contract_matrix.py` 从单文件 north-star contract bucket 拆成 boundary/facade/authority 三类 topical suites，让失败可以直接指向 boundary decoder、facade runtime 还是 fixture authority/replay contract，而不必在 giant matrix 中人工二次定位。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/phases/87-assurance-hotspot-decomposition-and-no-regrowth-guards/87-CONTEXT.md
@.planning/phases/87-assurance-hotspot-decomposition-and-no-regrowth-guards/87-RESEARCH.md
@.planning/reviews/V1_23_TERMINAL_AUDIT.md
@.planning/reviews/RESIDUAL_LEDGER.md
@tests/core/api/test_protocol_contract_matrix.py
@tests/core/api/conftest.py
@tests/fixtures/api_contracts
@tests/harness/protocol.py

<interfaces>
执行后必须保留：
- unified protocol root smoke
- boundary decoder canonical truth
- replay manifest / fixture authority coverage
且不得把上述 truth 拆散成无法追踪的 helper folklore。
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: split the protocol matrix by contract family rather than by historical file gravity</name>
  <files>tests/core/api/test_protocol_contract_matrix.py, tests/core/api/test_protocol_contract_boundary_decoders.py, tests/core/api/test_protocol_contract_facade_runtime.py, tests/core/api/test_protocol_contract_fixture_authority.py</files>
  <action>按真实 concern 拆分：boundary decoder canonicalization、facade/runtime smoke、fixture authority/replay contract 三类各回 topical suite。原始 matrix root 若保留，只能做明确的 anchor shell。不要把 replay/authority coverage 降成 conversation-only 认知。</action>
  <acceptance_criteria>
    - topical suite 名称直接暴露 boundary/facade/authority concern
    - fixture authority assertions 仍明确绑定既有 authority paths
    - bulk assertions 不再堆积在原 matrix root
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_protocol_contract_*.py</automated>
  </verify>
  <done>protocol contract hotspot 已按具体 contract family 切薄。</done>
</task>

<task type="auto">
  <name>Task 2: preserve stable anchor paths without legalizing a second contract story</name>
  <files>tests/core/api/test_protocol_contract_matrix.py</files>
  <action>若现有命令/文档/file-matrix 仍锚定 `test_protocol_contract_matrix.py`，可保留该路径为 thin shell/anchor。但它不得重新吸附所有断言，也不得通过 wildcard imports 掩盖真正 topical homes。</action>
  <acceptance_criteria>
    - 原路径若保留，其内容明显小于原 giant suite
    - topical home 比 root shell 更诚实地承载 bulk truth
    - 没有出现新的 mega helper / umbrella suite
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_protocol_contract_*.py</automated>
  </verify>
  <done>anchor root 稳定但不再承担 giant truth-carrier 职责。</done>
</task>
</tasks>

<risks>
- 不要复制 fixtures 或 replay manifests，必须继续复用唯一 authority paths。
- 不要把 protocol root smoke 从 north-star 故事中拆散到不可发现的位置。
- 不要改动生产侧 protocol facade / decoder 正式行为。
</risks>

<non_goals>
- 不处理 diagnostics API 或 MQTT runtime 热点。
- 不在本 plan 内补 docs/review/baseline truth。
</non_goals>

<verification>
- `uv run pytest -q tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_protocol_contract_*.py`
- `uv run ruff check tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_protocol_contract_*.py`
</verification>

<success_criteria>
- protocol contract hotspot 已按 boundary/facade/authority concern family 切薄
- fixture authority/replay truth 未漂移
- original root 若保留，也只剩 anchor / thin shell 语义
</success_criteria>

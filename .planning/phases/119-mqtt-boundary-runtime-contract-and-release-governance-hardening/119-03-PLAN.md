---
phase: 119-mqtt-boundary-runtime-contract-and-release-governance-hardening
plan: "03"
type: execute
title: harden release namespace and refresh governance and docs freshness from canonical route truth
wave: 2
depends_on:
  - 119-01
  - 119-02
files_modified:
  - .github/workflows/release.yml
  - CHANGELOG.md
  - tests/meta/governance_current_truth.py
  - tests/meta/test_governance_route_handoff_smoke.py
  - tests/meta/governance_followup_route_current_milestones.py
  - tests/meta/test_governance_followup_route.py
  - tests/meta/test_governance_release_contract.py
  - tests/meta/test_governance_release_docs.py
  - tests/meta/test_version_sync.py
  - docs/developer_architecture.md
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - .planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-03-SUMMARY.md
autonomous: true
requirements:
  - GOV-76
  - GOV-77
  - TST-41
must_haves:
  truths:
    - "`.github/workflows/release.yml` only reacts to semver public release tags and no longer treats internal milestone tags as release candidates"
    - "governance route helper and tests read canonical current-route truth from `.planning/PROJECT.md` instead of maintaining a second Python dict contract"
    - "`CHANGELOG.md` and current-route docs tell one public release story aligned with the active route and archived evidence pointer"
  artifacts:
    - .github/workflows/release.yml
    - tests/meta/governance_current_truth.py
    - tests/meta/test_governance_release_contract.py
    - CHANGELOG.md
    - docs/developer_architecture.md
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
  key_links:
    - "release tag filter -> governance release contract test -> version sync guard all admit the same semver-only namespace"
    - "`tests/meta/governance_current_truth.py` reads machine-readable route truth from `.planning/PROJECT.md`, and the route-followup tests consume that helper"
    - "`CHANGELOG.md` and maintainer/developer docs reference current active route + archived evidence truth without reviving stale phase or internal-tag narratives"
---

# Plan 119-03: 收紧 release/governance/docs freshness 到单一 current-route truth

<objective>
让 `.github/workflows/release.yml` 只认 semver public release tags，并把 governance route helper、`CHANGELOG.md` 与 current-route docs 刷新到当前 active route truth；同时用 focused tests 把这条治理故事冻结下来。

Purpose: 满足 `GOV-76`、`GOV-77` 与跨面验证要求 `TST-41`，避免内部 milestone tag、平行 Python dict contract 或过期 changelog 叙事继续污染 public release story。
Output: 更新后的 release workflow、governance truth helper/tests、fresh docs/changelog、以及 `119-03-SUMMARY.md`。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-CONTEXT.md
@.github/workflows/release.yml
@CHANGELOG.md
@tests/meta/governance_current_truth.py
@tests/meta/test_governance_route_handoff_smoke.py
@tests/meta/governance_followup_route_current_milestones.py
@tests/meta/test_governance_followup_route.py
@tests/meta/test_governance_release_contract.py
@tests/meta/test_governance_release_docs.py
@tests/meta/test_version_sync.py
@docs/developer_architecture.md
@docs/MAINTAINER_RELEASE_RUNBOOK.md
</context>

<mapping>
<decisions>
- D-05: `release.yml` push tags 必须收窄到 semver namespace；`CHANGELOG.md` 只讲 public release story，不再暴露过期 phase / archived pointer 叙事。
- D-06: governance route tests 应从 canonical contract 读取 current truth，而不是维护第二份 Python dict 真源。
</decisions>
<north_star>
- Rule 2 / 单一正式主链：release workflow、route helper、runbook 与 changelog 只讲一条 public semver + canonical route contract 故事。
- Rule 6 / 可验证、可仲裁：workflow tests、route smoke 与 docs suites 必须从同一 canonical truth 失败/通过。
- Rule 7 / 迁移残留持续收口：stale `v1.32 archived`、`V1_29`、`Phase 10 completed` 叙事必须显式清退，不能继续作为“暂时合法”故事线。
</north_star>
<requirements>
- `GOV-76`: `.github/workflows/release.yml` 只接受 semver public release tag namespace。
- `GOV-77`: governance route helper/test 改为从 `.planning/PROJECT.md` canonical contract 读取 current truth，而不是硬编码 Python dict。
- `TST-41`: 用 focused release/governance/docs suites 冻结 semver namespace、route-truth dedupe 与 changelog/docs freshness。
</requirements>
</mapping>

<tasks>
<task type="auto">
  <name>Task 119-03-01: narrow release tags to semver and dedupe route truth through the canonical helper</name>
  <files>.github/workflows/release.yml, tests/meta/governance_current_truth.py, tests/meta/test_governance_route_handoff_smoke.py, tests/meta/governance_followup_route_current_milestones.py, tests/meta/test_governance_followup_route.py, tests/meta/test_governance_release_contract.py, tests/meta/test_version_sync.py</files>
  <read_first>.planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-CONTEXT.md, .planning/PROJECT.md, .github/workflows/release.yml, tests/meta/governance_current_truth.py, tests/meta/test_governance_route_handoff_smoke.py, tests/meta/test_governance_release_contract.py</read_first>
  <action>按 D-05（`release.yml` 只接受 semver public release tags）与 D-06（governance route tests 从 canonical contract 读取 current truth）执行：把 `.github/workflows/release.yml` 的 push tag 触发范围收窄到 semver namespace，并让 `tests/meta/governance_current_truth.py` 直接读取 `.planning/PROJECT.md` 中 machine-readable route contract，成为 `test_governance_route_handoff_smoke.py`、`governance_followup_route_current_milestones.py`、`test_governance_followup_route.py` 与 `test_governance_release_contract.py` 共享的唯一 current-truth helper。不要保留第二份 Python dict contract，也不要改写 archived milestone 资产身份或 invent repo-external continuity。</action>
  <verify>
    <automated>uv run pytest tests/meta/test_governance_route_handoff_smoke.py tests/meta/test_governance_followup_route.py tests/meta/test_governance_release_contract.py tests/meta/test_version_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `release.yml` only matches semver-style public tags for release automation
    - governance route tests consume `tests/meta/governance_current_truth.py` instead of carrying their own current-route dict truth
    - release/governance tests continue to admit the active route from `.planning/PROJECT.md` and the archived evidence pointer from the same canonical contract family
  </acceptance_criteria>
  <done>Release namespace and route helper truth are unified, and the workflow/tests no longer maintain a shadow governance contract.</done>
</task>

<task type="auto">
  <name>Task 119-03-02: refresh changelog and maintainer-facing docs to tell one current-route story</name>
  <files>CHANGELOG.md, docs/developer_architecture.md, docs/MAINTAINER_RELEASE_RUNBOOK.md, tests/meta/test_governance_release_docs.py</files>
  <read_first>.planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-CONTEXT.md, CHANGELOG.md, docs/developer_architecture.md, docs/MAINTAINER_RELEASE_RUNBOOK.md, tests/meta/test_governance_release_docs.py</read_first>
  <action>继续按 D-05 与 D-06 收尾：刷新 `CHANGELOG.md` 的 `Unreleased` 与相关 current-story wording，使其只讲 public release namespace、当前 active route 与 archived evidence pointer，不再直接暴露过期 phase/completion slogan、内部 milestone tag 或 archived pointer folklore。同步更新 `docs/developer_architecture.md` 与 `docs/MAINTAINER_RELEASE_RUNBOOK.md`，让维护者/开发者文档与 canonical governance contract 讲同一条故事。不要在此计划改写 `Coordinator` public home，也不要新增第二份 current-route prose source。</action>
  <verify>
    <automated>uv run pytest tests/meta/test_governance_release_docs.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` no longer advertises stale phase-completion or internal-tag release narratives
    - developer and maintainer docs align with the active route and archived evidence truth already recorded in `.planning/PROJECT.md`
    - docs freshness is guarded by `tests/meta/test_governance_release_docs.py`
  </acceptance_criteria>
  <done>Public-facing release/governance documentation tells one current story and stays in sync with the canonical route contract.</done>
</task>
</tasks>

<verification>
- `uv run pytest tests/meta/test_governance_route_handoff_smoke.py tests/meta/test_governance_followup_route.py tests/meta/test_governance_release_contract.py tests/meta/test_governance_release_docs.py tests/meta/test_version_sync.py -q`
- `.github/workflows/release.yml` only allows semver public release tags
- `tests/meta/governance_current_truth.py` is the shared helper for current route truth, sourced from `.planning/PROJECT.md`
- `CHANGELOG.md`, `docs/developer_architecture.md`, and `docs/MAINTAINER_RELEASE_RUNBOOK.md` do not revive stale internal release narratives
</verification>

<success_criteria>
- `GOV-76` and `GOV-77` are satisfied through a semver-only release workflow and canonicalized route-truth consumption
- `TST-41` is satisfied for the governance/docs slice via focused tests that freeze release namespace, route helper, and docs freshness
- the plan keeps archived milestone identity and `Coordinator` public runtime home unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-03-SUMMARY.md`
</output>

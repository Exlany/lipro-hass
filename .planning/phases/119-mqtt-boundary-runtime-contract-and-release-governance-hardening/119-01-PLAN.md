---
phase: 119-mqtt-boundary-runtime-contract-and-release-governance-hardening
plan: "01"
type: execute
title: restore one-way MQTT boundary authority and freeze focused guards
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/protocol/boundary/mqtt_decoder.py
  - custom_components/lipro/core/mqtt/payload.py
  - custom_components/lipro/core/mqtt/topics.py
  - custom_components/lipro/core/mqtt/message_processor.py
  - tests/core/mqtt/test_mqtt_payload.py
  - tests/core/mqtt/test_topics.py
  - tests/core/mqtt/test_message_processor.py
  - tests/meta/test_phase119_mqtt_boundary_guards.py
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/DEPENDENCY_MATRIX.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-01-SUMMARY.md
autonomous: true
requirements:
  - ARC-30
  - TST-41
must_haves:
  truths:
    - protocol-boundary MQTT decode authority flows one-way from `custom_components/lipro/core/protocol/boundary/mqtt_decoder.py` into localized MQTT helpers, never back the other way
    - `payload.py`, `topics.py`, and `message_processor.py` keep their localized helper roles without reviving lazy-import folklore or second authority homes
    - focused regressions and baseline docs fail fast if reverse imports or boundary-owned support drift return
  artifacts:
    - custom_components/lipro/core/protocol/boundary/mqtt_decoder.py
    - custom_components/lipro/core/mqtt/payload.py
    - custom_components/lipro/core/mqtt/topics.py
    - custom_components/lipro/core/mqtt/message_processor.py
    - tests/meta/test_phase119_mqtt_boundary_guards.py
    - .planning/baseline/DEPENDENCY_MATRIX.md
  key_links:
    - `payload.py` / `topics.py` / `message_processor.py` import boundary-owned decode/support helpers directly, while `mqtt_decoder.py` stops importing any `core/mqtt/*` helper
    - phase-local MQTT guards and baseline verification text describe the same one-way dependency story
---

# Plan 119-01: restore one-way MQTT boundary authority and freeze focused guards

<objective>
把 MQTT topic/payload decode authority 拉回 `protocol.boundary -> core/mqtt` 单向主链，删掉 boundary ↔ transport 的 reverse import / lazy-import folklore，并用 focused guards + baseline docs 冻结该约束。

Purpose: 落实 `ARC-30` 与 `TST-41`，兑现 `119-CONTEXT.md` 的 D-01 / D-02，不让 boundary support truth 再被 `core/mqtt/*` 反向定义。
Output: boundary-owned MQTT decode/support 实现、focused MQTT regressions/guards、以及同步后的 dependency/public-surface verification wording。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-CONTEXT.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@custom_components/lipro/core/protocol/boundary/mqtt_decoder.py
@custom_components/lipro/core/mqtt/payload.py
@custom_components/lipro/core/mqtt/topics.py
@custom_components/lipro/core/mqtt/message_processor.py
@tests/core/mqtt/test_mqtt_payload.py
@tests/core/mqtt/test_topics.py
@tests/core/mqtt/test_message_processor.py
</context>

<mapping>
<decisions>
- D-01: MQTT payload size limit 与 biz-id normalization 属于 protocol-boundary family shared support truth，不再由 `core/mqtt` 反向定义给 boundary 使用。
- D-02: `payload.py`、`topics.py`、`message_processor.py` 可以继续作为 MQTT transport/localized helpers，但只能单向消费 protocol-boundary family。
</decisions>
<north_star>
- Rule 2 / 单一正式主链：MQTT canonical topic/payload authority 只留在 boundary family。
- Rule 3 / 边界归一化优于内部兼容供应商形态：topic/payload normalization 在 protocol boundary 完成。
- Rule 5 / 平面分离：`core/mqtt/*` 是 localized transport helpers，不回写 protocol truth。
- Rule 6 / 可验证优于“看起来能跑”：guards 与 baseline docs 共同冻结该链路。
</north_star>
<requirements>
- `ARC-30`: 斩断 `mqtt_decoder.py` 对 `core/mqtt/*` 的反向依赖，并让 localized helpers 直接复用 boundary decode/support。
- `TST-41`: 用 focused tests / guards / docs 冻结 one-way MQTT boundary dependency。
</requirements>
</mapping>

<tasks>

<task type="auto" tdd="true">
  <name>Task 119-01-01: move MQTT decode/support ownership fully into the protocol-boundary family</name>
  <files>custom_components/lipro/core/protocol/boundary/mqtt_decoder.py, custom_components/lipro/core/mqtt/payload.py, custom_components/lipro/core/mqtt/topics.py, custom_components/lipro/core/mqtt/message_processor.py</files>
  <behavior>
    - `mqtt_decoder.py` no longer imports `custom_components.lipro.core.mqtt.payload` or `custom_components.lipro.core.mqtt.topics`.
    - `payload.py`, `topics.py`, and `message_processor.py` stop using `import_module("custom_components.lipro.core.protocol.boundary")` and instead consume boundary-owned helpers directly.
    - Existing topic/payload normalization behavior for valid and malformed fixtures remains stable.
  </behavior>
  <action>按 D-01 / D-02 把 MQTT decode/support truth 收回 boundary family：让 `mqtt_decoder.py` 自持 payload-size limit、biz-id normalization 与 topic/envelope/property canonicalization所需私有 helpers；更新 `payload.py`、`topics.py` 与 `message_processor.py` 为直接 import boundary-owned decode/support entrypoints（可以直接 import `mqtt_decoder.py` 内 formal helpers，也可以 import 同层 private support module，但不要新增 boundary package outward export，也不要修改 `core/mqtt/__init__.py`/`core/protocol/boundary/__init__.py` 去制造第二条 public story）。同时保留 `payload.py`、`topics.py`、`message_processor.py` 的 localized helper 角色，不把 `MqttTransport` 或 transport family 提升为 authority home。</action>
  <verify>
    <automated>uv run pytest -q tests/core/mqtt/test_mqtt_payload.py tests/core/mqtt/test_topics.py tests/core/mqtt/test_message_processor.py</automated>
  </verify>
  <done>`protocol.boundary -> core/mqtt` 成为唯一依赖方向；生产代码里不再存在 boundary 反向 import transport helper 的链路。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 119-01-02: add focused MQTT regressions that reject reverse imports and lazy-import folklore</name>
  <files>tests/core/mqtt/test_mqtt_payload.py, tests/core/mqtt/test_topics.py, tests/core/mqtt/test_message_processor.py, tests/meta/test_phase119_mqtt_boundary_guards.py</files>
  <behavior>
    - focused tests fail if `payload.py` / `topics.py` / `message_processor.py` reintroduce lazy boundary imports.
    - guards fail if `mqtt_decoder.py` again imports `core/mqtt/payload.py` or `core/mqtt/topics.py`.
    - the suite proves `mqtt_decoder.py` remains the only canonical MQTT decode authority.
  </behavior>
  <action>在现有 MQTT regression suites 上补足 direct-import / one-way-authority 断言，并新增 `tests/meta/test_phase119_mqtt_boundary_guards.py` 作为本 phase focused guard home：显式拒绝 `import_module("custom_components.lipro.core.protocol.boundary")` 出现在 `payload.py` / `topics.py` / `message_processor.py`，拒绝 `mqtt_decoder.py` 对 `core/mqtt/*` 的导入，同时保留 malformed payload/topic 失败语义与 canonical fixture authority。不要把这些断言散回 giant generic suite；本 phase 需要一个独立、可点名执行的 focused guard home。</action>
  <verify>
    <automated>uv run pytest -q tests/core/mqtt/test_mqtt_payload.py tests/core/mqtt/test_topics.py tests/core/mqtt/test_message_processor.py tests/meta/test_phase119_mqtt_boundary_guards.py</automated>
  </verify>
  <done>MQTT-focused tests and phase guard 会在 authority drift / lazy-import 回流时立即失败。</done>
</task>

<task type="auto">
  <name>Task 119-01-03: synchronize baseline dependency and verification truth to the new one-way boundary story</name>
  <files>.planning/baseline/PUBLIC_SURFACES.md, .planning/baseline/DEPENDENCY_MATRIX.md, .planning/baseline/VERIFICATION_MATRIX.md</files>
  <action>同步 baseline docs 到 D-01 / D-02 对应的正式叙事：把 `topics.py` 的 legacy lazy-import 说明升级为 direct boundary consumer story；明确 `mqtt_decoder.py` 继续是唯一 canonical MQTT topic/payload decode authority；把 `tests/meta/test_phase119_mqtt_boundary_guards.py` 和本计划的 focused pytest lane 写入 verification proof。不要把 boundary-owned support helper 写成新的 public surface，也不要改写 `MqttTransport` 的 localized concrete transport 身份。</action>
  <verify>
    <automated>uv run pytest -q tests/meta/test_phase119_mqtt_boundary_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_dependency_guards.py</automated>
  </verify>
  <done>Baseline docs、focused guards 与 production imports 讲同一条 one-way boundary 故事，不再保留 lazy-import folklore wording。</done>
</task>

</tasks>

<verification>
- `uv run pytest -q tests/core/mqtt/test_mqtt_payload.py tests/core/mqtt/test_topics.py tests/core/mqtt/test_message_processor.py tests/meta/test_phase119_mqtt_boundary_guards.py`
- `uv run pytest -q tests/meta/test_public_surface_guards.py tests/meta/test_dependency_guards.py`
- `uv run ruff check custom_components/lipro/core/protocol/boundary/mqtt_decoder.py custom_components/lipro/core/mqtt/payload.py custom_components/lipro/core/mqtt/topics.py custom_components/lipro/core/mqtt/message_processor.py tests/core/mqtt/test_mqtt_payload.py tests/core/mqtt/test_topics.py tests/core/mqtt/test_message_processor.py tests/meta/test_phase119_mqtt_boundary_guards.py`
</verification>

<success_criteria>
- `custom_components/lipro/core/protocol/boundary/mqtt_decoder.py` 不再 import `core/mqtt/*` helpers。
- `custom_components/lipro/core/mqtt/payload.py`、`custom_components/lipro/core/mqtt/topics.py` 与 `custom_components/lipro/core/mqtt/message_processor.py` 直接复用 boundary-owned decode/support helpers，且没有 lazy `import_module(...)` folklore。
- focused guard 与 baseline docs 会在 reverse import、authority ambiguity 或 stale dependency wording 回流时失败。
</success_criteria>

<output>
After completion, create `.planning/phases/119-mqtt-boundary-runtime-contract-and-release-governance-hardening/119-01-SUMMARY.md`.
</output>

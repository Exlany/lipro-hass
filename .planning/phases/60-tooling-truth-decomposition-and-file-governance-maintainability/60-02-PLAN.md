---
phase: 60-tooling-truth-decomposition-and-file-governance-maintainability
plan: "02"
type: execute
title: topicize toolchain truth guards behind a thin daily runnable shell
wave: 2
depends_on:
  - 60-01
files_modified:
  - tests/meta/test_toolchain_truth.py
  - tests/meta/toolchain_truth_python_stack.py
  - tests/meta/toolchain_truth_release_contract.py
  - tests/meta/toolchain_truth_docs_fast_path.py
  - tests/meta/toolchain_truth_ci_contract.py
  - tests/meta/toolchain_truth_testing_governance.py
  - tests/meta/toolchain_truth_checker_paths.py
autonomous: true
requirements:
  - TST-12
must_haves:
  truths:
    - `tests/meta/test_toolchain_truth.py` becomes a thin daily runnable root instead of the only giant truth home
    - toolchain/release/docs/CI/testing/checker concerns live in named topic modules with narrower failure radius
    - topicization preserves existing guard intent without creating a second helper maze or drifting command semantics
  artifacts:
    - tests/meta/test_toolchain_truth.py
    - tests/meta/toolchain_truth_python_stack.py
    - tests/meta/toolchain_truth_release_contract.py
    - tests/meta/toolchain_truth_docs_fast_path.py
    - tests/meta/toolchain_truth_ci_contract.py
    - tests/meta/toolchain_truth_testing_governance.py
    - tests/meta/toolchain_truth_checker_paths.py
  key_links:
    - thin runnable root -> topic modules by truth family -> narrower toolchain failure localization
    - toolchain truth -> dependency/version guards -> unchanged daily meta verification story
---

# Plan 60-02: topicize toolchain truth guards behind a thin daily runnable shell

<objective>
把 `tests/meta/test_toolchain_truth.py` 从 mixed bucket topicize 成命名清晰的 truth-family modules，同时保留一个 daily runnable root。

Purpose: 让 toolchain / release / docs / CI / testing / checker truth 的失败半径更窄、更诚实，而不是继续把多条故事线挤在一个 megasuite 里。
Output: thin shell `test_toolchain_truth.py` 与一组 non-`test_` topic modules，持续复用当前 daily command story。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.12-MILESTONE-AUDIT.md
@.planning/reviews/V1_12_EVIDENCE_INDEX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/codebase/TESTING.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-PRD.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-CONTEXT.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-RESEARCH.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-VALIDATION.md
@tests/meta/test_toolchain_truth.py
@tests/meta/test_governance_guards.py

<interfaces>
Current concern families already visible in `tests/meta/test_toolchain_truth.py` and should become named topical homes:

```python
test_python_toolchain_truth_is_aligned_to_314
test_release_workflow_keeps_identity_evidence_tools_in_sync
test_bilingual_readmes_capture_release_asset_identity_truth
test_pre_push_contract_runs_translation_and_governance_truth_early
test_testing_map_counts_and_script_boundary_notes_match_repo_facts
test_verification_matrix_and_checker_guard_active_path_truth
test_develop_script_smoke_mode_preserves_other_integrations
```

The root daily command must remain valid after topicization:

```bash
uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_dependency_guards.py tests/meta/test_version_sync.py
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: split Python, release, docs, and CI assertions into named toolchain truth modules</name>
  <files>tests/meta/test_toolchain_truth.py, tests/meta/toolchain_truth_python_stack.py, tests/meta/toolchain_truth_release_contract.py, tests/meta/toolchain_truth_docs_fast_path.py, tests/meta/toolchain_truth_ci_contract.py</files>
  <action>把 Python/toolchain pin、release identity、docs fast-path / terminology、CI / pre-push / marker contract 从 giant root 提取到 named non-`test_` modules。`tests/meta/test_toolchain_truth.py` 只保留 thin shell import surface 与最少量 file-local glue；不要把 split 变成 arbitrary line slicing，也不要复制 shared repo helpers 到新的 ad-hoc utility layer。</action>
  <acceptance_criteria>
    - each new module has a single obvious truth family
    - `tests/meta/test_toolchain_truth.py` remains the one obvious daily runnable root
    - split does not change assertions about toolchain pins, release workflows, or docs fast path
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_dependency_guards.py tests/meta/test_version_sync.py</automated>
  </verify>
  <done>Python/release/docs/CI truth is localized to named modules instead of one giant bucket.</done>
</task>

<task type="auto">
  <name>Task 2: isolate testing-map and checker-path truth without changing the root invocation story</name>
  <files>tests/meta/test_toolchain_truth.py, tests/meta/toolchain_truth_testing_governance.py, tests/meta/toolchain_truth_checker_paths.py</files>
  <action>把 testing-map counts、codebase-map authority notes、governance registry continuity、verification-matrix / checker-path truth 与 `scripts/develop` smoke 断言收口到 dedicated modules；保留 root shell 作为 single invocation target。新 modules 命名不要使用 `test_` 前缀，避免在 `60-02` 之前就扩张 `TESTING.md` 的 test-file counts；相关 inventory / docs freeze 统一留给 `60-03`。</action>
  <acceptance_criteria>
    - testing/governance continuity assertions are visibly separated from release/docs assertions
    - root shell still collects the whole toolchain truth story in one command
    - topicization reduces failure radius without forcing broader doc rewrites in this wave
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_dependency_guards.py tests/meta/test_version_sync.py</automated>
  </verify>
  <done>Toolchain truth becomes topicized and debuggable by concern family while the daily root command remains unchanged.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_toolchain_truth.py tests/meta/test_dependency_guards.py tests/meta/test_version_sync.py`
</verification>

<success_criteria>
- `tests/meta/test_toolchain_truth.py` 退为 thin shell runnable root，topic modules 承担真实断言主体。
- topic modules 按 truth family 命名，而不是机械平均拆行。
- daily invocation 仍然只需跑 `tests/meta/test_toolchain_truth.py`，不会让维护者追着多个新 root file 找入口。
</success_criteria>

<output>
After completion, create `.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-02-SUMMARY.md`.
</output>

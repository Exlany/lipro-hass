---
phase: 60-tooling-truth-decomposition-and-file-governance-maintainability
plan: "01"
type: execute
title: decompose the file-matrix checker into a thin root and focused internal truth families
wave: 1
depends_on: []
files_modified:
  - scripts/check_file_matrix.py
  - scripts/check_file_matrix_inventory.py
  - scripts/check_file_matrix_registry.py
  - scripts/check_file_matrix_markdown.py
  - scripts/check_file_matrix_validation.py
  - .planning/reviews/FILE_MATRIX.md
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - HOT-14
must_haves:
  truths:
    - `scripts/check_file_matrix.py` remains the only public CLI/import root for file-matrix governance checks
    - inventory/classifier/markdown/validator responsibilities move into named internal families instead of one monolith
    - existing importers keep using `scripts.check_file_matrix` without internal path churn
  artifacts:
    - scripts/check_file_matrix.py
    - scripts/check_file_matrix_inventory.py
    - scripts/check_file_matrix_registry.py
    - scripts/check_file_matrix_markdown.py
    - scripts/check_file_matrix_validation.py
    - .planning/reviews/FILE_MATRIX.md
    - tests/meta/test_governance_guards.py
  key_links:
    - thin checker root -> focused internal modules -> unchanged `repo_root/classify_path/run_checks` contract
    - new internal script files -> `FILE_MATRIX.md` minimal sync -> `uv run python scripts/check_file_matrix.py --check` stays green
---

# Plan 60-01: decompose the file-matrix checker into a thin root and focused internal truth families

<objective>
把 `scripts/check_file_matrix.py` 从 giant truth hotspot 收口成 thin root + focused internal families，同时保持 outward CLI / import contract 稳定。

Purpose: 先把 `checker inward decomposition` 做对，缩小 file-governance tooling 的维护半径，而不制造第二条治理故事线。
Output: thin-root checker、named internal helper modules、保持同步的 `FILE_MATRIX.md` 最小拓扑更新，以及证明 outward contract 未漂移的 focused guard。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.12-MILESTONE-AUDIT.md
@.planning/reviews/V1_12_EVIDENCE_INDEX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-PRD.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-CONTEXT.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-RESEARCH.md
@.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-VALIDATION.md
@scripts/check_file_matrix.py
@tests/meta/test_governance_guards.py
@tests/meta/test_evidence_pack_authority.py
@tests/meta/test_governance_closeout_guards.py

<interfaces>
From `scripts/check_file_matrix.py` the root-owned public contract must stay importable from the same module path:

```python
def repo_root(start: Path | None = None) -> Path
def iter_python_files(root: Path) -> list[str]
def classify_path(path: str) -> FileGovernanceRow
def parse_file_matrix_paths(text: str) -> list[str]
def extract_reported_total(text: str) -> int
def run_checks(root: Path) -> list[str]
def main() -> int
```

Current importers already prove the contract surface:

```python
from scripts.check_file_matrix import (
    extract_reported_total,
    iter_python_files,
    parse_file_matrix_paths,
    repo_root,
    run_checks,
)
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: extract inventory and classification truth behind the existing checker root</name>
  <files>scripts/check_file_matrix.py, scripts/check_file_matrix_inventory.py, scripts/check_file_matrix_registry.py, .planning/reviews/FILE_MATRIX.md</files>
  <action>把 repo-root/path 常量、inventory walk、`FileGovernanceRow` / override registry、以及 path classification inward split 到 named internal modules；`scripts/check_file_matrix.py` 继续保留为唯一 outward home，并从 root re-export `repo_root`、`iter_python_files`、`classify_path`。允许对 `FILE_MATRIX.md` 做最小必要同步，只登记新 internal checker files 的 ownership / fate；不要在这里扩张 broader current-story prose，那留给 `60-03`。</action>
  <acceptance_criteria>
    - `scripts.check_file_matrix` import path and symbol names remain stable
    - classification truth is no longer embedded in one giant root file
    - minimal `FILE_MATRIX.md` sync keeps checker inventory truthful after new internal files appear
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_evidence_pack_authority.py tests/meta/test_phase31_runtime_budget_guards.py tests/meta/test_phase45_hotspot_budget_guards.py tests/meta/test_phase50_rest_typed_budget_guards.py tests/meta/test_governance_closeout_guards.py</automated>
  </verify>
  <done>`scripts/check_file_matrix.py` no longer owns inventory + registry + classifier bulk directly, yet all current importers still pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: extract markdown and validation passes while preserving CLI behavior</name>
  <files>scripts/check_file_matrix.py, scripts/check_file_matrix_markdown.py, scripts/check_file_matrix_validation.py, tests/meta/test_governance_guards.py</files>
  <action>把 markdown render/parse helpers 与 doc/codebase-map validation passes 拆到 focused internal modules；root `run_checks()` 仍负责按原顺序 orchestrate `validate_file_matrix`、active-doc truth、active-source truth、verification-matrix path truth 与 codebase-map policy。保持 `--write`、`--check`、`--report` 行为与输出语义稳定，不新增第二个 CLI root，也不把 authority 规则迁出 `FILE_MATRIX / VERIFICATION_MATRIX`。</action>
  <acceptance_criteria>
    - `uv run python scripts/check_file_matrix.py --check` remains the single daily checker command
    - root file becomes visibly thinner and mostly orchestration/re-export/CLI glue
    - guard coverage proves contract stability instead of forcing importer rewrites
  </acceptance_criteria>
  <verify>
    <automated>uv run python scripts/check_file_matrix.py --check && uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_evidence_pack_authority.py tests/meta/test_phase31_runtime_budget_guards.py tests/meta/test_phase45_hotspot_budget_guards.py tests/meta/test_phase50_rest_typed_budget_guards.py tests/meta/test_governance_closeout_guards.py</automated>
  </verify>
  <done>The checker becomes a thin outward shell over focused internal modules while the CLI/import contract and authority chain remain unchanged.</done>
</task>
</tasks>

<verification>
- [ ] `uv run python scripts/check_file_matrix.py --check`
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_evidence_pack_authority.py tests/meta/test_phase31_runtime_budget_guards.py tests/meta/test_phase45_hotspot_budget_guards.py tests/meta/test_phase50_rest_typed_budget_guards.py tests/meta/test_governance_closeout_guards.py`
</verification>

<success_criteria>
- `scripts/check_file_matrix.py` 退为 thin root，但 `repo_root`、`iter_python_files`、`classify_path`、`parse_file_matrix_paths`、`extract_reported_total`、`run_checks` 与 CLI 行为保持稳定。
- new internal checker modules 只承接 inward decomposition，不定义新的 public root 或第二治理文档。
- `FILE_MATRIX.md` 对新增 internal checker files 的记录足以支撑 `--check` 继续通过。
</success_criteria>

<output>
After completion, create `.planning/phases/60-tooling-truth-decomposition-and-file-governance-maintainability/60-01-SUMMARY.md`.
</output>

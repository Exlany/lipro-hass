---
phase: 19-headless-consumer-proof-adapter-demotion
plan: "04"
type: execute
title: harden second-root governance and sync long-term truth
wave: 4
depends_on:
  - 19-01
  - 19-02
  - 19-03
files_modified:
  - .planning/baseline/{PUBLIC_SURFACES,DEPENDENCY_MATRIX,ARCHITECTURE_POLICY,VERIFICATION_MATRIX}.md
  - .planning/reviews/{FILE_MATRIX,RESIDUAL_LEDGER,KILL_LIST}.md
  - .planning/{STATE,ROADMAP,REQUIREMENTS}.md
  - scripts/check_architecture_policy.py
  - tests/meta/test_dependency_guards.py
  - tests/meta/test_public_surface_guards.py
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - headless boot / proof 只能被登记为 adapter/proof consumer，不能进入 public export surface
    - second-root / assurance-backflow bans 必须进入 baseline + script + meta guards 同一 enforcement chain
    - baseline/review/phase-complete truth sync 必须与实际 Phase 19 结果同轮完成
  artifacts:
    - `.planning/baseline/PUBLIC_SURFACES.md`
    - `.planning/baseline/ARCHITECTURE_POLICY.md`
    - `.planning/reviews/RESIDUAL_LEDGER.md`
    - `.planning/STATE.md`
    - `tests/meta/test_dependency_guards.py`
  key_links:
    - headless proof identity -> baseline wording -> public-surface guards
    - residual / kill-list disposition -> review truth
    - Phase 19 completion -> `STATE/ROADMAP/REQUIREMENTS`
---

# Plan 19-04: 加固 second-root 治理并同步长期真相

<preconditions>
- `19-01`、`19-02`、`19-03` 的代码与 focused regressions 已完成。
- 当前 Phase 19 的实际实现结果已足以写回 baseline/review/phase-complete truth。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/ARCHITECTURE_POLICY.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-CONTEXT.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 回写 baseline 与 phase-complete 最小真相</name>
  <files>.planning/baseline/{PUBLIC_SURFACES,DEPENDENCY_MATRIX,ARCHITECTURE_POLICY,VERIFICATION_MATRIX}.md, .planning/{STATE,ROADMAP,REQUIREMENTS}.md</files>
  <action>把 Phase 19 的最小长期真相写回 baseline 与 phase-complete docs：明确 headless boot / proof 的非 public-root 身份、以及 Phase 19 完成后 `STATE/ROADMAP/REQUIREMENTS` 的最小同步范围；不在这里重新接管 authority source 扩面 owner。</action>
  <verify>`uv run python scripts/check_file_matrix.py --check && uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`</verify>
  <done>长期真源与 Phase 19 实际实现对齐，不再把最小同步推迟到 Phase 22。</done>
</task>

<task type="auto">
  <name>Task 2: 回写 residual 与 no-change / delete-gate disposition</name>
  <files>.planning/reviews/{FILE_MATRIX,RESIDUAL_LEDGER,KILL_LIST}.md</files>
  <action>把 headless boot home、proof harness、remaining adapter-local shims 与 delete gates 写回 review docs；若 authority surface 没有继续扩面，则在 review truth 中显式登记 no-change，而不是在本 plan 无条件重开 `AUTHORITY_MATRIX.md` owner。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_evidence_pack_authority.py`</verify>
  <done>Phase 19 的 residual / kill-list / authority no-change disposition 进入可审计状态。</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 second-root 与 assurance backflow 守卫</name>
  <files>scripts/check_architecture_policy.py, tests/meta/test_dependency_guards.py, tests/meta/test_public_surface_guards.py</files>
  <action>新增或收紧针对 headless boot/proof 的守卫：阻断 root/core package export、`homeassistant` import、`helpers.platform` backflow、`Coordinator` / `control/*` import 以及 assurance→production backflow；让所有规则进入同一 enforcement chain。</action>
  <verify>`uv run python scripts/check_architecture_policy.py --check && uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`</verify>
  <done>第二 root 与 assurance backflow 风险具备 fail-fast 护栏。</done>
</task>
</tasks>

<verification>
- [ ] `uv run python scripts/check_architecture_policy.py --check`
- [ ] `uv run python scripts/check_file_matrix.py --check`
- [ ] `uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_evidence_pack_authority.py`
- [ ] baseline/review/phase-complete truth 已与 Phase 19 实现同轮对齐
</verification>

<success_criteria>
- headless boot / proof 的长期身份被锁死为 adapter/proof consumer，而不是第二 root
- residual、authority no-change、delete gate 与 phase-complete docs 不再存在 silent carry-forward
- second-root / assurance-backflow 风险进入单一治理执行链
</success_criteria>

<output>
After completion, create `.planning/phases/19-headless-consumer-proof-adapter-demotion/19-04-SUMMARY.md`
</output>

---
phase: 19-headless-consumer-proof-adapter-demotion
plan: "01"
type: execute
title: shared headless boot contract and adapter consumer convergence
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/headless/__init__.py
  - custom_components/lipro/headless/boot.py
  - custom_components/lipro/config_flow.py
  - custom_components/lipro/entry_auth.py
  - custom_components/lipro/flow/credentials.py
  - custom_components/lipro/flow/login.py
  - tests/core/test_headless_boot.py
  - tests/flows/test_config_flow.py
  - tests/core/test_init.py
  - tests/core/test_token_persistence.py
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - headless boot 只是 adapter-only composition home，不是第二 public root
    - `config_flow.py` 与 `entry_auth.py` 共用同一 boot contract，而不是各自维持 parallel wiring
    - `AuthBootstrapSeed` / `build_protocol_auth_context()` / `AuthSessionSnapshot` 继续是唯一 auth/bootstrap 正式故事线
  artifacts:
    - `custom_components/lipro/headless/boot.py`
    - `tests/core/test_headless_boot.py`
    - `tests/flows/test_config_flow.py`
  key_links:
    - `config_flow.py` login wiring -> shared boot contract
    - `entry_auth.py` setup wiring -> shared boot contract
    - focused boot regression -> import boundary + consumer stability
---

# Plan 19-01: 建立共享 headless boot contract，并收拢 adapter consumer

<preconditions>
- `Phase 18` verification 已确认 host-neutral nucleus、adapter projection seam 与治理 guards 全部通过。
- `19-CONTEXT.md`、`19-RESEARCH.md` 与 `19-VALIDATION.md` 已固定“proof-only、non-root、no dual-entry”前提。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-CONTEXT.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-RESEARCH.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-VALIDATION.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 建立 headless adapter boot home</name>
  <files>custom_components/lipro/headless/__init__.py, custom_components/lipro/headless/boot.py</files>
  <action>新增一个最小、非 public-root 的 headless adapter boot home，只负责显式装配 `aiohttp.ClientSession`、`AuthBootstrapSeed`、`LiproProtocolFacade`、`LiproAuthManager` 与 formal auth/session collaborators；它不能成为 package export、不能承担 runtime root 语义，也不能依赖 `homeassistant`、`Coordinator`、`helpers.platform` 或 control/runtime helpers。</action>
  <verify>`uv run ruff check custom_components/lipro/headless && uv run mypy custom_components/lipro/headless`</verify>
  <done>仓库存在一条可复用、可测试、非 public-root 的 headless boot contract。</done>
</task>

<task type="auto">
  <name>Task 2: 让 config-flow 与 entry-auth 共用同一 boot contract</name>
  <files>custom_components/lipro/config_flow.py, custom_components/lipro/entry_auth.py</files>
  <action>把 `config_flow.py` 的登录装配与 `entry_auth.py` 的 setup 装配 inward 到同一 boot contract；两者仍保留 HA session 获取、ConfigEntry persistence、HA exception mapping、token callback 等宿主职责，但不再各自维护 parallel bootstrap composition。</action>
  <verify>`uv run pytest -q tests/flows/test_config_flow.py tests/core/test_init.py tests/core/test_token_persistence.py`</verify>
  <done>HA adapter 两条装配路径只剩宿主差异，不再复制共享 auth/bootstrap 真相。</done>
</task>

<task type="auto">
  <name>Task 3: 收紧 mixed-purity flow helpers</name>
  <files>custom_components/lipro/flow/credentials.py, custom_components/lipro/flow/login.py</files>
  <action>把 headless proof 需要复用的 pure credential/auth helper 与 HA-only projection/error mapping 继续分层：前者直接服务 boot contract，后者明确留在 flow/adapter shell，避免 `flow/*` 被误读为 shared business root。</action>
  <verify>`uv run ruff check custom_components/lipro/flow/credentials.py custom_components/lipro/flow/login.py custom_components/lipro/headless`</verify>
  <done>`flow/*` 的 helper 纯度更清晰，future host 不会再经由 HA flow 包复用通用能力。</done>
</task>

<task type="auto">
  <name>Task 4: 锁定 boot seam 的 focused regression</name>
  <files>tests/core/test_headless_boot.py, tests/flows/test_config_flow.py, tests/core/test_init.py, tests/core/test_token_persistence.py</files>
  <action>新增 focused regression，直接锁死 headless boot seam 的 import 边界与 outward consumer 行为，确认 `config_flow.py` / `entry_auth.py` 的 outward seam 身份稳定，不因 inward-move 破坏既有 init/token persistence 回归。</action>
  <verify>`uv run pytest -q tests/core/test_headless_boot.py tests/flows/test_config_flow.py tests/core/test_init.py tests/core/test_token_persistence.py`</verify>
  <done>Phase 19 的 boot contract 有了第一层 fail-fast 保护。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_headless_boot.py tests/flows/test_config_flow.py tests/core/test_init.py tests/core/test_token_persistence.py`
- [ ] `uv run ruff check custom_components/lipro/headless custom_components/lipro/config_flow.py custom_components/lipro/entry_auth.py custom_components/lipro/flow/credentials.py custom_components/lipro/flow/login.py`
- [ ] `uv run mypy custom_components/lipro/headless custom_components/lipro/core/auth custom_components/lipro/config_flow.py custom_components/lipro/entry_auth.py`
- [ ] headless boot 仍然不是 public export、不是 second root、也不是 runtime story
</verification>

<success_criteria>
- 共享的 headless boot contract 已形成，并可被 adapter 与 proof consumer 共同消费
- `config_flow.py` / `entry_auth.py` 不再维持 parallel auth/bootstrap composition
- `flow/credentials.py` / `flow/login.py` 的 mixed-purity helper 已继续收口
</success_criteria>

<output>
After completion, create `.planning/phases/19-headless-consumer-proof-adapter-demotion/19-01-SUMMARY.md`
</output>

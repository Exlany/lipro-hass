---
phase: 19-headless-consumer-proof-adapter-demotion
plan: "03"
type: execute
title: demote remaining ha-only adapter shells
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - custom_components/lipro/helpers/platform.py
  - custom_components/lipro/{light,cover,fan,select,switch,update}.py
  - custom_components/lipro/control/runtime_access.py
  - tests/platforms/test_entity_behavior.py
  - tests/integration/test_headless_consumer_proof.py
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - platform setup、runtime locator 与 compat shim 继续停留在 adapter/control plane
    - headless proof 不得借机把 `ConfigEntry` / `runtime_data` / platform semantics 提升成 shared contract
    - `helpers/platform.py` 仍然只是 HA platform projection home
  artifacts:
    - `custom_components/lipro/helpers/platform.py`
    - `custom_components/lipro/control/runtime_access.py`
    - `tests/platforms/test_entity_behavior.py`
  key_links:
    - platform setup shell -> adapter helper
    - runtime locator wording -> control-plane only
    - platform/headless regression -> no backflow to nucleus
---

# Plan 19-03: 继续 demote HA-only adapter shells

<preconditions>
- `19-01` 已建立共享 boot contract。
- `19-02` 已证明 headless consumer 不依赖 HA adapter 才能跑通核心链路。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-CONTEXT.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 收薄平台 setup 重复壳</name>
  <files>custom_components/lipro/helpers/platform.py, custom_components/lipro/{light,cover,fan,select,switch,update}.py</files>
  <action>把与 Phase 19 直接相关的平台 setup 重复壳继续 inward 到 adapter helper：允许收薄重复 wiring，但不得把 platform strings、entity setup 语义或 category/capability truth 推回 host-neutral nucleus。</action>
  <verify>`uv run pytest -q tests/core/test_helpers.py tests/platforms/test_entity_behavior.py`</verify>
  <done>平台 setup 仍留在 adapter 层，但重复壳更薄、更不易被误判成 shared logic。</done>
</task>

<task type="auto">
  <name>Task 2: 收紧 runtime locator 的 adapter-private 身份</name>
  <files>custom_components/lipro/control/runtime_access.py</files>
  <action>继续收紧 `runtime_access.py` 的 wording / shim / compat 边界，确保它只表达 control-plane runtime locator，不给 future headless consumer 造成 shared contract 幻觉。</action>
  <verify>`uv run pytest -q tests/core/test_control_plane.py -k runtime_access && uv run pytest -q tests/integration/test_headless_consumer_proof.py`</verify>
  <done>runtime locator 的宿主身份更清楚，headless proof 不会误吸这条 adapter seam。</done>
</task>

<task type="auto">
  <name>Task 3: 锁定 adapter-shell regression</name>
  <files>tests/platforms/test_entity_behavior.py, tests/integration/test_headless_consumer_proof.py</files>
  <action>补齐或刷新回归，直接证明 Phase 19 之后的平台 setup、runtime locator 与 headless proof 之间只有 adapter-level 协作，没有 nucleus backflow 或 shared-root 语义串味。</action>
  <verify>`uv run pytest -q tests/core/test_helpers.py tests/core/test_control_plane.py tests/platforms/test_entity_behavior.py tests/integration/test_headless_consumer_proof.py`</verify>
  <done>adapter shell demotion 有稳定的回归护栏，而不是停留在文案层。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/platforms/test_entity_behavior.py tests/integration/test_headless_consumer_proof.py`
- [ ] 平台 setup 仍然只在 adapter 层复用，不回流到 nucleus
- [ ] `runtime_access.py` 仍然只是 control-plane locator，而不是 shared consumer contract
</verification>

<success_criteria>
- HA-only adapter shells 被进一步收薄，但没有反向污染 host-neutral nucleus
- `helpers/platform.py` 与 `runtime_access.py` 的身份更清楚
- 平台/headless regression 能及时暴露 adapter/backflow 回归
</success_criteria>

<output>
After completion, create `.planning/phases/19-headless-consumer-proof-adapter-demotion/19-03-SUMMARY.md`
</output>

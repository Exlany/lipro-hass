---
phase: 19-headless-consumer-proof-adapter-demotion
plan: "02"
type: execute
title: prove auth device replay evidence reuse one nucleus
wave: 2
depends_on:
  - 19-01
files_modified:
  - custom_components/lipro/headless/boot.py
  - tests/harness/headless_consumer.py
  - tests/integration/test_headless_consumer_proof.py
  - tests/core/api/test_protocol_contract_matrix.py
  - tests/core/capability/test_registry.py
  - tests/snapshots/test_device_snapshots.py
  - tests/integration/test_protocol_replay_harness.py
  - tests/integration/test_ai_debug_evidence_pack.py
  - tests/harness/evidence_pack/sources.py
  - tests/meta/test_evidence_pack_authority.py
autonomous: true
requirements:
  - CORE-02
must_haves:
  truths:
    - `auth -> device discovery -> replay/evidence` 必须由同一套 nucleus 与 formal public path 支撑
    - proof harness 只能是 assurance/proof consumer，不能反向成为 production dependency
    - `Coordinator`、`config_flow.py`、`entry_auth.py`、`helpers.platform` 与 `homeassistant` 都不是 headless proof 的必要依赖
  artifacts:
    - `tests/harness/headless_consumer.py`
    - `tests/integration/test_headless_consumer_proof.py`
    - `tests/integration/test_protocol_replay_harness.py`
    - `tests/integration/test_ai_debug_evidence_pack.py`
  key_links:
    - headless boot -> proof harness -> device discovery
    - protocol/device proof -> replay/evidence proof
    - evidence authority update -> only when source surface changes
---

# Plan 19-02: 证明 auth / device / replay / evidence 使用同一 nucleus

<preconditions>
- `19-01` 已建立共享 headless boot contract，并且 focused boot regressions 通过。
- `tests/harness/protocol/replay_driver.py` 与 `scripts/export_ai_debug_evidence_pack.py` 的 assurance-only 身份继续有效。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/baseline/AUTHORITY_MATRIX.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-CONTEXT.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-RESEARCH.md
@.planning/phases/19-headless-consumer-proof-adapter-demotion/19-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 建立 proof-only headless consumer harness</name>
  <files>tests/harness/headless_consumer.py, custom_components/lipro/headless/boot.py</files>
  <action>新增一个 proof-only headless consumer harness，只允许消费 `custom_components/lipro/headless/boot.py`、formal protocol/device contracts 与 assurance helpers；它要显式完成认证、获取设备列表并 materialize `LiproDevice` / `CapabilityRegistry`，而不是复制 `config_flow.py`、`entry_auth.py` 或 `Coordinator` 逻辑。</action>
  <verify>`uv run ruff check tests/harness/headless_consumer.py custom_components/lipro/headless/boot.py`</verify>
  <done>仓库拥有一个真正基于 shared nucleus 的最小 headless consumer proof harness。</done>
</task>

<task type="auto">
  <name>Task 2: 用 integration smoke 证明 headless 不依赖 HA adapter</name>
  <files>tests/integration/test_headless_consumer_proof.py</files>
  <action>补一条 focused integration smoke，证明 headless consumer 不需要 `homeassistant`、`helpers.platform`、`config_flow.py`、`entry_auth.py`、`Coordinator`、`control/*` 才能完成 auth + device discovery 主链。</action>
  <verify>`uv run pytest -q tests/integration/test_headless_consumer_proof.py tests/core/api/test_protocol_contract_matrix.py tests/core/capability/test_registry.py tests/snapshots/test_device_snapshots.py`</verify>
  <done>headless proof 的成功来自复用同一 nucleus，而不是借用 HA adapter 壳。</done>
</task>

<task type="auto">
  <name>Task 3: 串起 replay 与 evidence 的单链路证明</name>
  <files>tests/integration/test_protocol_replay_harness.py, tests/integration/test_ai_debug_evidence_pack.py, tests/core/api/test_protocol_contract_matrix.py, tests/core/capability/test_registry.py, tests/snapshots/test_device_snapshots.py</files>
  <action>以最小改动把 device/protocol proof 与 replay/evidence proof 串成同一条验证故事线：复用现有 canonical contracts、device snapshots、replay driver 与 evidence pack tests，不复制第二套 canonicalization、replay 或 evidence pipeline。</action>
  <verify>`uv run pytest -q tests/integration/test_headless_consumer_proof.py tests/integration/test_protocol_replay_harness.py tests/integration/test_ai_debug_evidence_pack.py`</verify>
  <done>`CORE-02` 的证明从点状回归升级为单链路、可复验的正式证据。</done>
</task>

<task type="auto">
  <name>Task 4: 条件性同步 evidence authority</name>
  <files>tests/harness/evidence_pack/sources.py, tests/meta/test_evidence_pack_authority.py</files>
  <action>仅当 headless proof 新增 evidence source、proof output 或 allowlist consumer 时，才同步扩展 source registry 与 authority guards；若 proof 只是复用既有 source，则显式保持 authority surface 不扩面，避免 silent allowlist。</action>
  <verify>`uv run pytest -q tests/meta/test_evidence_pack_authority.py tests/integration/test_ai_debug_evidence_pack.py`</verify>
  <done>evidence/authority 的扩展具备明确条件，不会因 proof harness 引入隐性长期真源。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/integration/test_headless_consumer_proof.py tests/core/api/test_protocol_contract_matrix.py tests/core/capability/test_registry.py tests/snapshots/test_device_snapshots.py`
- [ ] `uv run pytest -q tests/integration/test_protocol_replay_harness.py tests/integration/test_ai_debug_evidence_pack.py tests/meta/test_evidence_pack_authority.py`
- [ ] `uv run ruff check custom_components/lipro/headless tests/harness/headless_consumer.py tests/integration/test_headless_consumer_proof.py`
- [ ] proof harness 仍然只是 assurance/proof consumer，而不是新 production dependency
</verification>

<success_criteria>
- headless consumer 通过同一 boot contract / protocol root / device truth 完成最小闭环
- replay / evidence 证明链继续走 formal public path，而不是旁路或复制实现
- authority surface 只有在必要时才扩展，并有测试与 allowlist 同步
</success_criteria>

<output>
After completion, create `.planning/phases/19-headless-consumer-proof-adapter-demotion/19-02-SUMMARY.md`
</output>

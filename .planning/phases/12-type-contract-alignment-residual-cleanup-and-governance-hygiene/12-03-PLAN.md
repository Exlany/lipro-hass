---
phase: 12-type-contract-alignment-residual-cleanup-and-governance-hygiene
plan: "03"
title: compat surface narrowing and core api skeleton slimming
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - custom_components/lipro/core/api/__init__.py
  - custom_components/lipro/core/api/client.py
  - custom_components/lipro/core/api/client_base.py
  - custom_components/lipro/core/protocol/facade.py
  - custom_components/lipro/core/device/capabilities.py
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/DEPENDENCY_MATRIX.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/KILL_LIST.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/FILE_MATRIX.md
  - tests/core/api/test_protocol_contract_matrix.py
  - tests/meta/test_dependency_guards.py
  - tests/meta/test_public_surface_guards.py
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - CMP-01
  - CMP-02
---

<objective>
在类型面收口后，继续清理仍登记的 compat seam：`core.api.LiproClient`、`LiproProtocolFacade.get_device_list`、`LiproMqttFacade.raw_client`、`DeviceCapabilities` 与 `core/api` 的 `_ClientBase`/helper mixin 时代骨架。目标是“进一步删除或缩窄”，而不是为了旧测试或旧叙事保留第二套合法入口。
</objective>

<tasks>
<task>盘点 `LiproClient`、`get_device_list`、`raw_client`、`DeviceCapabilities` 与 `_ClientBase` 的 direct consumers，逐项裁决：物理删除、缩窄访问面，或继续保留但把 delete gate 收紧到最小。</task>
<task>把 `custom_components/lipro/core/api/__init__.py`、`custom_components/lipro/core/protocol/facade.py` 与 `custom_components/lipro/core/device/capabilities.py` 的 compat 叙事回写到 active governance truth，确保 `PUBLIC_SURFACES`、`DEPENDENCY_MATRIX`、`VERIFICATION_MATRIX`、`FILE_MATRIX`、kill list 与 residual ledger 与实现现状一致。</task>
<task>继续去骨 `core/api` 的 compat spine：若 `_ClientBase` / endpoint helper mixin 仍需保留，只允许作为显式 transitional seam，不得继续扮演 formal root 或 implicit public surface。</task>
<task>补 public-surface / dependency / governance guards，锁定已关闭项不能回流：`services/wiring.py` 与 `_ClientEndpointsMixin` aggregate export 必须继续保持关闭，不得被误列为本 phase 活跃残留。</task>
</tasks>

## Verification Commands

- `uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/core/api/test_protocol_contract_matrix.py`
- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`

## Verification Criteria

- remaining compat seams 要么被删除，要么被进一步缩窄并写清 delete gate；
- active governance truth（含 file/dependency/verification matrices）与真实 residual inventory 一致；
- `core/api` 历史骨架不再被误读为正式 architecture root；
- 已关闭的 Phase 11 residual 不会被重新合法化。

## Must Haves

- compat only shrinks, never expands
- no reintroduction of `services/wiring.py`
- no `_ClientEndpointsMixin` aggregate export comeback
- governance truth updated with every residual decision

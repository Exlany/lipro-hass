---
phase: 12-type-contract-alignment-residual-cleanup-and-governance-hygiene
plan: "01"
title: coordinator and runtime type contract convergence
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/runtime_types.py
  - custom_components/lipro/core/coordinator/coordinator.py
  - custom_components/lipro/entities/base.py
  - custom_components/lipro/light.py
  - custom_components/lipro/cover.py
  - custom_components/lipro/climate.py
  - custom_components/lipro/fan.py
  - custom_components/lipro/binary_sensor.py
  - custom_components/lipro/sensor.py
  - custom_components/lipro/select.py
  - custom_components/lipro/switch.py
  - custom_components/lipro/update.py
  - tests/core/coordinator/test_entity_protocol.py
  - tests/platforms/test_entity_base.py
autonomous: true
requirements:
  - TYP-01
---

<objective>
让 `LiproCoordinator` public protocol、`Coordinator` concrete implementation、平台 setup consumers 与 `entities/base.py` 的返回/属性语义对齐，消除 current baseline 中最核心的 mypy contract drift，而不是继续用 `Any` / `object` 掩盖真实分歧。
</objective>

<tasks>
<task>明确 `devices` 的正式 public 语义（只读 mapping 还是可写容器），并让 `custom_components/lipro/runtime_types.py`、`custom_components/lipro/core/coordinator/coordinator.py` 与平台/entity 消费者统一遵循同一 truth。</task>
<task>收口 `async_query_ota_info()` 的 public return contract，使 protocol / coordinator / platform call sites 不再在 `list[dict[str, Any]]`、`object` 与更窄结构之间漂移。</task>
<task>修复 `custom_components/lipro/entities/base.py` 与各平台 setup 入口里的 `Any` 泄漏、arg-type mismatch 与 no-any-return，使 platform creation helpers 依赖正式 coordinator surface 而不是 concrete accident。</task>
</tasks>

## Verification Criteria

- `uv run mypy` 在 coordinator/runtime/entity/platform 相关文件上不再报 `arg-type` / `no-any-return` / protocol mismatch；
- 平台 setup 入口全部可通过正式 coordinator public surface 类型检查；
- `devices` 与 OTA query 的 contract 在协议、实现与消费者之间只有一个 truth。

## Must Haves

- no `Any`/`object` masking for coordinator public contract
- devices semantics are explicit and consistent
- OTA query return type has one formal truth
- platform/entity setup sites type-check against the formal surface

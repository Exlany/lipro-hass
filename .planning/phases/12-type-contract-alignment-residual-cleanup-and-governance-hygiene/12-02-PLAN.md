---
phase: 12-type-contract-alignment-residual-cleanup-and-governance-hygiene
plan: "02"
title: rest facade and diagnostics typed return cleanup
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/api/client.py
  - custom_components/lipro/core/api/schedule_service.py
  - custom_components/lipro/control/diagnostics_surface.py
  - custom_components/lipro/services/diagnostics/helpers.py
  - tests/core/api/test_api.py
  - tests/core/test_diagnostics.py
  - tests/services/test_services_diagnostics.py
autonomous: true
requirements:
  - TYP-02
---

<objective>
收口 `LiproRestFacade`、typed service protocols 与 diagnostics helpers 的 typed return/container drift，让 canonical structured rows、mutable containers 与 lookup contracts 真正成为静态类型可表达的正式故事线。
</objective>

<tasks>
<task>对齐 `custom_components/lipro/core/api/client.py` 与 `custom_components/lipro/core/api/schedule_service.py` 的 typed protocol：凡 canonical row / structured mapping 已存在，就不要继续把 `dict[str, Any]` / `object` 当作长期 public return。</task>
<task>收窄 `custom_components/lipro/control/diagnostics_surface.py` 中 runtime/diagnostics containers 的类型，使 indexed assignment、mutation 与 snapshot assembly 不再依赖宽泛 collection typing。</task>
<task>修复 `custom_components/lipro/services/diagnostics/helpers.py` 的 coordinator lookup / runtime-entry helper typing，并在必要处连带收窄已经可识别的 broad error contract。</task>
</tasks>

## Verification Criteria

- `LiproRestFacade` 与 typed service protocols 在 mypy 下不再冲突；
- diagnostics surfaces 的 mutation / lookup typing 不再报错；
- diagnostics / API regression tests 继续证明行为未漂移。

## Must Haves

- typed service protocols match concrete facade methods
- diagnostics containers are typed as actually mutable structures
- no long-term `object` fallback where structured results already exist
- targeted diagnostics and API tests keep behavior stable

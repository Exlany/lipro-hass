---
phase: 141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening
plan: "04"
type: execute
title: harden the device aggregate boundary and isolate runtime side-cars
wave: 4
depends_on:
  - 141-03
files_modified:
  - custom_components/lipro/core/device/device.py
  - custom_components/lipro/core/device/device_runtime.py
  - custom_components/lipro/core/device/extras_support.py
  - custom_components/lipro/control/diagnostics_surface.py
  - tests/core/device/test_device.py
  - tests/core/test_outlet_power.py
  - tests/core/test_outlet_power_runtime.py
  - tests/core/coordinator/runtime/test_device_runtime.py
  - tests/core/test_diagnostics_config_entry.py
  - tests/platforms/test_sensor.py
  - tests/platforms/test_entity_behavior.py
autonomous: true
requirements:
  - HOT-73
  - TST-61
must_haves:
  truths:
    - `LiproDevice` stays an explicit composition-based aggregate instead of regressing into mixin, inheritance, or dynamic delegation folklore
    - MQTT freshness, outlet-power payload retention, and legacy `extra_data` cleanup are isolated behind explicit runtime-sidecar helpers rather than leaking across the aggregate surface
    - device/platform/diagnostics tests prove formal aggregate state stays readable while legacy side-car backdoors remain closed
  artifacts:
    - custom_components/lipro/core/device/device.py
    - custom_components/lipro/core/device/device_runtime.py
    - custom_components/lipro/core/device/extras_support.py
    - custom_components/lipro/control/diagnostics_surface.py
    - tests/core/device/test_device.py
    - tests/core/test_outlet_power.py
    - tests/core/test_outlet_power_runtime.py
    - tests/core/coordinator/runtime/test_device_runtime.py
    - tests/core/test_diagnostics_config_entry.py
  key_links:
    - `custom_components/lipro/core/device/device.py` must keep aggregate-owned surface explicit while delegating runtime-sidecar mechanics to local helper functions instead of mixins/inheritance
    - `custom_components/lipro/control/diagnostics_surface.py` must read formal device primitives rather than revived `extra_data["power_info"]` folklore
    - `tests/core/coordinator/runtime/test_device_runtime.py` must remain in the focused verify lane so direct runtime-sidecar behavior cannot drift outside the plan baseline
    - device/platform tests must freeze the no-legacy-sidecar-backdoor story along with MQTT freshness semantics
---

# Plan 141-04: harden the device aggregate boundary and isolate runtime side-cars

<objective>
把 `LiproDevice` 从“显式组合但 aggregate 宽面仍偏大”继续收紧：明确哪些 surface 仍属于 aggregate formal home，哪些应该退回 runtime-sidecar helper，同时关闭 `extra_data` / MQTT freshness / outlet power 的 lingering folklore seam。

Purpose: 在 runtime contract root 已缩窄之后，再把 device aggregate 边界与 runtime-sidecar 语义收口，避免 future refactor 继续背 relay-wall 与 side-car cleanup 税。
Output: 更聚焦的 `LiproDevice` aggregate、显式 runtime-sidecar helper 归属、更新后的 device/outlet-power/diagnostics/platform tests。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening/141-CONTEXT.md
@.planning/phases/141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening/141-RESEARCH.md
@.planning/phases/137-hotspot-burn-down-command-observability-and-governance-compression/137-SUMMARY.md
@custom_components/lipro/core/device/device.py
@custom_components/lipro/core/device/device_runtime.py
@custom_components/lipro/core/device/extras_support.py
@custom_components/lipro/control/diagnostics_surface.py
@tests/core/device/test_device.py
@tests/core/test_outlet_power.py
@tests/core/test_outlet_power_runtime.py
@tests/core/test_diagnostics_config_entry.py
@tests/platforms/test_sensor.py
@tests/platforms/test_entity_behavior.py

Phase 141 requirement IDs: `HOT-73` = device aggregate boundary hardening and runtime side-car isolation without mixin/inheritance regressions, `TST-61` = focused control/runtime/device/meta/governance lanes freeze the change.
</context>

<tasks>
<task type="auto">
  <name>Task 141-04-01: codify the aggregate-owned device surface and trim relay-wall spillover</name>
  <files>custom_components/lipro/core/device/device.py, custom_components/lipro/core/device/device_runtime.py</files>
  <read_first>custom_components/lipro/core/device/device.py, custom_components/lipro/core/device/device_runtime.py, tests/core/device/test_device.py</read_first>
  <action>以 `LiproDevice` 仍是 aggregate façade 为前提，明确哪些 relay/property/method 仍应保留在 aggregate formal surface，哪些应收回 `device_runtime.py` 一类 local helper。优先通过显式组合、函数抽离、helper delegation 继续瘦身；绝对不要用 mixin、继承层次、动态 `__getattr__` 或“基类 + patch”回退。保留 dataclass + composed views/state/extras 的 north-star story，同时减少 aggregate 直接背负 side-car cleanup 的机会。</action>
  <acceptance_criteria>
    - `LiproDevice` 仍是 explicit composition façade，而不是 mixin/inheritance comeback
    - aggregate formal surface 与 runtime-sidecar helper 归属更清晰，relay wall 不再继续无边界生长
    - device surface tests 能直接证明 explicit properties/methods 仍然可读且无动态 delegation 回流
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/device/test_device.py tests/core/coordinator/runtime/test_device_runtime.py tests/platforms/test_sensor.py tests/platforms/test_entity_behavior.py</automated>
  </verify>
  <done>`LiproDevice` 继续保持单一 aggregate façade 身份，但 runtime-sidecar 语义不再散落在 aggregate surface 各处。</done>
</task>

<task type="auto">
  <name>Task 141-04-02: isolate MQTT freshness and outlet-power side-cars behind local helpers</name>
  <files>custom_components/lipro/core/device/device.py, custom_components/lipro/core/device/device_runtime.py, custom_components/lipro/core/device/extras_support.py, custom_components/lipro/control/diagnostics_surface.py</files>
  <read_first>custom_components/lipro/core/device/device.py, custom_components/lipro/core/device/device_runtime.py, custom_components/lipro/core/device/extras_support.py, custom_components/lipro/control/diagnostics_surface.py, tests/core/test_outlet_power.py, tests/core/test_outlet_power_runtime.py, tests/core/coordinator/runtime/test_device_runtime.py, tests/core/test_diagnostics_config_entry.py</read_first>
  <action>把 MQTT freshness timestamp、`outlet_power_info` formal primitive、legacy `extra_data["power_info"]` cleanup 与 diagnostics read-model 明确收敛到本地 side-car helper 语义：`device.py` 只保留 aggregate-facing property/method contract，side-car bookkeeping 尽量移回 `device_runtime.py`/local helper，而 `diagnostics_surface.py` 只读取 formal primitive。不得让 `extra_data` 再次成为 power-info backdoor，也不得把 runtime-sidecar 语义传播到 platform/entity callers。</action>
  <acceptance_criteria>
    - outlet-power 与 MQTT freshness 的 formal home 清晰，可从 helper/function boundary 直接读出
    - `diagnostics_surface.py` 继续通过 formal primitive 导出，而不是回流 `extra_data` legacy field
    - runtime-sidecar logic 只在本地 helper 范围内演化，不污染 aggregate outward story
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_outlet_power.py tests/core/test_outlet_power_runtime.py tests/core/coordinator/runtime/test_device_runtime.py tests/core/test_diagnostics_config_entry.py</automated>
  </verify>
  <done>MQTT freshness 与 outlet-power/legacy cleanup 已被隔离到明确 local helper seam；aggregate outward story 保持稳定。</done>
</task>

<task type="auto">
  <name>Task 141-04-03: expand device-focused guards for side-car regressions</name>
  <files>tests/core/device/test_device.py, tests/core/test_outlet_power.py, tests/core/test_outlet_power_runtime.py, tests/core/coordinator/runtime/test_device_runtime.py, tests/core/test_diagnostics_config_entry.py, tests/platforms/test_sensor.py, tests/platforms/test_entity_behavior.py</files>
  <read_first>tests/core/device/test_device.py, tests/core/test_outlet_power.py, tests/core/test_outlet_power_runtime.py, tests/core/test_diagnostics_config_entry.py, tests/platforms/test_sensor.py, tests/platforms/test_entity_behavior.py</read_first>
  <action>补齐 focused regressions，冻结三件事：`LiproDevice` 没有回到 dynamic delegation / mixin/inheritance 叙事；`outlet_power_info` 仍是 formal primitive 且 legacy `power_info` 不回流；MQTT freshness 与 device state/platform projections 的读法没有漂回 side-car folklore。测试应覆盖 aggregate、runtime、diagnostics、platform projection 四个视角，但保持 focused，不把 unrelated protocol/control concerns 混入。</action>
  <acceptance_criteria>
    - device-focused tests 能直接抓到 `extra_data` backdoor、MQTT freshness semantics drift 与 aggregate relay-wall 回流
    - diagnostics/platform tests 证明 consumer 仍通过 formal primitives/aggregate surface 读取 device truth
    - 任何 mixin/inheritance comeback 都会被 guard lane 或 explicit-surface assertions 立刻击中
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/device/test_device.py tests/core/test_outlet_power.py tests/core/test_outlet_power_runtime.py tests/core/coordinator/runtime/test_device_runtime.py tests/core/test_diagnostics_config_entry.py tests/platforms/test_sensor.py tests/platforms/test_entity_behavior.py</automated>
  </verify>
  <done>Device aggregate hardening 已被 focused tests 冻结；runtime side-car isolation 不再依赖人为记忆维持。</done>
</task>
</tasks>

<verification>
- `uv run pytest -q tests/core/device/test_device.py tests/core/test_outlet_power.py tests/core/test_outlet_power_runtime.py tests/core/coordinator/runtime/test_device_runtime.py tests/core/test_diagnostics_config_entry.py tests/platforms/test_sensor.py tests/platforms/test_entity_behavior.py`
- `uv run ruff check custom_components/lipro/core/device custom_components/lipro/control/diagnostics_surface.py tests/core/device/test_device.py tests/core/test_outlet_power.py tests/core/test_outlet_power_runtime.py tests/core/test_diagnostics_config_entry.py tests/platforms/test_sensor.py tests/platforms/test_entity_behavior.py`
</verification>

<success_criteria>
- `LiproDevice` aggregate boundary 更窄、更显式，且没有通过 mixin/inheritance/dynamic delegation 回退
- MQTT freshness、outlet-power primitive、legacy cleanup 的 runtime side-car 归属清晰且只在本地 helper 演化
- focused device/diagnostics/platform lanes 能自动阻止 legacy side-car backdoor 回流
</success_criteria>

<output>
After completion, create `.planning/phases/141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening/141-04-SUMMARY.md`
</output>

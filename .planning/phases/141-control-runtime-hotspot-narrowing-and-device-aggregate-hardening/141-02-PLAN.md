---
phase: 141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening
plan: "02"
type: execute
title: slim entry-root lazy imports and wire explicit factories
wave: 2
depends_on:
  - 141-01
files_modified:
  - custom_components/lipro/control/entry_root_support.py
  - custom_components/lipro/control/entry_root_wiring.py
  - custom_components/lipro/__init__.py
  - tests/core/test_entry_root_wiring.py
  - tests/core/test_init_runtime_registry_refresh.py
  - tests/meta/test_phase103_root_thinning_guards.py
autonomous: true
requirements:
  - ARC-50
  - TST-61
must_haves:
  truths:
    - root setup keeps factory-on-call semantics and test patchability without relying on raw module-name folklore or generic `load_module()` plumbing everywhere
    - `entry_root_support.py` exposes explicit lazy factory helpers and service-registry/controller wiring seams instead of opaque string-module-name indirection
    - predecessor guards catch any second bootstrap story or loss of root-adapter thinness immediately
  artifacts:
    - custom_components/lipro/control/entry_root_support.py
    - custom_components/lipro/control/entry_root_wiring.py
    - custom_components/lipro/__init__.py
    - tests/core/test_entry_root_wiring.py
    - tests/core/test_init_runtime_registry_refresh.py
    - tests/meta/test_phase103_root_thinning_guards.py
  key_links:
    - `custom_components/lipro/__init__.py` must continue to build factories on call, not eagerly import runtime collaborators at module import time
    - `custom_components/lipro/control/entry_root_wiring.py` must receive explicit controller/service-registry factories rather than raw controller module names and generic loaders
    - `tests/meta/test_phase103_root_thinning_guards.py` must freeze the single HA root-adapter story and reject a second bootstrap narrative
---

# Plan 141-02: slim entry-root lazy imports and wire explicit factories

<objective>
降低 `entry_root_support.py` / `__init__.py` 的 lazy-import tax，把 generic string module names + generic loader folklore 收口为显式 factory wiring，同时保住 HA root adapter 的 thinness、factory-on-call semantics 与测试 patchability。

Purpose: 让维护者不必在 root adapter、support helper 与 wiring helper 之间靠字符串推理真实依赖链；同时保持 `Phase 103` 已冻结的单一 root-adapter 故事。
Output: 更显式的 entry-root support/wiring seam、更诚实的 root factory wiring、更新后的 root wiring tests 与 predecessor guard。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening/141-CONTEXT.md
@.planning/phases/141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening/141-RESEARCH.md
@.planning/phases/103-root-adapter-thinning-test-topology-second-pass-and-terminology-contract-normalization/103-03-SUMMARY.md
@custom_components/lipro/control/entry_root_support.py
@custom_components/lipro/control/entry_root_wiring.py
@custom_components/lipro/__init__.py
@tests/core/test_entry_root_wiring.py
@tests/core/test_init_runtime_registry_refresh.py
@tests/meta/test_phase103_root_thinning_guards.py

Phase 141 requirement IDs: `ARC-50` = explicit entry-root lazy factory wiring without second bootstrap story, `TST-61` = focused control/runtime/device/meta/governance lanes freeze the change.
</context>

<tasks>
<task type="auto">
  <name>Task 141-02-01: replace generic module-name folklore with explicit lazy factory helpers</name>
  <files>custom_components/lipro/control/entry_root_support.py, custom_components/lipro/control/entry_root_wiring.py</files>
  <read_first>custom_components/lipro/control/entry_root_support.py, custom_components/lipro/control/entry_root_wiring.py, custom_components/lipro/__init__.py</read_first>
  <action>把 `entry_root_support.py` 里的 `_CORE_MODULE_NAME` / `_COORDINATOR_MODULE_NAME` / `_ENTRY_AUTH_MODULE_NAME` / `_SERVICE_REGISTRY_MODULE_NAME` + generic `load_module()` usage 收敛成显式 lazy loader/factory helper：维护者应能直接看出 core/coordinator/entry-auth/service-registry/controller 的 wiring 入口，而不必跨文件追字符串。同步收窄 `entry_root_wiring.py`，让它接收明确 controller/service-registry factory seams，而不是再透传 raw module name + generic loader。保留 runtime-only import 姿态与 module-level patchability；不得把 eager import 带回 `__init__.py`，也不得制造第二 root adapter home。</action>
  <acceptance_criteria>
    - root wiring 真实依赖链可以从 helper 名称直接读出，而不是通过字符串拼接推理
    - 仍然保留 lazy factory-on-call 行为，避免在模块导入时提前绑定 runtime-heavy collaborators
    - `entry_root_wiring.py` 更像 explicit assembly helper，而不是 generic module-loader relay
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_entry_root_wiring.py tests/core/test_init_runtime_registry_refresh.py</automated>
  </verify>
  <done>Entry-root lazy wiring 变成可解释的 explicit factories；维护成本从字符串 folklore 降到可读组合关系。</done>
</task>

<task type="auto">
  <name>Task 141-02-02: keep the HA root adapter thin and patch-friendly under the new factory wiring</name>
  <files>custom_components/lipro/__init__.py, tests/core/test_entry_root_wiring.py, tests/core/test_init_runtime_registry_refresh.py, tests/meta/test_phase103_root_thinning_guards.py</files>
  <read_first>custom_components/lipro/__init__.py, tests/core/test_entry_root_wiring.py, tests/core/test_init_runtime_registry_refresh.py, tests/meta/test_phase103_root_thinning_guards.py</read_first>
  <action>更新 `custom_components/lipro/__init__.py`，让它继续只承担 thin adapter + on-demand factory binding，而不再继续向下透传 controller module name / generic load module folklore。同步测试与 `Phase 103` predecessor guard：断言 `LiproProtocolFacade`、`LiproAuthManager`、`Coordinator` 仍是可 patch 的 lazy factories，`__init__.py` 仍然只讲一个 HA root story，`entry_root_support.py` 只是 support helper 而不是第二 bootstrap root。不要在本任务顺手引入新的 root-level re-export 或 runtime singleton。</action>
  <acceptance_criteria>
    - `__init__.py` 的 outward contract 仍是 thin root adapter + lazy factories
    - tests 可以在不依赖 raw module-name strings 的前提下 patch collaborators
    - `test_phase103_root_thinning_guards.py` 能直接抓到第二 bootstrap story 或 eager-import drift
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_entry_root_wiring.py tests/core/test_init_runtime_registry_refresh.py tests/meta/test_phase103_root_thinning_guards.py</automated>
  </verify>
  <done>Root adapter 继续保持单一 sanctioned 入口，但 wiring tax 与字符串推理成本显著下降。</done>
</task>
</tasks>

<verification>
- `uv run pytest -q tests/core/test_entry_root_wiring.py tests/core/test_init_runtime_registry_refresh.py tests/meta/test_phase103_root_thinning_guards.py`
- `uv run ruff check custom_components/lipro/control/entry_root_support.py custom_components/lipro/control/entry_root_wiring.py custom_components/lipro/__init__.py tests/core/test_entry_root_wiring.py tests/core/test_init_runtime_registry_refresh.py tests/meta/test_phase103_root_thinning_guards.py`
</verification>

<success_criteria>
- `entry_root_support.py` 与 `entry_root_wiring.py` 讲的是显式 lazy factory wiring，而不是 generic module-name folklore
- `custom_components/lipro/__init__.py` 继续保持 single HA root adapter / factory-on-call semantics / patch-friendly setup
- predecessor guard 与 focused root wiring tests 能自动阻止第二 bootstrap story 回流
</success_criteria>

<output>
After completion, create `.planning/phases/141-control-runtime-hotspot-narrowing-and-device-aggregate-hardening/141-02-SUMMARY.md`
</output>

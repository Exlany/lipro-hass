---
phase: 32-truth-convergence-gate-honesty-and-quality-10-closeout
plan: "02"
type: execute
title: align repo-wide gate honesty and toolchain reality
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .planning/baseline/VERIFICATION_MATRIX.md
  - CONTRIBUTING.md
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - tests/meta/test_toolchain_truth.py
autonomous: true
requirements:
  - QLT-05
must_haves:
  truths:
    - repo-wide gate claims must match the actual enforced scope of `ruff`, `mypy`, CI, and contributor guidance
    - gate honesty is more important than preserving a flattering but false repo-wide green story
    - no new toolchain narrative may bypass the existing `ci.yml` truth chain
  artifacts:
    - .github/workflows/ci.yml
    - .planning/baseline/VERIFICATION_MATRIX.md
    - CONTRIBUTING.md
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_toolchain_truth.py
  key_links:
    - ci -> verification matrix -> runbook/contributing -> toolchain truth guards
---

# Plan 32-02: stop overstating toolchain truth

<context>
@.github/workflows/ci.yml
@.planning/baseline/VERIFICATION_MATRIX.md
@CONTRIBUTING.md
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@tests/meta/test_toolchain_truth.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: align CI, verification matrix, and maintainer guidance on actual blocking gates</name>
  <files>.github/workflows/ci.yml, .planning/baseline/VERIFICATION_MATRIX.md, docs/MAINTAINER_RELEASE_RUNBOOK.md, CONTRIBUTING.md</files>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/baseline/VERIFICATION_MATRIX.md
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - CONTRIBUTING.md
  </read_first>
  <action>梳理 `ruff` / `mypy` / governance scripts / meta suites 在 CI、verification matrix、runbook、贡献者文档中的真实地位：若它们已经是 blocking gate，就显式写清；若仍只是 target truth 或分区 gate，就统一收窄口径，禁止继续出现 repo-wide full-green 幻象。</action>
  <acceptance_criteria>
    - CI, verification matrix, runbook, and CONTRIBUTING describe one coherent blocking-gate story
    - no touched doc implies a stronger repo-wide `mypy` or CI contract than what is actually enforced
    - `uv run pytest -q tests/meta/test_toolchain_truth.py -k "toolchain or runbook or contributing"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_toolchain_truth.py -k "toolchain or runbook or contributing"`</verify>
  <done>toolchain truth becomes honest and machine-guarded instead of spread across optimistic prose.</done>
</task>

<task type="auto">
  <name>Task 2: make `ruff`/`mypy` truth machine-checkable instead of socially implied</name>
  <files>.github/workflows/ci.yml, tests/meta/test_toolchain_truth.py</files>
  <read_first>
    - .github/workflows/ci.yml
    - tests/meta/test_toolchain_truth.py
  </read_first>
  <action>把 `ruff` 与 `mypy` 的 truth 从“大家知道应该跑”升级为脚本/guard 可判定的契约：要么让 repo-wide gate 真正成立，要么在 CI / docs / tests 中显式写明实际范围与 future target，避免继续依赖维护者脑内知识。</action>
  <acceptance_criteria>
    - touched tests fail if docs/CI overstate repo-wide gate truth again
    - `uv run ruff check .` remains green
    - `uv run pytest -q tests/meta/test_toolchain_truth.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run ruff check . && uv run pytest -q tests/meta/test_toolchain_truth.py`</verify>
  <done>repo-wide gate honesty stops being a soft convention and becomes an explicit, testable contract.</done>
</task>
</tasks>

<verification>
- [ ] `uv run ruff check . && uv run pytest -q tests/meta/test_toolchain_truth.py`
</verification>

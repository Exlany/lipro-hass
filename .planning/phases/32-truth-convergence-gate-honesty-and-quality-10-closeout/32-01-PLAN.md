---
phase: 32-truth-convergence-gate-honesty-and-quality-10-closeout
plan: "01"
type: execute
title: converge planning truth and repair requirement traceability
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - tests/meta/test_governance_closeout_guards.py
autonomous: true
requirements:
  - GOV-24
must_haves:
  truths:
    - active planning truth must say one story about `Phase 25 -> 31` complete and `Phase 32` pending
    - coverage counts and traceability rows must reflect current reality rather than historical seed snapshots
    - retained handoff and audit pointers may stay as historical baselines but must not masquerade as current route truth
  artifacts:
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - tests/meta/test_governance_closeout_guards.py
  key_links:
    - project -> roadmap -> requirements -> state -> governance closeout guards
---

# Plan 32-01: planning truth convergence before any more execution

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@tests/meta/test_governance_closeout_guards.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: converge active planning truth onto `Phase 32` as the current final tranche</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>把 `PROJECT` / `ROADMAP` / `STATE` 对 v1.3 continuation 的 current story 统一改写为：`Phase 25 -> 31` 已完成、`Phase 32` 是当前 active final-closeout tranche；同时显式说明 `v1.3-HANDOFF.md` / `v1.3-MILESTONE-AUDIT.md` 继续保留历史 closeout baseline 身份，但不再是 current route 的唯一真源。</action>
  <acceptance_criteria>
    - `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` all mention `Phase 32` as the active next tranche
    - no active planning doc still describes `Phase 25 -> 31` as the final closeout-ready story
    - `uv run pytest -q tests/meta/test_governance_closeout_guards.py -k "phase_32 or follow_through"` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_closeout_guards.py -k "phase_32 or follow_through"`</verify>
  <done>active planning truth now has one route story instead of a stale closeout snapshot plus an untracked new tranche.</done>
</task>

<task type="auto">
  <name>Task 2: repair requirement ledger, traceability coverage, and guard truth for the new tranche</name>
  <files>.planning/REQUIREMENTS.md, tests/meta/test_governance_closeout_guards.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - tests/meta/test_governance_closeout_guards.py
  </read_first>
  <action>把本轮终审要求正式映射成 `GOV-24 / QLT-05 / GOV-25 / GOV-26 / HOT-07 / TST-04 / TYP-08 / ERR-06 / RES-06`，并修正 `Seed Coverage` / traceability 表 / meta guard，使 requirement truth 对 `20 complete + 9 pending` 讲实话，而不是继续沿用旧 seed snapshot。</action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains 9 new `Phase 32` requirements and `Pending` traceability rows
    - `Seed Coverage` reports `29 total / 29 mapped / 20 complete / 9 pending / 0 unmapped`
    - `uv run pytest -q tests/meta/test_governance_closeout_guards.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_closeout_guards.py`</verify>
  <done>Phase 32 is no longer a spoken intention; it becomes a routed, counted, machine-guarded requirement tranche.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_closeout_guards.py`
</verification>

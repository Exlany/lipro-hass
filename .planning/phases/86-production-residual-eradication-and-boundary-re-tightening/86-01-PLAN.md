---
phase: 86-production-residual-eradication-and-boundary-re-tightening
plan: "01"
type: execute
title: remove ShareWorkerClient compatibility seams and converge tests on typed outcomes
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/anonymous_share/share_client.py
  - tests/core/test_share_client_primitives.py
  - tests/core/test_share_client_submit.py
autonomous: true
requirements:
  - HOT-37
  - ARC-22
must_haves:
  truths:
    - `ShareWorkerClient` 继续是 anonymous-share worker client 的唯一正式 outward home。
    - `_safe_read_json()` backward alias 与 bool-only `submit_share_payload()` shim 被正式删除，而不是迁移到新的 compat shell。
    - touched tests 只承认 `safe_read_json()` 与 `submit_share_payload_with_outcome()` 的 outcome-native contract。
  artifacts:
    - custom_components/lipro/core/anonymous_share/share_client.py
    - tests/core/test_share_client_primitives.py
    - tests/core/test_share_client_submit.py
  key_links:
    - ShareWorkerClient.safe_read_json -> share_client_flows.safe_read_json
    - ShareWorkerClient.submit_share_payload_with_outcome -> OperationOutcome-based submit truth
---

# Plan 86-01: remove ShareWorkerClient compatibility seams and converge tests on typed outcomes

<objective>
把 `ShareWorkerClient` 中最后两条 symbol-level compat seam 一次删除干净：`_safe_read_json()` backward alias 与 bool-only `submit_share_payload()` shim 不再继续作为 formal surface 的影子合同。同步把相邻测试收口到 typed outcome / formal reader 语义，避免 residual 只是从 production 代码转移到测试 folklore。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-production-residual-eradication-and-boundary-re-tightening/86-CONTEXT.md
@.planning/phases/86-production-residual-eradication-and-boundary-re-tightening/86-RESEARCH.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@.planning/reviews/FILE_MATRIX.md
@custom_components/lipro/core/anonymous_share/share_client.py
@custom_components/lipro/core/anonymous_share/share_client_flows.py
@custom_components/lipro/core/anonymous_share/share_client_support.py
@custom_components/lipro/core/anonymous_share/share_client_refresh.py
@custom_components/lipro/core/anonymous_share/share_client_submit.py
@custom_components/lipro/core/anonymous_share/manager.py
@tests/core/test_share_client_primitives.py
@tests/core/test_share_client_submit.py
@tests/core/test_share_client_refresh.py

<interfaces>
执行后必须保留的正式 surface：

```python
class ShareWorkerClient:
    async def safe_read_json(
        self,
        response: JsonReadableResponse,
    ) -> WorkerResponsePayload | None: ...

    async def refresh_install_token_with_outcome(
        self,
        session: aiohttp.ClientSession,
    ) -> OperationOutcome: ...

    async def submit_share_payload_with_outcome(
        self,
        session: aiohttp.ClientSession,
        report: SharePayload,
        *,
        label: str,
        ensure_loaded: Callable[[], Awaitable[None]],
    ) -> OperationOutcome: ...
```

本 plan 不得新增任何新的 compat alias / bool wrapper 取代已删除的方法。
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: delete the remaining ShareWorkerClient compat methods without moving the formal owner</name>
  <files>custom_components/lipro/core/anonymous_share/share_client.py</files>
  <action>删除 `_safe_read_json()` backward-compatible alias 与 bool-only `submit_share_payload()` shim；保留 `safe_read_json()` 与 `submit_share_payload_with_outcome()` 作为本 phase 要收口的正式 contract。`refresh_install_token_with_outcome()` / `refresh_install_token()` 的现有双层表达不属于本 phase 删除范围。不得把兼容逻辑迁到别的模块、不得新增 adapter，也不要把 typed submit truth 从 `ShareWorkerClient` 挪走。</action>
  <acceptance_criteria>
    - `share_client.py` 不再定义 `_safe_read_json()`
    - `share_client.py` 不再定义 bool `submit_share_payload()`
    - formal home 仍然是 `ShareWorkerClient`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_share_client_primitives.py tests/core/test_share_client_refresh.py tests/core/test_share_client_submit.py</automated>
  </verify>
  <done>ShareWorkerClient 只保留正式 JSON reader 与 typed submit/refresh contracts。</done>
</task>

<task type="auto">
  <name>Task 2: rewrite adjacent tests so they speak the typed formal contract directly</name>
  <files>tests/core/test_share_client_primitives.py, tests/core/test_share_client_submit.py</files>
  <action>把 touched tests 从 legacy alias / bool assertion 收口到正式 contract：primitive tests 只检查 `safe_read_json()`；submit tests 统一改用 `submit_share_payload_with_outcome()` 并直接断言 `OperationOutcome.is_success`、`reason_code`、`http_status`、`handling_policy` 等 typed semantics。不要为了保留旧断言而在测试里再造 shim。</action>
  <acceptance_criteria>
    - 没有测试再调用 `client._safe_read_json(...)`
    - 没有 touched test 继续把 `ShareWorkerClient.submit_share_payload()` 当作主合同
    - 失败 / 限流 / token rejection / payload-too-large 等路径继续由 typed outcome 明确表达
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_share_client_primitives.py tests/core/test_share_client_submit.py</automated>
  </verify>
  <done>share-client tests 不再承认 legacy compatibility seam，而是直接锁住 typed outcome truth。</done>
</task>
</tasks>

<risks>
- 不要把 manager 层现有 scope-local bool helper 误当作本 plan 的强制删除目标；本 plan 只清 `ShareWorkerClient` formal home 内已登记的 compat seam。
- 不要为了保持旧测试写法而新增 fixture adapter 或 monkeypatch wrapper。
- 不要改变 Worker submit / refresh 的 outward behavior，只收口 contract 表达方式。
</risks>

<non_goals>
- 不处理 `manager.py` / `manager_submission.py` 的额外 API redesign。
- 不删除 `refresh_install_token()` bool wrapper；它当前仍有 refresh-focused tests 消费，不在已登记 delete gate 范围内。
- 不做 anonymous-share family 的大规模 module reshaping；formal home 与 flow/support 分工保持现状。
</non_goals>

<verification>
- `uv run pytest -q tests/core/test_share_client_primitives.py tests/core/test_share_client_refresh.py tests/core/test_share_client_submit.py tests/core/anonymous_share/test_manager_submission.py`
- `uv run pytest -q tests/meta/test_phase61_formal_home_budget_guards.py tests/meta/test_phase70_governance_hotspot_guards.py`
- `uv run ruff check custom_components/lipro/core/anonymous_share/share_client.py tests/core/test_share_client_primitives.py tests/core/test_share_client_submit.py`
</verification>

<success_criteria>
- `ShareWorkerClient` 不再保留 `_safe_read_json()` 与 bool `submit_share_payload()`
- share-client tests 只承认 `safe_read_json()` 与 `submit_share_payload_with_outcome()`
- formal ownership 没有漂移，也没有新增 compat shell
</success_criteria>

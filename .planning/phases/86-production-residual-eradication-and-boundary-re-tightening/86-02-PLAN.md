---
phase: 86-production-residual-eradication-and-boundary-re-tightening
plan: "02"
type: execute
title: inward split runtime_infra listener and reload mechanics without moving the runtime root
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/runtime_infra.py
  - custom_components/lipro/runtime_infra_device_registry.py
  - tests/core/test_runtime_infra.py
  - tests/core/test_init_runtime_setup_entry.py
  - tests/meta/test_phase69_support_budget_guards.py
  - tests/meta/test_phase72_runtime_bootstrap_route_guards.py
  - tests/meta/dependency_guards_service_runtime.py
autonomous: true
requirements:
  - HOT-37
  - ARC-22
must_haves:
  truths:
    - `runtime_infra.py` 继续是 runtime infra 的唯一 outward formal home。
    - device-registry listener / reload planning / pending-task bookkeeping inward split 到 local helper module，而不是生成 second root。
    - `__init__.py`、control wiring、runtime setup 路径继续消费同一批 outward entry points，不新增 backdoor。
  artifacts:
    - custom_components/lipro/runtime_infra.py
    - custom_components/lipro/runtime_infra_device_registry.py
    - tests/core/test_runtime_infra.py
    - tests/meta/test_phase69_support_budget_guards.py
    - tests/meta/test_phase72_runtime_bootstrap_route_guards.py
  key_links:
    - __init__.py -> runtime_infra.py -> runtime_infra_device_registry.py
    - runtime_infra public entry points -> unchanged control/runtime callers
---

# Plan 86-02: inward split runtime_infra listener and reload mechanics without moving the runtime root

<objective>
把 `runtime_infra.py` 中 device-registry listener / reload orchestration / pending-task bookkeeping 的密集实现 inward split 到 local helper module，降低 formal home 的维护密度；同时保留 `runtime_infra.py` 作为唯一 outward truth，不生成 second root，不把 helper 公开给 control/runtime 调用方。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/86-production-residual-eradication-and-boundary-re-tightening/86-CONTEXT.md
@.planning/phases/86-production-residual-eradication-and-boundary-re-tightening/86-RESEARCH.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/FILE_MATRIX.md
@custom_components/lipro/runtime_infra.py
@custom_components/lipro/__init__.py
@custom_components/lipro/control/entry_lifecycle_support.py
@custom_components/lipro/control/entry_root_wiring.py
@tests/core/test_runtime_infra.py
@tests/core/test_init_runtime_setup_entry.py
@tests/core/test_init.py
@tests/meta/test_phase69_support_budget_guards.py
@tests/meta/test_phase72_runtime_bootstrap_route_guards.py
@tests/meta/dependency_guards_service_runtime.py

<interfaces>
执行后必须继续由 `custom_components/lipro/runtime_infra.py` 暴露的 outward contract：

```python
def async_setup_device_registry_listener(
    hass: HomeAssistant,
    *,
    domain: str,
    logger: logging.Logger,
    reload_entry: Callable[[str], Awaitable[object]],
) -> CALLBACK_TYPE: ...

def setup_device_registry_listener(
    hass: HomeAssistant,
    *,
    logger: logging.Logger,
) -> None: ...

def remove_device_registry_listener(hass: HomeAssistant) -> None: ...

def get_runtime_infra_lock(hass: HomeAssistant) -> asyncio.Lock | None: ...

async def async_ensure_runtime_infra(
    hass: HomeAssistant,
    *,
    setup_services: Callable[[HomeAssistant], Awaitable[None]],
    setup_device_registry_listener: Callable[[HomeAssistant], None],
) -> None: ...

def has_other_runtime_entries(
    hass: HomeAssistant,
    *,
    exclude_entry_id: str,
) -> bool: ...
```

新 helper module 只允许被 `runtime_infra.py` inward import；不得从 `__init__.py`、control plane 或 tests 之外的 production callers 直接公开。
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: extract device-registry listener and reload bookkeeping into one local helper module</name>
  <files>custom_components/lipro/runtime_infra.py, custom_components/lipro/runtime_infra_device_registry.py</files>
  <action>把 device-registry update 解析、reload plan 构建、pending reload task bookkeeping、listener callback mechanics 从 `runtime_infra.py` inward split 到 `runtime_infra_device_registry.py` 或等价单一 local helper module。`runtime_infra.py` 继续承接 outward entry points，并只通过 local import 调用 helper。若 lock/bootstrap orchestration 仍足够薄，可留在 `runtime_infra.py`；不要为了拆而再造第二个 helper root。</action>
  <acceptance_criteria>
    - `runtime_infra.py` 继续是唯一 outward import home
    - 新 helper module 的 production importer 只允许是 `custom_components/lipro/runtime_infra.py`
    - listener/reload logic 的行为与现有 tests 保持一致
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_runtime_infra.py tests/core/test_init_runtime_setup_entry.py tests/core/test_init.py</automated>
  </verify>
  <done>`runtime_infra.py` 变薄但 formal ownership 不变；device-registry mechanics 收口到 local helper module。</done>
</task>

<task type="auto">
  <name>Task 2: update focused tests and meta guards so the new helper remains local and truthful</name>
  <files>tests/core/test_runtime_infra.py, tests/core/test_init_runtime_setup_entry.py, tests/meta/test_phase69_support_budget_guards.py, tests/meta/test_phase72_runtime_bootstrap_route_guards.py, tests/meta/dependency_guards_service_runtime.py</files>
  <action>同步更新 focused unit/meta guards：验证 runtime infra outward API 仍然稳定；为新 helper module 记录 import locality / line-budget truth；确保 service/runtime boundary prose 仍把 listener truth 归到 `runtime_infra.py`，而不是让 helper 抢走 formal-home 叙事。</action>
  <acceptance_criteria>
    - runtime-focused tests 继续覆盖 listener setup / teardown / reload scheduling / shared-lock behavior
    - phase69/72 meta guards truthful 记录新 helper locality
    - dependency guard 继续保证 `runtime_infra.py` 而非其它模块承担 runtime infra current truth
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/meta/test_phase69_support_budget_guards.py tests/meta/test_phase72_runtime_bootstrap_route_guards.py tests/meta/dependency_guards_service_runtime.py</automated>
  </verify>
  <done>拆分后的 runtime infra 既更易维护，又仍受 focused/meta guards 约束，不会回长或回流成 second root。</done>
</task>
</tasks>

<risks>
- 不要把 `runtime_infra_device_registry.py` 公开成新 outward home 或让 `__init__.py` 直接导入它。
- 不要让 helper 反向定义 `runtime_infra.py` 的 public truth；正式名字、formal owner 与 file-matrix 归属必须仍锚定 `runtime_infra.py`。
- 不要顺手改 control/service/runtime 的其它边界故事，例如 `runtime_access`、`service_registry` 或 entry lifecycle API。
</risks>

<non_goals>
- 不重写 `entry_lifecycle_support.py` 或 `entry_root_wiring.py` 的依赖注入结构。
- 不处理 giant assurance suites，也不做 broader runtime architecture redesign。
</non_goals>

<verification>
- `uv run pytest -q tests/core/test_runtime_infra.py tests/core/test_init_runtime_setup_entry.py tests/core/test_init.py`
- `uv run pytest -q tests/meta/test_phase69_support_budget_guards.py tests/meta/test_phase72_runtime_bootstrap_route_guards.py tests/meta/dependency_guards_service_runtime.py`
- `uv run ruff check custom_components/lipro/runtime_infra.py custom_components/lipro/runtime_infra_device_registry.py tests/core/test_runtime_infra.py tests/meta/test_phase69_support_budget_guards.py tests/meta/test_phase72_runtime_bootstrap_route_guards.py`
</verification>

<success_criteria>
- `runtime_infra.py` 继续暴露原有 outward entry points，但 listener/reload 机械细节被 inward split
- 新 helper module 保持 production-local import locality
- runtime/service boundary truth 与 meta guard 一致，不出现 second root / backdoor / helper-owned public truth
</success_criteria>

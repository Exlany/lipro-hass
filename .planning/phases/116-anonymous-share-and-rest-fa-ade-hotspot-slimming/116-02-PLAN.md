# Plan 116-02: Clarify anonymous-share scope-state and aggregate submit semantics

## Objective
让 `AnonymousShareManager` 继续 inward split：内部命名更清晰、collector delegation 更薄、aggregate/scoped submit outcome contract 更明确，但 outward manager / registry/factory truth 不变。

<tasks>
<task>
  <id>116-02-01</id>
  <title>Clarify internal scope-state naming and delegation</title>
  <action>把 `custom_components/lipro/core/anonymous_share/manager.py` 内部 `_state` / `_primary_state` 收敛为更清晰的 scope-state 语义，并让 `scope_state_property()` / 相关 support helper 跟随这一 discoverability 改进；不得改变 outward manager contract。</action>
  <verify>`uv run pytest -q tests/core/anonymous_share/test_manager_scope_views.py tests/core/anonymous_share/test_manager_recording.py tests/core/anonymous_share/test_observability.py` passes</verify>
  <acceptance_criteria>
    - scope-state terminology becomes more explicit inside formal home
    - scoped manager behavior remains unchanged for recording / observability flows
    - no second manager family or compat layer is introduced
  </acceptance_criteria>
</task>

<task>
  <id>116-02-02</id>
  <title>Freeze aggregate-view outcome sharing and slim collector forwarding noise</title>
  <action>继续在 `manager.py` / `manager_support.py` 内部收窄 collector forwarding boilerplate，并补 focused tests 冻结 aggregate views 共享 submit-outcome state 的 contract。</action>
  <verify>`uv run pytest -q tests/core/anonymous_share/test_manager_submission.py tests/core/test_anonymous_share_cov_missing.py tests/core/anonymous_share/test_manager_scope_views.py` passes</verify>
  <acceptance_criteria>
    - aggregate sibling views observe the same shared submit outcome state
    - record/submit helpers keep current behavior without new production paths
    - registry / factory / scoped-manager contracts remain stable
  </acceptance_criteria>
</task>
</tasks>

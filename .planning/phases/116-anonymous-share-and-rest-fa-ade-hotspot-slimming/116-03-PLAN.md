# Plan 116-03: Close Phase 116 with route-sync and governance truth freeze

## Objective
在 `116-01` 与 `116-02` 完成后，把 `Phase 116` 的 execution evidence、planning selector family 与 focused meta truth 收口到单一 current story，确保 `$gsd-next` 自然前推到 `Phase 117 discuss-ready`。

<tasks>
<task>
  <id>116-03-01</id>
  <title>Write phase summaries and verification evidence</title>
  <action>为 `116-01`、`116-02` 生成对应 summary，并补齐 phase-level summary / verification，明确记录 REST façade 与 anonymous-share manager hotspot slimming 的完成事实与 focused proof chain。</action>
  <verify>`uv run pytest -q tests/core/api/test_api.py tests/core/api/test_api_status_service_wrappers.py tests/core/anonymous_share/test_manager_recording.py tests/core/anonymous_share/test_manager_submission.py tests/core/anonymous_share/test_manager_scope_views.py tests/core/anonymous_share/test_observability.py` passes</verify>
  <acceptance_criteria>
    - `116-01-SUMMARY.md` and `116-02-SUMMARY.md` exist
    - `116-SUMMARY.md` and `116-VERIFICATION.md` exist
    - verification records the exact focused commands that froze HOT-49
  </acceptance_criteria>
</task>

<task>
  <id>116-03-02</id>
  <title>Advance current-route truth to Phase 117 discuss-ready</title>
  <action>更新 `.planning/PROJECT.md`、`.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md`、`.planning/STATE.md` 与 focused meta truth（至少 `tests/meta/governance_current_truth.py`、`tests/meta/test_governance_route_handoff_smoke.py`），让 current route 明确变为 `Phase 116 complete; Phase 117 discuss-ready`，并保持 default next command 与 machine-readable contract 一致。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_bootstrap_smoke.py tests/meta/test_governance_route_handoff_smoke.py tests/meta/test_phase112_formal_home_governance_guards.py` passes</verify>
  <acceptance_criteria>
    - planning selector family shares the same machine-readable route contract
    - `HOT-49` is marked complete in active-route traceability
    - `$gsd-next` resolves to the Phase 117 continuation rather than Phase 116 or archived closeout
  </acceptance_criteria>
</task>
</tasks>

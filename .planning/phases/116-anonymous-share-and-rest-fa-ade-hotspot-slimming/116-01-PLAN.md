# Plan 116-01: Slim REST façade state and collaborator wrapper density

## Objective
让 `LiproRestFacade` 更像真正的 formal composition root：保留 outward surface 不变，但把手写 session-state 代理与薄 wrapper 密度压缩成 declarative inward binding。

<tasks>
<task>
  <id>116-01-01</id>
  <title>Adopt declarative session-state bindings in rest_facade</title>
  <action>在 `custom_components/lipro/core/api/rest_facade.py` 复用 `custom_components/lipro/core/api/session_state.py` 已存在的 `session_state_property()`，替换 `phone_id`、`session`、`request_timeout`、`entry_id`、`access_token`、`refresh_token`、`user_id`、`biz_id`、`refresh_lock`、`on_token_refresh` 等手写 state proxy，保留 setter/readonly 语义不变。</action>
  <verify>`uv run pytest -q tests/core/api/test_api.py tests/core/api/test_protocol_contract_facade_runtime.py` passes</verify>
  <acceptance_criteria>
    - injected `RestSessionState` values are reflected through façade properties
    - `session` setter still syncs via transport executor
    - `client.py` remains the stable import shell for `LiproRestFacade`
  </acceptance_criteria>
</task>

<task>
  <id>116-01-02</id>
  <title>Reduce transport/request wrapper boilerplate without adding new outward roots</title>
  <action>在 `rest_facade.py` 内部使用 declarative collaborator delegation 收窄 `_transport_executor`、`_request_gateway`、`_auth_recovery` 的薄 wrapper 密度，但不新增 compat shell / second root / package re-export。</action>
  <verify>`uv run pytest -q tests/core/api/test_api.py tests/core/api/test_api_status_service_wrappers.py tests/core/api/test_protocol_contract_facade_runtime.py` passes</verify>
  <acceptance_criteria>
    - façade wrapper density decreases while outward names stay stable
    - no new REST root/home or adapter layer appears
    - focused protocol/runtime contract tests remain green
  </acceptance_criteria>
</task>
</tasks>

---
phase: 07.3-runtime-telemetry-exporter
plan: "01"
status: completed
completed: 2026-03-13
requirements:
  - OBS-01
  - OBS-02
---

# Summary 07.3-01

## Outcome

- 新增 `custom_components/lipro/core/telemetry/` formal family，明确 `TelemetrySnapshot`、`TelemetryViews`、`TelemetrySensitivity`、`CardinalityBudget`、`RuntimeTelemetryExporter` 与稳定 sink projections 的正式家园。
- `custom_components/lipro/control/telemetry_surface.py` 已落地为 control-plane 唯一 telemetry bridge：负责从 entry/runtime 装配 exporter，并输出 diagnostics / system-health / developer / CI 共用视图。
- exporter 规则已收敛到单一 contracts：允许真实时间戳、允许报告内稳定且跨报告不可关联的 `entry_ref` / `device_ref`，并把 token / secret / `password_hash` / 原始手机号等凭证等价物阻断在 sinks 之外。
- `tests/core/telemetry/test_models.py`、`tests/core/telemetry/test_exporter.py`、`tests/core/telemetry/test_sinks.py` 已建立 Wave 0 锚点，覆盖 schema、伪匿名引用、cardinality budget 与 sink projection 的最小回归面。
- baseline / residual 文档已补登记：`RuntimeTelemetryExporter` 被明确为 observer-only telemetry truth home；consumer convergence delete gate 留给 `07.3-02` 继续收口。

## Verification

- `uv run ruff check custom_components/lipro/core/telemetry custom_components/lipro/control/telemetry_surface.py tests/core/telemetry`
- `uv run pytest -q -x tests/core/telemetry/test_models.py tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py`
- `uv run pytest -q tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py`

## Governance Notes

- `scripts/check_file_matrix.py` 已补充 `core/telemetry/*`、`control/telemetry_surface.py` 与 `tests/core/telemetry/*` 的 `Phase 7.3` 归属，用于后续 `FILE_MATRIX.md` 重生成。
- `RESIDUAL_LEDGER.md` 已新增 `Telemetry consumer convergence` residual family，明确 `diagnostics/system health/developer report` 的统一收口 gate 在 `07.3-02`。
- 本计划**无新增 compat shell**；exporter family 是 observer-only surface，不得被解释为新的 runtime root 或 control root。

---
status: testing
phase: 07.3-runtime-telemetry-exporter
source: 07.3-01-SUMMARY.md, 07.3-02-SUMMARY.md
started: 2026-03-13T14:59:57Z
updated: 2026-03-13T14:59:57Z
---

## Current Test

number: 1
name: Home Assistant 主链已打通
expected: |
  运行 `uv run pytest -q -x tests/integration/test_telemetry_exporter_integration.py` 应通过。
  这表示 `setup_entry -> runtime_data -> exporter -> diagnostics/system_health`
  的整条链路成立，而且 consumers 读取的是 exporter projection，
  而不是 coordinator 私有字段。
awaiting: user response

## Tests

### 1. Home Assistant 主链已打通
expected: 运行 `uv run pytest -q -x tests/integration/test_telemetry_exporter_integration.py` 应通过，并证明 `setup_entry -> runtime_data -> exporter -> diagnostics/system_health` 主链成立，且 consumers 使用 exporter projection。
result: [pending]

### 2. 四类核心运行信号都能导出
expected: 运行 `uv run pytest -q -x tests/core/coordinator/runtime/test_runtime_telemetry_methods.py tests/core/coordinator/services/test_telemetry_service.py tests/core/test_command_confirmation_helpers.py tests/core/api/test_auth_recovery_telemetry.py tests/integration/test_mqtt_coordinator_integration.py` 应通过，并覆盖 `auth recovery`、`MQTT reconnect`、`command confirmation latency/timeout`、`refresh latency` 四类结构化信号。
result: [pending]

### 3. 多个 consumers 共享同一 exporter 真相
expected: 运行 `uv run pytest -q -x tests/core/test_diagnostics.py tests/core/test_system_health.py tests/services/test_services_diagnostics.py tests/core/test_init.py -k 'developer_report or developer_feedback'` 应通过，并证明 `diagnostics`、`system health`、开发者报告都 pull 同一 exporter view，而不是各自维护 telemetry truth。
result: [pending]

### 4. 敏感字段不会泄露
expected: 运行 `uv run pytest -q -x tests/core/telemetry/test_sinks.py tests/core/telemetry/test_exporter.py tests/integration/test_telemetry_exporter_integration.py` 应通过，且任一 telemetry / diagnostics / system health 输出都不会泄露 `password_hash`、token、`secret`、`access_key`、`refresh_access_key`、原始手机号或 raw payload。
result: [pending]

### 5. 报告保留排障上下文但跨报告不可关联
expected: 运行 `uv run pytest -q -x tests/core/telemetry/test_models.py tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py` 应通过，并证明 exporter 保留真实时间戳，`entry_ref` / `device_ref` 仅在单份报告内稳定，跨报告不可直接关联。
result: [pending]

### 6. 导出结果是有界摘要而非事件洪流
expected: 运行 `uv run pytest -q -x tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py` 应通过，并证明 exporter 受 cardinality budget 约束，输出的是有界结构化摘要，而不是无限增长的原始事件总线。
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0

## Gaps


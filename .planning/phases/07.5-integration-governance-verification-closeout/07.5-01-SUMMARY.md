---
phase: 07.5-integration-governance-verification-closeout
plan: "01"
status: completed
completed: 2026-03-13
requirements:
  - GOV-06
  - GOV-07
---

# Summary 07.5-01

## Outcome

- 回写 `.planning/baseline/AUTHORITY_MATRIX.md`：补齐 `07.3` telemetry exporter family、`07.4` replay run summary family，以及 `v1.1 closeout evidence index` 的 authority / sync direction，明确它们都只能 pull 正式真源。
- 回写 `.planning/baseline/PUBLIC_SURFACES.md`：把 `core/telemetry/*`、`control/telemetry_surface.py`、`tests/harness/protocol/*` 与 `V1_1_EVIDENCE_INDEX.md` 明确裁决为 assurance-only / pull-only surfaces，禁止反向成为 runtime/control/public root。
- 回写 `.planning/baseline/VERIFICATION_MATRIX.md`：把 `GOV-*` requirement 与 `Phase 7.5` 的 exit contract 补齐到 v1.1 closeout 链路，明确 `08` 只能 pull evidence index + 正式真源。
- 回写 `.planning/reviews/RESIDUAL_LEDGER.md` 与 `.planning/reviews/KILL_LIST.md`：完成 replay scenario coverage 的 closeout arbitration，显式将非 representative replay families 标记为 v1.1 de-scope；同时明确 `07.5` 无新增 file-level kill target。
- `FILE_MATRIX` 本轮无需新增 Python inventory 条目：`07.3/07.4` 新增 Python 资产此前已完成 file-level 登记，因此 `07.5` 只做治理复核，不人为制造无意义 diff。

## Verification

- `uv run python scripts/check_file_matrix.py --check && uv run pytest -q -x tests/meta/test_governance_guards.py tests/meta/test_public_surface_guards.py`

## Governance Notes

- `07.5-01` 只做治理仲裁，不改写 `07.3` telemetry truth 与 `07.4` replay truth。
- `07.5-02` 将在此基础上新增 `V1_1_EVIDENCE_INDEX.md`、`07.5-SUMMARY.md` 并同步 `ROADMAP / REQUIREMENTS / STATE`。

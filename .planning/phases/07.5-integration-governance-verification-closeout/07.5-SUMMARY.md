---
phase: 07.5-integration-governance-verification-closeout
status: completed
completed: 2026-03-13
requirements:
  - GOV-06
  - GOV-07
---

# Phase 07.5 Summary

## Locked Truth

- `07.1` 锁定了 protocol boundary authority：REST / MQTT decode truth 继续以 boundary inventory、registry-backed decoder family 与 authority fixtures 为正式来源。
- `07.2` 锁定了 enforcement taxonomy：plane/root/surface/authority 规则只能由 baseline docs 与 architecture policy 派生，禁止 helper/tests 反向定义规则。
- `07.3` 锁定了 telemetry truth：`RuntimeTelemetryExporter` 是 observer-only、pull-first 的唯一 telemetry evidence 输出面，允许真实时间戳，继续执行 redaction / pseudo-id / cardinality 裁决。
- `07.4` 锁定了 replay truth：replay harness 只作为 assurance-only consumer，manifest 与 run summary 只能 pull authority fixtures 与 exporter truth。

## Closeout Outputs

- 已同步 `AUTHORITY_MATRIX / PUBLIC_SURFACES / VERIFICATION_MATRIX / RESIDUAL_LEDGER / KILL_LIST`，让 `07.1-07.4` 新增资产统一进入同一治理链。
- 已新增 `.planning/reviews/V1_1_EVIDENCE_INDEX.md`，为 `08` 与后续 AI / tooling 提供 pull-only evidence index。
- 已把 `Replay scenario coverage` 做出显式 arbitration：v1.1 保留 representative replay corpus，其余 replay-worthy families 明确 de-scope，而不是悬空遗留。

## Residual / Delete Gates

- `Replay scenario coverage` 继续保留为显式 residual，但已完成 closeout 决议：未来若需扩 replay family，必须以新 phase 补 manifest / evidence，`08` 不直接扩大 corpus。
- `Protocol-boundary family coverage` 继续作为 carry-forward residual 保留：`rest.list-envelope.v1`、`rest.schedule-json.v1`、`mqtt.topic.v1`、`mqtt.message-envelope.v1` 仍以 `RESIDUAL_LEDGER.md` 中既定 exit condition 等待后续 boundary 扩展或 retire 裁决。
- `services/wiring.py`、legacy protocol public names、`DeviceCapabilities` compat alias 等 file-level delete gates 维持既有裁决，不因 `07.5` 被模糊化或静默放行。
- `V1_1_EVIDENCE_INDEX.md` 与本 phase summaries 只是 closeout governance artifacts，不进入 kill list。

## Verification

- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest -q -x tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py tests/meta/test_protocol_replay_assets.py`

## Handoff To Phase 8

- `08` 只能 pull：`V1_1_EVIDENCE_INDEX.md`、baseline docs、`07.3` exporter evidence、`07.4` replay evidence、active residual/delete gates。
- `08` 不得重新扫描仓库临时拼装第二套事实，也不得越权重写 telemetry / replay / governance truth。
- 若 `08` 需要新增 evidence 字段、schema 或 replay family，必须先回写 baseline/review docs，再实现 exporter / pack schema。

# Phase 132 Summary

status: complete

## Delivered

- 完成 current-story compression：planning docs、developer architecture 与 maintainer runbook 首屏现在共同指向 `v1.38` active selector。
- 完成 governance helper narrowing：archive-history literals 退出 `governance_current_truth.py` current home，predecessor guards 复用统一 route-marker helper。
- 完成 promoted/handoff boundary cleanup：recent promoted asset coverage 回流 dedicated suite，route handoff smoke 只保留 docs + GSD fast-path proof。

## Remaining Honest Boundary

- production hotspot（如 `runtime_types.py`、`request_policy.py`、`dispatch.py`、`auth/manager.py`、`firmware_update.py`）仍是后续显式 phase 的主题，本轮未混入 docs/test governance compression。
- `v1.38` 目前已具备 milestone closeout 条件，但 archive promotion 仍需通过 `$gsd-complete-milestone v1.38` 正式完成。

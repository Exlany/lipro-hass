---
phase: 52-protocol-root-second-round-slimming-and-request-policy-isolation
plan: "01"
type: execute
title: 收窄 protocol root 的 child-facing 视野并内聚 MQTT/diagnostics 机械细节
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/protocol/facade.py
  - custom_components/lipro/core/protocol/rest_port.py
  - custom_components/lipro/core/protocol/mqtt_facade.py
  - tests/core/api/test_protocol_contract_matrix.py
  - tests/core/api/test_api_transport_and_schedule.py
autonomous: true
requirements:
  - ARC-08
must_haves:
  truths:
    - `LiproProtocolFacade` 继续是唯一正式 protocol-plane root，但 root body 不再直接暴露整块 REST child surface 的 forwarding 密度
    - `LiproRestFacade` 与 `LiproMqttFacade` 继续只是 child façade，`rest_port.py` 与 `mqtt_facade.py` 只承担局部 collaborator / child-facing contract 职责
    - MQTT attach、diagnostics snapshot、shared session/telemetry context 仍由 protocol root 负责，只把机械拼装下沉，不创造第二根
  artifacts:
    - custom_components/lipro/core/protocol/facade.py
    - custom_components/lipro/core/protocol/rest_port.py
    - custom_components/lipro/core/protocol/mqtt_facade.py
    - tests/core/api/test_protocol_contract_matrix.py
    - tests/core/api/test_api_transport_and_schedule.py
  key_links:
    - `LiproProtocolFacade` public methods -> concern-local REST child ports -> `LiproRestFacade`
    - protocol root MQTT entrypoints -> localized MQTT mechanics -> `LiproMqttFacade`
---

# Plan 52-01: 收窄 protocol root 的 child-facing 视野并内聚 MQTT/diagnostics 机械细节

<context>
@.planning/phases/52-protocol-root-second-round-slimming-and-request-policy-isolation/52-CONTEXT.md
@.planning/phases/52-protocol-root-second-round-slimming-and-request-policy-isolation/52-RESEARCH.md
@.planning/phases/52-protocol-root-second-round-slimming-and-request-policy-isolation/52-VALIDATION.md
@custom_components/lipro/core/protocol/facade.py
@custom_components/lipro/core/protocol/rest_port.py
@custom_components/lipro/core/protocol/mqtt_facade.py
@custom_components/lipro/core/api/rest_facade.py
@tests/core/api/test_protocol_contract_matrix.py
@tests/core/api/test_api_transport_and_schedule.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: 把 protocol root 对 REST child 的认知收窄为 concern-local port</name>
  <files>custom_components/lipro/core/protocol/facade.py, custom_components/lipro/core/protocol/rest_port.py, tests/core/api/test_protocol_contract_matrix.py</files>
  <action>在不新增 façade / wrapper / root 的前提下，继续把 `custom_components/lipro/core/protocol/facade.py` 里的 REST forwarding tail 按 concern 收口：优先把 `_RestFacadePort` 拆成更窄的内部 protocol/port 族，或等价的 concern-local child-facing contract，使 protocol root 不再以“整块 23-method REST surface”思考；`LiproProtocolFacade` 的 public methods 必须保持稳定，允许保留 thin delegates，但复杂分发/成组 forwarding 必须 inward 下沉到 `rest_port.py` 所定义的局部契约，而不是靠 rename、`__getattr__`、新导出或包级替身偷渡瘦身。</action>
  <acceptance_criteria>
    - `LiproProtocolFacade` 仍保留现有 formal protocol methods/properties，不新增 public import story、package export 或第二 protocol mainline
    - `rest_port.py` 不再只暴露“一整个 REST child surface”的宽口 contract；protocol root 看到的是更窄、更诚实的 concern-local seam
    - root body 的 slimming 发生在真实 forwarding/topic cluster，而不是把相同复杂度搬成另一层 façade/wrapper
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/api/test_protocol_contract_matrix.py -q`</verify>
  <done>protocol root 已从整块 REST child forwarding 尾部收口到更窄的 child-facing contract，且 single protocol-root truth 不漂移。</done>
</task>

<task type="auto">
  <name>Task 2: 保留 root 对 MQTT/diagnostics 的正式职责，只下沉机械拼装细节</name>
  <files>custom_components/lipro/core/protocol/facade.py, custom_components/lipro/core/protocol/mqtt_facade.py, tests/core/api/test_protocol_contract_matrix.py, tests/core/api/test_api_transport_and_schedule.py</files>
  <action>围绕 `build_mqtt_facade()`、`attach_mqtt_facade()`、`_build_protocol_diagnostics_snapshot()` 与 `protocol_diagnostics_snapshot()` 做第二轮瘦身：formal entrypoints 继续留在 `LiproProtocolFacade`，但 MQTT attach/bind、telemetry/diagnostics snapshot 组装等机械细节应 inward 下沉到 `custom_components/lipro/core/protocol/mqtt_facade.py` 或同文件内的非导出局部 helper；不得把 shared session state、telemetry、diagnostics context 的 ownership 从 protocol root 挪走，也不得把 `LiproMqttFacade` 讲成第二根。</action>
  <acceptance_criteria>
    - `build_mqtt_facade()`、`attach_mqtt_facade()` 与 `protocol_diagnostics_snapshot()` 仍是 protocol root 的正式入口，调用方 story 不变
    - MQTT mechanical binding 与 diagnostics snapshot assembly 不再直接膨胀 `facade.py` 主体，但 `LiproMqttFacade` 继续只是 child façade
    - 相关 contract tests 继续证明 root 持有 shared session/telemetry/diagnostics truth，没有新 root / new wrapper / export drift
  </acceptance_criteria>
  <verify>`uv run pytest tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_api_transport_and_schedule.py -q`</verify>
  <done>MQTT attach 与 diagnostics 机械细节已 inward localization，而 protocol root 的正式职责与唯一身份保持稳定。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest tests/core/api/test_protocol_contract_matrix.py tests/core/api/test_api_transport_and_schedule.py tests/meta/test_public_surface_guards.py -q`
</verification>

# 25.2-01 Summary

## Outcome

- `runtime_types.LiproCoordinator` 现显式承认 `protocol` 与 `telemetry_service` 两个 bridge 所需的最小正式 surface；control-plane 不再依赖隐式 ghost seam 猜测。
- `custom_components/lipro/control/telemetry_surface.py` 已切到 `Coordinator.protocol`，并明确拒绝 `client` fallback；anti-MagicMock-ghost 策略保留，但 formal binding 变得诚实。
- telemetry exporter / system health 现有回归夹具全部迁移到 `protocol=`；并补了一条 `client`-only coordinator 不再被接受的回归断言。

## Key Files

- `custom_components/lipro/runtime_types.py`
- `custom_components/lipro/control/telemetry_surface.py`
- `tests/integration/test_telemetry_exporter_integration.py`
- `tests/core/test_system_health.py`

## Validation

- `uv run pytest -q tests/integration/test_telemetry_exporter_integration.py tests/core/test_system_health.py`
- `uv run ruff check custom_components/lipro/runtime_types.py custom_components/lipro/control/telemetry_surface.py tests/integration/test_telemetry_exporter_integration.py tests/core/test_system_health.py`

## Notes

- 本 tranche 只关闭 source-binding honesty seam；`RuntimeTelemetryExporter` schema / sink payload 没有扩题重写。

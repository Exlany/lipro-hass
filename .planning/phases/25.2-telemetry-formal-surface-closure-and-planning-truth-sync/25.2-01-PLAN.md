---
phase: 25.2-telemetry-formal-surface-closure-and-planning-truth-sync
plan: "01"
type: execute
title: formalize telemetry source contracts and close the ghost seam
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/runtime_types.py
  - custom_components/lipro/control/telemetry_surface.py
  - tests/integration/test_telemetry_exporter_integration.py
  - tests/core/test_system_health.py
autonomous: true
requirements:
  - OBS-04
must_haves:
  truths:
    - control-plane telemetry consumers must read the formal protocol surface rather than a legacy client alias
    - runtime type contracts must explicitly expose the minimal protocol and telemetry surfaces used by the bridge
    - client-only fake coordinators must stop passing telemetry bridge regressions
  artifacts:
    - custom_components/lipro/runtime_types.py
    - custom_components/lipro/control/telemetry_surface.py
    - tests/integration/test_telemetry_exporter_integration.py
    - tests/core/test_system_health.py
  key_links:
    - runtime type contract -> telemetry bridge -> diagnostics/system-health regression slice
---

# Plan 25.2-01: formal telemetry source contract + ghost seam closure

<context>
@custom_components/lipro/runtime_types.py
@custom_components/lipro/control/telemetry_surface.py
@tests/integration/test_telemetry_exporter_integration.py
@tests/core/test_system_health.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: expose the formal telemetry source surface on the runtime contract</name>
  <files>custom_components/lipro/runtime_types.py, custom_components/lipro/control/telemetry_surface.py</files>
  <read_first>
    - custom_components/lipro/runtime_types.py
    - custom_components/lipro/control/telemetry_surface.py
    - custom_components/lipro/core/protocol/facade.py
    - custom_components/lipro/core/coordinator/services/telemetry_service.py
  </read_first>
  <action>在 `runtime_types.py` 增加 bridge 真正需要的最小 telemetry-related protocols（例如 protocol diagnostics source / runtime telemetry service），并让 `LiproCoordinator` 显式暴露 `protocol` 与 `telemetry_service`。同步改写 `telemetry_surface.py`：仅通过 `coordinator.protocol` 构造 protocol telemetry source，不保留 `client` fallback；保留现有 anti-MagicMock-ghost 读取策略，但 formal binding 必须诚实。</action>
  <acceptance_criteria>
    - `custom_components/lipro/runtime_types.py` contains `protocol`
    - `custom_components/lipro/runtime_types.py` contains `telemetry_service`
    - `custom_components/lipro/control/telemetry_surface.py` does not contain `"client"`
    - `uv run pytest -q tests/integration/test_telemetry_exporter_integration.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/integration/test_telemetry_exporter_integration.py`</verify>
  <done>`LiproCoordinator` formal surface 诚实承认 bridge 所需的 `protocol` / `telemetry_service`；`telemetry_surface.py` 不再接受 `coordinator.client` 作为协议源。</done>
</task>

<task type="auto">
  <name>Task 2: migrate consumer regressions to protocol-only coordinators</name>
  <files>tests/integration/test_telemetry_exporter_integration.py, tests/core/test_system_health.py</files>
  <read_first>
    - tests/integration/test_telemetry_exporter_integration.py
    - tests/core/test_system_health.py
    - custom_components/lipro/control/telemetry_surface.py
  </read_first>
  <action>把现有 telemetry exporter / system health 测试夹具从 `client=` 迁移到 `protocol=`，并在现有文件中补一条明确回归：`client`-only coordinator 不应再产生 telemetry exporter / system health protocol snapshot。不要新建测试文件。</action>
  <acceptance_criteria>
    - `tests/integration/test_telemetry_exporter_integration.py` does not contain `client=SimpleNamespace`
    - `tests/core/test_system_health.py` does not contain `client=SimpleNamespace`
    - `tests/integration/test_telemetry_exporter_integration.py` contains `protocol=`
    - `uv run pytest -q tests/core/test_system_health.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/test_system_health.py`</verify>
  <done>consumer regressions 只用 `protocol` surface 搭建 fake coordinator，并覆盖 legacy `client` seam 已失效的行为。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/integration/test_telemetry_exporter_integration.py tests/core/test_system_health.py`
</verification>

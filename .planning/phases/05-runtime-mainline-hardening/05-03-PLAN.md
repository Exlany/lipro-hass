---
phase: 05-runtime-mainline-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - custom_components/lipro/core/coordinator/services/telemetry_service.py
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
autonomous: true
requirements:
  - ARCH-02
  - RUN-04
must_haves:
  - runtime telemetry snapshot schema 冻结为正式 handoff 面
  - governance 文档明确声明 shadow chain 不得复活
  - Phase 6/7 只消费已定型 runtime truth，不再回头改主链
---

# Plan 05-03: 冻结 runtime telemetry handoff 与治理回写

## Tasks

1. **冻结 telemetry snapshot schema**
   - 固定 `signals`、`mqtt`、`command`、`status`、`tuning` 的正式快照面；
   - 明确哪些指标属于 Phase 5 已交付，哪些属于 Phase 6 Assurance 扩展。

2. **回写治理与 phase 文档**
   - 同步 `ROADMAP / STATE / REQUIREMENTS / FILE_MATRIX / RESIDUAL_LEDGER / KILL_LIST`；
   - 明确哪些 residual 已收口、哪些留给 Phase 6/7。

3. **形成下游 handoff**
   - 为 Phase 6 的 Assurance Plane 正式化提供稳定文档基础；
   - 明确 `connect-status` 不是 runtime 主链，shadow chain 不得复活。

## Verification

- `uv run pytest tests/core/coordinator/services/test_telemetry_service.py tests/core/coordinator/runtime/test_runtime_telemetry_methods.py tests/core/test_coordinator.py -q`
- `uv run pytest tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_governance_guards.py -q`

## Success Criteria

- Phase 5 完成后，Phase 6 只需 formalize guard/CI，不再回头改 runtime 主链。
- Phase 7 只做 residual/document/governance closeout，不再重新打开 runtime 设计争论。

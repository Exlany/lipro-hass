---
phase: 05-runtime-mainline-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - tests/core/coordinator/runtime/test_command_runtime.py
  - tests/core/coordinator/runtime/test_mqtt_runtime.py
  - tests/core/coordinator/runtime/test_runtime_telemetry_methods.py
  - tests/core/coordinator/services/test_command_service.py
  - tests/core/coordinator/services/test_device_refresh_service.py
  - tests/core/coordinator/services/test_mqtt_service.py
  - tests/core/coordinator/services/test_state_service.py
  - tests/core/test_coordinator.py
  - tests/integration/test_mqtt_coordinator_integration.py
  - tests/snapshots/test_coordinator_public_snapshots.py
autonomous: true
requirements:
  - RUN-03
  - RUN-04
must_haves:
  - 单一 runtime mainline 的不变量具备自动化验证
  - command / refresh / state / mqtt / telemetry 不再依赖旧 shadow 语义
  - 重复 setup、重复 transport、异常吞没、group reconcile 失联、shadow chain 回流均可被阻断
---

# Plan 05-02: 建立 command/refresh/state/mqtt invariant suite

## Tasks

1. **补齐 runtime invariant tests**
   - 单一 MQTT transport owner；
   - repeated setup 幂等；
   - shutdown / reconnect 不遗留旧 façade；
   - shadow chain 不得重新进入生产 wiring。

2. **固化 command / refresh / telemetry 主链**
   - command confirmation 与 post-refresh 不旁路；
   - group reconcile 只经 refresh service；
   - runtime telemetry snapshot 成为稳定断言面。

3. **调整快照/集成测试**
   - 将旧 private-field assertions 尽量迁移到 formal service surface / telemetry snapshot；
   - 为 Phase 6 的 CI/meta guard 铺平结构。

## Verification

- `uv run pytest tests/core/coordinator/runtime/test_command_runtime.py tests/core/coordinator/runtime/test_mqtt_runtime.py tests/core/coordinator/runtime/test_runtime_telemetry_methods.py -q`
- `uv run pytest tests/core/coordinator/services/test_command_service.py tests/core/coordinator/services/test_device_refresh_service.py tests/core/coordinator/services/test_mqtt_service.py tests/core/coordinator/services/test_state_service.py -q`
- `uv run pytest tests/core/test_coordinator.py tests/integration/test_mqtt_coordinator_integration.py tests/snapshots/test_coordinator_public_snapshots.py -q`

## Success Criteria

- Phase 5 的结构性裁决具备独立回归防线；
- 测试不再默许 legacy seam 或 shadow semantics；
- Phase 6 可以直接复用这套 tests 作为 assurance seed。

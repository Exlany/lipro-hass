---
phase: 79-governance-tooling-hotspot-decomposition-and-release-contract-topicization
plan: "02"
type: execute
title: decompose the file-matrix registry hotspot into slimmer governed homes
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/check_file_matrix_registry.py
  - scripts/check_file_matrix_validation.py
  - tests/test_refactor_tools.py
  - .planning/reviews/FILE_MATRIX.md
autonomous: true
requirements:
  - HOT-35
  - QLT-32
must_haves:
  truths:
    - the registry hotspot no longer relies on one giant classification chain
    - outward registry contract stays stable for `check_file_matrix.py` and validation consumers
    - focused tests prove override integrity and representative path classification
  artifacts:
    - scripts/check_file_matrix_registry.py
    - scripts/check_file_matrix_validation.py
    - tests/test_refactor_tools.py
    - .planning/reviews/FILE_MATRIX.md
---

# Plan 79-02: decompose the file-matrix registry hotspot into slimmer governed homes

<objective>
把 `scripts/check_file_matrix_registry.py` 从“正确但难维护”的 monolith 收口为更薄、更清晰的治理工具根，同时保持 file-matrix outward contract 不变。
</objective>

<context>
@AGENTS.md
@scripts/check_file_matrix_registry.py
@scripts/check_file_matrix_validation.py
@tests/test_refactor_tools.py
@.planning/reviews/FILE_MATRIX.md
@.planning/phases/79-governance-tooling-hotspot-decomposition-and-release-contract-topicization/79-CONTEXT.md
@.planning/phases/79-governance-tooling-hotspot-decomposition-and-release-contract-topicization/79-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 79-02-01: split registry rules and add focused regression coverage</name>
  <files>scripts/check_file_matrix_registry.py, scripts/check_file_matrix_validation.py, tests/test_refactor_tools.py, .planning/reviews/FILE_MATRIX.md</files>
  <action>把 override truth families、path-classifier rules 与 dispatch glue 做有意义的拆分：可引入 companion modules / table-driven rules / focused helpers，但不能创造第二条 truth chain。同步更新 validation 逻辑与 file-matrix rows，使新文件拥有诚实 ownership。为 representative path classification、override uniqueness 与 maintainability floor 增加 focused tests。目标是清除 registry 当前的 `C901` 热点。</action>
  <acceptance_criteria>
    - `uv run ruff check scripts/check_file_matrix_registry.py --select C901` 通过
    - 代表性 component/test/script paths 仍得到与旧行为一致的分类结果
    - override truth-family uniqueness / path totals 继续被机器验证
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check scripts/check_file_matrix_registry.py scripts/check_file_matrix_validation.py tests/test_refactor_tools.py && uv run python scripts/check_file_matrix.py --check && uv run pytest -q tests/test_refactor_tools.py</automated>
  </verify>
  <done>The file-matrix registry becomes a thinner governed root with focused regression coverage.</done>
</task>
</tasks>

<verification>
- `uv run ruff check scripts/check_file_matrix_registry.py scripts/check_file_matrix_validation.py tests/test_refactor_tools.py`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest -q tests/test_refactor_tools.py`
</verification>

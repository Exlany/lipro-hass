---
phase: 15-support-feedback-contract-hardening-governance-truth-repair-and-maintainability-follow-through
plan: "05"
title: tooling security and residual policy arbitration
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
  - scripts/coverage_diff.py
  - scripts/refactor_tools.py
  - scripts/check_architecture_policy.py
  - .planning/baseline/ARCHITECTURE_POLICY.md
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - tests/meta/test_governance_guards.py
  - tests/meta/test_public_surface_guards.py
  - custom_components/lipro/core/api/client_base.py
  - custom_components/lipro/core/mqtt/mqtt_client.py
  - .planning/phases/15-support-feedback-contract-hardening-governance-truth-repair-and-maintainability-follow-through/15-VALIDATION.md
autonomous: true
requirements:
  - QLT-01
  - RES-01
---

<objective>
对 Phase 15 复核出的 tooling / security / residual gaps 做最终裁决：凡是 marker、coverage diff、benchmark、dev-audit、闲置插件与 residual naming 相关问题，都必须落到“真正启用 / 明确删除 / guard-backed documented arbitration”之一；其中 `coverage_diff.py` 必须被裁决为“coverage floor + optional baseline diff”或同轮更名/重定义，并继续把 `_ClientBase` 与 `LiproMqttClient` 限定在本地 residual 语义内。
</objective>

<tasks>
<task>清点 `github` / `integration` / `slow` markers、`pytest-xdist`、`pytest-mypy-plugins`、benchmark、dev dependency audit 与 `coverage_diff.py` 的真实使用情况，对每项做落地、删除或 documented arbitration 的单一裁决。</task>
<task>把 `coverage_diff.py` 的合同裁决为“coverage floor + optional baseline diff”或同步更名/重定义脚本职责；无论选择哪条路，都要让 CI、contributor docs 与 `15-VALIDATION.md` 使用同一语义，杜绝伪门禁。</task>
<task>更新 baseline/review assets、architecture/public-surface guards 与 residual ledgers，继续把 `_ClientBase` helper spine、`LiproMqttClient` legacy naming 限定为本地 residual，而不是物理 rename 或第二轮大拆迁。</task>
<task>把 wave-end verification contract 回写到基线真源与 `15-VALIDATION.md`，确保 Phase 15 的 tooling/security 决策进入正式治理链，而不是只停留在 workflow 细节。</task>
</tasks>

## Verification Commands

- `uv run ruff check .`
- `uv run mypy`
- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest tests/ -v --ignore=tests/benchmarks --cov=custom_components/lipro --cov-fail-under=95 --cov-report=json --cov-report=xml --cov-report=term-missing`
- `uv run pytest tests/snapshots/ -v`
- `uv run python scripts/coverage_diff.py coverage.json --minimum 95`
- `uv run python scripts/refactor_tools.py --coverage-json coverage.json --minimum-coverage 95`
- `./scripts/lint`

## Verification Criteria

- 每个 tooling/security gap 都有单一正式结论，不再以“配置里声明但仓库里不用”或“看似门禁、实际无基线”形式悬空；
- 若保留 `coverage_diff.py` 名称，其验证契约必须被澄清为“coverage floor + optional baseline diff”并与 CI/文档一致；若重定义职责，也同步回写脚本、文档、guards 与 `15-VALIDATION.md`；
- `_ClientBase` 与 `LiproMqttClient` 继续被限制在本地 residual 语义，guards 能阻断其回流为正式 public surface；
- Phase 15 的 wave-end verification 与 baseline/review truth 同步，不再只有 phase-local 草案。

## Must-Haves

- every tooling gap resolves to implement, delete, or documented arbitration
- no pseudo-gates for benchmark, dev-audit, or coverage semantics
- `_ClientBase` and `LiproMqttClient` stay localized residuals, not rename campaigns
- governance and residual truth remain on the existing north-star mainline

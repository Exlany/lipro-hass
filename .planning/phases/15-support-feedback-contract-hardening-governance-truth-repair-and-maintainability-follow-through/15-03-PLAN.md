---
phase: 15-support-feedback-contract-hardening-governance-truth-repair-and-maintainability-follow-through
plan: "03"
title: install support and version truth sync
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - README_zh.md
  - CONTRIBUTING.md
  - SUPPORT.md
  - SECURITY.md
  - .github/ISSUE_TEMPLATE/bug.yml
  - .github/workflows/ci.yml
  - pyproject.toml
  - hacs.json
  - tests/meta/test_version_sync.py
autonomous: true
requirements:
  - DOC-01
---

<objective>
把最低支持 Home Assistant 版本与 private-repo HACS caveat 统一成单一对外真相：`pyproject.toml` 作为 canonical version source，README / SUPPORT / CONTRIBUTING / SECURITY / bug template / CI / metadata 全部跟随同一口径。
</objective>

<tasks>
<task>把 `README.md` 与 `README_zh.md` 的安装段前置当前 `pyproject.toml` 所声明的最低 HA 版本要求，并把 private-repo 场景下 HACS validation 不执行的 caveat 提升到用户可见入口。</task>
<task>同步 `CONTRIBUTING.md`、`SUPPORT.md`、`SECURITY.md`、bug template、`hacs.json` 与 workflow 描述，使 contributor-facing 与 user-facing 入口不再各说各话。</task>
<task>以 `pyproject.toml` 的 HA pin 为唯一版本输入，扩展 `tests/meta/test_version_sync.py`，强制 README / README_zh / SECURITY / CONTRIBUTING / SUPPORT / metadata 跟随同一版本真源。</task>
<task>保持这次同步是文档与 metadata truth repair，而不是额外引入新的 install authority 文件或第二套 CI 叙事。</task>
</tasks>

## Verification Commands

- `uv run pytest -q -x tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_firmware_support_manifest_repo_asset.py`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run ruff check .`

## Verification Criteria

- 用户在 README / README_zh / SUPPORT 首屏即可看到 `pyproject.toml` 当前声明的最低 HA 版本与 private-repo HACS caveat；
- `pyproject.toml`、`hacs.json`、`SECURITY.md`、bug template、`CONTRIBUTING.md`、CI 说明与 README 不再出现版本/验证口径分叉；
- `tests/meta/test_version_sync.py` 能阻断后续版本文案漂移，而不是只校验局部 metadata；
- 文档同步不引入新的 authority 文件，仍由既有 north-star 治理链裁决。

## Must-Haves

- `pyproject.toml` remains the single HA-version truth source
- `README.md` and `README_zh.md` surface user-facing install caveats early
- contributor-facing and user-facing guidance say the same thing
- no second install/support governance storyline

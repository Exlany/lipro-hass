---
phase: 15-support-feedback-contract-hardening-governance-truth-repair-and-maintainability-follow-through
plan: "02"
title: governance truth repair and phase-state guard hardening
wave: 2
depends_on:
  - "01"
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/15-support-feedback-contract-hardening-governance-truth-repair-and-maintainability-follow-through/15-VALIDATION.md
  - scripts/check_file_matrix.py
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - GOV-13
---

<objective>
修补 active governance truth 的 phase/status/date/footer/source-path 漂移，让 `PROJECT.md`、`ROADMAP.md`、`STATE.md` 与 Phase 15 phase assets 重新讲同一条主链故事，并让 guards 对缺失 source path 与 phase-state drift fail-fast。
</objective>

<tasks>
<task>回写 `PROJECT.md`、`ROADMAP.md`、`STATE.md` 与 `15-VALIDATION.md` 的 active 叙事，消除 Phase 14 complete / milestone closeout pending 等过期表述。</task>
<task>扩展 `scripts/check_file_matrix.py` 或等价守卫，覆盖 active docs source-path existence 与 Phase 15 planning truth，而不只检查一组静态文件是否存在。</task>
<task>加强 `tests/meta/test_governance_guards.py`，让 `Current mode`、phase totals、footer/date/closeout 叙事具备跨文档一致性断言，而不是只校验字符串格式。</task>
<task>保持守卫的结构化与可维护性，避免把 phase/date/footer 写成脆弱硬编码，防止下一 phase 产生低价值噪音。</task>
</tasks>

## Verification Commands

- `uv run pytest -q -x tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_firmware_support_manifest_repo_asset.py`
- `uv run ruff check .`
- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`

## Verification Criteria

- `PROJECT.md`、`ROADMAP.md`、`STATE.md` 与 Phase 15 assets 的 phase/status/date/footer narrative 一致，不再互相指向不同完成态；
- active governance docs 中声明的 source paths 均存在，缺失路径会被脚本或 meta guards 直接阻断；
- meta tests 能识别 Phase 15 的 planning truth，而不是继续停留在 Phase 14；
- 治理修补只收紧真源与 guards，不额外创造新的 authority 文档链。

## Must-Haves

- active governance docs reference only existing source paths
- phase/status/date/footer checks are fail-fast but not brittle
- one governance story across `PROJECT.md`, `ROADMAP.md`, `STATE.md`, and Phase 15 assets
- no second authority chain outside the north-star mainline

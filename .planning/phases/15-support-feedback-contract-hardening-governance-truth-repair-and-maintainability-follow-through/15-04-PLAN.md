---
phase: 15-support-feedback-contract-hardening-governance-truth-repair-and-maintainability-follow-through
plan: "04"
title: support hotspot decomposition and runtime-access typing narrowing
wave: 2
depends_on:
  - "01"
files_modified:
  - custom_components/lipro/control/service_router.py
  - custom_components/lipro/control/developer_router_support.py
  - custom_components/lipro/control/runtime_access.py
  - custom_components/lipro/services/diagnostics/helpers.py
  - custom_components/lipro/services/diagnostics/types.py
  - tests/services/test_services_diagnostics.py
  - tests/services/test_services_registry.py
  - tests/core/test_report_builder.py
  - tests/core/test_developer_report.py
  - tests/core/test_anonymous_share.py
autonomous: true
requirements:
  - HOT-03
  - TYP-03
---

<objective>
在不改变 `service_router.py` public handler home 身份的前提下，继续把 developer/support upload-only glue 下沉到 focused helper，并把 `RuntimeAccess` 与相邻 support seams 的宽类型收窄到正式 runtime contracts。
</objective>

<tasks>
<task>把 developer feedback upload-only shaping、share-preview glue 与 report handoff 继续从 `service_router.py` 下沉到现有 helper home，确保 router 只保留 public orchestration。</task>
<task>局部收窄 `control/runtime_access.py`、diagnostics helper types 与相邻 control seams 的 `Any` / host-side 宽口，让 support path 依赖更清晰的 runtime contract。</task>
<task>同步 focused service/core tests 与 mocks，确保 patch-first seams 仍可测试，但不再靠 `Any` 掩盖真实 contract 漂移。</task>
<task>严格限制变更边界在 support/developer 邻接面，不把 Phase 15 扩成新的 runtime 重构或第二条 control story。</task>
</tasks>

## Verification Commands

- `uv run pytest -q tests/core/test_developer_report.py tests/services/test_services_diagnostics.py tests/core/test_anonymous_share.py tests/core/test_report_builder.py`
- `uv run mypy`
- `uv run ruff check .`

## Verification Criteria

- `service_router.py` 热点进一步下降，但 public handlers 仍由该模块持有；
- upload-only shaping 不再与 local debug report 共享含混对象图，support glue home 更聚焦；
- `RuntimeAccess` 与相邻 support seams 的 typing 更窄、更接近正式 contract，且不会打爆现有测试补丁点；
- 变更只服务于 Phase 15 主链，不新开 runtime/control helper 根。

## Must-Haves

- keep `service_router.py` as the public handler home
- narrow `Any` only where it strengthens the existing support/runtime seam
- preserve patch-first testability while removing false type slack
- no new control-plane backdoor or second orchestration chain

---
phase: 02.5-protocol-root-unification
plan: "03"
type: execute
wave: 3
depends_on:
  - "02.5-02"
files_modified:
  - custom_components/lipro/**/*.py
  - tests/core/api/**/*.py
  - tests/core/mqtt/**/*.py
  - tests/snapshots/test_api_snapshots.py
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/DEPENDENCY_MATRIX.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - .planning/phases/02.5-protocol-root-unification/02.5-03-SUMMARY.md
autonomous: true
requirements:
  - PROT-05
  - PROT-06
must_haves:
  - Runtime-facing allowed consumers treat protocol plane as one formal surface only
  - Legacy split-root surfaces are demoted to explicit compat shells or removed
  - Downstream phases receive clean public-surface, dependency, verification, residual, and kill-tracking outputs
---

# Plan 02.5-03: 清退旧协议双根 public surface 与 compat adapters

<context>
@.planning/phases/02.5-protocol-root-unification/02.5-CONTEXT.md
@.planning/phases/02.5-protocol-root-unification/02.5-ARCHITECTURE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@custom_components/lipro/
@tests/core/api/
@tests/core/mqtt/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 runtime-facing allowed consumers 到 unified protocol root</name>
  <files>custom_components/lipro/**/*.py, tests/core/api/**/*.py, tests/core/mqtt/**/*.py</files>
  <action>把获准依赖 protocol plane 的 runtime-facing consumers 与相关 tests 从 split-root 语义迁移到 `LiproProtocolFacade`，禁止继续把 child façade 当成正式 public root。</action>
  <verify>`uv run pytest tests/core/api tests/core/mqtt tests/snapshots/test_api_snapshots.py -q`</verify>
  <done>consumer 视角只剩单一正式协议面。</done>
</task>

<task type="auto">
  <name>Task 2: 降级或删除 compat shells</name>
  <files>custom_components/lipro/**/*.py, .planning/reviews/RESIDUAL_LEDGER.md, .planning/reviews/KILL_LIST.md</files>
  <action>将 `LiproClient`、legacy wrappers、split-root exports 标记为显式 compat shell 或直接删除，并写明 residual owner、downstream consumers、kill condition。</action>
  <verify>人工检查 `RESIDUAL_LEDGER.md` 与 `KILL_LIST.md` 是否完整记录 compat 清退路径。</verify>
  <done>历史命名不再伪装成正式设计。</done>
</task>

<task type="auto">
  <name>Task 3: 回写 baseline public surface 与 verification truth</name>
  <files>.planning/baseline/PUBLIC_SURFACES.md, .planning/baseline/DEPENDENCY_MATRIX.md, .planning/baseline/VERIFICATION_MATRIX.md</files>
  <action>把 unified protocol root 的 public direction、forbidden dependency、consumer migration 结果写回 baseline 真源，为 `Phase 2.6` 与 `Phase 3` 提供可直接引用的 handoff。</action>
  <verify>人工检查 baseline 文档是否已不再承认 split-root semantics 为正式入口。</verify>
  <done>后续 phases 获得清晰、单口径的协议边界输入。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/core/api tests/core/mqtt tests/snapshots/test_api_snapshots.py -q`
- [ ] `PUBLIC_SURFACES.md` 已把 `LiproProtocolFacade` 标记为唯一正式协议入口
- [ ] `DEPENDENCY_MATRIX.md` 已可据此禁止高层继续依赖 child façade internals
- [ ] `VERIFICATION_MATRIX.md` 已纳入 unified-root closeout proof
- [ ] compat shells 仅剩显式登记残留，且有 kill condition
</verification>

<success_criteria>

- split-root public semantics 已从 runtime-facing allowed consumers 视角消失
- 旧协议兼容层已被清点、降级或删除
- `Phase 2.6` 与 `Phase 3` 获得可直接引用的统一协议根 handoff artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-protocol-root-unification/02.5-03-SUMMARY.md`
</output>

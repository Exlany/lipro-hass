---
phase: 02.5-protocol-root-unification
plan: "01"
type: execute
wave: 1
depends_on:
  - "02-04"
files_modified:
  - .planning/phases/02.5-protocol-root-unification/02.5-ARCHITECTURE.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/phases/02.5-protocol-root-unification/02.5-01-SUMMARY.md
autonomous: true
requirements:
  - PROT-05
  - PROT-06
must_haves:
  - Unified protocol root ownership is explicit, testable, and downstream-usable
  - Protocol-boundary normalization ownership is formally defined before implementation starts
  - Root-facing files, compat shells, and residuals are classified before code migration
---

# Plan 02.5-01: 锁定 unified protocol root 设计、normalization owner 与治理边界

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@.planning/phases/02.5-protocol-root-unification/02.5-CONTEXT.md
@.planning/phases/02.5-protocol-root-unification/02.5-ARCHITECTURE.md
@.planning/phases/02.5-protocol-root-unification/02.5-RESEARCH.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 锁定 unified root 与 normalization ownership</name>
  <files>.planning/phases/02.5-protocol-root-unification/02.5-ARCHITECTURE.md</files>
  <action>把 `LiproProtocolFacade` 的唯一正式根地位、child façade 边界、canonical normalization owner、compat demotion 规则写成正式设计真源。</action>
  <verify>人工检查设计文档是否明确回答“谁是唯一协议根、谁拥有 canonical normalized contracts、哪些能力留在 child façade”。</verify>
  <done>`PROT-05/06` 的设计裁决不再漂移。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展治理矩阵到 protocol-root slice</name>
  <files>.planning/reviews/FILE_MATRIX.md, .planning/reviews/RESIDUAL_LEDGER.md, .planning/reviews/KILL_LIST.md</files>
  <action>补齐 `core/api/**/*.py`、`core/mqtt/**/*.py` 及 root-shared support files 的 keep / refactor / compat / delete 分类，并登记 split-root 残留与 kill path。</action>
  <verify>人工检查矩阵是否覆盖 unified-root slice、compat shell、root-owned shared collaborators。</verify>
  <done>Phase 2.5 具备正式治理边界。</done>
</task>

<task type="auto">
  <name>Task 3: 写清 2.6 / 3 的 downstream contract</name>
  <files>.planning/phases/02.5-protocol-root-unification/02.5-ARCHITECTURE.md</files>
  <action>把 `Phase 2.6` 与 `Phase 3` 依赖的 protocol-root 输出、禁止依赖方向、handoff artifacts 写入 architecture 文档。</action>
  <verify>人工检查 architecture 文档是否能回答“2.6/3 可以依赖什么，绝不能再依赖什么”。</verify>
  <done>后续 phase 不再各自发明协议边界解释。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `FILE_MATRIX.md` 已覆盖 unified protocol-root slice
- [ ] `RESIDUAL_LEDGER.md` 与 `KILL_LIST.md` 已登记 split-root / compat 残留
- [ ] `02.5-ARCHITECTURE.md` 已显式锁定 `PROT-05` 与 `PROT-06` 的 owner / boundary / downstream handoff
</verification>

<success_criteria>

- `LiproProtocolFacade` 的 ownership 与 child-façade 边界已被正式定义
- canonical normalization owner 已被写成 protocol-plane 正式裁决
- Phase 2.5 的文件簇、compat shells 与残留已进入治理矩阵
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-protocol-root-unification/02.5-01-SUMMARY.md`
</output>

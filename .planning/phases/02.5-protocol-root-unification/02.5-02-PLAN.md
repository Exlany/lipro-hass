---
phase: 02.5-protocol-root-unification
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.5-01"
files_modified:
  - custom_components/lipro/core/api/**/*.py
  - custom_components/lipro/core/mqtt/**/*.py
  - tests/core/api/**/*.py
  - tests/core/mqtt/**/*.py
  - tests/snapshots/test_api_snapshots.py
  - .planning/phases/02.5-protocol-root-unification/02.5-02-SUMMARY.md
autonomous: true
requirements:
  - PROT-05
  - PROT-06
must_haves:
  - MQTT and REST are unified under one formal protocol root
  - canonical normalized contracts are produced at the protocol boundary, not leaked upward as raw vendor shapes
  - runtime-facing allowed consumers can treat protocol plane as one formal surface
---

# Plan 02.5-02: 建立 unified protocol root 实现骨架并上收 shared truth

<context>
@.planning/phases/02.5-protocol-root-unification/02.5-CONTEXT.md
@.planning/phases/02.5-protocol-root-unification/02.5-ARCHITECTURE.md
@.planning/phases/02.5-protocol-root-unification/02.5-RESEARCH.md
@custom_components/lipro/core/api/
@custom_components/lipro/core/mqtt/
@tests/core/api/
@tests/core/mqtt/
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 unified protocol root skeleton</name>
  <files>custom_components/lipro/core/api/**/*.py, custom_components/lipro/core/mqtt/**/*.py</files>
  <action>引入 `LiproProtocolFacade` 及其 root-owned shared collaborators，让 `LiproRestFacade` / `LiproMqttFacade` 成为显式 child façades。</action>
  <verify>`uv run pytest tests/core/api tests/core/mqtt -q`</verify>
  <done>protocol plane 拥有单一正式根。</done>
</task>

<task type="auto">
  <name>Task 2: 上收 auth / telemetry / protocol diagnostics context</name>
  <files>custom_components/lipro/core/api/**/*.py, custom_components/lipro/core/mqtt/**/*.py</files>
  <action>把协议级共享 auth、telemetry、protocol diagnostics context 从 split-root 结构中抽离并上收到统一协议根，禁止 REST/MQTT 各自保留平行真源。</action>
  <verify>`uv run pytest tests/core/api tests/core/mqtt tests/snapshots/test_api_snapshots.py -q`</verify>
  <done>shared truth 不再散落在 REST / MQTT 两边。</done>
</task>

<task type="auto">
  <name>Task 3: 落实 normalization-at-boundary 规则</name>
  <files>custom_components/lipro/core/api/**/*.py, custom_components/lipro/core/mqtt/**/*.py, tests/core/api/**/*.py, tests/core/mqtt/**/*.py</files>
  <action>确保 raw vendor payload 只停留在 transport adapter 内部，对外提供 canonical normalized contracts，并为 runtime-facing allowed consumers 建立可回归 proof。</action>
  <verify>`uv run pytest tests/core/api tests/core/mqtt tests/snapshots/test_api_snapshots.py -q`</verify>
  <done>`PROT-05` 在实现与测试中闭环。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/core/api tests/core/mqtt tests/snapshots/test_api_snapshots.py -q`
- [ ] `LiproProtocolFacade` 成为唯一正式协议根
- [ ] raw vendor payload 不再越过 protocol boundary 暴露给上层消费者
- [ ] split-root 语义只剩显式临时 adapter，且有删除路径
</verification>

<success_criteria>

- REST / MQTT 的正式主链已统一到 `LiproProtocolFacade`
- shared auth / telemetry / protocol diagnostics context ownership 已上收
- canonical normalized contracts 已成为 runtime-facing allowed consumers 的唯一协议输入形态
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-protocol-root-unification/02.5-02-SUMMARY.md`
</output>

---
phase: 25-quality-10-remediation-master-plan-and-routing
plan: "03"
type: execute
title: sync roadmap, requirements, project, state and handoff truths
wave: 3
depends_on:
  - "02"
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/v1.3-HANDOFF.md
autonomous: true
requirements:
  - GOV-19
must_haves:
  truths:
    - active truth and handoff truth must describe the same route map
    - next focus and next command must point to the first child tranche, not the parent phase
    - route ownership must be reviewable without phase-local context
  artifacts:
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/v1.3-HANDOFF.md
  key_links:
    - roadmap -> requirements -> state -> handoff -> next command
---

# Plan 25-03: 让 active truth 与 handoff truth 对新路线图讲同一条故事

<preconditions>
- `25-02` 已锁定 child phases 的边界与顺序。
- 本 plan 只同步真源，不追加新的执行主题。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.3-HANDOFF.md
@.planning/phases/25-quality-10-remediation-master-plan-and-routing/25-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: sync active truth to the new parent/child route map</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>把 `Phase 25` 母相、`25.1 / 25.2 / 26 / 27` 子相、以及 next focus / next command 统一写入 active truth；明确下一步是规划 `25.1`，而不是继续执行母相本体。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_toolchain_truth.py tests/meta/test_version_sync.py`</verify>
  <done>任何只读 active truth 的代理都能直接得出正确下一步。</done>
</task>

<task type="auto">
  <name>Task 2: sync handoff truth to the same route order and exclusions</name>
  <files>.planning/v1.3-HANDOFF.md, .planning/STATE.md</files>
  <action>把 handoff doc 中旧的 typed/exception/naming seed 顺序替换为新的母相 + 子相执行顺序，并同步写入 `MD5` 协议约束裁决。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_toolchain_truth.py tests/meta/test_version_sync.py`</verify>
  <done>handoff-ready 资产与 active truth 不再冲突。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_toolchain_truth.py tests/meta/test_version_sync.py`
</verification>

<success_criteria>
- active truth 与 handoff truth 已对齐新路线图。
- next command 指向 `Phase 25.1` planning 入口。
- 旧 seed 顺序不再误导后续执行者。
</success_criteria>

<output>
After completion, create `.planning/phases/25-quality-10-remediation-master-plan-and-routing/25-03-SUMMARY.md`
</output>

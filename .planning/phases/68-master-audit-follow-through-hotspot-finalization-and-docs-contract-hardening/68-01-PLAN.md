---
phase: 68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening
plan: "01"
type: execute
title: split telemetry contract density and stage MQTT message processing without reopening public roots
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/telemetry/models.py
  - custom_components/lipro/core/telemetry/outcomes.py
  - custom_components/lipro/core/telemetry/json_payloads.py
  - custom_components/lipro/core/telemetry/sinks.py
  - custom_components/lipro/core/mqtt/message_processor.py
  - custom_components/lipro/core/mqtt/payload.py
  - custom_components/lipro/core/mqtt/topics.py
  - tests/core/telemetry/test_models.py
  - tests/core/telemetry/test_sinks.py
  - tests/core/mqtt/test_message_processor.py
  - tests/core/mqtt/test_mqtt_payload.py
  - tests/core/mqtt/test_topics.py
autonomous: true
requirements:
  - ARC-15
  - HOT-24
  - TST-18
  - QLT-26
must_haves:
  truths:
    - telemetry failure/outcome/json helpers stop crowding one mega contract file while `models.py` stays the outward telemetry home
    - MQTT ingress handling is staged into topic validation, payload decode, callback dispatch, and outcome mapping without reviving a second protocol root
    - focused tests catch telemetry payload drift and message-processor stage regressions locally instead of relying on broad repo audits
  artifacts:
    - custom_components/lipro/core/telemetry/models.py
    - custom_components/lipro/core/telemetry/outcomes.py
    - custom_components/lipro/core/telemetry/json_payloads.py
    - custom_components/lipro/core/telemetry/sinks.py
    - custom_components/lipro/core/mqtt/message_processor.py
    - tests/core/telemetry/test_models.py
    - tests/core/telemetry/test_sinks.py
    - tests/core/mqtt/test_message_processor.py
  key_links:
    - telemetry outward imports -> `models.py` remains the single contract home while inward helpers stay package-local
    - `message_processor.py` stages ingress work, but `custom_components/lipro/core/protocol/boundary/mqtt_decoder.py` remains the only canonical topic/payload decode authority
    - focused telemetry/MQTT tests -> stage-specific helpers fail locally before meta governance suites run
---

# Plan 68-01: split telemetry contract density and stage MQTT message processing without reopening public roots

<objective>
把 `core/telemetry/models.py` 与 `core/mqtt/message_processor.py` 这两个仍然过厚的热点压回更窄的 inward collaborators，同时保持单一 north-star 正式主链：`models.py` 继续是 telemetry outward contract home，`message_processor.py` 继续是 MQTT ingress 协调 home，不新增 public root、compat shell 或 helper-owned second story。

Purpose: 落实 `ARC-15 / HOT-24`，把维护成本从大块长流程转成局部 helper + focused tests。
Output: telemetry inward helper modules、staged MQTT processing path、对应 focused regressions。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@.planning/phases/68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening/68-PRD.md
@.planning/phases/68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening/68-CONTEXT.md
@.planning/phases/67-typed-contract-convergence-toolchain-hardening-and-mypy-closure/67-SUMMARY.md

<interfaces>
From `custom_components/lipro/core/telemetry/models.py`:

```python
class TelemetrySnapshot:
class TelemetryViews:
def build_failure_summary(...)
def build_operation_outcome(...)
```

From `custom_components/lipro/core/mqtt/message_processor.py`:

```python
def decode_payload_text(raw_payload: object, device_id: str) -> str | None
class MqttMessageProcessor:
    def process_message(...)
```

From `custom_components/lipro/core/mqtt/topics.py`:

```python
def parse_topic(topic: str, *, expected_biz_id: str | None = None) -> str | None
```

From `custom_components/lipro/core/mqtt/payload.py`:

```python
def parse_mqtt_payload(payload: Any) -> dict[str, Any]
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: split telemetry failure/outcome/json helpers into inward collaborators</name>
  <files>custom_components/lipro/core/telemetry/models.py, custom_components/lipro/core/telemetry/outcomes.py, custom_components/lipro/core/telemetry/json_payloads.py, custom_components/lipro/core/telemetry/sinks.py, tests/core/telemetry/test_models.py, tests/core/telemetry/test_sinks.py</files>
  <behavior>
    - `build_failure_summary()` and `build_operation_outcome()` keep the same observable payloads for existing exporter/sink consumers.
    - JSON-safe coercion and key-normalization helpers move out of the outward contract file, but `TelemetrySnapshot`, `TelemetryViews`, and TypedDict contracts stay importable from `models.py`.
    - `sinks.py` continues to build diagnostics/system-health/developer/CI views from the same snapshot truth.
  </behavior>
  <action>先为现有 telemetry behavior 补齐/固定 focused tests，然后把 failure-category / handling-policy / outcome-building / JSON coercion helper 从 `models.py` inward split 到 `outcomes.py` 与 `json_payloads.py`。`models.py` 只保留 outward contracts、dataclass views 与稳定 re-export glue；不要修改 `custom_components/lipro/core/telemetry/__init__.py` 去制造新的 package-level public root。同步收薄 `sinks.py` 的 helper 依赖，让它消费新的 inward helpers 而不是复制分类/转换逻辑。整个任务必须遵守 locked decision「keep the single north-star story intact」。</action>
  <verify>
    <automated>uv run pytest -q tests/core/telemetry/test_models.py tests/core/telemetry/test_sinks.py tests/integration/test_telemetry_exporter_integration.py</automated>
  </verify>
  <done>`models.py` 不再同时承载厚重 helper taxonomy 与 outward snapshot/view contracts，且 telemetry focused suites 继续全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: turn MQTT message processing into explicit stages over existing topic/payload helpers</name>
  <files>custom_components/lipro/core/mqtt/message_processor.py, custom_components/lipro/core/mqtt/payload.py, custom_components/lipro/core/mqtt/topics.py, tests/core/mqtt/test_message_processor.py, tests/core/mqtt/test_mqtt_payload.py</files>
  <behavior>
    - invalid topic, invalid payload, callback failure, and canonical fixture success each map to one deterministic `OperationOutcome` branch.
    - `process_message()` becomes a thin stage coordinator instead of mixing parse/log/callback/outcome logic inline.
    - `custom_components/lipro/core/protocol/boundary/mqtt_decoder.py` stays the only canonical MQTT topic/payload decode authority; `topics.py` may only host localized route helpers or boundary-backed adapters.
  </behavior>
  <action>把 `MqttMessageProcessor.process_message()` 重写成显式阶段：topic validation → payload text decode → canonical payload parse → callback dispatch → outcome mapping。必须显式声明 `custom_components/lipro/core/protocol/boundary/mqtt_decoder.py` 是唯一 canonical topic/payload decode authority；若触碰 `topics.py`，它只能继续承担 localized route helper / boundary-backed adapter，不能定义第二套 grammar 或 decode truth。优先复用 `payload.py` 的 canonical payload shim；若需要新增私有 helper，只能留在 `message_processor.py` 内或下沉到既有 sibling homes，不能新增新的 package export。增强 `tests/core/mqtt/test_message_processor.py`，让每个阶段都由 focused case 命中，并补 `tests/core/mqtt/test_mqtt_payload.py` 与 `tests/core/mqtt/test_topics.py` 的 boundary parity / regression 覆盖。</action>
  <verify>
    <automated>uv run pytest -q tests/core/mqtt/test_message_processor.py tests/core/mqtt/test_mqtt_payload.py tests/integration/test_mqtt_coordinator_integration.py</automated>
  </verify>
  <done>`process_message()` 退化为 stage coordinator，MQTT focused regressions 仍证明单一路径有效且日志/错误语义不漂移。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/telemetry/test_models.py tests/core/telemetry/test_sinks.py tests/integration/test_telemetry_exporter_integration.py`
- [ ] `uv run pytest -q tests/core/mqtt/test_message_processor.py tests/core/mqtt/test_mqtt_payload.py tests/integration/test_mqtt_coordinator_integration.py`
</verification>

<success_criteria>
- telemetry outward truth 仍以 `models.py` 为准，但 helper density 明显下降。
- MQTT ingress 失败定位从单个大函数变成可测试的 stage seams，且 topic/payload truth 继续只归 `protocol.boundary`。
- touched code 继续服从单一 north-star 主链，没有新 public root 或 compat 回流。
</success_criteria>

<output>
After completion, create `.planning/phases/68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening/68-01-SUMMARY.md`.
</output>

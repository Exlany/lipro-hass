---
phase: 68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening
plan: "02"
type: execute
title: thin anonymous-share and OTA query flows without broad rewrites or new outward homes
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/anonymous_share/share_client_flows.py
  - custom_components/lipro/core/anonymous_share/share_client_support.py
  - custom_components/lipro/core/anonymous_share/share_client.py
  - custom_components/lipro/core/api/diagnostics_api_ota.py
  - custom_components/lipro/core/api/request_policy_support.py
  - tests/core/test_share_client.py
  - tests/services/test_services_share.py
  - tests/core/test_anonymous_share_storage.py
  - tests/core/api/test_api_diagnostics_service.py
  - tests/core/api/test_api_request_policy.py
autonomous: true
requirements:
  - ARC-15
  - HOT-24
  - TST-18
  - QLT-26
must_haves:
  truths:
    - anonymous-share submit/refresh flows localize token/preflight/attempt logic without changing the formal outward share client story
    - OTA diagnostics queries localize fallback/result-building branches without creating another diagnostics API root
    - focused regressions prove rate-limit, invalid-param, degraded fallback, and submit retry behavior through narrow helpers
  artifacts:
    - custom_components/lipro/core/anonymous_share/share_client_flows.py
    - custom_components/lipro/core/anonymous_share/share_client_support.py
    - custom_components/lipro/core/api/diagnostics_api_ota.py
    - custom_components/lipro/core/api/request_policy_support.py
    - tests/core/test_share_client.py
    - tests/core/api/test_api_diagnostics_service.py
  key_links:
    - `share_client_flows.py` -> `share_client_support.py` stays inward-only and does not become a second outward root
    - `diagnostics_api_ota.py` -> `request_policy_support.py` reuses existing request-policy helpers instead of duplicating backoff/error folklore
    - focused share/OTA suites -> verify degraded and rate-limited branches before repo-wide bundles run
---

# Plan 68-02: thin anonymous-share and OTA query flows without broad rewrites or new outward homes

<objective>
对 `anonymous_share` 与 diagnostics OTA 查询这两条仍然偏厚的 protocol-plane 流程做 inward decomposition，把 token refresh / submit attempts / OTA fallback branches 抽成更小的 helper/result seams，同时保持 `ShareWorkerClient` 与 diagnostics API service family 的正式 outward home 不变。

Purpose: 落实 `ARC-15 / HOT-24`，让 share/OTA 维护成本下降但 formal-home 清晰度不回退。
Output: 收薄后的 share/OTA flows、focused regressions、未新增 second story 的 protocol collaborators。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/FILE_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
@.planning/reviews/KILL_LIST.md
@.planning/phases/68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening/68-PRD.md
@.planning/phases/68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening/68-CONTEXT.md
@.planning/phases/58-repository-audit-refresh-and-next-wave-routing/58-REMEDIATION-ROADMAP.md

<interfaces>
From `custom_components/lipro/core/anonymous_share/share_client_flows.py`:

```python
async def refresh_install_token_with_outcome(...)
async def submit_share_payload_with_outcome(...)
```

From `custom_components/lipro/core/anonymous_share/share_client.py`:

```python
class ShareWorkerClient:
```

From `custom_components/lipro/core/api/diagnostics_api_ota.py`:

```python
async def query_ota_info_with_outcome(...)
async def query_ota_info(...)
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: localize anonymous-share preflight, token-refresh, and submit-attempt branches</name>
  <files>custom_components/lipro/core/anonymous_share/share_client_flows.py, custom_components/lipro/core/anonymous_share/share_client_support.py, custom_components/lipro/core/anonymous_share/share_client.py, tests/core/test_share_client.py, tests/services/test_services_share.py, tests/core/test_anonymous_share_storage.py</files>
  <behavior>
    - submit payload preflight, token refresh, retry-after handling, and one-variant attempt resolution remain behaviorally identical for callers.
    - `share_client_support.py` stays inward-only; `share_client_flows.py` and `share_client.py` remain the outward family homes.
    - rate-limited and degraded outcomes stay explicit and machine-testable.
  </behavior>
  <action>围绕 `refresh_install_token_with_outcome()` 与 `submit_share_payload_with_outcome()` 先补 focused failing cases，再把 token refresh scheduling、variant attempt resolution、retry-after parsing/normalization 与 payload preflight 抽回 `share_client_support.py` 或现有 sibling helpers。不要新增新的 package exports，也不要把 bool-only compat bridge 重新合法化。`ShareWorkerClient` 只保留 outward client contract 与 minimal orchestration。</action>
  <verify>
    <automated>uv run pytest -q tests/core/test_share_client.py tests/services/test_services_share.py tests/core/test_anonymous_share_storage.py tests/core/test_init_service_handlers_share_reports.py</automated>
  </verify>
  <done>Share flow 的长分支被切薄，submit/token behavior 仍由 focused suites 锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reduce OTA query decision density through localized result/fallback helpers</name>
  <files>custom_components/lipro/core/api/diagnostics_api_ota.py, custom_components/lipro/core/api/request_policy_support.py, tests/core/api/test_api_diagnostics_service.py, tests/core/api/test_api_request_policy.py</files>
  <behavior>
    - v1/v2/rich-v2/controller fallback order stays the same for OTA diagnostics callers.
    - invalid-param, other API error, degraded fallback, and primary-failure reporting remain explicit outcomes.
    - request/backoff helper usage stays within the existing request-policy family instead of a new diagnostics helper root.
  </behavior>
  <action>把 `diagnostics_api_ota.py` 中的 outcome resolution、HTTP-status extraction、fallback sequencing 与 row merge/dedupe 的 thick branches 收窄为更小 helper clusters，优先复用 `request_policy_support.py` 中已存在的 request-policy semantics，而不是复制 policy folklore。保持 `diagnostics_api_service.py -> diagnostics_api_ota.py` 的 thin outward relationship不变，禁止新增第二 diagnostics API root。</action>
  <verify>
    <automated>uv run pytest -q tests/core/api/test_api_diagnostics_service.py tests/core/api/test_api_request_policy.py tests/services/test_services_diagnostics.py</automated>
  </verify>
  <done>OTA query flow 仍走同一 outward path，但 primary/degraded branches 已能由 focused helpers 和测试局部定位。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_share_client.py tests/services/test_services_share.py tests/core/test_anonymous_share_storage.py tests/core/test_init_service_handlers_share_reports.py`
- [ ] `uv run pytest -q tests/core/api/test_api_diagnostics_service.py tests/core/api/test_api_request_policy.py tests/services/test_services_diagnostics.py`
</verification>

<success_criteria>
- `anonymous_share` 与 OTA diagnostics 继续是单一 outward story，helper 只做 inward split。
- 失败/降级/速率限制分支不再埋在长流程里。
- focused suites 覆盖真实 hotspot branches，而不是靠 broad regression 才能发现偏航。
</success_criteria>

<output>
After completion, create `.planning/phases/68-master-audit-follow-through-hotspot-finalization-and-docs-contract-hardening/68-02-SUMMARY.md`.
</output>

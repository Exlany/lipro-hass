---
phase: 27-hotspot-slimming-dependency-strategy-and-maintainability-follow-through
plan: "03"
type: execute
title: sync hotspot dependency and residual governance truth
wave: 3
depends_on:
  - 27-02
files_modified:
  - .planning/codebase/STRUCTURE.md
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/DEPENDENCY_MATRIX.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
autonomous: true
requirements:
  - HOT-05
  - RES-04
must_haves:
  truths:
    - governance docs must say protocol capability consumers now depend on protocol_service rather than coordinator pure forwarders
    - coordinator residual story must record forwarder retirement and phase-residue cleanup without inventing a second root
    - codebase maps remain derived views, not new authority sources
  artifacts:
    - .planning/codebase/STRUCTURE.md
    - .planning/baseline/PUBLIC_SURFACES.md
    - .planning/baseline/DEPENDENCY_MATRIX.md
    - .planning/baseline/VERIFICATION_MATRIX.md
    - .planning/reviews/RESIDUAL_LEDGER.md
  key_links:
    - runtime formal contract -> dependency truth -> residual truth
---

# Plan 27-03: governance truth sync for hotspot slimming

<context>
@.planning/codebase/STRUCTURE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@.planning/baseline/VERIFICATION_MATRIX.md
@.planning/reviews/RESIDUAL_LEDGER.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: sync baseline and derived maps to the protocol_service story</name>
  <files>.planning/codebase/STRUCTURE.md, .planning/baseline/PUBLIC_SURFACES.md, .planning/baseline/DEPENDENCY_MATRIX.md, .planning/baseline/VERIFICATION_MATRIX.md</files>
  <read_first>
    - .planning/codebase/STRUCTURE.md
    - .planning/baseline/PUBLIC_SURFACES.md
    - .planning/baseline/DEPENDENCY_MATRIX.md
    - .planning/baseline/VERIFICATION_MATRIX.md
  </read_first>
  <action>把 touched docs 统一改写为：schedule / diagnostics / OTA capability consumers 走 `coordinator.protocol_service`；coordinator 顶层 pure forwarders 已不再是 formal surface。保留 codebase map 的 derived-view 身份说明，不把 phase workspace 资产升级为 authority。</action>
  <acceptance_criteria>
    - `.planning/baseline/PUBLIC_SURFACES.md` contains `protocol_service`
    - `.planning/baseline/DEPENDENCY_MATRIX.md` contains `protocol_service`
    - `.planning/baseline/VERIFICATION_MATRIX.md` contains `Phase 27`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_version_sync.py`</verify>
  <done>baseline / derived maps 现在对 27 的 runtime/protocol formal surface 讲同一条故事。</done>
</task>

<task type="auto">
  <name>Task 2: record residual delta without re-legalizing removed seams</name>
  <files>.planning/reviews/RESIDUAL_LEDGER.md</files>
  <read_first>
    - .planning/reviews/RESIDUAL_LEDGER.md
    - .planning/baseline/PUBLIC_SURFACES.md
  </read_first>
  <action>登记 `Phase 27` residual delta：说明关闭的是 coordinator protocol-forwarder cluster 与历史 phase narration seam，而不是引入新的 compat shell / second-root story。若 `LiproRestFacade` 仍是 child façade hotspot，只能诚实记为 remaining hotspot，不得假装已经不存在。</action>
  <acceptance_criteria>
    - `.planning/reviews/RESIDUAL_LEDGER.md` contains `## Phase 27 Residual Delta`
    - `.planning/reviews/RESIDUAL_LEDGER.md` contains `protocol_service`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_public_surface_guards.py`</verify>
  <done>residual truth 诚实记录 27 的删除与剩余热点，不制造新残留叙事。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_version_sync.py`
</verification>

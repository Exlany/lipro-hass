---
phase: 27-hotspot-slimming-dependency-strategy-and-maintainability-follow-through
plan: "02"
type: execute
title: remove coordinator protocol forwarders and phase-residue comments
wave: 2
depends_on:
  - 27-01
files_modified:
  - custom_components/lipro/core/coordinator/coordinator.py
  - tests/core/test_coordinator.py
  - tests/test_coordinator_public.py
  - tests/core/test_init.py
autonomous: true
requirements:
  - HOT-05
  - RES-04
must_haves:
  truths:
    - coordinator should no longer expose removable pure protocol forwarders once external consumers are migrated
    - outlet-power polling must use the owned protocol_service directly rather than self-forwarding through coordinator
    - runtime docstrings/comments should stop narrating historical phase labels in formal code paths
  artifacts:
    - custom_components/lipro/core/coordinator/coordinator.py
    - tests/core/test_coordinator.py
    - tests/test_coordinator_public.py
  key_links:
    - protocol_service migration -> forwarder deletion -> runtime residue cleanup
---

# Plan 27-02: coordinator forwarder removal + runtime residue cleanup

<context>
@custom_components/lipro/core/coordinator/coordinator.py
@tests/core/test_coordinator.py
@tests/test_coordinator_public.py
@tests/core/test_init.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: delete the pure protocol-forwarder cluster from Coordinator</name>
  <files>custom_components/lipro/core/coordinator/coordinator.py, tests/core/test_coordinator.py, tests/test_coordinator_public.py</files>
  <read_first>
    - custom_components/lipro/core/coordinator/coordinator.py
    - tests/core/test_coordinator.py
    - tests/test_coordinator_public.py
  </read_first>
  <action>删除已无生产调用者的 schedule/diagnostics/OTA/outlet-power pure forwarders，并把 coordinator 内部 outlet-power polling 改为直接使用 `self.protocol_service.async_fetch_outlet_power_info`。同步收敛 public/runtime tests，确保 entity-facing helpers 仍保留，但 protocol forwarders 不再是 coordinator formal surface。</action>
  <acceptance_criteria>
    - `custom_components/lipro/core/coordinator/coordinator.py` does not contain `async_query_command_result(`
    - `custom_components/lipro/core/coordinator/coordinator.py` does not contain `async_query_ota_info(`
    - `uv run pytest -q tests/core/test_coordinator.py tests/test_coordinator_public.py` exits `0`
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/test_coordinator.py tests/test_coordinator_public.py`</verify>
  <done>coordinator 已切掉 protocol-forwarder cluster，内部 wiring 也不再自转发。</done>
</task>

<task type="auto">
  <name>Task 2: remove historical phase narration from formal runtime code</name>
  <files>custom_components/lipro/core/coordinator/coordinator.py</files>
  <read_first>
    - custom_components/lipro/core/coordinator/coordinator.py
  </read_first>
  <action>清理正式 runtime docstring / comments 中的 `Phase C`、`Phase H4` 等历史迁移叙事，只保留当前语义与行为说明，不再把已完成的重构阶段写进 production code narration。</action>
  <acceptance_criteria>
    - `custom_components/lipro/core/coordinator/coordinator.py` does not contain `Phase C`
    - `custom_components/lipro/core/coordinator/coordinator.py` does not contain `Phase H4`
  </acceptance_criteria>
  <verify>`uv run ruff check custom_components/lipro/core/coordinator/coordinator.py`</verify>
  <done>runtime 正式代码不再夹带历史 phase 叙事噪声。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_coordinator.py tests/test_coordinator_public.py`
- [ ] `uv run ruff check custom_components/lipro/core/coordinator/coordinator.py`
</verification>

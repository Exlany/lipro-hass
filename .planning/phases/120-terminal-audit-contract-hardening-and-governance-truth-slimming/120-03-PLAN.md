---
phase: 120-terminal-audit-contract-hardening-and-governance-truth-slimming
plan: "03"
type: execute
title: slim tooling and docs truth while activating stable current-route and continuity pointers
wave: 1
depends_on: []
files_modified:
  - scripts/check_file_matrix.py
  - scripts/lint
  - tests/conftest.py
  - tests/meta/toolchain_truth_checker_paths.py
  - tests/meta/test_phase89_tooling_decoupling_guards.py
  - tests/meta/governance_current_truth.py
  - tests/meta/governance_followup_route_current_milestones.py
  - tests/meta/test_governance_release_docs.py
  - docs/developer_architecture.md
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - docs/architecture_archive.md
  - .github/pull_request_template.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-03-SUMMARY.md
autonomous: true
requirements:
  - GOV-78
  - GOV-79
  - DOC-10
  - OSS-15
  - TST-42
must_haves:
  truths:
    - checker imports, changed-surface assurance, and their meta guards rely on stable structure/behavior truth rather than phase-specific string folklore
    - developer/runbook/template/current-route docs use stable selector families or latest archived pointers instead of hard-coded `.planning/v1.33...` paths
    - active route truth for `Phase 120` is machine-readable across `.planning/{PROJECT,ROADMAP,REQUIREMENTS,STATE,MILESTONES}.md`, while `v1.33` remains pull-only archived evidence
    - maintainer continuity docs tell one honest freeze-posture story without implying hidden delegates or solved external fallback
  artifacts:
    - scripts/check_file_matrix.py
    - scripts/lint
    - docs/developer_architecture.md
    - docs/architecture_archive.md
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - .github/pull_request_template.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/MILESTONES.md
  key_links:
    - checker/lint entrypoints and meta guards share one import/config truth without phase-specific naming debt
    - docs/runbook/template reference current selector family plus latest archived evidence pointer, not version-pinned audit filenames as current truth
    - planning-route contract blocks stay identical across all live planning docs while preserving archived v1.33 evidence identity
---

# Plan 120-03: slim tooling and docs truth while activating stable current-route and continuity pointers

<objective>
把 tooling / docs / governance current-story residual 一次性收口：checker imports 与 changed-surface assurance 去 phase-specific folklore，developer/runbook/template 切到稳定 pointer family，同时把 `.planning` live docs 激活到 `v1.34 / Phase 120 planned` 单一 machine-readable route，并诚实 codify maintainer continuity freeze posture。

Purpose: 落实 `GOV-78`、`GOV-79`、`DOC-10`、`OSS-15` 与 `TST-42`，让当前路线与 archived evidence 各归其位，避免 docs/tooling 继续依赖 brittle string 或版本号写死的 internal pointer。
Output: 更新后的 tooling/docs/governance current-story surfaces、focused guards、active-route planning docs，以及 `120-03-SUMMARY.md`。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-CONTEXT.md
@.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-RESEARCH.md
@scripts/check_file_matrix.py
@scripts/lint
@tests/conftest.py
@tests/meta/toolchain_truth_checker_paths.py
@tests/meta/test_phase89_tooling_decoupling_guards.py
@tests/meta/governance_current_truth.py
@tests/meta/governance_followup_route_current_milestones.py
@tests/meta/test_governance_release_docs.py
@docs/developer_architecture.md
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@.github/pull_request_template.md
</context>

<mapping>
<decisions>
- D-01: 继续坚持 `single mainline / formal homes / no second root / honesty over invented continuity`。
- D-04: tooling guards 优先验证行为、AST/import graph 或 machine-readable pointers，不再依赖脆弱字符串 folklore。
- D-05: docs / runbook / template 应优先引用稳定 current selector family 与 latest archived pointer，而不是版本号写死的 audit/evidence 文件名。
- D-06: archived milestone v1.33 的证据链继续存在，但不再承担 current-route selector 身份。
</decisions>
<north_star>
- Rule 2 / 单一正式主链：current selector truth 只来自 `.planning/{PROJECT,ROADMAP,REQUIREMENTS,STATE,MILESTONES}.md` 共享 route contract，而非 runbook / PR template / hard-coded tests 各讲各的。
- Rule 6 / 可验证、可仲裁：checker/lint/docs truth 必须可由 focused tests 与 machine-readable contract 自动验证。
- Rule 7 / 迁移残留持续收口：`phase113_*` 命名债、`v1.33` current-pointer folklore、developer doc 历史 appendix 混层都必须显式清退。
</north_star>
<requirements>
- `GOV-78`: current selector family 与 docs/template 只使用稳定入口 / latest archived pointer，不写死当前版本审计路径。
- `GOV-79`: checker imports 与 changed-surface assurance 改为单一 import/config truth，meta guards 转向结构/行为断言。
- `DOC-10`: `docs/developer_architecture.md` 只保留 current developer guidance，历史 appendix 下沉到 archive doc。
- `OSS-15`: maintainer continuity 仅 codify honest freeze posture，不暗示 hidden delegate / solved external fallback。
- `TST-42`: focused guards 覆盖 tooling/docs/governance truth slimming 与 active-route activation。
</requirements>
</mapping>

<tasks>

<task type="auto" tdd="true">
  <name>Task 120-03-01: de-brittle checker and changed-surface tooling truth</name>
  <files>scripts/check_file_matrix.py, scripts/lint, tests/conftest.py, tests/meta/toolchain_truth_checker_paths.py, tests/meta/test_phase89_tooling_decoupling_guards.py</files>
  <behavior>
    - checker imports use one import-home strategy without `TYPE_CHECKING` / direct-script / package triple-branch duplication
    - changed-surface assurance no longer exposes `phase113_*` naming debt in variables or user-facing output
    - meta guards validate import targets / behavior / stable command families rather than brittle string trivia; test fixture docs use `uv` wording
  </behavior>
  <action>按 D-04 执行：精简 `scripts/check_file_matrix.py` 的 import-home 逻辑到单一、清晰的 strategy（可用 importlib / module prefix 选择，但不得回引 `sys.path.insert` 或 tests-owned helper）；把 `scripts/lint` 中 `phase113_*` changed-surface assurance 改成 data-driven / concern-driven rule family，并同步刷新终端文案。更新 `tests/conftest.py` 的安装提示到 `uv` 规范；重写 `tests/meta/toolchain_truth_checker_paths.py` 与 `tests/meta/test_phase89_tooling_decoupling_guards.py`，优先断言 AST/import target、helper ownership、behavior contract，而不是简单字符串包含/排除。不要引入新依赖，也不要在 tooling tests 中硬编码新的 phase folklore。</action>
  <verify>
    <automated>uv run pytest tests/meta/toolchain_truth_checker_paths.py tests/meta/test_phase89_tooling_decoupling_guards.py -q</automated>
  </verify>
  <done>checker/lint/toolchain guards share one import/config truth, phase113 naming debt disappears from changed-surface assurance, and fixture/tooling docs align with `uv` entrypoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 120-03-02: slim docs/governance truth and activate the new current route</name>
  <files>docs/developer_architecture.md, docs/architecture_archive.md, docs/MAINTAINER_RELEASE_RUNBOOK.md, .github/pull_request_template.md, tests/meta/governance_current_truth.py, tests/meta/governance_followup_route_current_milestones.py, tests/meta/test_governance_release_docs.py, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/MILESTONES.md</files>
  <behavior>
    - `docs/developer_architecture.md` keeps current guidance only, while historical phase notes move to an archive appendix file
    - runbook and PR template point to stable current selector family / latest archived pointer instead of hard-coded `.planning/v1.33...` paths
    - live planning docs switch from archived-only route to a machine-readable active `v1.34 / Phase 120 planned` route, while `v1.33` stays archived evidence only
    - continuity wording remains honest about missing delegate / private fallback and defines freeze posture from one current source
  </behavior>
  <action>以 D-05 / D-06 为核心执行：把 `docs/developer_architecture.md` 的历史 appendix 下沉到 dedicated archive doc（例如 `docs/architecture_archive.md`），主文档只保留 current developer guidance；更新 `docs/MAINTAINER_RELEASE_RUNBOOK.md` 与 `.github/pull_request_template.md` 到 stable pointer family（current selector docs + latest archived evidence pointer），避免继续写死 `.planning/v1.33-MILESTONE-AUDIT.md` / `V1_33_EVIDENCE_INDEX.md` 作为 current truth。随后同步改写 `.planning/{PROJECT,ROADMAP,REQUIREMENTS,STATE,MILESTONES}.md` 的 route contract，激活 `v1.34` current milestone 与 `Phase 120` planned state，并更新 `tests/meta/governance_current_truth.py`、`tests/meta/governance_followup_route_current_milestones.py`、`tests/meta/test_governance_release_docs.py` 以匹配新的 active-route machine truth。注意：`v1.33` archived audit/evidence 仍保留 pull-only identity；不得伪造 delegate 或 repo-external continuity 已解决。</action>
  <verify>
    <automated>uv run pytest tests/meta/governance_followup_route_current_milestones.py tests/meta/test_governance_release_docs.py -q && uv run python scripts/check_file_matrix.py --check</automated>
  </verify>
  <done>developer/runbook/template/current planning docs tell one active-route story, archived v1.33 assets remain evidence-only, and continuity wording is honest and centrally guarded.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/meta/toolchain_truth_checker_paths.py tests/meta/test_phase89_tooling_decoupling_guards.py tests/meta/governance_followup_route_current_milestones.py tests/meta/test_governance_release_docs.py -q`
- `uv run python scripts/check_file_matrix.py --check`
- checker/lint no longer expose phase-specific naming debt, and docs/current-route pointers no longer hard-code current version audit paths as live truth
</verification>

<success_criteria>
- `GOV-78` and `DOC-10` are satisfied through stable pointer families plus a clean split between current guidance and archived appendix
- `GOV-79` is satisfied through single-strategy checker imports, data-driven changed-surface assurance, and de-brittled meta guards
- `OSS-15` is satisfied through one honest continuity freeze-posture story with no invented delegate
- `TST-42` is satisfied for the tooling/docs/governance slice via focused guards and `check_file_matrix` validation
</success_criteria>

<output>
After completion, create `.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-03-SUMMARY.md`
</output>

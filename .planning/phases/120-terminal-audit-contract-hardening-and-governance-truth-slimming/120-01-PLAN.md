---
phase: 120-terminal-audit-contract-hardening-and-governance-truth-slimming
plan: "01"
type: execute
title: tighten runtime and service contracts while collapsing send_command validation onto one schema truth
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/runtime_types.py
  - custom_components/lipro/services/contracts.py
  - custom_components/lipro/services/command.py
  - custom_components/lipro/control/runtime_access.py
  - tests/core/test_runtime_access.py
  - tests/services/test_service_resilience.py
  - tests/meta/test_runtime_contract_truth.py
  - .planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-01-SUMMARY.md
autonomous: true
requirements:
  - ARC-32
  - HOT-52
  - TST-42
must_haves:
  truths:
    - runtime/service public protocols expose named typed carriers instead of ad-hoc `list[dict[str, str]]` folklore where an existing formal contract already exists
    - `send_command` request validation has one formal schema truth; handler code no longer keeps a parallel hand-written type gate
    - runtime diagnostics snapshots derive degraded/mapping fallback through formal runtime-access helpers, not raw coordinator fields
  artifacts:
    - custom_components/lipro/runtime_types.py
    - custom_components/lipro/services/contracts.py
    - custom_components/lipro/services/command.py
    - custom_components/lipro/control/runtime_access.py
    - tests/meta/test_runtime_contract_truth.py
  key_links:
    - `runtime_types.py` typed aliases are consumed by both service handlers and coordinator-facing protocols without creating a second contract home
    - `services/command.py` delegates direct payload normalization to `services/contracts.py` only once
    - `runtime_access.py` snapshots use `get_runtime_device_mapping()` / `is_runtime_device_mapping_degraded()` instead of raw fallback reads
---

# Plan 120-01: tighten runtime and service contracts while collapsing send_command validation onto one schema truth

<objective>
把 runtime/service-facing public contract 收紧到既有 formal homes：用命名 typed carrier 取代裸 payload folklore，把 `send_command` 输入验证压回 `services/contracts.py` 单一 schema truth，并让 `runtime_access` 快照只经 formal helper 归一化。

Purpose: 落实 `ARC-32`、`HOT-52` 与 `TST-42`，继续沿 `single mainline / formal homes / no second root` 收口 runtime/service residual，而不是靠局部 wrapper 或 duplicate validation 缝补。
Output: 更新后的 runtime/service contract 家族、focused regressions、以及 `120-01-SUMMARY.md`。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-CONTEXT.md
@.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-RESEARCH.md
@custom_components/lipro/runtime_types.py
@custom_components/lipro/services/contracts.py
@custom_components/lipro/services/command.py
@custom_components/lipro/control/runtime_access.py
@tests/core/test_runtime_access.py
@tests/services/test_service_resilience.py
@tests/meta/test_runtime_contract_truth.py
</context>

<mapping>
<decisions>
- D-01: 继续坚持 `single mainline / formal homes / no second root / honesty over invented continuity`。
- D-02: runtime/service public typing 只允许收紧，不得把 compat shell 或 additional protocol 再洗成正式 home。
- D-04: tooling guards 优先验证行为、AST/import graph 或 machine-readable pointers，不再依赖脆弱字符串 folklore。
</decisions>
<north_star>
- Rule 1 / 显式组合优于继承聚合：既有 contract home 提供显式 typed alias / strict helper，不在 handler 侧散落 duplicate validation。
- Rule 4 / 领域真源优于重复表达：`services/contracts.py` 继续是 `send_command` payload 的 formal schema truth。
- Rule 5 / 平面分离：`runtime_access.py` 只经 formal helper/read-model surface 读取 runtime facts，不回退直接摸 coordinator raw field。
- Rule 6 / 可验证、可仲裁：focused tests 与 meta guard 必须能机器证明 single-source truth 成立。
</north_star>
<requirements>
- `ARC-32`: runtime/service public protocols 使用命名 typed carrier，而不是裸 `list[dict[str, str]]` / broad mapping folklore。
- `HOT-52`: `send_command` 输入校验收敛到单一 schema 真源。
- `TST-42`: focused regressions / guards 冻结 runtime snapshot fallback 与 service schema single-source truth。
</requirements>
</mapping>

<tasks>

<task type="auto" tdd="true">
  <name>Task 120-01-01: tighten runtime-facing protocol carriers and snapshot normalization</name>
  <files>custom_components/lipro/runtime_types.py, custom_components/lipro/control/runtime_access.py, tests/core/test_runtime_access.py, tests/meta/test_runtime_contract_truth.py</files>
  <behavior>
    - runtime-facing command property carriers reuse one named typed alias instead of repeated `list[dict[str, str]]`
    - protocol diagnostics snapshot surfaces expose narrower JSON-like aliases where the current formal API types already exist
    - runtime diagnostics projections compute degraded device state through formal helper functions rather than raw coordinator-device fallback
  </behavior>
  <action>按 D-01 / D-02 执行：在 `runtime_types.py` 中引入/复用命名 typed carrier（例如基于 `ServiceProperty`、`JsonObject` 的 alias）并替换 `CommandServiceLike`、`LiproRuntimeCoordinator` 及相关 telemetry protocol 上的裸 `list[dict[str, str]]` / `Mapping[str, object]` 暴露；在 `runtime_access.py` 中把 `device_count` fallback 与 degraded detection 收回 `get_runtime_device_mapping()` / `is_runtime_device_mapping_degraded()` formal helper，避免继续直接依赖 raw coordinator facts。同步更新 `tests/core/test_runtime_access.py` 与 `tests/meta/test_runtime_contract_truth.py`，冻结 typed carrier 与 snapshot normalization truth。不要改写 `Coordinator` public home，也不要引入新的 contract module。</action>
  <verify>
    <automated>uv run pytest tests/core/test_runtime_access.py tests/meta/test_runtime_contract_truth.py -q</automated>
  </verify>
  <done>runtime-facing contracts no longer advertise raw property-list folklore, and runtime snapshots use the formal runtime-access helper surface for device mapping/degraded detection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 120-01-02: collapse send_command direct payload validation onto one schema truth</name>
  <files>custom_components/lipro/services/contracts.py, custom_components/lipro/services/command.py, tests/services/test_service_resilience.py, tests/meta/test_runtime_contract_truth.py</files>
  <behavior>
    - direct `send_command` payloads reject invalid field types without relying on a second hand-written validation pass
    - schema normalization still rejects coercion-prone inputs and preserves max-length / pattern constraints
    - command service handler uses one support/schema entrypoint, not duplicate business rules in handler code
  </behavior>
  <action>以 `services/contracts.py` 为 formal truth 执行 `HOT-52`：把 strict string/list/property-item validation 内聚到 schema/support helper 中（可新增 strict helper function 或 typed normalization function，但必须留在 `services/contracts.py` family），然后让 `services/command.py` 仅调用该单一 normalization truth，而不是先跑 `_validate_send_command_payload_types()` 再跑 `SERVICE_SEND_COMMAND_SCHEMA`。同步刷新 `tests/services/test_service_resilience.py` 与 `tests/meta/test_runtime_contract_truth.py`，确保 invalid direct payload、边界长度与 single-source contract 都被 focused regressions 冻结。避免新增新依赖，也不要改变 service translation key contract。</action>
  <verify>
    <automated>uv run pytest tests/services/test_service_resilience.py tests/meta/test_runtime_contract_truth.py -q</automated>
  </verify>
  <done>`send_command` validation has exactly one schema-owned truth, and handler code no longer duplicates field-type rules already expressed in the service contract family.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/core/test_runtime_access.py tests/services/test_service_resilience.py tests/meta/test_runtime_contract_truth.py -q`
- runtime/service contract truth stays inside `runtime_types.py` + `services/contracts.py` without adding a second public home
- runtime snapshot fallback uses formal helper APIs instead of raw coordinator-device field access
</verification>

<success_criteria>
- `ARC-32` is satisfied through named typed carriers and narrower runtime-facing protocol signatures
- `HOT-52` is satisfied through one schema-owned `send_command` normalization truth
- `TST-42` is satisfied for the runtime/service slice via focused regressions proving helper-owned snapshot fallback and single-source validation
</success_criteria>

<output>
After completion, create `.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-01-SUMMARY.md`
</output>

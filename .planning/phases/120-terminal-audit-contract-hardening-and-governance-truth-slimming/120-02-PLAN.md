---
phase: 120-terminal-audit-contract-hardening-and-governance-truth-slimming
plan: "02"
type: execute
title: harden flow error taxonomy and freeze explicit translated failure semantics
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/flow/login.py
  - custom_components/lipro/flow/submission.py
  - custom_components/lipro/translations/en.json
  - custom_components/lipro/translations/zh-Hans.json
  - tests/flows/test_config_flow_user.py
  - tests/flows/test_config_flow_reauth.py
  - tests/flows/test_config_flow_reconfigure.py
  - tests/flows/test_flow_submission.py
  - .planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-02-SUMMARY.md
autonomous: true
requirements:
  - QLT-47
  - TST-42
must_haves:
  truths:
    - config flows distinguish invalid credentials, connectivity failure, malformed upstream response, invalid persisted entry state, and unexpected internal failure via stable translated keys
    - reauth/reconfigure stored-entry validation no longer reports every persisted-entry defect as generic `unknown`
    - translations and focused regressions freeze the new taxonomy so UX and debugging signals stay aligned
  artifacts:
    - custom_components/lipro/flow/login.py
    - custom_components/lipro/flow/submission.py
    - custom_components/lipro/translations/en.json
    - custom_components/lipro/translations/zh-Hans.json
    - tests/flows/test_config_flow_user.py
    - tests/flows/test_config_flow_reauth.py
    - tests/flows/test_config_flow_reconfigure.py
    - tests/flows/test_flow_submission.py
  key_links:
    - `map_login_error()` and hashed-login exception handling produce explicit translated keys consumed by the flow UI
    - persisted-entry validation failures in `flow/submission.py` map to stable base errors instead of collapsing into `unknown`
    - translation catalogs and focused flow tests assert the same error vocabulary
---

# Plan 120-02: harden flow error taxonomy and freeze explicit translated failure semantics

<objective>
把 user / reauth / reconfigure flow 的错误语义从粗粒度 `unknown` 收紧为可翻译、可回归、可定位的 taxonomy，并同步刷新 translations 与 focused regressions。

Purpose: 落实 `QLT-47` 与 `TST-42`，提高用户提示与回归定位质量，同时继续遵守 `single mainline / honesty / no second root`，不通过 conversation-only 说明掩盖 persisted-entry 或 malformed-response 问题。
Output: 更新后的 flow helpers、双语错误文案、focused flow regressions，以及 `120-02-SUMMARY.md`。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-CONTEXT.md
@.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-RESEARCH.md
@custom_components/lipro/flow/login.py
@custom_components/lipro/flow/submission.py
@custom_components/lipro/translations/en.json
@custom_components/lipro/translations/zh-Hans.json
@tests/flows/test_config_flow_user.py
@tests/flows/test_config_flow_reauth.py
@tests/flows/test_config_flow_reconfigure.py
@tests/flows/test_flow_submission.py
</context>

<mapping>
<decisions>
- D-01: 继续坚持 `single mainline / formal homes / no second root / honesty over invented continuity`。
- D-03: flow error taxonomy 必须新增明确键值表达不同失败原因，但不能牺牲现有 auth/connectivity 语义。
</decisions>
<north_star>
- Rule 3 / 边界归一化优于内部 folklore：flow helper 必须把 upstream / persisted-entry failure 归一化到明确的 UI-facing error taxonomy。
- Rule 6 / 可验证、可观测：translation keys、双语文案与 focused tests 必须共同冻结该 taxonomy。
- Rule 7 / 迁移残留持续收口：generic `unknown` 只能保留给 truly unexpected failures，不能继续为 malformed-response / invalid-entry 打掩护。
</north_star>
<requirements>
- `QLT-47`: login / reauth / reconfigure 必须区分 auth、connectivity、malformed-response、invalid-entry 等错误语义。
- `TST-42`: focused regressions 必须冻结 flow taxonomy 与 translation vocabulary。
</requirements>
</mapping>

<tasks>

<task type="auto" tdd="true">
  <name>Task 120-02-01: refine hashed-login failure mapping into explicit translated categories</name>
  <files>custom_components/lipro/flow/login.py, custom_components/lipro/translations/en.json, custom_components/lipro/translations/zh-Hans.json, tests/flows/test_config_flow_user.py</files>
  <behavior>
    - auth and connectivity failures keep existing semantics
    - malformed upstream response uses an explicit translated key instead of `unknown`
    - non-auth/non-connectivity API failures and truly unexpected failures are reported through explicit, reviewable keys rather than collapsing into one bucket
  </behavior>
  <action>按 D-03 改造 `flow/login.py`：保留 `invalid_auth` 与 `cannot_connect` contract，同时把 malformed upstream response、generic API failure、unexpected internal failure 映射到明确可翻译的 key（命名可基于现有风格，例如 `invalid_response` / `api_error` / `unexpected_error`，但必须保持语义稳定且与 translations 对齐）。同步更新 `custom_components/lipro/translations/en.json` 与 `custom_components/lipro/translations/zh-Hans.json`，并在 `tests/flows/test_config_flow_user.py` 中补齐/更新 focused regressions。不要改动成功路径、entry-data projection 或 reauth user-mismatch contract。</action>
  <verify>
    <automated>uv run pytest tests/flows/test_config_flow_user.py -q</automated>
  </verify>
  <done>user-step login failures report explicit translated categories for malformed/API/unexpected cases while preserving existing auth/connectivity semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 120-02-02: map persisted-entry validation failures to explicit flow errors</name>
  <files>custom_components/lipro/flow/submission.py, custom_components/lipro/translations/en.json, custom_components/lipro/translations/zh-Hans.json, tests/flows/test_config_flow_reauth.py, tests/flows/test_config_flow_reconfigure.py, tests/flows/test_flow_submission.py</files>
  <behavior>
    - invalid stored phone / phone_id / persisted entry state no longer reports generic `unknown`
    - reauth/reconfigure still surface field-level `invalid_password` / `invalid_phone` errors where appropriate
    - focused submission and config-flow regressions freeze the new persisted-entry error taxonomy
  </behavior>
  <action>继续执行 `QLT-47`：在 `flow/submission.py` 中把 reauth / reconfigure persisted-entry defects（缺失或非法 `phone` / `phone_id` / identity mismatch precondition 之外的 invalid-entry cases）映射到显式 base error，而不是继续写 `unknown`。同步刷新 translations，并在 `tests/flows/test_config_flow_reauth.py`、`tests/flows/test_config_flow_reconfigure.py` 与 `tests/flows/test_flow_submission.py` 中更新期望与新增覆盖。保留现有 field-level `invalid_phone` / `invalid_password` 与 `reauth_user_mismatch` 行为，不要扩张到新的 control-plane story。</action>
  <verify>
    <automated>uv run pytest tests/flows/test_config_flow_reauth.py tests/flows/test_config_flow_reconfigure.py tests/flows/test_flow_submission.py -q</automated>
  </verify>
  <done>reauth/reconfigure persisted-entry failures use explicit translated base errors, and focused regressions prove field-level validation and identity-mismatch semantics remain intact.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/flows/test_config_flow_user.py tests/flows/test_config_flow_reauth.py tests/flows/test_config_flow_reconfigure.py tests/flows/test_flow_submission.py -q`
- translation catalogs contain the same new flow error vocabulary in English and Simplified Chinese
- flow helpers reserve `unknown` only for cases that remain truly unclassified after this taxonomy pass
</verification>

<success_criteria>
- `QLT-47` is satisfied through explicit, translated flow error categories for auth/connectivity/malformed-entry/malformed-response classes
- `TST-42` is satisfied for the flow slice via focused regressions covering user, reauth, reconfigure, and submission helpers
- success flows and existing identity-mismatch behavior remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/120-terminal-audit-contract-hardening-and-governance-truth-slimming/120-02-SUMMARY.md`
</output>

---
phase: 08-ai-debug-evidence-pack
plan: "02"
title: 实现 evidence pack exporter + tests + governance handoff
wave: 2
depends_on:
  - 08-01
files_modified:
  - scripts/export_ai_debug_evidence_pack.py
  - tests/harness/evidence_pack/collector.py
  - tests/integration/test_ai_debug_evidence_pack.py
  - tests/meta/test_evidence_pack_authority.py
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
autonomous: true
requirements:
  - AID-01
  - AID-02
---

<objective>
实现 `AI Debug Evidence Pack` 导出入口与验证闭环：从正式真源拉取 telemetry/replay/boundary/governance 证据，生成稳定 JSON 包与 index，同时确保 authority / redaction / residual handoff 都在治理链路内可见。
</objective>

<tasks>
<task>新增 `scripts/export_ai_debug_evidence_pack.py` 作为唯一导出入口：调用 `tests/harness/evidence_pack/collector.py`，生成 `JSON pack + markdown index`，并把 source/authority trace 一并写入输出。</task>
<task>在 `collector.py` 中组合 `07.3` exporter views、`07.4` replay run summaries、`07.5` evidence index 与 governance pointers；collector 只能做 pull/merge/serialize，不得引入新的事实来源。</task>
<task>新增 `tests/integration/test_ai_debug_evidence_pack.py`：验证 pack 的 sections、source trace、真实时间戳保留、伪匿名引用稳定性与敏感字段阻断。</task>
<task>新增 `tests/meta/test_evidence_pack_authority.py`：验证 evidence pack 是否只使用正式真源、未泄露凭证等价物、未把未登记路径升级为合法 source。</task>
<task>更新 `.planning/baseline/VERIFICATION_MATRIX.md` 与 `.planning/reviews/RESIDUAL_LEDGER.md`：登记 `AID-01 / AID-02` 的 evidence，并把未被自动化覆盖的 evidence-pack residual 显式移交到下一轮 closeout，而不是隐含保留。</task>
</tasks>

## Verification Criteria

- evidence pack exporter 只有一个正式入口；
- 所有 pack sections 都能回溯 formal source / authority；
- 凭证等价物不会出现在 pack 中，真实时间戳与伪匿名引用策略正确；
- `AID-01 / AID-02` 已被 integration/meta evidence 明确覆盖。

## Must Haves

- single exporter entrypoint
- formal-source-only collector
- integration + meta evidence
- governance residual handoff

---
phase: 08-ai-debug-evidence-pack
plan: "01"
title: 定义 evidence pack schema、pseudo-id 与 redaction 策略
wave: 1
depends_on: []
files_modified:
  - tests/harness/evidence_pack/__init__.py
  - tests/harness/evidence_pack/schema.py
  - tests/harness/evidence_pack/sources.py
  - tests/harness/evidence_pack/redaction.py
  - tests/fixtures/evidence_pack/README.md
  - .planning/baseline/AUTHORITY_MATRIX.md
  - .planning/reviews/FILE_MATRIX.md
autonomous: true
requirements:
  - AID-01
  - AID-02
---

<objective>
建立 `AI Debug Evidence Pack` 的正式 schema / source adapters / pseudo-id / redaction 规则：让 pack 只 pull 正式真源，并在不引入新 runtime root 的前提下满足 AI 调试与分析需求。
</objective>

<tasks>
<task>新增 `tests/harness/evidence_pack/` 作为 tool-only formal home，以 `schema.py / sources.py / redaction.py` 定义 pack 结构、source authority 映射与最终脱敏规则。</task>
<task>在 `schema.py` 中定义 pack version、top-level sections、authority trace、required fields 与 JSON-first 输出结构；设计需保持 backend-swappable，不在本 phase 绑定 `msgspec` 或 `pydantic v2`。</task>
<task>在 `sources.py` 中明确只允许 pull 以下正式真源：`07.3 exporter views`、`07.4 replay run summaries`、`07.5 V1_1_EVIDENCE_INDEX.md`、boundary/governance authority docs；禁止从未登记临时路径构造第二套事实。</task>
<task>在 `redaction.py` 中落地伪匿名与脱敏规则：
  - `entry_ref` / `device_ref` 报告内稳定、跨报告不可关联；
  - 允许真实时间戳；
  - token/secret/`password_hash` 等凭证等价物永不出现。</task>
<task>同步 `.planning/baseline/AUTHORITY_MATRIX.md` 与 `.planning/reviews/FILE_MATRIX.md`：登记 evidence pack 的 assurance/tooling 归属与正式 source 关系。</task>
</tasks>

## Verification Criteria

- evidence pack schema 稳定、版本化、JSON-first；
- source adapters 只 pull 正式真源；
- pseudo-id / redaction 规则与 `07.3 / 07.5` 保持一致，不产生第二套政策；
- evidence pack tooling home 已被 authority/file-matrix 正式登记。

## Must Haves

- stable evidence-pack schema
- pull-only source adapters
- pseudo-id and redaction policy
- no new heavy dependency binding

---
phase: 73-service-family-deduplication-diagnostics-helper-convergence-and-runtime-surface-formalization
plan: "01"
type: execute
title: re-cut service-router forwarding families into thinner control-owned callback bundles
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/control/service_router.py
  - custom_components/lipro/control/service_router_handlers.py
  - custom_components/lipro/control/service_router_support.py
  - tests/core/test_init_service_handlers_commands.py
  - tests/core/test_init_service_handlers_device_resolution.py
  - tests/core/test_init_service_handlers_schedules.py
autonomous: true
requirements:
  - ARC-19
  - HOT-33
  - TYP-21
must_haves:
  truths:
    - `custom_components/lipro/control/service_router.py` 继续是唯一 HA service callback outward home，而不是重新长出第二个 router root。
    - send-command、schedule 与 developer callback family 共享显式 control-owned collaborator bundles，不再重复堆叠 forwarding glue。
    - 设备/协调器解析仍保持 `service -> control -> runtime_access` 单向桥接，不回流到 `services/` 或直接 `.runtime_data` 读取。
  artifacts:
    - custom_components/lipro/control/service_router.py
    - custom_components/lipro/control/service_router_handlers.py
    - custom_components/lipro/control/service_router_support.py
    - tests/core/test_init_service_handlers_commands.py
    - tests/core/test_init_service_handlers_device_resolution.py
    - tests/core/test_init_service_handlers_schedules.py
  key_links:
    - service_router.py -> service_router_handlers.py -> services/* leaf handlers
    - service_router_support.py -> runtime_access.find_runtime_device_and_coordinator
    - topical service-handler tests -> callback wiring and outward response stability
---

# Plan 73-01: 重切 service-router forwarding family，让 control callback home 更诚实

<objective>
继续把 `service_router` family 内部重复转发、schedule forwarding glue 与 runtime lookup wiring 压回 control plane 的既有 formal homes，让 `service_router.py` 只承担 outward callback export 与冻结后的 collaborator 组装。

Purpose: 按 `ARC-19` / `HOT-33` / `TYP-21` 继续 inward split `service_router` 热点，避免 service callback story 重新分叉成第二合法 root。
Output: 更薄的 `service_router.py`、更显式的 handler/support contracts、以及锁住 routing 语义的 focused topical tests。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-service-family-deduplication-diagnostics-helper-convergence-and-runtime-surface-formalization/73-CONTEXT.md
@.planning/phases/73-service-family-deduplication-diagnostics-helper-convergence-and-runtime-surface-formalization/73-RESEARCH.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@custom_components/lipro/control/service_router.py
@custom_components/lipro/control/service_router_handlers.py
@custom_components/lipro/control/service_router_support.py
@tests/core/test_init_service_handlers_commands.py
@tests/core/test_init_service_handlers_device_resolution.py
@tests/core/test_init_service_handlers_schedules.py
@.planning/phases/43-control-services-boundary-decoupling-and-typed-runtime-access/43-03-SUMMARY.md

<interfaces>
从 `custom_components/lipro/control/service_router.py` 保持不漂移的 outward callback shape：

```python
async def async_handle_send_command(
    hass: HomeAssistant,
    call: ServiceCall,
) -> SendCommandResult: ...

async def async_handle_get_schedules(
    hass: HomeAssistant,
    call: ServiceCall,
) -> dict[str, object]: ...
```

从 `custom_components/lipro/control/service_router_support.py` 继续复用的 control -> runtime bridge：

```python
def build_device_and_coordinator_getter(
    *,
    domain: str,
    serial_pattern: re.Pattern[str],
    attr_device_id: str,
) -> BoundDeviceAndCoordinatorGetter: ...
```

从 `custom_components/lipro/control/service_router_handlers.py` 当前可收口的重复 schedule forwarding seam：

```python
async def _async_handle_schedule_request(
    service_handler: ScheduleHandler,
    hass: HomeAssistant,
    call: ServiceCall,
    *,
    get_device_and_coordinator: RuntimeDeviceAndCoordinatorGetter,
    logger: logging.Logger,
    **kwargs: object,
) -> dict[str, object]: ...
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: 将 router forwarding glue 收口成显式 control-owned bundles，而不是继续散落 partial/kwargs 注入</name>
  <read_first>
    - custom_components/lipro/control/service_router.py
    - custom_components/lipro/control/service_router_handlers.py
    - custom_components/lipro/control/service_router_support.py
    - .planning/phases/43-control-services-boundary-decoupling-and-typed-runtime-access/43-03-SUMMARY.md
  </read_first>
  <files>custom_components/lipro/control/service_router.py, custom_components/lipro/control/service_router_handlers.py, custom_components/lipro/control/service_router_support.py</files>
  <action>按 `ARC-19/HOT-33/TYP-21` 继续把 `service_router.py` 压成薄 outward shell：将 send/schedule/developer family 共享的 logger、resolver、developer-mode iterator 与 service-constant glue 收口为命名清晰的 control-owned bundle 或等价显式 contract，放在既有 `service_router_handlers.py` / `service_router_support.py` 家族中消费。保留 `control.service_router` 作为唯一 HA callback export home，不把主实现放回 `services/registrations.py`、`services/*` 或新建 router root；`service_router_support.py` 仍是唯一 device/coordinator 解析桥，禁止在 touched router files 中新增 direct `.runtime_data`、`iter_runtime_entries()` 或其他 runtime internals 读取。</action>
  <acceptance_criteria>
    - `service_router.py` 只剩 outward callback export 与冻结后的 bundle 装配。
    - schedule/send/developer handler 不再各自复制 resolver/logger/service-constant 注入逻辑。
    - runtime lookup 继续只经 `service_router_support.py` -> `runtime_access` 的正式桥接链路完成。
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_init_service_handlers_commands.py tests/core/test_init_service_handlers_device_resolution.py tests/core/test_init_service_handlers_schedules.py</automated>
  </verify>
  <done>`service_router` forwarding family 被压回 control-owned bundles，outward callback shape 保持不变，且无新 router root / helper-owned public surface 产生。</done>
</task>

<task type="auto">
  <name>Task 2: 用 focused topical tests 锁住一跳式 routing 语义与 thin callback home</name>
  <read_first>
    - tests/core/test_init_service_handlers_commands.py
    - tests/core/test_init_service_handlers_device_resolution.py
    - tests/core/test_init_service_handlers_schedules.py
    - custom_components/lipro/control/service_router.py
  </read_first>
  <files>tests/core/test_init_service_handlers_commands.py, tests/core/test_init_service_handlers_device_resolution.py, tests/core/test_init_service_handlers_schedules.py</files>
  <action>补或重排 topical suites，直接证明 send-command、device resolution 与 schedule handlers 仍由 `control.service_router` outward shell 暴露，且通过 control-owned getter 解析目标设备/协调器；若需要新增断言，优先落在现有 topical suites，而不是回灌 `tests/core/test_init_service_handlers.py` 这类聚合文件。断言应聚焦 routing/locality 和 outward response/error shape，不写 phase prose 耦合断言。</action>
  <acceptance_criteria>
    - touched tests 会在 callback 重新长厚、resolver 回流到 `services/`、或 schedule handler 改写 outward response shape 时失败。
    - 没有把本轮断言重新塞回 mega-test 聚合文件。
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/test_init_service_handlers_commands.py tests/core/test_init_service_handlers_device_resolution.py tests/core/test_init_service_handlers_schedules.py tests/services/test_services_registry.py</automated>
  </verify>
  <done>service-router 一跳式 routing 语义已有 focused regression proof，可独立证明 control-plane callback home 没有 regrow。</done>
</task>
</tasks>

<risks>
- 不要把 `service_router_handlers.py` 或 `service_router_support.py` 重新讲成第二正式 router root；它们只能是 `control.service_router` 的 inward collaborators。
- 不要为了消除重复而把 runtime lookup 下放回 `services/`，这会破坏 `43-03` 已建立的一跳式 control bridge。
</risks>

<non_goals>
- 不在本 plan 内建立新的 schedule runtime service（属于 `73-03`）。
- 不处理 diagnostics/runtime_access helper convergence 或平台/实体 runtime strategy（属于 `73-02` / `73-03`）。
</non_goals>

<verification>
- [ ] `uv run ruff check custom_components/lipro/control/service_router.py custom_components/lipro/control/service_router_handlers.py custom_components/lipro/control/service_router_support.py tests/core/test_init_service_handlers_commands.py tests/core/test_init_service_handlers_device_resolution.py tests/core/test_init_service_handlers_schedules.py`
- [ ] `uv run mypy --follow-imports=silent custom_components/lipro/control/service_router.py custom_components/lipro/control/service_router_handlers.py custom_components/lipro/control/service_router_support.py`
- [ ] `uv run pytest -q tests/core/test_init_service_handlers_commands.py tests/core/test_init_service_handlers_device_resolution.py tests/core/test_init_service_handlers_schedules.py tests/services/test_services_registry.py`
</verification>

<success_criteria>
- `service_router.py` 的职责退回 outward callback export，不再堆叠重复 forwarding glue。
- `service_router_support.py` 仍是唯一 device/coordinator runtime bridge，routing 不回流到 `services/`。
- send/schedule/device-resolution topical suites 可单独证明 callback wiring 与 outward behavior 稳定。
</success_criteria>

<output>
After completion, create `.planning/phases/73-service-family-deduplication-diagnostics-helper-convergence-and-runtime-surface-formalization/73-01-SUMMARY.md`
</output>

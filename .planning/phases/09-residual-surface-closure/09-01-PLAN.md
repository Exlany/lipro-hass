---
phase: 09-residual-surface-closure
plan: "01"
title: 收窄 protocol root surface 与 compat exports
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/protocol/facade.py
  - custom_components/lipro/core/protocol/__init__.py
  - custom_components/lipro/core/protocol/compat.py
  - custom_components/lipro/__init__.py
  - custom_components/lipro/config_flow.py
  - custom_components/lipro/core/api/__init__.py
  - custom_components/lipro/core/__init__.py
  - custom_components/lipro/core/mqtt/__init__.py
  - tests/integration/test_mqtt_coordinator_integration.py
  - tests/meta/test_public_surface_guards.py
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - RSC-01
  - RSC-02
---

<objective>
把 `LiproProtocolFacade` 的正式 contract 收窄为显式 public surface，并把 `raw_client` / `LiproClient` / `LiproMqttClient` 等历史入口压缩到显式 compat seam，避免 child surface 与根模块导出继续反向定义 protocol root。
</objective>

<tasks>
<task>梳理 `custom_components/lipro/core/protocol/facade.py` 中当前由 `__getattr__` / `__dir__` 提供的 implicit root surface，改为显式 root contract：正式消费者通过 `.rest` / `.mqtt` 访问 child façade；如仍需保留少量 legacy entrypoint，迁入显式 compat home，并在代码中清楚区分 formal surface 与 compat seam。</task>
<task>收窄 `LiproMqttFacade.raw_client` 的责任边界：若能删除则删除；若暂不能删除，降级为仅限 compat/test seam 的显式入口，并同步更新对应 integration tests，不再让其处于 formal public-surface 叙事中。</task>
<task>同步清理/降级根模块与包级 compat exports：`custom_components/lipro/__init__.py`、`config_flow.py`、`core/api/__init__.py`、`core/__init__.py`、`core/mqtt/__init__.py` 不再把 legacy names 作为正式 public surface 扩散；必要残留必须转为显式 compat alias 并登记 delete gate。</task>
<task>回写 `.planning/baseline/PUBLIC_SURFACES.md`、`RESIDUAL_LEDGER.md` 与 `KILL_LIST.md`，并补 `tests/meta/test_public_surface_guards.py` / `tests/meta/test_governance_guards.py`，确保 protocol root/compat seam 的边界被自动化守住。</task>
</tasks>

## Verification Criteria

- `LiproProtocolFacade` formal surface 显式可见，child members 不再隐式扩展 root contract；
- `raw_client` 与 legacy public names 不再被 formal public-surface guards 认定为正式出口；
- runtime-facing / integration tests 仍可通过显式 child façade 或受控 compat seam 工作；
- governance truth 与代码事实一致。

## Must Haves

- explicit protocol root contract
- compat seams are explicit and counted
- no child-defined formal root semantics
- public-surface and governance guards updated

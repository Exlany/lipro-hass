---
phase: 09-residual-surface-closure
plan: "05"
title: 收敛 runtime/platform/integration 旧测试到正式 surfaces
wave: 4
depends_on:
  - 09-04
files_modified:
  - tests/conftest.py
  - tests/platforms/test_sensor.py
  - tests/platforms/test_entity_behavior.py
  - tests/platforms/test_platform_entities_behavior.py
  - tests/core/test_diagnostics.py
  - tests/test_coordinator_public.py
  - tests/integration/test_mqtt_coordinator_integration.py
  - tests/core/mqtt/test_mqtt.py
  - tests/meta/test_public_surface_guards.py
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - RSC-02
  - RSC-03
  - RSC-04
---

<objective>
把 runtime/platform/integration 的旧测试彻底收敛到当前正式 surface：统一使用 `tests/conftest.py` 中的共享 doubles/factories、read-only `coordinator.devices`、`device.outlet_power_info` 与显式 compat seam，压缩散落的 `raw_client` 直驱、`extra_data["power_info"]` 旧断言和 ad-hoc MagicMock wiring。
</objective>

<tasks>
<task>把 `tests/conftest.py` 明确成 runtime/platform 测试的唯一优先支撑层：扩展并复用 `_CoordinatorDouble`、`make_device`、shared MQTT doubles 与 power payload builders，替换 `tests/platforms/test_sensor.py`、`tests/platforms/test_entity_behavior.py`、`tests/platforms/test_platform_entities_behavior.py`、`tests/core/test_diagnostics.py`、`tests/test_coordinator_public.py` 中重复的本地夹具与 wiring。</task>
<task>重构 `tests/integration/test_mqtt_coordinator_integration.py` 与 `tests/core/mqtt/test_mqtt.py`：把 `raw_client` 直接驱动限制到少量显式 compatibility smoke/helper，其余断言统一围绕 `LiproMqttFacade`、runtime callback、coordinator state update、background-task lifecycle 与 formal side effects 展开。</task>
<task>清除平台/runtime 测试中的旧 truth path：`outlet_power_info` 成为唯一 primary power assertion path；`extra_data["power_info"]` 最多只保留单点兼容桥 smoke；所有公开断言都站在只读 `coordinator.devices` 上，而不是 `_state.devices` 的 live mutable 细节。</task>
<task>必要时同步更新 `tests/meta/test_public_surface_guards.py` 与 `tests/meta/test_governance_guards.py`，确保测试重构后仍能阻断三类回退：`raw_client` 从 explicit seam 扩散为通用测试入口、`extra_data["power_info"]` 回流为正式真源、live mutable registry 重新成为 public contract。</task>
</tasks>

## Verification Criteria

- runtime/platform/integration 测试默认围绕正式 surface 与共享 doubles，而不是本地 ad-hoc wiring；
- `raw_client` 与 `extra_data["power_info"]` 只剩显式、窄化的 compat smoke；
- `outlet_power_info`、只读 `devices` view、shared coordinator/mqtt doubles 成为统一测试真源；
- targeted runtime/platform/integration/meta regressions 继续通过，且重复 fixture/wiring 明显减少。

## Must Haves

- tests/conftest.py is the primary runtime/platform harness home
- outlet_power_info is the only primary power assertion path
- raw_client kept as explicit narrow seam only
- no mutable-registry or legacy-power fallback spread across test files

---
phase: 09-residual-surface-closure
plan: "03"
title: 回写 governance residual ledger、delete gate 与 regression guards
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/baseline/AUTHORITY_MATRIX.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - docs/COMPREHENSIVE_AUDIT_2026-03-13.md
  - tests/meta/test_public_surface_guards.py
  - tests/meta/test_governance_guards.py
  - tests/meta/test_dependency_guards.py
autonomous: true
requirements:
  - RSC-01
  - RSC-02
  - RSC-03
  - RSC-04
---

<objective>
把 `09-01` / `09-02` 的代码收口结果同步登记到 governance 真源与 meta guards，使 residual seam、delete gate、formal surfaces 与验证证据全部可仲裁，且不会在后续 phase 中回流。
</objective>

<tasks>
<task>基于 `09-01` / `09-02` 的最终代码事实，回写 `PUBLIC_SURFACES.md`、`AUTHORITY_MATRIX.md`、`VERIFICATION_MATRIX.md`、`FILE_MATRIX.md`：明确谁是 formal root、谁只剩 compat seam、outlet power truth home 在哪里、哪些 requirement 由哪些证据覆盖。</task>
<task>更新 `RESIDUAL_LEDGER.md` 与 `KILL_LIST.md`：关闭已收口 residual，重新界定仍需保留的 compat seam，并为剩余 delete targets 写明 owner、gate、退出条件，避免“逻辑上已完成，治理上仍含混”。</task>
<task>加固 `tests/meta/test_public_surface_guards.py`、`tests/meta/test_governance_guards.py`、`tests/meta/test_dependency_guards.py`，确保以下偏航会被自动阻断：protocol root 再次由 child members 隐式扩面、legacy exports 回流为正式出口、runtime live mutable public surface 回流、`extra_data` 再次承担 outlet power 正式路径。</task>
<task>回写 `docs/COMPREHENSIVE_AUDIT_2026-03-13.md` 或 phase execution closeout 所需 evidence pointers，使审查报告与 governance closeout 对 `Phase 9` 的 verdict 一致，并为后续 `$gsd-verify-work 9` 提供稳定证据入口。</task>
</tasks>

## Verification Criteria

- governance truth sources 与代码事实同步；
- 剩余 compat seams 都有显式 delete gate；
- meta guards 能阻断 `09` 目标范围内的回流；
- 审查报告与 phase closeout 证据不互相打架。

## Must Haves

- governance matrices aligned with code
- residual seams explicitly owned and gated
- regression guards block surface re-expansion
- evidence pointers ready for verify-work

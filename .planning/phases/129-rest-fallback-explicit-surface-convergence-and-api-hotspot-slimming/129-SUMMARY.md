# Phase 129 Summary

status: complete

## Delivered

- 完成 `rest_facade.py` 的 explicit-surface convergence：generic helper-magic 已退出正式 surface，request / endpoint wrapper topology 保持稳定。
- historical `Phase 113` hotspot budget guard 已随 explicit-surface truth 重标：`rest_facade.py` 的 no-regrowth ceiling 现在冻结在当前显式 carrier，而不是沿用 helper-magic 时代的旧行数上限。
- 完成 `status_fallback_support.py` 的 fallback seam tightening：primary query、fallback dispatch 与深度记录分段更清晰，public route 仍只承认 `status_fallback.py`。
- 同步补齐 focused regressions、Phase 129 execution assets、queued phase placeholders，以及 `PROJECT / ROADMAP / REQUIREMENTS / STATE / MILESTONES / GOVERNANCE_REGISTRY / VERIFICATION_MATRIX / TESTING / review ledgers` 的 route truth。

## Governance Notes

- `.planning/reviews/FILE_MATRIX.md` 已把 `rest_facade.py` / `status_fallback_support.py` 的热点 owner 前推到 `Phase 129`。
- `.planning/reviews/RESIDUAL_LEDGER.md` 与 `.planning/reviews/KILL_LIST.md` 未新增 active residual / active kill target；Phase 129 只是把 sanctioned hotspot 继续 inward slimming，并把下一跳诚实前推到 `Phase 130` / `Phase 131`。
- 当前 active route 已前推到 `active / phase 129 complete; phase 130 planning-ready (2026-04-01)`；按 `$gsd-next` 语义，下一步是 `$gsd-plan-phase 130`。

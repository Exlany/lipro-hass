---
plan_id: 59-02
phase: 59
title: split device-refresh mega suite by parsing, filter semantics, and runtime behavior
status: planned
created: 2026-03-22
files:
  - tests/core/test_device_refresh.py
key_links:
  - parsing truth -> filter semantics -> runtime refresh behavior -> focused device-refresh suites
---

# Plan 59-02: split device-refresh mega suite by parsing, filter semantics, and runtime behavior

<context>
@.planning/phases/59-verification-localization-and-governance-guard-topicization/59-CONTEXT.md
@.planning/phases/59-verification-localization-and-governance-guard-topicization/59-VALIDATION.md
@tests/core/test_device_refresh.py
@.planning/phases/58-repository-audit-refresh-and-next-wave-routing/58-REMEDIATION-ROADMAP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: separate device-refresh test concerns into narrower topical suites</name>
  <files>tests/core/test_device_refresh.py</files>
  <action>把 `test_device_refresh.py` 里的 config parsing / normalization、device-filter semantics、runtime refresh behavior 三类 concern 拆成更窄的 suites 或 modules。保持 fixture 与 behavior 不漂移，避免为 split 引入新的 broad helper folklore。</action>
  <acceptance_criteria>
    - parsing/filter/runtime concerns are visibly separated
    - existing behavior remains intact after the split
    - suite names and file boundaries reflect real concerns instead of arbitrary chunking
  </acceptance_criteria>
  <verify>`uv run pytest -q tests/core/test_device_refresh.py`</verify>
  <done>Device-refresh verification can be run and debugged by concern family rather than as one mega bucket.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/test_device_refresh.py`
</verification>

---
phase: 17-final-residual-retirement-typed-contract-tightening-and-milestone-closeout
plan: "03"
title: mqtt transport naming demotion and locality guard hardening
wave: 2
depends_on:
  - "01"
files_modified:
  - custom_components/lipro/core/mqtt/mqtt_client.py
  - custom_components/lipro/core/mqtt/client_runtime.py
  - custom_components/lipro/core/protocol/facade.py
  - custom_components/lipro/core/protocol/contracts.py
  - tests/core/coordinator/runtime/test_mqtt_runtime.py
  - tests/integration/test_mqtt_coordinator_integration.py
  - tests/core/mqtt/test_client_refactored.py
  - tests/core/mqtt/test_mqtt.py
  - tests/meta/test_dependency_guards.py
  - tests/meta/test_public_surface_guards.py
requirements:
  - MQT-01
---

<objective>
继续 demote `LiproMqttClient` legacy transport naming：生产代码与非 transport tests 优先切到 canonical transport naming / `MqttTransportFacade` contract，并补一条 focused pytest guard 锁定 `ENF-IMP-MQTT-TRANSPORT-LOCALITY`，防止 concrete transport 再向外扩散。
</objective>

<tasks>
<task>在 `custom_components/lipro/core/mqtt/mqtt_client.py` 中引入 canonical transport naming（可保留 `LiproMqttClient` 作为 local compatibility alias），让 production code 优先依赖 canonical name，而不是继续把 legacy name 当成正式 transport identity。</task>
<task>同步 `custom_components/lipro/core/protocol/facade.py` 与 `custom_components/lipro/core/protocol/contracts.py`，使 `LiproMqttFacade` 更明确依赖 `MqttTransportFacade` / canonical transport type，而不是扩大对 legacy concrete name 的语义依赖。</task>
<task>把 `tests/core/coordinator/runtime/test_mqtt_runtime.py` 与 `tests/integration/test_mqtt_coordinator_integration.py` 这类非 transport-focused tests 迁到 protocol contract / fake transport story，避免不必要地固化 legacy concrete name。</task>
<task>保留 transport-local tests 对 concrete client 的 focused coverage，但让它们明确是在验证 transport 实现，而非为 legacy naming 提供跨层合法性。</task>
<task>扩展 `tests/meta/test_dependency_guards.py` 与 `tests/meta/test_public_surface_guards.py`，给 `ENF-IMP-MQTT-TRANSPORT-LOCALITY` 增加 focused pytest 守卫。</task>
</tasks>

## Verification Commands

- `uv run pytest -q tests/core/mqtt tests/core/coordinator/runtime/test_mqtt_runtime.py tests/integration/test_mqtt_coordinator_integration.py tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py`
- `uv run mypy custom_components/lipro/core/mqtt custom_components/lipro/core/protocol`
- `uv run ruff check custom_components/lipro/core/mqtt custom_components/lipro/core/protocol tests/core/mqtt tests/core/coordinator/runtime tests/integration tests/meta`

## Verification Criteria

- production code 的 transport-facing path 已优先依赖 canonical naming / protocol contract，而不是继续扩散 `LiproMqttClient` legacy identity；
- 非 transport-focused tests 不再把 legacy concrete name 当成跨层正式 contract；
- transport-local tests 仍覆盖具体实现，不降低 MQTT transport 行为验证；
- focused meta guard 可以 fail-fast 阻断 future direct-production-consumer drift。

## Must-Haves

- `LiproProtocolFacade` 仍是唯一正式 protocol root
- `LiproMqttFacade` 仍是 protocol child façade，不得被 concrete transport 反客为主
- 若保留 legacy alias，必须明确其仅为 local compatibility，而非正式 naming truth
- focused meta guard 要锁 locality，而不是禁止 transport-local implementation 自身存在

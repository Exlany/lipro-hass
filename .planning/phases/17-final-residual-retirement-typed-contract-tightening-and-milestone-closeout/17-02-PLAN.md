---
phase: 17-final-residual-retirement-typed-contract-tightening-and-milestone-closeout
plan: "02"
title: auth session and outlet-power contract convergence
wave: 2
depends_on:
  - "01"
files_modified:
  - custom_components/lipro/entry_auth.py
  - custom_components/lipro/core/auth/manager.py
  - custom_components/lipro/core/api/power_service.py
  - custom_components/lipro/core/api/endpoints/misc.py
  - custom_components/lipro/core/protocol/facade.py
  - custom_components/lipro/core/protocol/contracts.py
  - custom_components/lipro/core/coordinator/runtime/outlet_power_runtime.py
  - tests/core/test_init.py
  - tests/core/test_auth.py
  - tests/core/api/test_api.py
  - tests/core/api/test_helper_modules.py
  - tests/core/test_outlet_power_runtime.py
requirements:
  - TYP-05
---

<objective>
删除 `persist_entry_tokens_if_changed()` 中的 `get_auth_data()` fallback，把 token persistence 完全收口到 `AuthSessionSnapshot`；同时把 `power_service.py` 的 helper-level compatibility envelope 改成显式 typed outlet-power contract，避免 synthetic `{"data": rows}` 再向上漂移。
</objective>

<tasks>
<task>把 `custom_components/lipro/entry_auth.py` 的 token persistence 逻辑统一到 `auth_manager.get_auth_session()`，删除 `get_auth_data()` fallback 分支，并同步相关 tests / mocks 到 `AuthSessionSnapshot` 正式契约。</task>
<task>梳理 `custom_components/lipro/core/auth/manager.py` 中 `get_auth_session()` / `get_auth_data()` 的 formal vs compat 角色，确保删除 fallback 后不会重新打开旧 dict contract 合法故事线。</task>
<task>在 `custom_components/lipro/core/api/power_service.py`、`custom_components/lipro/core/protocol/contracts.py` 与 `custom_components/lipro/core/protocol/facade.py` 中引入显式 typed outlet-power result contract，去掉 synthetic `{"data": rows}` 包装。</task>
<task>同步 `custom_components/lipro/core/coordinator/runtime/outlet_power_runtime.py` 的归一化逻辑与 tests，让 runtime 消费的是明确 contract，而不是 helper-level compat wrapper。</task>
<task>补齐 focused tests，覆盖 token persistence、single-row / multi-row power payload、invalid-param skip 与 runtime normalization 行为。</task>
</tasks>

## Verification Commands

- `uv run pytest -q tests/core/test_init.py tests/core/test_auth.py tests/core/api/test_api.py tests/core/api/test_helper_modules.py tests/core/test_outlet_power_runtime.py`
- `uv run mypy custom_components/lipro/entry_auth.py custom_components/lipro/core/auth/manager.py custom_components/lipro/core/api/power_service.py custom_components/lipro/core/protocol custom_components/lipro/core/coordinator/runtime/outlet_power_runtime.py`
- `uv run ruff check custom_components/lipro/entry_auth.py custom_components/lipro/core/auth custom_components/lipro/core/api custom_components/lipro/core/protocol custom_components/lipro/core/coordinator/runtime/outlet_power_runtime.py tests/core`

## Verification Criteria

- token persistence 只依赖 `AuthSessionSnapshot` 正式契约，仓内不再需要 `get_auth_data()` fallback 才能完成 entry token 更新；
- `power_service.py` 输出的 power payload contract 被显式化，helper-level synthetic wrapper 已从 formal path 退出；
- runtime outlet-power 更新逻辑在 single-row / multi-row / invalid payload 情况下仍行为稳定；
- auth / power tests 通过后，不会重新引入旧 dict auth contract 或 helper-level payload compatibility drift。

## Must-Haves

- `AuthSessionSnapshot` 继续是唯一正式 auth/session truth
- outlet-power canonical contract 必须在 protocol boundary 明确，而不是让 runtime 再承担 helper compatibility
- 不得为了兼容旧 tests 恢复 `get_auth_data()` 主路径
- 不得让 raw vendor payload shape 绕过 formal normalization 回流到 runtime

---
phase: 17-final-residual-retirement-typed-contract-tightening-and-milestone-closeout
plan: "04"
title: phase 17 governance closeout and final repo audit
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/baseline/PUBLIC_SURFACES.md
  - .planning/baseline/ARCHITECTURE_POLICY.md
  - .planning/baseline/VERIFICATION_MATRIX.md
  - .planning/reviews/FILE_MATRIX.md
  - .planning/reviews/RESIDUAL_LEDGER.md
  - .planning/reviews/KILL_LIST.md
  - docs/developer_architecture.md
  - tests/meta/test_governance_guards.py
requirements:
  - GOV-15
---

<objective>
把 Phase 17 的 planning / execution / closeout 真相完整回写到 `ROADMAP`、`REQUIREMENTS`、`STATE`、baseline、reviews、milestone audit 与 architecture docs，并以 final repo audit 锁定本次 residual closeout 的完成态，不留下 silent defer。
</objective>

<tasks>
<task>更新 `.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md` 与 `.planning/STATE.md`：写明 Phase 17 goal / requirements / plans / current mode / roadmap evolution / traceability。</task>
<task>同步 `.planning/baseline/PUBLIC_SURFACES.md`、`.planning/baseline/ARCHITECTURE_POLICY.md`、`.planning/baseline/VERIFICATION_MATRIX.md`、`.planning/reviews/FILE_MATRIX.md`、`.planning/reviews/RESIDUAL_LEDGER.md` 与 `.planning/reviews/KILL_LIST.md`，让 `_ClientTransportMixin` 退场、`_ClientBase` demotion、auth fallback 删除、power contract 收口、MQTT naming/locality 变化拥有一致 ledger truth。</task>
<task>更新 `docs/developer_architecture.md` 与 `.planning/v1.1-MILESTONE-AUDIT.md`，把本次 physical cleanup 和 final audit 结果并入长期治理真源。</task>
<task>执行 final repo audit：`ruff`、`mypy`、`pytest -q` 与 residual recount（`Any` / `except Exception` / `type: ignore` / targeted residual scans），并把 before/after 结果写入 Phase 17 `VERIFICATION.md` / `VALIDATION.md`。</task>
<task>扩展 `tests/meta/test_governance_guards.py`，确保 Phase 17 closeout wording、residual owner/delete-gate/evidence 与 milestone audit status 不再漂移。</task>
</tasks>

## Verification Commands

- `uv run python scripts/check_architecture_policy.py --check`
- `uv run python scripts/check_file_matrix.py --check`
- `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_dependency_guards.py tests/meta/test_public_surface_guards.py tests/meta/test_version_sync.py`
- `uv run ruff check .`
- `uv run mypy`
- `uv run pytest -q`

## Verification Criteria

- `ROADMAP` / `REQUIREMENTS` / `STATE` / baseline / reviews / milestone audit / developer architecture 对 Phase 17 讲同一条故事；
- 每个 remaining residual 都有 disposition、owner、delete gate 与 evidence，不会留下新的 silent defer；
- 全仓 runnable proof 通过，且 final recount 反映本次 cleanup 的真实收益；
- Phase 17 形成 archive-ready closeout story，而不是只写本地执行痕迹。

## Must-Haves

- 治理文档必须服务于单一北极星主链
- 不得把 phase-local planning 资产错误提升为 authority chain
- 不得用“文档补丁”掩盖代码未完成的 residual
- 最终 audit 必须给出真实 counts / evidence，而不是估计值

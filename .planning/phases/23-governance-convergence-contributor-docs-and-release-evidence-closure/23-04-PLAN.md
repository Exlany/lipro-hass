---
phase: 23-governance-convergence-contributor-docs-and-release-evidence-closure
plan: "04"
type: execute
title: fix user-facing contract gaps and remove low-risk boundary hazards
wave: 4
depends_on: ["23-03"]
files_modified:
  - custom_components/lipro/fan.py
  - custom_components/lipro/control/runtime_access.py
  - install.sh
  - README.md
  - README_zh.md
  - docs/TROUBLESHOOTING.md
  - tests/platforms/test_fan.py
  - tests/core/test_system_health.py
  - tests/core/test_diagnostics.py
  - tests/meta/test_governance_guards.py
autonomous: true
requirements:
  - GOV-16
  - GOV-17
must_haves:
  truths:
    - 用户可见的 preset/options 契约必须与实体实际返回值一致
    - control-plane runtime access 不得再通过修改对象 `__dict__` 伪造 entry 视图
    - installer 与 README 的默认安装主路径必须继续是 `ARCHIVE_TAG=latest`
    - 默认 latest 与可复现 pinned tag 必须在文档中被明确区分，而不是互相替代
  artifacts:
    - custom_components/lipro/fan.py
    - custom_components/lipro/control/runtime_access.py
    - README.md
    - README_zh.md
    - docs/TROUBLESHOOTING.md
    - tests/platforms/test_fan.py
  key_links:
    - user-facing contract -> platform tests -> troubleshooting/install docs
    - runtime access safety -> diagnostics/system health/control snapshots
---

# Plan 23-04: 修复用户可见契约缺口与低风险边界异常

<preconditions>
- `23-01..03` 已作为历史完成记录存在，不在本 plan 中重写。
- 本 plan 只收口真实功能/契约缺口与低风险 boundary hazard，不进入大规模架构重构。
</preconditions>

<context>
@.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-CONTEXT.md
@.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-RESEARCH.md
@.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-VALIDATION.md
@custom_components/lipro/fan.py
@custom_components/lipro/control/runtime_access.py
@README.md
@README_zh.md
@docs/TROUBLESHOOTING.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: repair fan preset/options contract mismatches</name>
  <files>custom_components/lipro/fan.py, tests/platforms/test_fan.py</files>
  <action>修复 `gentle_wind` 与 ventilation `off` 的 preset/options 契约不一致问题；确保 `_attr_preset_modes`、`preset_mode`、incoming preset mapping 与 HA 实体可见行为讲同一条故事，并补充最小充分回归测试。</action>
  <verify>`uv run pytest tests/platforms/test_fan.py -q`</verify>
  <done>风扇与换气扇的 preset 选项集合、默认值、返回值和设置路径完全一致，不再出现“当前值不在 options 中”的 UI 契约裂缝。</done>
</task>

<task type="auto">
  <name>Task 2: remove side-effectful runtime entry coercion</name>
  <files>custom_components/lipro/control/runtime_access.py, tests/core/test_system_health.py, tests/core/test_diagnostics.py</files>
  <action>消除 `_as_runtime_entry()` 通过修改对象 `__dict__` 注入 `entry_id/options` 的做法，改为显式安全投影或更窄的协议；同时收紧 runtime snapshot 构建在 telemetry 缺失时的 fallback 行为，避免新的 control-plane bypass。</action>
  <verify>`uv run pytest tests/core/test_system_health.py tests/core/test_diagnostics.py -q`</verify>
  <done>control/runtime access 不再依赖对象污染或隐式副作用；snapshot 构建只消费显式 contract 或已登记 fallback。</done>
</task>

<task type="auto">
  <name>Task 3: lock installer/docs default path to latest while keeping reproducible alternatives</name>
  <files>install.sh, README.md, README_zh.md, docs/TROUBLESHOOTING.md, tests/meta/test_governance_guards.py</files>
  <action>明确并守住“默认安装主路径 = `ARCHIVE_TAG=latest`”的用户体验裁决；保留 pinned tag / `main` / mirror 作为高级场景，并在文档中解释各自适用性与风险，不得把默认入口改成固定 tag。</action>
  <verify>`uv run pytest tests/meta/test_governance_guards.py -q`</verify>
  <done>installer、README、troubleshooting 对默认 latest 与高级安装分支的叙事一致，且不会因治理优化反转默认用户路径。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest tests/platforms/test_fan.py -q`
- [ ] `uv run pytest tests/core/test_system_health.py tests/core/test_diagnostics.py -q`
- [ ] `uv run pytest tests/meta/test_governance_guards.py -q`
- [ ] `uv run ruff check .`
</verification>

<success_criteria>
- 用户可见契约缺口被关闭，且有定向回归保护。
- runtime access 不再依赖对象污染式 coercion。
- 默认安装主路径继续是 `ARCHIVE_TAG=latest`，高级路径清晰但不篡位。
</success_criteria>

<output>
After completion, create `.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-04-SUMMARY.md`
</output>

---
phase: 23-governance-convergence-contributor-docs-and-release-evidence-closure
plan: "03"
type: execute
title: close release evidence index and workflow gate alignment
wave: 3
depends_on: ["23-01", "23-02"]
files_modified:
  - .planning/reviews/V1_2_EVIDENCE_INDEX.md
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - docs/MAINTAINER_RELEASE_RUNBOOK.md
  - tests/meta/test_governance_guards.py
  - tests/meta/test_version_sync.py
autonomous: true
requirements:
  - GOV-17
must_haves:
  truths:
    - v1.2 必须有单一 release evidence pointer/index
    - release workflow 必须继续复用 CI；任何“无需修改 workflow”的判断也必须被 runbook / guards / evidence index 明确记录
    - 本 plan 只做到 release-ready，不做 Phase 24 的 milestone audit / archive / handoff
  artifacts:
    - .planning/reviews/V1_2_EVIDENCE_INDEX.md
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - docs/MAINTAINER_RELEASE_RUNBOOK.md
    - tests/meta/test_governance_guards.py
  key_links:
    - phase 21/22 closeout evidence -> v1.2 evidence index -> ci/release gate -> maintainer runbook
---

# Plan 23-03: 完成 release evidence index 与 workflow gate 对齐

<preconditions>
- `23-01` 已同步治理真源，`23-02` 已同步 public entry docs。
- 本 plan 不执行 final milestone audit，也不写 v1.3 handoff。
</preconditions>

<context>
@.github/workflows/ci.yml
@.github/workflows/release.yml
@docs/MAINTAINER_RELEASE_RUNBOOK.md
@.planning/reviews/V1_1_EVIDENCE_INDEX.md
@.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-RESEARCH.md
@.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: create or refresh a v1.2 release evidence index</name>
  <files>.planning/reviews/V1_2_EVIDENCE_INDEX.md, docs/MAINTAINER_RELEASE_RUNBOOK.md</files>
  <action>建立 `v1.2` 单一 release evidence pointer doc：明确列出 phase 21-22 的关键 evidence、必须通过的 verify commands、权威真源与 maintainer release flow 的引用方式；runbook 应直接引用它，而不是让维护者手动拼接 phase outputs。</action>
  <verify>`uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`</verify>
  <done>`v1.2` release-ready evidence 具备单一入口，后续 Phase 24 可直接消费。</done>
</task>

<task type="auto">
  <name>Task 2: align workflow gates and tests to the same release narrative</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, tests/meta/test_governance_guards.py, tests/meta/test_version_sync.py</files>
  <action>确认 CI/release workflow 与 governance guards 讲同一条 release gate 故事：若 workflow 本身已正确，也要让 tests/runbook/evidence index 明确记录“为何正确且无需再改”；若需改动，必须保持 CI reuse、tag/version guard 与 authority chain 不变。</action>
  <verify>`uv run python scripts/check_file_matrix.py --check && uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py tests/meta/test_public_surface_guards.py tests/meta/test_dependency_guards.py`</verify>
  <done>release workflow 不再存在“实现是一套、runbook是一套、tests 又是一套”的多故事线。</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/meta/test_governance_guards.py tests/meta/test_version_sync.py`
- [ ] `uv run python scripts/check_file_matrix.py --check`
- [ ] `uv run pytest -q tests/meta/test_public_surface_guards.py tests/meta/test_dependency_guards.py`
- [ ] `uv run ruff check .`
</verification>

<success_criteria>
- `GOV-17` 的 release evidence / workflow gate closure 完成。
- v1.2 release-ready 资产对 maintainer 与 tests 都有单一 pointer。
- `Phase 24` 只需要消费这些资产做 final audit/archive/handoff，而不用再次补 release narrative。
</success_criteria>

<output>
After completion, create `.planning/phases/23-governance-convergence-contributor-docs-and-release-evidence-closure/23-03-SUMMARY.md`
</output>

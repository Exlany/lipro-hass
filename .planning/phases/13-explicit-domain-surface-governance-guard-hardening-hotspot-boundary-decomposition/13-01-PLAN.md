---
phase: 13-explicit-domain-surface-governance-guard-hardening-hotspot-boundary-decomposition
plan: "01"
title: explicit device and state surfaces
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/device/device.py
  - custom_components/lipro/core/device/state.py
  - custom_components/lipro/core/device/state_accessors.py
  - custom_components/lipro/core/device/device.pyi
  - custom_components/lipro/core/device/device_delegation.py
  - tests/core/device/test_device.py
  - tests/core/device/test_state.py
autonomous: true
requirements:
  - DOM-01
  - DOM-02
---

<objective>
删除设备域动态 `__getattr__`，让 `LiproDevice` / `DeviceState` 的正式表面变成显式 property / method 集合，同时不恢复任何 compat seam。
</objective>

<tasks>
<task>把 `DeviceState` 的 bool/int/derived 属性改为显式 property，而不是运行时字符串委托。</task>
<task>把 `LiproDevice` 的 delegated leaf surface 改成显式 façade property / method 集合，并删除 `device_delegation.py`。</task>
<task>补 focused tests 锁定“无 `__getattr__`、表面显式可枚举”的新约。</task>
</tasks>

## Verification Criteria

- `LiproDevice` / `DeviceState` 类字典中不存在 `__getattr__`；
- 设备域行为回归保持绿色；
- 不恢复任何 Phase 12 已删除 compat seam。

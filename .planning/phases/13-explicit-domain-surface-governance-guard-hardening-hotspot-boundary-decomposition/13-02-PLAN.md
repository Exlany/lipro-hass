---
phase: 13-explicit-domain-surface-governance-guard-hardening-hotspot-boundary-decomposition
plan: "02"
title: runtime hotspot decomposition and protocol terminology convergence
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/coordinator/orchestrator.py
  - custom_components/lipro/core/coordinator/runtime/device_runtime.py
  - custom_components/lipro/core/coordinator/mqtt_lifecycle.py
  - custom_components/lipro/core/coordinator/coordinator.py
  - custom_components/lipro/core/api/status_service.py
  - tests/core/coordinator/runtime/test_device_runtime.py
  - tests/core/test_device_refresh.py
  - tests/core/api/test_api_status_service.py
  - tests/core/api/test_api_status_service_regressions.py
autonomous: true
requirements:
  - RUN-02
  - RUN-03
---

<objective>
继续拆薄 runtime/status 热点文件，让装配、fallback 与 cleanup 边界更可审计，并在 runtime 内部把协议协作者从 `client` 术语继续向 `protocol` 收口。
</objective>

<tasks>
<task>把 `RuntimeOrchestrator.build_runtimes()` 拆成更小的 builder helpers / ports，不改变正式 runtime story。</task>
<task>把 `status_service` 的 binary-split fallback 拆成 context / accumulator / batch helpers，保留原行为。</task>
<task>把 `mqtt_lifecycle` cleanup path 明确为 helper，避免 setup 失败分支重复蔓延。</task>
</tasks>

## Verification Criteria

- runtime/status 相关回归全部通过；
- 热点函数长度明显下降，边界职责更清晰；
- 内部 `protocol` 术语不破坏现有对外消费点。

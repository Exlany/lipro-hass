# Plan 115-01: Normalize empty-input entry semantics for status fallback

## Objective
把 `query_with_fallback()` 与 binary-split support 在空输入场景下共享的显式语义冻结为正式 contract：无 I/O、空结果、可预测的 fallback depth。

<tasks>
<task>
  <id>115-01-01</id>
  <title>Freeze empty-id short-circuit semantics in query_with_fallback_impl</title>
  <action>把 `custom_components/lipro/core/api/status_fallback_support.py` 中 `query_with_fallback_impl()` 的空输入短路视为正式 contract：避免继续构建 setup、避免触发 primary batch 请求，并与 binary-split entry 保持一致。</action>
  <verify>`uv run pytest -q tests/core/api/test_api_status_service_fallback.py` passes</verify>
  <acceptance_criteria>
    - empty ids returns `[]`
    - `iot_request` is not awaited for empty ids
    - `record_fallback_depth` receives `0`
  </acceptance_criteria>
</task>

<task>
  <id>115-01-02</id>
  <title>Add focused regression coverage</title>
  <action>在 `tests/core/api/test_api_status_service_fallback.py` 增加空输入 regression，冻结 side-effect contract。</action>
  <verify>`uv run pytest -q tests/core/api/test_api_status_service_fallback.py` passes</verify>
  <acceptance_criteria>
    - new regression covers empty ids at the public entry
    - existing fallback tests remain green
  </acceptance_criteria>
</task>
</tasks>

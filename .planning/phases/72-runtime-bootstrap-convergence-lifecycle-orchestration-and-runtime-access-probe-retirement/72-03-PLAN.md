---
phase: 72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement
plan: "03"
type: execute
title: converge lifecycle orchestration into typed root wiring and slimmer controller-support seams
wave: 2
depends_on:
  - "02"
files_modified:
  - custom_components/lipro/control/entry_root_wiring.py
  - custom_components/lipro/control/entry_lifecycle_controller.py
  - custom_components/lipro/control/entry_lifecycle_support.py
  - custom_components/lipro/__init__.py
  - tests/core/test_entry_root_wiring.py
  - tests/core/test_entry_lifecycle_controller.py
  - tests/core/test_init_runtime_setup_entry.py
  - tests/core/test_init_runtime_unload_reload.py
autonomous: true
requirements:
  - ARC-19
  - HOT-32
  - TYP-21
  - TST-22
  - QLT-30
must_haves:
  truths:
    - `custom_components/lipro/__init__.py` 仍是 thin adapter，但不再通过巨大 `dict[str, object]` / kwargs bag 组装 controller。
    - `EntryLifecycleController` 只保留 control-plane policy/orchestration ownership，机械步骤继续 inward 落到 support seam。
    - lifecycle wiring 继续保持 lazy binding / patch-friendly，不回退为 eager singleton 或模块级依赖缓存。
  artifacts:
    - custom_components/lipro/control/entry_root_wiring.py
    - custom_components/lipro/control/entry_lifecycle_controller.py
    - custom_components/lipro/control/entry_lifecycle_support.py
    - custom_components/lipro/__init__.py
    - tests/core/test_entry_root_wiring.py
    - tests/core/test_entry_lifecycle_controller.py
  key_links:
    - __init__.py factory-on-call -> typed lifecycle bundle -> EntryLifecycleController
    - EntryLifecycleController -> EntryLifecycleSupport -> runtime_infra / service_registry formal helpers
---

# Plan 72-03: 用 typed root wiring 收口 lifecycle orchestration，并继续切薄 controller/support

<objective>
把 `entry_root_wiring.py` / `__init__.py` 的 generic kwargs bag 替换为 typed collaborator contract，把 `EntryLifecycleController` / `EntryLifecycleSupport` 的职责边界进一步拉直，同时保留 HA root adapter 的 lazy/patch-friendly 语义。

Purpose: 去掉生命周期装配里的“袋子传参 + controller 过厚”残留，又不破坏现有正式控制面叙事。
Output: typed lifecycle bundle、收薄后的 controller/support、focused lifecycle/root-wiring topical tests。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<preconditions>
- `72-02` 已把 `runtime_infra` 与 `runtime_access` 的 formal contracts 收紧，lifecycle 不再需要围绕宽口 seam 兜底。
</preconditions>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement/72-CONTEXT.md
@.planning/phases/72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement/72-RESEARCH.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@custom_components/lipro/__init__.py
@custom_components/lipro/control/entry_root_wiring.py
@custom_components/lipro/control/entry_lifecycle_controller.py
@custom_components/lipro/control/entry_lifecycle_support.py
@custom_components/lipro/runtime_infra.py
@tests/core/test_control_plane.py
@tests/core/test_init_runtime_setup_entry.py
@tests/core/test_init_runtime_unload_reload.py

<interfaces>
从 `custom_components/lipro/control/entry_root_wiring.py` 保持不变的 root-adapter contract：

```python
class EntryLifecycleControllerLike(Protocol):
    async def async_setup_component(self, hass: HomeAssistant, config: object) -> bool: ...
    async def async_setup_entry(self, hass: HomeAssistant, entry: object) -> bool: ...
    async def async_unload_entry(self, hass: HomeAssistant, entry: object) -> bool: ...
    async def async_reload_entry(self, hass: HomeAssistant, entry: object) -> None: ...
```

从 `custom_components/lipro/control/entry_lifecycle_support.py` 继续复用的 named artifact：

```python
@dataclass(frozen=True, slots=True)
class EntrySetupArtifacts:
    auth_manager: LiproAuthManager
    coordinator: Coordinator
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: 用 typed collaborator bundle 替换 root wiring 的 kwargs bag</name>
  <files>custom_components/lipro/control/entry_root_wiring.py, custom_components/lipro/__init__.py, tests/core/test_entry_root_wiring.py</files>
  <action>把 `build_entry_lifecycle_controller_kwargs()` / `controller_kwargs` 这条 generic bag 叙事替换为显式、可类型检查的 collaborator bundle（dataclass / named builder contract / 等价显式类型均可），由 `entry_root_wiring.py` 负责构建，由 `__init__.py` 保持按调用时现取依赖的薄 adapter。严禁改成 eager binding、模块级单例、预先缓存 `Coordinator` / `LiproProtocolFacade` / `LiproAuthManager`。</action>
  <verify>`uv run pytest -q tests/core/test_entry_root_wiring.py tests/core/test_control_plane.py`</verify>
  <done>root wiring 不再通过 `dict[str, object]` / `**kwargs` 袋子装配 controller，且 patch seam 仍稳定。</done>
</task>

<task type="auto">
  <name>Task 2: 让 controller 保留策略 ownership，让 support 吸收机械 lifecycle 步骤</name>
  <files>custom_components/lipro/control/entry_lifecycle_controller.py, custom_components/lipro/control/entry_lifecycle_support.py, tests/core/test_entry_lifecycle_controller.py</files>
  <action>继续把 setup/unload/reload 里的机械步骤抽成命名明确的 support operations / artifacts，让 `EntryLifecycleController` 只保留 failure contract、日志语义与 control-plane ownership。允许在 support seam 内增加更诚实的私有 helper，但不得新建第二 orchestration root，也不得让 support 反向定义 public surface。</action>
  <verify>`uv run pytest -q tests/core/test_entry_lifecycle_controller.py tests/core/test_init_runtime_setup_entry.py tests/core/test_init_runtime_unload_reload.py`</verify>
  <done>controller/support 职责边界清晰，长方法/构造器密度下降，outward lifecycle 行为保持稳定。</done>
</task>

<task type="auto">
  <name>Task 3: 为 root adapter 与 lifecycle 收口补 focused topical 回归</name>
  <files>tests/core/test_entry_root_wiring.py, tests/core/test_entry_lifecycle_controller.py, tests/core/test_init_runtime_setup_entry.py, tests/core/test_init_runtime_unload_reload.py</files>
  <action>新增/调整 topical tests，直接验证 lazy builder、typed collaborator bundle、setup/unload/reload 契约、service sync 与 listener teardown 顺序。优先把新断言放进专属 topical suite，不把 touched coverage 回灌成更大的 init mega-test。</action>
  <verify>`uv run pytest -q tests/core/test_entry_root_wiring.py tests/core/test_entry_lifecycle_controller.py tests/core/test_init_runtime_setup_entry.py tests/core/test_init_runtime_unload_reload.py`</verify>
  <done>lifecycle/root-wiring touched surface 有独立 focused lane，可持续阻止 kwargs bag 与 controller regrowth 回流。</done>
</task>
</tasks>

<risks>
- 不要把 `__init__.py` 变回 eager singleton/root cache，避免破坏现有 patch-based 回归。
- 不要让 `EntryLifecycleSupport` 反客为主，重新长成第二 control root。
</risks>

<non_goals>
- 不做 `service_router` family 重切或 diagnostics/helper dedup（Phase 73）。
- 不做 auth legacy snapshot / compatibility wrapper 退役（Phase 74）。
</non_goals>

<verification>
- [ ] `uv run ruff check custom_components/lipro/control/entry_root_wiring.py custom_components/lipro/control/entry_lifecycle_controller.py custom_components/lipro/control/entry_lifecycle_support.py custom_components/lipro/__init__.py tests/core/test_entry_root_wiring.py tests/core/test_entry_lifecycle_controller.py tests/core/test_init_runtime_setup_entry.py tests/core/test_init_runtime_unload_reload.py`
- [ ] `uv run mypy --follow-imports=silent custom_components/lipro/control/entry_root_wiring.py custom_components/lipro/control/entry_lifecycle_controller.py custom_components/lipro/control/entry_lifecycle_support.py custom_components/lipro/__init__.py`
- [ ] `uv run pytest -q tests/core/test_entry_root_wiring.py tests/core/test_entry_lifecycle_controller.py tests/core/test_init_runtime_setup_entry.py tests/core/test_init_runtime_unload_reload.py`
</verification>

<success_criteria>
- `__init__.py` 与 `entry_root_wiring.py` 不再依赖 generic bag assembly。
- `EntryLifecycleController` / `EntryLifecycleSupport` 继续是一条 formal control-plane story，而不是 controller 过厚 + support 兜底的混合叙事。
- focused lifecycle/root-wiring suites 全绿并冻结 lazy-binding 语义。
</success_criteria>

<output>
After completion, create `.planning/phases/72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement/72-03-SUMMARY.md`
</output>

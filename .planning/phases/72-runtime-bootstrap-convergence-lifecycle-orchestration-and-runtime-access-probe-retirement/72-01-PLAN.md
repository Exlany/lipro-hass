---
phase: 72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement
plan: "01"
type: execute
title: formalize coordinator bootstrap collaborators and trim runtime-root startup density
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/coordinator/coordinator.py
  - custom_components/lipro/core/coordinator/factory.py
  - custom_components/lipro/core/coordinator/orchestrator.py
  - tests/core/coordinator/test_runtime_root.py
  - tests/core/coordinator/conftest.py
autonomous: true
requirements:
  - ARC-19
  - HOT-32
  - TYP-21
  - TST-22
  - QLT-30
must_haves:
  truths:
    - Coordinator 构造函数不再同时承载 state/runtime/service/update-cycle 全装配叙事。
    - bootstrap 继续只存在于 `core/coordinator/` 正式 home，不长出第二条 builder/root 故事线。
    - 对外仍只通过 `Coordinator(...)` 构造，不要求调用方理解内部拆分。
  artifacts:
    - custom_components/lipro/core/coordinator/coordinator.py
    - custom_components/lipro/core/coordinator/factory.py
    - custom_components/lipro/core/coordinator/orchestrator.py
    - tests/core/coordinator/test_runtime_root.py
  key_links:
    - Coordinator.__init__ -> RuntimeOrchestrator / factory artifacts -> service layer
    - runtime-root tests -> bootstrap contracts -> outward behavior stability
---

# Plan 72-01: 收口 Coordinator bootstrap 协作者并压缩 runtime-root 启动密度

<objective>
把 `Coordinator` 启动期的 runtime/state/service/update-cycle 机械装配继续 inward split 到 `core/coordinator/` 正式 home，保持 `Coordinator` 作为唯一 runtime root。

Purpose: 降低 startup hotspot density，去掉 runtime root 内部的 builder folklore。
Output: 更薄的 `Coordinator.__init__`、显式 bootstrap artifact/contract、匹配的 runtime-root focused tests。
</objective>

<execution_context>
@/home/claudeuser/.codex/get-shit-done/workflows/execute-plan.md
@/home/claudeuser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement/72-CONTEXT.md
@.planning/phases/72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement/72-RESEARCH.md
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/baseline/DEPENDENCY_MATRIX.md
@custom_components/lipro/core/coordinator/coordinator.py
@custom_components/lipro/core/coordinator/factory.py
@custom_components/lipro/core/coordinator/orchestrator.py
@tests/core/coordinator/test_runtime_root.py

<interfaces>
从 `custom_components/lipro/core/coordinator/coordinator.py` 保持不变的 outward contract：

```python
class Coordinator(DataUpdateCoordinator[dict[str, "LiproDevice"]]):
    def __init__(
        self,
        hass: HomeAssistant,
        protocol: LiproProtocolFacade,
        auth_manager: LiproAuthManager,
        config_entry: ConfigEntry,
        update_interval: int = DEFAULT_SCAN_INTERVAL,
    ) -> None: ...
```

从 `custom_components/lipro/core/coordinator/orchestrator.py` 继续复用的 inward seam：

```python
class RuntimeOrchestrator:
    def build_state_containers(self) -> CoordinatorStateContainers: ...
    def build_runtimes(
        self,
        *,
        context: RuntimeContext,
        state: CoordinatorStateContainers,
        polling_updater: PollingIntervalUpdater,
    ) -> CoordinatorRuntimes: ...
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: 在 formal home 定义显式 bootstrap artifact，而不是继续堆在 Coordinator.__init__</name>
  <files>custom_components/lipro/core/coordinator/coordinator.py, custom_components/lipro/core/coordinator/factory.py, custom_components/lipro/core/coordinator/orchestrator.py</files>
  <action>把 `Coordinator.__init__` 中的 state 容器、runtime 构建、service-layer 绑定与 update-cycle 机械装配继续下沉到 `factory.py` / `orchestrator.py` 已存在 formal homes，通过命名清晰的 bootstrap artifact（dataclass / named tuple / 等价显式 contract 均可）交回 `Coordinator` 消费。保留 `Coordinator(...)` 构造签名与 `coordinator_entry.py` 薄 export 身份，不新增第二 runtime root，不把 bootstrap folklore 倒灌回 `coordinator_entry.py` 或新 helper root。</action>
  <verify>`uv run pytest -q tests/core/coordinator/test_runtime_root.py`</verify>
  <done>`Coordinator.__init__` 只保留 root-owned 字段绑定与正式 collaborator 消费；装配机械步骤转为可命名 artifact/contract。</done>
</task>

<task type="auto">
  <name>Task 2: 用 focused runtime-root 回归锁住 outward behavior 与 patch seam</name>
  <files>tests/core/coordinator/test_runtime_root.py, tests/core/coordinator/conftest.py</files>
  <action>补或调整 focused tests，证明 `Coordinator` 继续暴露同一 formal service layer、内部 runtimes registry 与 shutdown/update 行为；若 bootstrap artifact 需要 fixture 支撑，只在 `tests/core/coordinator/` 局部加最小夹具，不扩大 public surface，也不把断言散回 mega-test。</action>
  <verify>`uv run pytest -q tests/core/coordinator/test_runtime_root.py tests/integration/test_mqtt_coordinator_integration.py`</verify>
  <done>runtime-root focused suite 可以直接证明 bootstrap 收口未改变 outward behavior。</done>
</task>
</tasks>

<risks>
- 不要把 `RuntimeOrchestrator` 或 `factory.py` 讲成第二正式 root；它们只能是 `Coordinator` 的 inward collaborators。
- 不要改 `Coordinator` 的对外构造签名、`coordinator_entry.py` 的薄壳语义，或破坏现有 patch seam。
</risks>

<non_goals>
- 不处理 `service_router` family、`schedule.py` runtime surface 或 auth legacy wrapper。
- 不重写 diagnostics / telemetry outward surface，只处理 bootstrap 装配密度。
</non_goals>

<verification>
- [ ] `uv run ruff check custom_components/lipro/core/coordinator/coordinator.py custom_components/lipro/core/coordinator/factory.py custom_components/lipro/core/coordinator/orchestrator.py tests/core/coordinator/test_runtime_root.py tests/core/coordinator/conftest.py`
- [ ] `uv run mypy --follow-imports=silent custom_components/lipro/core/coordinator/coordinator.py custom_components/lipro/core/coordinator/factory.py custom_components/lipro/core/coordinator/orchestrator.py`
- [ ] `uv run pytest -q tests/core/coordinator/test_runtime_root.py tests/integration/test_mqtt_coordinator_integration.py`
</verification>

<success_criteria>
- `Coordinator.__init__` 不再承担整条 bootstrap/builder 叙事。
- bootstrap artifact 只存在于 `core/coordinator/` formal homes。
- runtime-root focused tests 全绿，且没有 public-surface drift。
</success_criteria>

<output>
After completion, create `.planning/phases/72-runtime-bootstrap-convergence-lifecycle-orchestration-and-runtime-access-probe-retirement/72-01-SUMMARY.md`
</output>

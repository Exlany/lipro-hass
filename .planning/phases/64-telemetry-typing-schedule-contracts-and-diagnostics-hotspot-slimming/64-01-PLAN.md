---
phase: 64-telemetry-typing-schedule-contracts-and-diagnostics-hotspot-slimming
plan: "01"
type: execute
title: converge telemetry exporter family onto explicit JSON-safe typed contracts
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/core/telemetry/models.py
  - custom_components/lipro/core/telemetry/ports.py
  - custom_components/lipro/core/telemetry/exporter.py
  - custom_components/lipro/core/telemetry/sinks.py
  - tests/core/telemetry/test_models.py
  - tests/core/telemetry/test_exporter.py
  - tests/core/telemetry/test_sinks.py
autonomous: true
requirements:
  - ARC-11
  - HOT-19
  - TYP-17
  - TST-14
must_haves:
  truths:
    - telemetry snapshot/source/sink contracts are expressed through explicit JSON-safe types instead of `Any`-centric mappings
    - exporter redaction, pseudonymous references, and cardinality budgets still flow through one shared typed snapshot story
    - diagnostics/system-health/developer/CI views keep stable outward payload shapes while consuming the tighter contracts
  artifacts:
    - custom_components/lipro/core/telemetry/models.py
    - custom_components/lipro/core/telemetry/ports.py
    - custom_components/lipro/core/telemetry/exporter.py
    - custom_components/lipro/core/telemetry/sinks.py
    - tests/core/telemetry/test_models.py
    - tests/core/telemetry/test_exporter.py
    - tests/core/telemetry/test_sinks.py
  key_links:
    - typed telemetry models -> source/sink protocols
    - exporter snapshot builder -> sink projections
    - focused telemetry tests -> stable JSON-safe assurance truth
---

# Plan 64-01: converge telemetry exporter family onto explicit JSON-safe typed contracts

<objective>
把 telemetry family（`models.py` / `ports.py` / `exporter.py` / `sinks.py`）继续收口到显式、JSON-safe、可复用的 typed contracts；避免 `Any`、`Mapping[str, Any]` 与 ad-hoc dict 继续充当 assurance-plane formal truth。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/baseline/PUBLIC_SURFACES.md
@.planning/reviews/FILE_MATRIX.md
@custom_components/lipro/core/telemetry/models.py
@custom_components/lipro/core/telemetry/ports.py
@custom_components/lipro/core/telemetry/exporter.py
@custom_components/lipro/core/telemetry/sinks.py
@tests/core/telemetry/test_models.py
@tests/core/telemetry/test_exporter.py
@tests/core/telemetry/test_sinks.py
@.planning/phases/64-telemetry-typing-schedule-contracts-and-diagnostics-hotspot-slimming/64-CONTEXT.md
@.planning/phases/63-governance-truth-realignment-typed-runtime-access-and-hidden-root-closure/63-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: replace `Any`-shaped telemetry contracts with explicit JSON-safe source and view types</name>
  <files>custom_components/lipro/core/telemetry/models.py, custom_components/lipro/core/telemetry/ports.py, tests/core/telemetry/test_models.py</files>
  <read_first>
    - custom_components/lipro/core/telemetry/models.py
    - custom_components/lipro/core/telemetry/ports.py
    - custom_components/lipro/core/api/types.py
    - tests/core/telemetry/test_models.py
  </read_first>
  <action>在 `models.py` 中为 telemetry snapshot / sink views / failure/outcome mapping 定义并导出显式 JSON-safe typed aliases or equivalent honest contracts（可复用 `JsonValue`/`JsonObject` 语义，但不要让 `Any` 继续成为 formal contract）；在 `ports.py` 中把 protocol/runtime source 与 sink 接口改读这些命名契约，而不是继续暴露 `Mapping[str, Any]`。保留当前 importer 入口与 assurance-plane 身份，不新增 public root、compat shell 或第二条 truth story。</action>
  <acceptance_criteria>
    - `TelemetrySnapshot` / `TelemetryViews` no longer advertise `dict[str, Any]` as their formal payload contract
    - `ProtocolTelemetrySource` / `RuntimeTelemetrySource` / `TelemetrySink` consume named JSON-safe contracts instead of raw `Mapping[str, Any]`
    - `tests/core/telemetry/test_models.py` asserts the stable typed dictionary shape without behavior drift
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/telemetry/test_models.py</automated>
  </verify>
  <done>Telemetry models and ports now define one explicit typed contract story that downstream exporter/sinks can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: refit exporter and sink projections onto the tightened telemetry contracts</name>
  <files>custom_components/lipro/core/telemetry/exporter.py, custom_components/lipro/core/telemetry/sinks.py, tests/core/telemetry/test_exporter.py, tests/core/telemetry/test_sinks.py</files>
  <read_first>
    - custom_components/lipro/core/telemetry/exporter.py
    - custom_components/lipro/core/telemetry/sinks.py
    - custom_components/lipro/core/telemetry/models.py
    - custom_components/lipro/core/telemetry/ports.py
    - tests/core/telemetry/test_exporter.py
    - tests/core/telemetry/test_sinks.py
  </read_first>
  <action>让 `RuntimeTelemetryExporter` 与全部 sink projections 统一消费 Task 1 的 typed snapshot/view contracts；保持 redaction、pseudo-id、failure-summary、header、summary metric 与 cardinality budget 现有语义不变，只收紧 contract honesty。若需要提取局部 helper，只允许留在 `core/telemetry/` family 内，避免再长出 helper/support public shell。</action>
  <acceptance_criteria>
    - exporter sanitization path returns the same outward shapes while consuming typed JSON-safe contracts end to end
    - diagnostics/system-health/developer/CI sinks build views without `dict[str, Any]` being the primary formal sink contract
    - focused exporter and sink suites pass without changing current report semantics
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py</automated>
  </verify>
  <done>The telemetry exporter family now speaks one typed, JSON-safe assurance contract from source snapshot to sink output.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/core/telemetry/test_models.py tests/core/telemetry/test_exporter.py tests/core/telemetry/test_sinks.py`
</verification>

<success_criteria>
- telemetry family no longer uses `Any` as the formal source/sink truth for exporter-facing payloads.
- one typed snapshot/view vocabulary carries redaction, cardinality, and sink projections without outward drift.
</success_criteria>

<output>
After completion, create `.planning/phases/64-telemetry-typing-schedule-contracts-and-diagnostics-hotspot-slimming/64-01-SUMMARY.md`.
</output>

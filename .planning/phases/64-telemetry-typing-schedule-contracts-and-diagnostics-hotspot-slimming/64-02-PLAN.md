---
phase: 64-telemetry-typing-schedule-contracts-and-diagnostics-hotspot-slimming
plan: "02"
type: execute
title: formalize schedule service contracts and remove dynamic mesh-context probing from the main path
wave: 1
depends_on: []
files_modified:
  - custom_components/lipro/services/schedule.py
  - tests/services/test_services_schedule.py
  - tests/core/test_init_service_handlers_schedules.py
autonomous: true
requirements:
  - ARC-11
  - HOT-19
  - TYP-17
  - TST-14
must_haves:
  truths:
    - schedule row normalization and mesh-context assembly go through explicit typed contracts instead of `Any` and broad `getattr()` probing
    - schedule service keeps the current outward home, shared executor usage, and `serial + schedules` response payload semantics
    - focused schedule regressions prove auth/error mapping, normalized row behavior, and handler payload stability remain stable
  artifacts:
    - custom_components/lipro/services/schedule.py
    - tests/services/test_services_schedule.py
    - tests/core/test_init_service_handlers_schedules.py
  key_links:
    - typed device/coordinator contract -> shared schedule executor
    - normalized schedule row -> stable service response shape
    - mesh-context helper -> no second contract story for gateway/member discovery
---

# Plan 64-02: formalize schedule service contracts and remove dynamic mesh-context probing from the main path

<objective>
把 `custom_components/lipro/services/schedule.py` 从 mixed tuple contract、`Any` 与 dynamic mesh-context probing 收口到显式 typed service contracts；保留 outward service home、shared executor 与现有 response shape，不引入第二套 helper story。
</objective>

<context>
@docs/NORTH_STAR_TARGET_ARCHITECTURE.md
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/baseline/PUBLIC_SURFACES.md
@custom_components/lipro/services/schedule.py
@custom_components/lipro/services/contracts.py
@custom_components/lipro/runtime_types.py
@tests/services/test_services_schedule.py
@tests/core/test_init_service_handlers_schedules.py
@.planning/phases/64-telemetry-typing-schedule-contracts-and-diagnostics-hotspot-slimming/64-CONTEXT.md
@.planning/phases/63-governance-truth-realignment-typed-runtime-access-and-hidden-root-closure/63-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: formalize schedule row, resolver, and mesh-context contracts inside the service home</name>
  <files>custom_components/lipro/services/schedule.py, tests/services/test_services_schedule.py, tests/core/test_init_service_handlers_schedules.py</files>
  <read_first>
    - custom_components/lipro/services/schedule.py
    - custom_components/lipro/services/contracts.py
    - custom_components/lipro/core/api/types.py
    - tests/services/test_services_schedule.py
    - tests/core/test_init_service_handlers_schedules.py
  </read_first>
  <action>把 `GetDeviceAndCoordinator = Callable[..., Awaitable[tuple[Any, Any]]]`、`_normalize_schedule_time_events()` 的 `Mapping[str, Any]` 输入与 `get_mesh_context()` 的 broad `getattr()` 主逻辑改成显式 typed aliases / protocols / typed rows（可留在 `schedule.py` 内，或只在 truly necessary 时抽出单一 concern-local contract file）；mesh gateway/member 读取优先走声明属性，IR fallback 保留为显式分支而不是模糊探测。保持 service outward home、handler registration、shared executor、auth/error mapping 与 response keys 不变。</action>
  <acceptance_criteria>
    - primary schedule resolver and normalized row contracts no longer rely on `tuple[Any, Any]` or `Mapping[str, Any]`
    - `get_mesh_context()` reads declared schedule-device fields first and only keeps explicit fallback branches
    - focused schedule tests still cover invalid row cleanup, mesh normalization, auth/error flows, and handler-facing response stability
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/services/test_services_schedule.py tests/core/test_init_service_handlers_schedules.py</automated>
  </verify>
  <done>Schedule service logic now expresses row normalization and mesh lookup through typed contracts instead of broad probing.</done>
</task>

<task type="auto">
  <name>Task 2: thread the tightened contracts through schedule handlers without outward drift</name>
  <files>custom_components/lipro/services/schedule.py, tests/services/test_services_schedule.py, tests/core/test_init_service_handlers_schedules.py</files>
  <read_first>
    - custom_components/lipro/services/schedule.py
    - tests/services/test_services_schedule.py
    - tests/core/test_init_service_handlers_schedules.py
  </read_first>
  <action>让 `normalize_schedule_row()`、`async_execute_schedule_operation()`、`async_call_schedule_service()` 与三个 schedule handlers 全部消费 Task 1 的 typed contracts；保留 shared executor 和 `raise_service_error` 路径，不新增 coordinator backdoor 或 raw protocol sidecar contract。必要时把 mesh context / normalized row 变成局部小 helper，但不要扩散新的 `*_support.py` / second-root 叙事。</action>
  <acceptance_criteria>
    - schedule service main path no longer depends on broad `Any` / `getattr()` for gateway/member discovery or row shaping
    - current get/add/delete handler response payloads remain `serial + schedules` and current behavior stays unchanged
    - `tests/services/test_services_schedule.py` and `tests/core/test_init_service_handlers_schedules.py` pass with no API/service drift
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/services/test_services_schedule.py tests/core/test_init_service_handlers_schedules.py</automated>
  </verify>
  <done>The schedule service remains the outward control-plane home, but its internals now use explicit formal contracts.</done>
</task>
</tasks>

<verification>
- [ ] `uv run pytest -q tests/services/test_services_schedule.py tests/core/test_init_service_handlers_schedules.py`
</verification>

<success_criteria>
- schedule service no longer lets `Any`, mixed tuple returns, or dynamic mesh probing define its formal contract.
- outward schedule behavior, shared-executor flow, and get/add/delete handler payload shape stay stable.
</success_criteria>

<output>
After completion, create `.planning/phases/64-telemetry-typing-schedule-contracts-and-diagnostics-hotspot-slimming/64-02-SUMMARY.md`.
</output>
